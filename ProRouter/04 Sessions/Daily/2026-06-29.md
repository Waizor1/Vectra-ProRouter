---
type: session
date: 2026-06-29
tags:
  - session
---

# Session 2026-06-29

## Summary

-

## Completion Updates

### 19:41

- Summary: Onboarded aleksandr-babkin (Cudy WBR3000UAX v1 ubootmod, 24.10.5, device vectra-51704493c3e5). Router was stuck in a lost-token 403 register-loop (state.json lost agent_token; panel anti-hijack refused re-register) and had imported empty (no subscription). Fix: deleted stale empty record b5fc9ebe -> agent self-re-registered as d49234da with a fresh token and a full 24-node import. Built fleet route-policy via local vitest normalizeFleetRoutePolicy: grafted DiscordVoiceUdp->zeiqMDbn (RU-entry PL, mux/xudp), DNS Yandex->Google DoH, Kinopub x5->WorldProxy; 4/5 slots came pre-bound compliant from the subscription; default_node=_direct, mainSwitch=true. Approve -> draft.save (59532adf) -> queue-apply (succeeded, no strand) -> request-reimport auto-adopted device-live as active 839aba4b: compliant / live-import. All 5 slots url_test_node=204. Fleet 26->27.
- Modules: [[02 Modules/Router Agent]]

### 19:49

- Summary: Read-only audit: root-caused permanent controller-agent token-loss brick. Single tokenless state.Save() (omitempty drops agent_token/router_id) atomically clobbers BOTH state.json and state.json.last-good; resulting file is valid JSON so Load() never salvages -> register path -> panel 403 anti-hijack loop forever. device_private_key is generated/persisted but never used; panel stores device_public_key but authenticateRouter only checks token hash -> no keypair recovery path exists.
- Modules: `vectra-controller-agent`

### 20:12

- Summary: Added ed25519 keypair-signed recovery path so a router that lost its bearer token but kept its device keypair can re-register without operator intervention (panel-side, TDD). Contract: optional recoveryProof {signedAt RFC3339, signature base64-std} on routerRegisterRequestSchema; canonical signed msg = 'vectra-router-reauth:v1\n'+deviceIdentifier+'\n'+signedAt; verified via stdlib crypto.verify(null,...) against most-recent credential devicePublicKey, +/-300s skew; logs router.reregister_via_signature on success. 11 new vitest pass, full vectra suite 217 pass, tsc 0 errors. Normal token path byte-for-byte unchanged.
- Modules: `vctl`

### 20:14

- Summary: Add control-plane DIRECT carve-out: fwmark 0x564354 (no collision with PassWall2 0x50535732/0xff) bypasses PassWall2 tproxy via dedicated inet vectra_controlplane nft table (output hook priority -160, accept) so controller agent SO_MARK'd traffic always reaches panel; render-config emits control_plane_fwmark, uci-default 93 + fw4 include for persistence; 23/23 tests pass
- Modules: `vectra-controller-agent`

### 20:30

- Summary: Adversarial code review of feat/controller-reliability-hardening (T1 state durability+watchdog, T2 ed25519 reauth recovery, T3 control-plane-always-direct carve-out) before canary: found CRITICAL boot-persistence gap (nft carve-out not re-applied after reboot; uci-default runs once then deleted, /var/etc include is tmpfs, no init.d boot hook), HIGH probe/check-in SO_MARK asymmetry; T2 security verified sound; all tests pass. Verdict: NOT safe as-is, 1 must-fix (boot persistence).
- Modules: `Xray controller strategy`

### 21:32

- Summary: Fixed CRITICAL reboot-persistence bug in T3 control-plane-direct carve-out: added /etc/hotplug.d/firewall/30-vectra-controlplane-direct + init.d boot() hook so the inet vectra_controlplane nft table + fw4 include are rebuilt on every reboot and fw4 reload (uci-default ran-once-then-deleted, so the carve-out previously never came back after reboot). Extended shell test to 40 assertions with a reboot-persistence regression (was 23). sh -n + dash -n clean, POSIX/ash, no live router touched.
- Modules: [[02 Modules/Router Agent]]

### 21:35

- Summary: Fixed two Go bugs in vectra-controller-agent (TDD): H1 — recovery panel-reachability probe now SO_MARK-stamped via new shared internal/netmark package so it measures the same direct path the marked check-in uses (RU/foreign probes stay unmarked); M1 — agent now self-heals back to proxy after a watchdog dead-man-trip direct (Idle/Monitoring + panel healthy + PassWall disabled routes into the existing cooldown-bounded, probe-gated PhasePasswallRetryWait path), honoring the watchdog header's auto-resume promise without touching the other lane's shell files. go test/vet + GOOS=linux GOARCH=arm64 build all green.
- Modules: [[02 Modules/Router Agent]]

### 21:43

- Summary: Re-reviewed C1/H1/M1 fixes on feat/controller-reliability-hardening: all closed, no regressions. C1 reboot-persistence (hotplug+boot()+fw4-include, 40/40 shell assertions, hotplug-call subshell isolation confirmed, no bashisms); H1 panel-probe mark scoped to ONLY collectPanelReachability (RU/foreign/TG/YT/IG stay unmarked, asserted by tests); M1 watchdog self-heal flap-safe via PhasePasswallRetryWait + 12h RebootCooldown anchor (resume/anti-thrash/isolation all tested). go test+vet clean, linux/arm64 cross-build OK. Verdict: SAFE for gated canary.
- Modules: [[02 Modules/Router Agent]]

### 21:50

- Summary: Controller reliability hardening implemented + fully validated on branch feat/controller-reliability-hardening (source-only, no live router touched). T1 state credential-floor+identity-mirror+watchdog dead-man's switch+recovery-gap fixes; T2 ed25519 keypair-signed re-register (token loss self-heals without operator); T3 SO_MARK 0x564354 + nft carve-out (panel always direct). Built via 4 parallel subagent lanes + integration, full TDD. Independent correctness review found C1(reboot persistence)/H1(panel-probe marking)/M1(watchdog auto-resume) — all fixed; re-review verdict GO. Security review LOW/no-must-fix. Green: go test ./... + -race + vet (darwin & linux/arm64), both-platform builds, 38 panel vitest, watchdog 35 + carve-out 40 shell, Go->TS reauth interop golden vector. aarch64 binary cross-compiled (static 6.9MB). On-device smoke test pending: direct SSH to test router 192.168.99.1 lost (operator's temporary direct connection ended); router healthy on panel. Canary/rollout gated on operator sign-off.
- Modules: [[02 Modules/Router Agent]]

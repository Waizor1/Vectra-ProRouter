---
type: session
date: 2026-07-26
tags:
  - session
---

# Session 2026-07-26

## Summary

-

## Completion Updates

### 15:04

- Summary: WorldProxy Германия->Польша, Фаза 2 выполнена 2026-07-26. Панель: задеплоены 7 файлов коммита daf661b точечным scp (прод отличался ровно на них, дрейфа не было), бэкап в /opt/vectra-prorouter/_rollback/20260725-worldproxy, пересборка образа потребовала чистки диска (был 96%: docker builder prune + journal vacuum). Канон в проде = 2026-07-02-v2. КЛЮЧЕВОЕ: контроллер r33 ре-нормализует слот САМ - достаточно update controller --channel beta, отдельный apply/normalize не нужен. Мигрировано 23 роутера (вкл. 1111111111 последним); канарейки kirill-msk и AlekseyHorev проверены: выход Польши 144.31.3.104, TG web 200/DC 404/api 302, Instagram 200 (на Германии деградировал). Не мигрированы: AndreyVK и sergeyavito (селектор sergeyavito НЕОДНОЗНАЧЕН - совпадает с childsergeyavito, ставить задания только по routerId), ar-filicity (guard exit 72), hh (exempt), 2 офлайновых. ИНЦИДЕНТЫ: dmitry-filicity потерял контроллер во время своего update (remove прошёл, install нет) - офлайн >50 мин, нужен локальный доступ; sergeyavito в direct с 11:07, обе ноды 000 (не достаёт ни один выход) - reconnect запрещён.
- Modules: [[02 Modules/Router Agent]]

### 15:57

- Summary: Добивка после миграции 2026-07-26: dmitry-filicity ВОССТАНОВИЛСЯ САМ (r33, прокси-режим) - контроллер до-установился после разрыва, локальное вмешательство не понадобилось. AndreyVK домигрирован (сбой постановки был из-за сбитой рабочей директории, не панели). ar-filicity домигрирован со второй попытки: гард self-update режет при RAM<48MB (apps/web/src/lib/controller-update-jobs.ts:111), у него скачет 45-60MB, zram на месте - ждать окна выше порога. ИТОГ: Policy OK 26 роутеров на польском каноне, дрейф 3 (2 офлайновых + sergeyavito), исключение 1 (hh). sergeyavito: НЕ подписка нод и НЕ сеть - TCP до ru5/ru4/ru3 проходит за 4-119мс, xray стартует, config OK, версии верные (app 26.5.1-r1/xray 26.5.9), но туннель не несёт трафик даже на голый IP -> выход отвергает сессию, похоже на истёкшую/отозванную подписку у поставщика; refresh subscriptions не помог, вернул тот же набор нод. hh: подписки нет НИ в live, НИ в authoritative, НИ в draft, maskedFields пуст - материала для рабочей ноды в системе нет, нужен URL подписки от оператора.
- Modules: [[02 Modules/Router Agent]]

### 20:04

- Summary: VPS disk: найдена и устранена КОРНЕВАЯ причина. Диск ел не ретеншен бэкапов (он корректен, BACKUP_KEEP_DAYS=7), а баг в apps/web/src/app/api/health/route.ts: проба записи в БД была одним запросом 'with inserted as (insert ... returning id) delete ... where id in (select id from inserted)'. В PostgreSQL все части одного запроса видят снимок ДО начала запроса, поэтому DELETE не видел только что вставленную строку и удалял 0. Каждый health-check оставлял строку навсегда: 8 611 602 строки health.db_write_probe = 99.73% журнала = 2.2 ГБ. Доказано эмпирически на боевой БД в транзакции с откатом (старый вариант DELETE 0 и строка выжила; новый INSERT 0 1 / DELETE 1 и осталось 0). Фикс: insert и delete разнесены в два запроса внутри db.transaction (паттерн уже применён в stuck-job-janitor.ts:151), tsc чисто. Чистка: удалены ТОЛЬКО строки этого типа пачками по 200k с контролем диска (FK на таблицу нет), затем vacuum full analyze за 16 с. РЕЗУЛЬТАТ: event_log 2227МБ -> 42МБ, база 4805МБ -> 2622МБ, диск 2.3ГБ -> 4.5ГБ свободно (94% -> 89%). Протечка подтверждённо остановлена (два замера, нулевой прирост). Бэкап старого файла в /opt/vectra-prorouter/_rollback/20260726-healthprobe/. ВНИМАНИЕ: фикс задеплоен, но НЕ закоммичен в git. Остаточные драйверы роста: router_inventory_snapshot 1277МБ (де-факто ретеншен ~14 дней) и secret_blob+desired_revision 1.2ГБ на 4490 ревизий для 30 роутеров (история ревизий не чистится).
- Modules: [[02 Modules/Router Agent]]

---
type: module
path: apps/web, apps/install-helper
stage: pilot
confidence: high
last-reviewed: 2026-05-15
tags:
  - module
  - web
  - nextjs
---

# Web Control Plane

## Confirmed

- 2026-05-15 automated onboarding r23 rollout closeout: web onboarding now gates typed `ensure_passwall_runtime` and `verify_passwall_routes` behind controller/LuCI `0.1.13-r23+`, scopes onboarding dedupe keys by attempt, and treats `done` as a resumable terminal state so enabled profiles do not spawn a fresh run after completion. Production rollout for `yuranrod-msk` finished cleanly, the router run ended `done`, and final route-smoke proof stayed green across all five managed slots.

- 2026-05-14 automated onboarding backend MVP: the intended web/control-plane shape is documented in `ai_docs/develop/features/router-automated-onboarding-workflow.md` and ADR-0002, and the local implementation now spans `apps/web/src/server/vectra/router-auto-onboarding.ts`, protected `onboarding` tRPC procedures, and the router-detail `Автонастройка роутера` card. The web layer has feature-flagged register/check-in/job-result hooks, profile/run persistence in `packages/db`, subscription-secret encryption/hash-only client output, and a state-machine lane for first import approval, hostname job, typed runtime repair, subscription apply/refresh, semantic fleet route baseline, typed route-smoke verification, final re-import, and safety gates. Auto-run remains off by default with `VECTRA_AUTO_ONBOARDING_ENABLED=false`; runtime repair is now queued only after core PassWall/Xray exist and advances only after green post-repair service/resource/action proof.

- 2026-05-13 local portal hardening pass: fixed review findings without deploying. Public router re-registration now passes authenticated router context and blocks token reissue for an existing `deviceIdentifier` unless the request is authenticated as that same router; prod env validation rejects `change-me` / dev secrets defaults; public install/register rate-limit identity uses trusted `X-Vectra-Client-IP` from Caddy and ignores raw `X-Forwarded-For`/`X-Real-IP`; tRPC timing logs are dev/debug-only; `Special` route normalization prefers the live-good RU-entry Netherlands fallback when available; login goes to `/install`, hides operator command/search pre-auth, and key fleet/install controls have larger touch targets plus `aria-pressed`. Local proof: full `@vectra/web` Vitest, lint, typecheck/build with temporary safe env values, Playwright mobile/tablet login→install smoke, and `git diff --check`.

- 2026-05-13 `Kirill-MSK` onboarding: the production panel import/rename/save/apply/re-import lane successfully moved router `297477dd-f91b-40b3-92bf-e63f059fceba` from `pending/import_review` as `Kirill_Home` to `active/approved` as `kirill-msk`. The raw `fleet.renameRouter` mutation queued a real router-side hostname update job, draft/apply stored the Google DoH + `myshunt` route baseline, re-import promoted the live router config to active revision `0a316ab7-0b07-465a-a104-6b7198d7286f`, and `draft.editorSurface` ended with `operationPreview=[]`, `unconfirmedChanges=none`, `configTrust=live-import`, and `fleetPolicyCompliance=compliant`.

- 2026-05-13 fleet policy rollout closeout: production web now carries the self-update shell newline hotfix (`6019194`) after the initial r21 rollout exposed an invalid OpenWrt shell `then;` command. The guarded release-slice deploy left `vectra-web` healthy, public operator/router API health at `200`, and the final fleet read model reports all 15 routers as `live-import` with `requiresReimport=false`; `fleetPolicyCompliance` is `compliant` for 14 non-`hh` routers and `exempt` for `hh`. A final `fleet.normalizeRoutePolicy` dry-run returned `already_compliant` for every non-`hh` router, so the panel now distinguishes revision trust from server-package compliance in live operations.

- 2026-05-12 fleet route policy guard: web/API now separates `configTrust` (live-import/revision sync) from `fleetPolicyCompliance` (semantic match against the canonical fleet server package). Fleet list, monitoring charts/alerts, router cards, and router detail can show `Policy drift` even when `requiresReimport=false` and `digestMismatch=false`; `hh` is an explicit policy exception. A protected `fleet.normalizeRoutePolicy` path supports read-only dry-run, draft creation, and guarded apply queueing from the latest live import without touching subscription URLs/secrets. Local proof: full `@vectra/web` Vitest, `lint`, `typecheck`, `build`, full controller `go test`, and `git diff --check` are green.

- 2026-05-12 r20 review hotfix: the controller-update queueing path now uses the legacy `controller-self-update-compat` purpose only for non-direct pre-r20 routers whose latest snapshot meets conservative RAM/overlay/tmp floors; direct-rescue routers keep the normal self-update purpose so proxy-resume semantics are preserved. The generated terminal self-update command also rechecks live RAM, `/overlay`, and `/tmp` before any staging/download/install, and launch-progress payloads stay compact to avoid web heap pressure during fleet rollouts. Local tests/typecheck/lint/build are green, and the production source/deploy smoke confirm the hotfix is live.

- 2026-05-12 router safety surfacing and rescue throttling: web monitoring now accepts agent `safetyEvents`, turns critical/warning router safety events into fleet alerts, and shows a `События безопасности` block on router detail. Auto-rescue now uses latest resource snapshots before queueing work: log collection is blocked below diagnostic floors, heavy repair actions (`refresh_rules`, `refresh_subscriptions`) are dropped under low/unknown RAM/tmp/overlay or resource safety events, and service-specific Telegram/foreign reachability repairs also avoid PassWall reconnect/restart under low resources so the panel does not kill a running Xray while investigating a service-only outage. Local proof: `pnpm --filter @vectra/web test -- --run src/server/vectra/auto-rescue.test.ts src/server/vectra/fleet-monitoring.test.ts`, `typecheck`, and `lint` are green.

- 2026-05-12 safe service-probe semantics: fleet UI copy now treats missing Telegram/YouTube probes as an intentional `нет проб` blind spot caused by rare checks, low memory, or non-running PassWall rather than as an outage. Auto-rescue now requires distinct blocked reachability `checkedAt` values before opening Telegram/foreign reachability cases, preventing repeated cached blocked snapshots from triggering repair loops. Local proof: full `@vectra/web` Vitest, `typecheck`, `lint`, and `build` are green.

- 2026-05-11 fleet service outage visibility: `fleet.monitoring` now raises Telegram/YouTube service-degraded alerts for reachable routers with `partial` or `blocked` probes, while the `Парк` surface exposes top quick filters for `Telegram сбои`, `YouTube сбои`, and `Нет проб`. The new `Нет проб` slice is a monitoring blind-spot indicator for fresh routers that did not send Telegram/YouTube probes, not an outage claim. Local proof is green on full `@vectra/web` Vitest, `@vectra/web typecheck`, `@vectra/web lint`, `@vectra/web build`, and `git diff --check`.

- 2026-05-11 low-memory package-status command hardening: the production web surfaces that generate router-side scripts no longer emit `opkg status` for controller self-update verification or AX3000T bootstrap package metadata reads. They read `/usr/lib/opkg/status` directly with `awk`, avoiding redundant `opkg` process forks during future controller updates/bootstrap repair flows. Verified with targeted Vitest (`install-presets`, `destructive-gating`), `@vectra/web typecheck`, production Docker build, smoke-check, and live grep/curl checks after deploy.

- 2026-05-11 `sergeyavito` onboarding: the production panel import/save/apply/re-import lane successfully configured new router Сергей Авито from first `pending/import_review` to clean `active/approved`. After approval, the operator draft applied the Google DoH contour and fleet ShuntRules, then a second narrow draft moved `Special` from plain NL `8deFvXR0` to the live-good RU-entry NL node `6rjcPYsb` after router-side `url_test_node` returned `000` on the plain NL node twice. Final editor state is `live-import`, `requiresReimport=false`, `digestMismatch=false`, no router/panel unconfirmed changes, and `pendingChanges=0`.
- 2026-05-11 live `1111111111` drift check: the production panel correctly exposed the post-refresh live import and apply receipts, which made it possible to prove the drift was not a hidden operator draft. Product rule was clarified: subscription `auto_update=1` should remain enabled, and the router controller should reconcile Vectra-managed shunt route intent after native PassWall subscription refreshes. The web/control-plane active revision stays approved while controller `0.1.13-r13` performs router-side self-heal; live proof on `1111111111` shows no queued update, controller/LuCI `0.1.13-r13`, `auto_update=1`, and restored shunt targets after a bounded drift injection.
- 2026-05-08 router-detail UX simplification: the approved router happy path no longer presents `import/re-import/trust` as a normal operator ritual. `ImportReviewActions` now collapses the manual router read into a secondary `Сверка с роутером` disclosure shown as normal working state, while onboarding/conflict paths use plain Russian around `стартовая база`, `сверка`, and `обновить данные с роутера`. The main action rail now says `Правки и применение на роутер`, `Сохранить черновик`, and `Сверка состояния`. Production browser proof on `1111111111` reports no visible bad import copy on the happy path and no console errors.
- 2026-05-08 router-detail re-import refresh: production router detail no longer relies on a single SSR/initial tRPC snapshot while a transient `requiresReimport` or drift state is clearing. `apps/web/src/components/router-detail-workspace.tsx` now refetches `draft.editorSurface` on mount/window focus/reconnect and polls during re-import or unconfirmed-change states. Live verification on `1111111111` after deploy shows no stale `Нужен re-import` banner in the browser, `configTrust.requiresReimport=false`, `configSourceMode=live-import`, the DiscordVoiceUdp shunt target remains `dqnJE64Y`, and the browser console is clean after escaping the hostname input pattern for modern HTML `v` regex validation.
- 2026-05-08 ShuntRule post-apply reset fix: live safe-probe testing on router `1111111111` showed the panel reset window was a workspace-base selection bug, not a failed UCI write. After `apply_passwall_config` succeeded, `draft.editorSurface` could choose an older matching live-import revision over the newer applied operator revision until the next import/check-in caught up. `apps/web/src/server/vectra/draft-selection.ts` now picks the newest baseline among current live import, imported baseline, and active applied revision, so the just-applied ShuntRule state stays visible in `draftConfig` during `stale-authoritative`. Deployed to production web and verified by adding/changing/removing a safe empty `CodexApplyProbe` ShuntRule on `1111111111`; final router-side UCI grep returned empty, so the probe was cleaned up.
- 2026-05-08 subscription prefix production deploy: the web slice carrying subscription ID canonicalization and the draft-selection catch-up guard is live on `router.vectra-pro.net`; production smoke is green and `draft.editorSurface` now returns canonical `draftConfig` subscription IDs for online routers (for example `1111111111` shows `vectra_sub_subscribe_list1` instead of the repeated-prefix chain).
- 2026-05-08 subscription prefix normalization: live read-only `draft editor` checks showed every fleet router carrying repeated `vectra_sub_` prefixes on the BloopCat `subscribe_list` ID after repeated apply/import cycles. `apps/web/src/server/vectra/subscription-runtime.ts` now collapses repeated Vectra subscription prefixes in the editor read-model so router detail shows the canonical single-prefix ID instead of an ever-growing internal section name. Local proof is green on targeted `subscription-runtime`/`passwall-contracts` Vitest, `@vectra/web lint`, focused TypeScript check, and `@vectra/web build`.
- 2026-05-08 upstream PassWall parameter parity: router detail now exposes/gates the latest upstream PassWall2 `26.5.1-1` option surface that matters for the panel: ShuntRule `quic`, node `mkcp_mtu`, node `tls_pinSHA256`, and per-subscription domain resolver fields from the newer `26.4.20+` line. The UI uses router `luci-app-passwall2` version telemetry and keeps unsupported controls inactive with explicit `Работает только с PassWall2 ...+` copy instead of letting operators save options older routers cannot use.
- 2026-05-08 PassWall parity follow-up: production router detail now surfaces node-local Xray/Mux/XUDP extras (`mux`, `mux_concurrency`, `xudp_concurrency`, `packet_encoding`) in the Node editor and read-only runtime node details. `draft.editorSurface` also registers dynamic PassWall extras for node/socks/subscription/global/rule/app surfaces, so imported extras that are preserved by apply now appear in panel diffs instead of hiding behind raw JSON. Verified locally with targeted Vitest, typecheck, lint, build, and in production with smoke/health plus live `draft.editorSurface` markers on `1111111111`.
- 2026-05-08 Shunt target apply fix: router detail now normalizes ShuntRule target bindings before validation/save/apply, so `basicSettings.shuntRules[*].outboundNodeId` cannot diverge from `myshunt` shunt-slot extras such as `WorldProxy`, `YouTube`, `Special`, `Tiktok`, or `DiscordVoiceUdp`. This closes the production symptom where apply jobs succeeded but server choices did not change. Deployed to production web and verified with targeted Vitest, typecheck/build, smoke/health, and browser route smoke.
- 2026-05-08 ShuntRules panel recovery: production router detail now exposes PassWall2 ShuntRule extras in Basic Settings (`protocol`, `inbound`, `network`, `source`, `port`, `invert`) and allows direct friendly router selectors like `/routers/1111111111?...` to redirect to the UUID route instead of throwing `Invalid uuid`. Verified with targeted Vitest, `@vectra/web typecheck`, `@vectra/web build`, production smoke/health, and headless browser proof on router `1111111111` showing `DiscordVoiceUdp` with `Transparent proxy`, `UDP`, and `19294-19344,50000-50100`.
- 2026-05-07 live DNS rollout: the operator panel apply lane successfully propagated the `1111111111`-tested Google DoH DNS contour to the online non-`hh` fleet (`AlekseyHorev`, `Vasily_Filicity`, `DmitryGubenko`, `AndreyVK`, `artempushkino`, `AndreyVK_Sochi`, `sairoutermsk`, `VagrandRouter`). Target DNS is `remoteDnsProtocol=doh`, `remoteDns=8.8.8.8`, `remoteDnsDoh=https://dns.google/dns-query`, `remoteDnsDetour=direct`, with `dns.google` host pins. `hh` remained excluded/no-touch, and stale `testrouter` was not queued because it already had 3 pending jobs. Verification used `scripts/VectraPanelCli.sh status|fleet list|draft editor|terminal run`: all eight target editor/live surfaces and router-terminal UCI reads match the source DNS, all eight target WorldProxy `url_test_node` checks returned `204`, open incidents stayed `0`, and queued jobs drained back to the pre-existing stale `testrouter` queue. A later fleet snapshot had Telegram/YouTube reachable on all online routers except a stale/partial YouTube snapshot on `AlekseyHorev`; direct terminal YouTube probes there were mostly green with one transient timeout.
- 2026-05-06 bootstrap PassWall start guard follow-up: the generated AX3000T installer no longer installs `vectra-controller-agent` after starting PassWall2. Root cause for the field stop was that bootstrap could start PassWall2 while `myshunt` was still selected but its Default/default_node remained a non-server fallback, so the later Vectra feed download could fail at `opkg_download` even though the artifact URL itself was healthy. `apps/web/src/app/enrollment/install-presets.ts` now auto-binds `myshunt.default_node` to the unique WorldProxy server remark, installs controller/LuCI packages before PassWall2 service start, and guards startup so PassWall2 is left disabled when the selected node/default is direct/default/blackhole/missing/nested-shunt. `install-presets.test.ts` pins the default-node rebind and execution order. Local proof is green on targeted Vitest, `@vectra/web typecheck`, `@vectra/web build`, and scoped diff check. This is deployed to production: release-slice sync created `/opt/vectra-prorouter-backups/web-release-20260506-144205`, Docker rebuilt `vectra-web`, smoke returned `307/200/200/200/400`, and live install scripts expose the new default-node rebind and PassWall start guard.
- 2026-05-06 local DNS-default follow-up: `apps/web/public/install/ax3000t-passwall2-baseline.uci`, the AX3000T global rollout seed, rollout-profile defaults, and global-template validators now target Google DoH (`8.8.8.8` plus `https://dns.google/dns-query`) with direct DNS detour and `dns.google` host pins. The previous UDP default remains historical context only. Local proof is green on `pnpm --filter @vectra/web test --run global-template`, `pnpm --filter @vectra/web test --run install-presets`, `pnpm --filter @vectra/web typecheck`, `pnpm --filter @vectra/web lint`, `pnpm --filter @vectra/web build`, and scoped `rtk git diff --check`. This was source-only; no live router apply was queued, and `hh` is a no-touch exception for future DNS rollout.

- 2026-05-05 Cudy WR3000H opkg architecture/libc hotfix: the affected router reached controller install but `opkg` refused base dependencies with `Packages ... found, but incompatible with the architectures configured`, ending at `Cannot satisfy ... libc` for `vectra-controller-agent`. `apps/web/src/app/enrollment/install-presets.ts` now adds `ensure_opkg_architecture()` before feed update/install, backing up `/etc/opkg/arch.conf` and ensuring `all`, `noarch`, and `DISTRIB_ARCH` (`aarch64_cortex-a53` on this target) are configured. This avoids telling operators to install libc manually and fixes the resolver layer instead. Local targeted Vitest, generated-shell syntax checks, `@vectra/web typecheck`, and `@vectra/web build` are green; production live script contains the new arch repair markers.
- 2026-05-05 Cudy/WR3000 controller-only bootstrap fallback: after the live router reached the storage-aware preflight, `/overlay` had only about `16.9 MB` available while `xray-core` alone needed about `30.3 MB`. The generated public bootstrap now treats this exact fresh-install low-overlay class as a controller-first lane: it detects PassWall2/Xray stack overflow, logs `controller-only bootstrap`, skips the installer-side PassWall2/Xray package/baseline/service steps, still installs and starts `vectra-controller-agent` plus LuCI, and tells the operator to finish PassWall2/Xray from the Vectra panel after first check-in. Local proof is green on targeted Vitest, generated-shell `bash -n`/`sh -n`, `@vectra/web typecheck`, and `@vectra/web build`; production `/install/ax3000t-bootstrap.sh` is redeployed and contains `CONTROLLER_ONLY_BOOTSTRAP`, `mark_controller_only_bootstrap()`, and the deferred PassWall2/Xray copy.
- 2026-05-04 Cudy/WR3000 bootstrap preflight hotfix: a live user rerun on OpenWrt `24.10.4` stopped at `В доступных OpenWrt feeds не найден обязательный пакет: libc`. The root cause is web-generated install preflight treating every PassWall2 dependency as feed-advertised, while OpenWrt `libc` is image/base-manifest installed and not necessarily present in `opkg list`. `apps/web/src/app/enrollment/install-presets.ts` now accepts already-installed packages before checking feed availability, and `install-presets.test.ts` pins that order. Local proof is green on targeted Vitest, `@vectra/web typecheck`, and `@vectra/web build`; the production `/install/ax3000t-bootstrap.sh` route was redeployed via the guarded release lane and now exposes the installed-first check with the old `В доступных OpenWrt feeds...` marker absent. The only remaining proof gap is affected-router rerun.
- 2026-05-04 Cudy/WR3000 opkg-update follow-up: the affected OpenWrt `24.10.4` router next stopped because `opkg update` returned non-zero after one target feed download failed with `wget returned 4`, even though other feed lists updated. The generated bootstrap now wraps `run_opkg update` in a warning-only branch and lets the existing package-specific preflight decide whether a required package is actually missing before any install/reclaim step runs. Local targeted Vitest, `@vectra/web typecheck`, and `@vectra/web build` are green; production `/install/ax3000t-bootstrap.sh` now contains the warning-only branch and health/smoke is green after redeploy. The remaining proof gap is the next affected-router rerun.
- 2026-05-04 Cudy/WR3000 libc-musl follow-up: the affected router still failed on `libc` after the installed-first check, so this firmware path does not expose base-image `libc` through `opkg status`. `install-presets.ts` now adds `openwrt_base_prereq_present()` and accepts `libc` when `/lib/libc.so` or `/lib/ld-musl-*.so.1` exists before checking opkg status/list. Targeted Vitest, `@vectra/web typecheck`, and `@vectra/web build` are green; production `/install/ax3000t-bootstrap.sh` now exposes the musl detector and standard public smoke is green. The remaining proof gap is the next affected-router rerun.
- 2026-05-03 production hotfix: live enrollment for stock-hostname `OpenWrt` was failing with repeated HTTP `400` on `/api/router/register` because the web-side contract rejected imported PassWall2 `socks` protocol nodes. After adding `socks` to the shared node protocol schema, rebuilding and redeploying `vectra-web`, the same source retried successfully with HTTP `201`; the router now appears as `OpenWrt` in `pending/import_review`.
- A 2026-04-19 local subscription source-of-truth follow-up now fixes the router-specific subscription/runtime ambiguity without treating `VagrandRouter` as a global baseline. `apps/web/src/server/vectra/draft-selection.ts` now picks the workspace base from the router's own latest editable operator draft (`draft/queued/failed`) or, when that does not exist, from the router's own current live import instead of a stale historical `operator_draft(status=applied)`; `apps/web/src/server/vectra/subscription-runtime.ts` and `subscription-audit.ts` now model subscription identity as `remark + sha256(url)`, node identity as a semantic fingerprint rather than PassWall section id, split router detail into editable manual nodes versus runtime managed/orphan subscription nodes, and expose only safe audit metadata such as `urlHash`, payload mode, and count/drift summaries without raw subscribe URLs. `apps/web/src/server/vectra/editor-surface.ts`, `src/server/api/routers/draft.ts`, `src/server/vectra/editor.ts`, and `src/components/router-detail-workspace.tsx` now use that read-model end-to-end, so stale applied drafts remain in revision history but no longer become the working editor base once a newer confirmed live import exists. Local proof is green on `pnpm --filter @vectra/web test --run draft-selection subscription-runtime editor-surface editor`, `pnpm --filter @vectra/web lint`, `pnpm --filter @vectra/web exec tsc --noEmit --incremental false --pretty false --allowJs true --checkJs false`, and `pnpm --filter @vectra/web build`. The motivating read-only production facts remain the same on the old deployed code path: `VagrandRouter` still has a latest snapshot and active revision at `17` nodes / `1` subscription while the last historical panel draft still stores `46` nodes / `1` subscription, so final operator UI proof for the new runtime/manual/orphan split still requires deployment of this web slice.
- A 2026-04-19 local UX-structure follow-up now makes the main operator surfaces read in one fixed order without changing contracts or flows: `apps/web/src/components/router-detail-workspace.tsx`, `fleet-monitoring-workspace.tsx`, `router-card.tsx`, `src/lib/router-config-trust.ts`, and `src/server/vectra/presentation.ts` now present router state as (1) what is happening now on the router, (2) what is saved in the panel, and (3) how much the panel trusts that comparison and why. The copy was narrowed toward plain Russian panel guidance instead of internal implementation jargon, while polling, tabs, mutations, routes, and data sources remained intact. Local proof is clean LSP diagnostics on all touched files plus green `pnpm --filter @vectra/web exec next typegen` and targeted `pnpm --filter @vectra/web test --run presentation`; the later `2026-04-20` cleanup also closes the old repo-wide typecheck tail by making `typecheck` self-generate Next route types and by fixing the `mobile-action-qa.spec.js` Playwright import/dependency path.
- A 2026-04-19 local trust-model follow-up now makes the operator UI honest about what is actually live versus merely authoritative. `apps/web/src/server/vectra/config-trust.ts` derives `live-import | authoritative | stale-authoritative | inventory-only` purely from the existing snapshot digest, authoritative digest, live import revisions, and reachability; `draft.editorSurface`, `fleet.monitoring`, `fleet.list/byId`, router cards, router detail, import/re-import actions, and draft/apply warnings now all use that same trust state so `approved` no longer implies “the panel knows current ShuntRules on the router.” Deep PassWall sections explicitly stay import-based until a matching live import exists, while snapshot-live facts such as selected node, versions, service health, and Telegram reachability remain visible as live telemetry. Local proof for this slice is green on targeted `config-trust` / `editor` / `fleet-monitoring` / `fleet-monitoring-data` / `presentation` Vitest suites plus `pnpm --filter @vectra/web lint` and `pnpm --filter @vectra/web build`; the separate `tests/mobile-action-qa.spec.js` Playwright/typecheck debt is now closed, and repo-wide `pnpm typecheck` is green after a cold `.next/types` wipe.
- A same-day production follow-up now also verifies the live operator contour rather than leaving this trust model as mostly local evidence. Using the tracked `scripts/VectraPanelCli.sh` helper against `router.vectra-pro.net`, the current fleet shows exact live-import review states on the routers inspected: `OpenWrt` still surfaces router-side pending import review with one exact field diff (`basicSettings.log.level: error -> warning`), `1111111111` still surfaces panel-side saved-draft-pending-apply with exact itemized changes, and additional `out_of_sync` routers such as `VagrandRouter` and `Vasily_Filicity` currently also report `configTrust.requiresReimport = false` with `configSourceMode = live-import`. The remaining unproven contour is now narrower and more concrete: no inspected live router currently exhibits the digest-only `stale-authoritative` / `re-import needed` fallback, because fresh imported revisions are arriving before that UI state can be observed.
- A 2026-04-17 local-plus-live bootstrap follow-up now closes the remaining Xray gap in the generated AX3000T install script: `apps/web/src/app/enrollment/install-presets.ts` no longer passes raw GitHub asset bytes into PassWall2 `api.to_download()`, but converts `data.size` to KiB the same way LuCI `App Update` does, and the generated script now runs a dedicated post-install `Xray` convergence step through the built-in PassWall updater for every bootstrap lane instead of only the reuse path. Local `@vectra/web` `test --run`, `typecheck`, and generated-script `sh -n` are green again; attended LAN verification on test router `OpenWrt` (`192.168.99.1`, `cudy,wr3000e-v1`, OpenWrt `24.10.5`) first proved the exact bug and fix end-to-end, then proved the full bootstrap itself on that same router: the script completed successfully in `upgrade existing PassWall stack` / reuse-lane mode, preserved one existing subscription plus imported nodes, re-bound all four `myshunt` slots by remark, kept both `passwall2` and `vectra-controller` running, and left the router on `passwall_node=myshunt` with runtime `xray version` `26.4.15` while `opkg` intentionally remained on package record `25.10.15-r1`.
- Пакет `@vectra/web` использует Next.js `15`, React `19`, tRPC `11`, Drizzle и Zod.
- Есть локальные команды `build`, `dev`, `lint`, `typecheck`, `test`, а также DB-скрипты через Drizzle.
- Production `next build` проходит и реально собран в Docker image на pilot VPS.
- Панель доступна через `https://router.vectra-pro.net`, router-facing REST доступен через `https://api.vectra-pro.net/api/router/*`.
- Operator auth уже работает через app-level login с `httpOnly` cookie; операторский root может легально редиректить `307` на `/login`, а `/api/health` используется как live health endpoint.
- Server-side artifact lane теперь имеет bounded admin tooling: `apps/web/scripts/sync-artifact-metadata.mjs` умеет читать OpenWrt feed `index.json`, считать SHA-256 опубликованных файлов и upsert-ить `artifacts`/`firmware_manifests`.
- Artifact lookup для router-facing API больше не отбрасывает подходящий артефакт из-за постфильтра после `limit 1`; фильтрация по `architecture`, `boardName` и `layoutFamily` перенесена в сам DB query.
- На production VPS уже реально выполнен `sync-artifact-metadata.mjs --apply` для controller feed; записи `vectra-controller-agent` и `luci-app-vectra-controller` обновлены в live PostgreSQL.
- Live router-facing import-first contract уже прошёл production path: register/check-in на `api.vectra-pro.net` создают imported baseline revision, а operator approve на `router.vectra-pro.net/api/trpc/fleet.approveImportedBaseline` реально переводит роутер в `approved`/`active`.
- Production DB сейчас подтверждает полную import-review цепочку для первого AX3000T: `vectra_router.import_state`, `pending_import_revision_id`, `active_revision_id`, `approved_at` и audit trail в `vectra_event_log` реально обновляются штатным кодом.
- После live remediation operator-facing routes больше не отдают `rawImportedSnapshot` в `fleet.*` и `draft.*` responses; текущая historical imported revision в production была отдельно очищена от сырого snapshot payload.
- Env-backed `Basic Auth` заменён на app-level operator login с `httpOnly` cookie session; route conflict на `/login` устранён переносом auth handlers в `/api/operator/login` и `/api/operator/logout`.
- В `draft.editorSurface` теперь используется единый server-side read model из `src/server/vectra/editor-surface.ts`, который возвращает masked fields, freshness, richer runtime summary и normalized apply history.
- `/routers/[routerId]` переведён в router-centric stable screen: русский операторский overview, import review actions, form-based editor, exact operation preview, apply history, version drift и rescue summary.
- В stable router detail добавлен операторский action `Отключить аварийный режим`: mutation `api.rescue.triggerReconnect` теперь ставит `reconnect` job с payload `{ resumeProxy: true, clearRescue: true }`, а client invalidates `fleet/rescue` caches и делает `router.refresh()`.
- Router detail уже различает live и stale rescue context: при отсутствии свежего check-in экран явно предупреждает, что режим/причина rescue ниже являются последней известной серверу записью и что команды будут доставлены только после нового controller check-in.
- `editor-surface` дополнительно синхронизирован с presence-логикой: если роутер не reachable по `lastSeenAt`, `Freshness` и field-level badges больше не остаются `live`, а переходят в `stale`.
- Live browser-проверка на `router.vectra-pro.net` уже подтвердила production surface для проблемного AX3000T: fleet card показывает `Нет связи`, router detail рендерит `Свежей связи нет`, disabled button `Роутер офлайн`, а рабочая поверхность помечает поля как `SOURCE STALE`.
- На живом recovery-пути AX3000T уже подтверждён следующий production state: после локального восстановления controller router снова делает check-in на `api.vectra-pro.net`, `fleet` показывает `Proxy mode` вместо ложного degraded/direct, `rescue`-экран не содержит live direct routers, а router detail снова работает в `APPROVED`/`live` режиме.
- Operator UI уже реально использовался для повторного approve свежего imported baseline revision `#15`, после чего `vectra_router.import_state=approved` и рабочая поверхность снова показывает live/current values для form-based editing.
- Queue/update lanes на web side теперь формируются через typed payload schemas из `@vectra/contracts`, а не только через ad hoc JSON payload.
- `update.ts` теперь кладёт в controller jobs explicit `packageArtifacts` для `vectra-controller-agent` и `luci-app-vectra-controller`, а firmware validation jobs несут `validationCommand` вместе с artifact metadata.
- Released production web build now also carries the expanded PassWall2 recovery defaults for `update_passwall_packages`, so queued jobs include `dnsmasq-full`, `chinadns-ng`, `v2ray-geo*`, and nft runtime kmods alongside the controller/LuCI package artifacts.
- Production `next build` for the released tree passed on the VPS after syncing the missing `src/server/vectra/support.ts` export and the `router-console.ts` type-guard hotfix; the local workspace still contains unrelated pending `.next` typing noise and `router-editor-state` schema/test drift outside this release slice, so repo-wide local green validation cannot currently be claimed.
- Живой `POST https://api.vectra-pro.net/api/router/register` уже принимает `passwallImport` с `transport="xhttp"` и возвращает `201`, то есть production router-facing schema синхронизирована с локальным контрактом.
- После прод-деплоя stale rescue cleanup backend больше не держит `last_rescue_reason` бесконечно: для AX3000T production DB уже очищается до пустого значения после очередного `proxy` check-in, а новый `resolveRescueReason()` покрыт unit-тестом.
- `deploy/scripts/smoke-check.sh` и runbook выровнены с новой auth-моделью: `307` на operator root больше не считается ложной production-ошибкой.
- HTTP smoke c реальным operator login уже подтверждает актуальный stable surface: `POST /api/operator/login` возвращает `303`, `/routers/a1c21287-d35f-4d7c-b8e0-a735b699898b` после cookie-auth отдаёт `200`, страница содержит `Прокси-режим` и `утверждено`, а старая stale rescue phrase больше не попадает в HTML.
- Первый production authoritative apply cycle на живом AX3000T пройден через operator API: draft `#20` изменил `basicSettings.log.level` с `error` на `warning`, job `2072eced-...` завершился `succeeded`, сервер записал normalized apply receipt и новый digest, а роутер остался `enabled=1`, `node=myshunt`, `rescueMode=proxy`.
- Во время первого apply обнаружен и исправлен stable-critical bug: post-apply import раньше переводил роутер в `out_of_sync`; теперь `shouldPromotePostApplyImport()` автоматически промотирует live import, если он пришёл с известным `appliedRevisionId` после server-issued apply.
- Rollback cycle тоже пройден в production: draft `#22` вернул `basicSettings.log.level` в исходный `error`, job `8ef4a21d-...` завершился `succeeded`, router record остался `active|approved`, `pending_import_revision_id` пустой, `active_revision_id` и `last_applied_revision_id` указывают на rollback draft, а `last_rescue_reason` пустой.
- В текущем workspace router detail больше не встраивает обязательный expert JSON editor в основной экран: основной путь остаётся form-based, а `/drafts?routerId=...` оставлен как явный резервный экспертный режим.
- Локальный operator UI в `apps/web` теперь переложен на плотный LuCI/PassWall-подобный chrome: глобальная навигация сохраняет русские маршруты `Парк/Черновики/Обновления/Восстановление/Установка`, а `/routers/[routerId]` перестроен в PassWall-recognizable workspace с primary tabs, secondary tabs для `Basic Settings`, status tiles, compact summary/actions и master-detail редакторами для node/subscription/shunt lists без fake live actions.
- Локальный follow-up `2026-04-18` закрыл ещё один router-detail UX-баг в этом PassWall workspace: переключение `DNS -> App Update` и другие `tab/section` переходы в `apps/web/src/components/router-detail-workspace.tsx` теперь идут через локальное состояние вкладок и route-local `history.replaceState(...)`, поэтому панель меняет только содержимое workspace и сохраняет `window.scrollY` вместо page-level jump/reload. Targeted `router-console` Vitest и file-level ESLint зелёные; последующий cleanup `2026-04-20` также закрыл старую unrelated дыру вокруг `tests/mobile-action-qa.spec.js`: spec импортирует `@playwright/test`, пакет объявлен в devDependencies, а cold repo-wide `pnpm typecheck` теперь проходит.
- Browser-level smoke на live operator UI выявил oversized `draft.preview` query на router detail; локальный tRPC browser link теперь принудительно использует `POST` через `methodOverride`, чтобы большие PassWall2 payload не превращались в длинный query string.
- После локального polish-pass остаточные пользовательские labels `Target`, `Config digest`, `cooldown`, `package-first`, `Firmware`, `rollout` и raw artifact channel/type output заменены на русские формулировки. Локально после этого прошли `pnpm --filter @vectra/web typecheck` и `pnpm --filter @vectra/web test --run`.
- После PassWall-aligned redesign локально снова зелёные `pnpm --filter @vectra/web typecheck`, `pnpm --filter @vectra/web test --run` и `pnpm --filter @vectra/web build`; build проходит с уже существующим lint warning в `src/server/api/routers/rescue.ts` про `import()` type annotations, не связанным с новым UI-срезом.
- Public contour smoke после локальных изменений снова зелёный: operator root возвращает `307`, `/healthz` и `/api/health` возвращают `200`, router API `/healthz` возвращает `200`, empty register возвращает ожидаемый `400`.
- Live deploy нового PassWall-aligned shell на `router.vectra-pro.net` уже выполнен и browser smoke пройден на desktop/mobile: `fleet`, `router detail`, `Node List`, `Node Subscribe`, `App Update`, `Rule Manage`, `Geo View`, disabled PassWall tabs, compact tables и query-param deep links реально рендерятся в production.
- Первый live redeploy нового shell вскрыл stable-critical defect: client уже слал `draft.editorSurface` как `POST`, но production tRPC handler не разрешал method override, из-за чего router workspace зависал на `Загружаю рабочую поверхность роутера...`; после включения `allowMethodOverride: true` в `/api/trpc/[trpc]` production router detail снова загружается штатно.
- Второй micro-hotfix после live smoke добавил `name` к router/draft form controls и убрал DevTools issue про `form field element should have an id or name attribute`; после этого production router detail рендерится без console issues.
- Follow-up parity hotfix against live user feedback теперь закрывает основные расхождения с PassWall на production router workspace для AX3000T `bdfdb919-5e06-4344-ad8b-67a16f3b6fcf`: `Shunt Rule` показывает `Default node/default preproxy`, `FakeDNS` и полный набор target modes (`Close`, `Use default node`, `Direct Connection`, `Blackhole`, реальные ноды), `Node Subscribe` показывает расширенные subscription fields и real `Обновить подписки сейчас`, а `Rule Manage` показывает точные schedule selectors и real `Обновить GEOIP и GEOSITE`; это browser-smoke подтверждено на production без запуска live jobs.
- 2026-04-08 live follow-up по операторскому фидбеку дополнительно перевёл `App Update` на component-scoped jobs вместо одного комбинированного обновления: `api.update.queuePasswallPackageUpdate` теперь принимает optional `packages`, production `App Update` показывает отдельные кнопки `Обновить PassWall2`, `Обновить Xray`, `Обновить sing-box`, `Обновить Hysteria`, `Обновить Geoview`, а `Shunt Rule` стабильно находит shunt-node даже для исторических import surfaces через эвристику `default_node/default_fakedns/default_proxy_tag/fakedns` и label-based fallback; локально после этого снова зелёные `pnpm --filter @vectra/web typecheck`, `pnpm --filter @vectra/web test --run`, `pnpm --filter @vectra/web build`, а production browser snapshot подтверждает `Default` section, полный target-mode selector и per-component update buttons.
- 2026-04-08 side-by-side comparison with the live LuCI `Rule Manage` screen is now closed on the web surface too: `apps/web` split the shunt editors by intent, so `Basic Settings > Shunt Rule` stays on `Target/FakeDNS/preproxy`, while production `Rule Manage` now shows a dedicated PassWall-like shunt match editor with `Name`, `Remarks`, `Protocol`, `Inbound Tag`, `Network`, `Source`, `Port`, `Domain`, `IP`, and `invert`; local `typecheck/test/build` are green again, the redeployed page was browser-verified on `router.vectra-pro.net`, and the live AX3000T workspace now matches the real LuCI mental model instead of reusing the outbound-focused editor.
- 2026-04-08 router cleanup lane is now implemented in the operator UI and deployed live: `api.fleet.deleteRouter` deletes a router with DB cascades plus audit event `router.deleted`, `Парк` cards now expose `Удалить из системы`, router detail shows `Удалить роутер из системы`, local `pnpm --filter @vectra/web typecheck/test/build` are green, production `vectra-web` was rebuilt successfully, and authenticated browser smoke confirms the new delete buttons render on `fleet` and on the live AX3000T detail page. A stale test router without a healthy editor surface still hangs on `Загружаю рабочую поверхность роутера...`, which appears to be pre-existing data debt rather than a regression in the delete flow.
- 2026-04-08 router action UX is now clearer on the live operator page too: the former ambiguous `Поставить в очередь` action was replaced with explicit `Сохранить и применить на роутере`, which first saves the current fields when needed and then queues `draft.queueApply`; the draft-only path now reads `Сохранить только в панели`; import actions now read `Принять импорт как эталон` and `Считать конфигурацию с роутера заново` with inline explanation text. Local `pnpm --filter @vectra/web typecheck/test/build` passed, the production `vectra-web` rebuild stayed green, and authenticated browser smoke confirmed the new labels on the live AX3000T detail page.
- Local enrollment/install lane now also has an AX3000T-specific bootstrap surface under `Установка`: `apps/web` serves a generated `/install/ax3000t-bootstrap.sh` route plus a static sanitized baseline file under `/install/ax3000t-passwall2-baseline.uci`; the page explains the current per-router authoritative-config model, exposes copy-ready bootstrap/install commands, keeps only the shunt structure from the live router config, and explicitly strips subscriptions plus real proxy nodes. On 2026-04-08 this slice was redeployed to production, `vectra-web` was rebuilt successfully, authenticated HTTP smoke confirmed `/enrollment` renders the bootstrap/baseline blocks after login, and the published `/install/ax3000t-bootstrap.sh` now passes `sh -n` after fixing the shell quoting in `DISTRIB_ARCH` detection plus the package-index parser.
- A same-day bootstrap reliability hotfix now also fixes the empty-router install path: `apps/web/src/app/enrollment/install-presets.ts` no longer assumes PassWall2 packages exist in the Vectra feed, downloads the official PassWall2 `26.4.5-1` release assets directly (`luci-app-passwall2` plus `passwall_packages_ipk_aarch64_cortex-a53.zip`), installs bundle-backed components (`xray-core`, `geoview`, `v2ray-geo*`, `chinadns-ng`) from that archive, explicitly replaces stock `dnsmasq` with `dnsmasq-full` before runtime install, and keeps controller packages on the signed Vectra feed. A second hotfix then removed the remaining tmpfs failure mode by moving the working directory from `/tmp` to `/root/vectra-bootstrap-work`, extracting only the selected `.ipk` files from the PassWall bundle instead of unpacking the full archive, and deleting the zip before `opkg install`. Local `@vectra/web` `typecheck/test/build` stayed green, production `vectra-web` was redeployed via the dedicated VPS key, public bootstrap fetch now returns `200`, and the published script text confirms the official PassWall release URLs, the `opkg remove dnsmasq` conflict fix, the persistent workdir, and the selective bundle extraction.
- The latest bootstrap remediation replaces that whole archive-based path with a slimmer per-package mirror: required PassWall2 `.ipk` files (`luci-app-passwall2`, `xray-core`, `geoview`, `v2ray-geo*`, `chinadns-ng`) are now mirrored under `https://api.vectra-pro.net/artifacts/bootstrap/passwall2/26.4.5-1/aarch64_cortex-a53/`, the generated router script downloads and installs them one-by-one from `/tmp/vectra-bootstrap-work`, and each file is removed immediately after `opkg install`. The same pass also hardened the `dnsmasq` swap by parking `/etc/config/dhcp`, removing stale `/etc/config/dhcp-opkg`, installing `dnsmasq-full`, then restoring the saved DHCP config and restarting dnsmasq. Local `@vectra/web` `test/typecheck/build` are green again, production `vectra-web` was rebuilt, and the live `/install/ax3000t-bootstrap.sh` route now no longer mentions `passwall_packages.zip` at all.
- A 2026-04-13 install-baseline hotfix now also disables `default_fakedns` and `direct_fakedns` in the published AX3000T baseline asset. The sanitized file served from `apps/web/public/install/ax3000t-passwall2-baseline.uci` no longer enables FakeDNS for `Default` or `Direct` shunt targets by default, and `src/app/enrollment/install-presets.test.ts` now locks that down with explicit regression assertions against both old `... '1'` values.
- A follow-up bootstrap hardening pass is now also committed locally: `apps/web` split AX3000T bootstrap dependencies into explicit required/optional mirrored sets, added preflight checks before any `opkg install/remove`, restores `tcping` as a first-class mirrored package, and introduced checked-in fixtures plus tests that validate `luci-app-passwall2` dependency coverage against the mirrored set and upstream bundle contents. The repo-side script is now natively callable as `scripts/Sync-PasswallBootstrapMirror.py`, which assembles `deploy/runtime/artifacts/bootstrap/passwall2/<tag>/<arch>/` from upstream release assets and writes `manifest.json`; local web verification for the consuming slice stayed green on `pnpm --filter @vectra/web test --run` and `build`, while `typecheck` still depends on `.next/types` being present and only passes after `next build`.
- The same bootstrap hardening slice is now deployed on production too: the VPS artifact path under `api.vectra-pro.net/artifacts/bootstrap/passwall2/26.4.5-1/aarch64_cortex-a53/` now serves `tcping`, optional mirrored packages, and `manifest.json` with `200`, `vectra-web` rebuilt successfully, both `/api/health` endpoints stayed `200`, and the live `/install/ax3000t-bootstrap.sh` now renders `run_preflight_checks`, `require_remote_ipk tcping`, and `download_and_install_ipk tcping`. Router execution itself is still intentionally left for a user-run LAN-attended test.
- A follow-up 2026-04-08 enrollment hotfix now closes the next real bootstrap blocker too: the published baseline asset no longer contains literal `\toption` lines in the shunt node section, the generated script no longer auto-installs optional mirrored components like `sing-box` and `hysteria`, and baseline validation now prints an explicit `uci` parse error instead of failing silently before controller install. Local `pnpm --filter @vectra/web test --run`, `build`, `exec next typegen`, and `typecheck` are green, production `vectra-web` was rebuilt successfully, and live route checks confirm `/install/ax3000t-bootstrap.sh` no longer contains optional mirrored installs while `/install/ax3000t-passwall2-baseline.uci` serves the cleaned shunt mapping lines with `200`.
- A same-day 2026-04-13 bootstrap-merge follow-up now also fixes the destructive install path for routers that already had PassWall before the primary Vectra controller bootstrap: `apps/web/src/app/enrollment/install-presets.ts` no longer blindly overwrites `/etc/config/passwall2` with the sanitized baseline, but instead backs up the current config, appends preserved `config subscribe_list` and imported `config nodes` sections from the backup, skips duplicating baseline node `myshunt`, and validates the merged UCI before copying it into place. `src/app/enrollment/install-presets.test.ts` now locks this down with explicit script-text regression coverage, and local `pnpm --filter @vectra/web test --run`, `build`, and post-build `typecheck` are green.
- A later same-day 2026-04-13 router-workspace follow-up now also fixes the next subscription-loss path after bootstrap: the destructive removal was not happening on plain controller check-in, but on the next `apply_passwall_config`, because the router page built its editable draft from the stale active revision and omitted live-added `subscribe_list`/imported `nodes` that appeared on the router after that revision. `apps/web/src/server/vectra/editor-surface.ts` now merges live-only router-local subscriptions, nodes, and related SOCKS entries into the editable draft base when the router is currently reachable, and only promotes the live selected node when the stale draft did not know that node at all. `src/server/vectra/editor-surface.test.ts` now covers this merge explicitly, and local `@vectra/web` `test --run`, `build`, and in-app `typecheck` are green.
- A local 2026-04-10 bootstrap pass now also makes the AX3000T installer storage-aware for low-overlay routers before the first mutation: `apps/web/src/app/enrollment/install-presets.ts` moved the mirrored PassWall set to a declarative managed-package catalog with role/version/download-size/installed-size/order metadata, added a pure planner for `fresh install` vs `upgrade existing PassWall stack` vs `repair drifted managed packages`, and generates a shell preflight that measures real staging plus `/overlay` space, skips already-current packages, refuses reclaim when `opkg whatdepends` shows external dependents, and reinstalls `luci-app-passwall2` last only when needed. The same slice also tightens feed-metadata validation so missing `Size` fields abort instead of undercounting staging budget, while `scripts/Sync-PasswallBootstrapMirror.ps1` now publishes per-package `version`, `downloadSizeBytes`, and `installedSizeBytes` in `manifest.json`; local `pnpm --filter @vectra/web typecheck`, `test --run`, `build`, and a generated-script `bash -n` syntax check are green.
- 2026-04-08 visual polish pass now also reduces the Russian/English typography split on production web: `apps/web` switched the operator shell from latin-only `Space Grotesk` to a unified `IBM Plex Sans` + `IBM Plex Mono` stack with `latin,cyrillic` subsets, form controls now inherit the same font family, dense labels/chips use shared utility classes instead of over-spaced uppercase, and the router workspace status tiles/preview labels moved from generic English operator copy (`Controller freshness`, `Apply Preview`, `Restart`, `Subscriptions`, `Rules`, `Packages`) to Russian while leaving PassWall-recognizable tab names intact. Local `@vectra/web` `test`, `build`, and direct `apps/web` `typecheck` are green; production `vectra-web` was rebuilt and public smoke stayed on the standard `307/200/200/200/400` contour.
- 2026-04-08 diagnostics follow-up is now live-proven on production web: `apps/web` gained `logs` tRPC routes, a router-log history mapper, and a `Watch Logs` workspace section for queued read-only `collect_router_logs` snapshots, and the release had to be deployed together with `packages/contracts` plus `packages/db` because the shared exports are part of the web build. After publishing and installing `vectra-controller-agent` plus `luci-app-vectra-controller` `0.1.12-r2` on the live AX3000T, `Watch Logs` is no longer a placeholder: production DB now records accepted plus success receipts for real snapshot jobs, and authenticated browser refresh confirms both the earlier `Все источники / 200` snapshot and the newer `System Log / 50` snapshot render on `router.vectra-pro.net`.
- 2026-04-08 UX-clarity follow-up is now deployed and browser-verified on `router.vectra-pro.net`: a shared `OperatorWorkflowMap` appears across `Парк`, `Черновики`, `Обновления`, `Установка`, and router detail so the shell explicitly teaches scope, `Обновления` is now the honest reserved home for future fleet-wide rollout, and the production contour stayed green on the standard public smoke `307/200/200/200/400`. A new unit test covers the workflow-map ordering/emphasis.
- 2026-04-09 controller-version clarity hotfix is now live too: `App Update` on the router page now separates `Установлено` from `Доступно` for controller updates and labels the button as `Обновить до <version>` / `Переустановить <version>`, because production investigation showed the old table was surfacing only the installed controller version even though `vectra_artifact` already advertises `0.1.12-r2`. Local `@vectra/web` `typecheck/test/build` are green, production `vectra-web` was rebuilt through the narrow in-place lane, and both public health endpoints stayed `200`.
- A later 2026-04-09 UI-only controller-unknown follow-up now also treats raw controller sentinels (`unknown`, `неизвестно`, empty values) as an unknown state instead of a real installed version: `apps/web` gained a shared normalization helper, `App Update` now renders `Не удалось определить` and suppresses false `(актуально)` labels when comparison is impossible, router cards/fleet summaries no longer leak raw `unknown`, and router detail now shows a compact Russian diagnostic hint built from the latest `update_controller` job/result (`ошибка`, `ещё выполняется`, or `роутер не прислал версию`). Local `@vectra/web` `typecheck/test/build` are green for this follow-up too.
- A 2026-04-10 attended production click-through now closes the controller update UX loop too: on the live AX3000T router page, `App Update -> Controller -> Переустановить 0.1.12-r5` created real tRPC `update.queueControllerUpdate`, the page temporarily showed queue growth and then reloaded back to `очередь задач: 0`, and the final `App Update` row still shows installed `0.1.12-r5`, available `0.1.12-r5 (актуально)`, and `Переустановить 0.1.12-r5`. Production DB recorded job `e4e664d8-39f8-4076-ba0c-10dc30e26bfd` as `succeeded`, so the button is now live-proven and no longer just browser wiring.
- A later 2026-04-10 controller-cleanup follow-up now proves that the same operator flow is durable, not just clickable: live AX3000T testing showed published `0.1.12-r6` still recreated `/etc/config/vectra-controller-opkg`, because package `postinst` exited on self-update before running `90_vectra_controller_defaults`; the published `0.1.12-r7` release reordered that migration, production artifact metadata was resynced, and a fresh production click-through now lands on installed `0.1.12-r7` without recreating the shadow conffile or stale auto-discovered controller fields.
- A same-day 2026-04-09 support-gating hotfix is now live too: `apps/web` no longer blocks operator actions for same-platform Filogic routers that stay in `pilot` rather than `certified`. `canRunDestructiveAction()` and the router-detail/apply/update/rescue gates now allow both `pilot` and `certified`, leaving only truly unsupported `blocked` tuples forbidden; local `@vectra/web` `test/typecheck/build` are green again, production `vectra-web` was rebuilt, and public smoke stayed `307/200/200/200/400`.
- A later 2026-04-09 fleet-monitoring UX pass is now live too: the `Парк` page no longer repeats the shell navigation via `OperatorWorkflowMap`, but instead renders a live monitoring workspace backed by the new `fleet.monitoring` snapshot. Production `router.vectra-pro.net/fleet` now shows current fleet stats, clickable operational/freshness charts that filter router cards below, an alert rail with direct links into problematic routers, and a browser-notification toggle that honestly explains the current scope (`while the panel tab is open`, no fake push infrastructure). Local `@vectra/web` `test/typecheck/build` are green, public smoke stayed `307/200/200/200/400`, and browser verification on the live fleet page confirmed the new overview plus working chart-driven filtering.
- A same-night 2026-04-09 follow-up now upgrades that lane from in-tab polling to real background browser push on supported clients: `apps/web` gained `notifications` tRPC routes, persisted operator push subscriptions plus deduped alert records in PostgreSQL, a static service worker at `/vectra-push-sw.js`, and a server-side monitor that starts from the existing `/api/health` lane and emits push notifications for urgent fleet events (`offline`, `direct/rescue`, active incidents) even after the tab is closed. Local `@vectra/web` `test/typecheck/build` are green, production DB now contains `vectra_operator_push_subscription` and `vectra_operator_push_alert`, public `/vectra-push-sw.js` returns `200`, and the standard public smoke remained `307/200/200/200/400` after deploy.
- The 2026-04-13 global-template/fleet-rollout slice is now also deployed on production as part of the 2026-04-14 web release: `packages/db` gained `vectra_operator_global_template`, `apps/web` gained `src/server/vectra/global-template.ts`, `update.globalTemplateWorkspace/saveGlobalTemplate/queueGlobalTemplateRollout`, a dynamic baseline route at `/api/install/ax3000t-passwall2-baseline.uci`, and a real `Обновления` workspace with tabs for `Глобальный baseline`, `Массовая рассылка`, and `История`. The batch path still intentionally reuses existing per-router draft/apply semantics instead of inventing a new router-agent job type, keeps local subscription URLs/private nodes/selected node/SOCKS on the target router, and production browser smoke now confirms that `Обновления` renders this workspace and explains the rollout path without actually launching a fleet job on the live park.
- A 2026-04-14 production follow-up now also deploys and live-proves the bounded router-terminal slice on the web side: `apps/web` serves new `terminal` tRPC routes and a `RouterTerminalSection` inside `Watch Logs`, production `drizzle-kit migrate` applied the enum extension for `run_terminal_command`, PostgreSQL on the VPS now advertises that job type, and the rebuilt `vectra-web` returned to healthy `200/200` external `/api/health` after the usual short startup `503` window. On the local production router `1111111111` (`bdfdb919-5e06-4344-ad8b-67a16f3b6fcf`), `Watch Logs -> Terminal` then executed safe read-only command `ubus call system board`, queued real job `d54da8ba-d405-427b-9a3c-c00a8762cbea`, and rendered successful stdout with Xiaomi AX3000T model/board plus OpenWrt `24.10.6`, so the terminal lane is now end-to-end live-proven for bounded read-only commands.
- A later 2026-04-14 UX clarity follow-up is now deployed and browser-walked against production data rather than staying local-only: `apps/web` gained a shared plain-language onboarding mapper for router import states, `Парк` now includes an explicit new-router onboarding panel plus more actionable router cards, router detail reorganizes its top actions into clear groups (`Подключение и эталон`, `Сохранение и применение`, `Recovery и журналы`, `Удаление роутера`), `Установка` was reduced to a fast command + step-by-step onboarding guide with technical detail moved behind disclosures, and `Обновления` now explains the fleet-rollout path in a short 1-2-3 operator guide. Production browser smoke walked `Парк -> Установка -> Обновления -> router 1111111111`, desktop/mobile layouts stayed clean, query-param tabs still worked, and the browser console remained empty.
- A later 2026-04-14 production diagnostics follow-up is now live-proven rather than local-only: `packages/contracts` exposes optional `telegramReachability` in router inventory snapshots, `apps/web` maps it into router detail and fleet summaries, and controller/LuCI `0.1.12-r10` were installed through the production `App Update -> Controller` path on safe router `1111111111` (`bdfdb919-5e06-4344-ad8b-67a16f3b6fcf`). Production snapshots first reported Telegram `partial` because `t.me` timed out, then stabilized at `reachable` `4/4`; router detail rendered both the compact summary and the per-target breakdown for `telegram.org`, `web.telegram.org`, `t.me`, and `api.telegram.org`. The same live pass uncovered a fleet-only server mapping bug where `Парк` still showed `нет данных`; `apps/web/src/server/vectra/fleet-monitoring.ts` plus `fleet-monitoring-data.ts` were patched, local `@vectra/web` `test --run`, `build`, and post-build `typecheck` stayed green, production was redeployed, and browser smoke now confirms Telegram status in both router detail and fleet for the updated router.
- A later 2026-04-14 mobile-first polish pass is now also deployed and live-proven on production phone viewport, not just desktop: `PageHeader` and `OperatorWorkflowMap` switch to shorter mobile copy, router detail hides save/apply help and draft diagnostics behind mobile disclosures, `Обновления` shows only one heavy baseline editor at a time on small screens, `Черновики` and `Установка` use denser headers, and `Парк` router cards now keep only compact top facts visible while moving rescue/version diagnostics behind a mobile disclosure. Local `@vectra/web` `typecheck`, `test --run`, and `build` stayed green, production `/api/health` returned `200` after redeploy, and live mobile smoke covered `Парк`, `Черновики`, `Обновления`, `Восстановление`, `Установка`, and router detail.
- A new local 2026-04-15 control-plane refresh slice is now deploy-ready in the workspace, though still not production-walked: `apps/web` widened and densified the shell chrome further, tightened shared primitives (`PageHeader`, `Panel`, `ActionStrip`, `DataTable`), pushed `fleet-monitoring-workspace.tsx` harder toward a desktop-first table/control-plane layout with stronger right-rail context, restructured the top of `router-detail-workspace.tsx` into a more rational split between summary/context and a sticky action rail while preserving existing tabs, mutations, and route semantics, and then tightened the two main operator-risk follow-ups found in review by making horizontal table overflow explicit and selected rows in router-detail editors visually obvious. Local `npm exec --yes pnpm -- --filter @vectra/web typecheck`, `test --run`, and `build` are green; the earlier build warning in `src/server/api/routers/rescue.ts` about `import()` type annotations was also removed. Local browser smoke confirms authenticated rendering for `/fleet`, global command routing into `/drafts`, and the refreshed `Установка` / `Обновления` / `Восстановление` surfaces with clean console output and `200` network responses on the exercised routes.
- A later same-day local operator-admin redesign pass now pushes that refresh from “cleanup/polish” into a more coherent shell grammar without changing route semantics or backend contracts: `apps/web` now centralizes the visual language in `src/styles/globals.css`, `src/app/layout.tsx`, and the shared primitives (`panel.tsx`, `page-header.tsx`, `action-strip.tsx`, `data-table.tsx`, `status-tile.tsx`, `tab-bar.tsx`), rewrites `operator-shell-header.tsx` into a stronger persistent shell, further clarifies the top of `router-detail-workspace.tsx` into current-state → actions → editor flow while preserving existing tab/query/apply behavior, keeps `fleet-monitoring-workspace.tsx` table-first with secondary context, and normalizes the utility surfaces (`draft-workspace.tsx`, `global-template-rollout-workspace.tsx`, `app/updates/page.tsx`, `app/drafts/page.tsx`, `app/enrollment/page.tsx`, `app/rescue/page.tsx`) onto the same design code. Local LSP diagnostics are clean on all touched TS/TSX files; this slice is still local-only and not yet browser-walked in production.
- A later 2026-04-16 local router-detail trust pass now also implements the Phase 1A UX-first polish requested for the highest-value operator slice without widening scope beyond `apps/web/src/components/router-detail-workspace.tsx`: the page now makes unsaved-vs-saved state explicit near the draft note block, explains that apply only uses a saved revision, promotes the safest next action in the sticky rail, shows blocked apply/save reasons in-place instead of only through muted button state, and makes router deletion read as a serious panel-side removal with explicit consequences. Backend contracts, route/query/tab/apply semantics, and shared primitives remain intact; local LSP diagnostics are clean on the touched file, while fuller test/build/browser proof is intentionally left to the user's follow-up run.
- A later same-day local Phase 1B fleet trust pass now also sharpens the `Парк` triage surface without widening scope beyond `apps/web/src/components/fleet-monitoring-workspace.tsx` and `apps/web/src/components/router-card.tsx`: notification state now explains whether the browser is in background push, in-tab-only, blocked, or unsupported mode; fleet rows now separate live vs stale/last-known presence explicitly instead of relying on ambiguous `fresh/watch/offline` wording; and mobile router cards now mirror the same trust model so operators can quickly tell when they are looking at a live working snapshot versus only the latest known server state. Backend contracts, polling cadence, search/query behavior, and routing remain unchanged; local LSP diagnostics are clean on both touched files, while broader test/build/browser proof is intentionally left to the user's follow-up run.
- A later 2026-04-16 local Phase 2 UX-hardening pass now also adds route-level loading/error/not-found discipline for the main operator routes without changing backend contracts or route semantics: `apps/web/src/components/route-state.tsx` provides one shared operator-grade presenter, `/fleet`, `/updates`, `/drafts`, `/enrollment`, and `/rescue` now have explicit `loading.tsx` and `error.tsx` boundaries, and `/routers/[routerId]` now also has route-level `loading.tsx`, `error.tsx`, and `not-found.tsx`. The router detail server entry no longer collapses every `api.fleet.byId()` failure into `notFound()`; only true tRPC `NOT_FOUND` now maps to the not-found route while real read failures surface through the new route error boundary. Local LSP diagnostics are clean on all touched files, `pnpm test --run` and `pnpm build` are green, and `pnpm typecheck` is also green after the build regenerated the expected `.next/types` files.
- A later same-day local Phase 3 UX-hardening pass now also tightens the dense utility surfaces without changing any backend/query/mutation behavior: `apps/web/src/app/updates/page.tsx` and `src/components/global-template-rollout-workspace.tsx` now lead with clearer operator order (`сначала эталон, потом rollout`) and demote release-channel detail into secondary context, `apps/web/src/app/drafts/page.tsx` plus `src/components/draft-workspace.tsx` now treat JSON editing as an explicit reserve path with a stronger save-then-apply hierarchy, `apps/web/src/app/enrollment/page.tsx` now opens with bootstrap → park → import as the primary flow while keeping technical details behind disclosures, and `apps/web/src/app/rescue/page.tsx` now leads with current direct-mode/incidents before rescue policy reference. Local LSP diagnostics are clean on all six touched files; broader runtime/test/build proof remains intentionally unclaimed in this pass.
- A later same-day local Phase 4 editorial pass now completes the UX-hardening program for the already-touched `apps/web` operator surfaces without changing backend behavior or route semantics: trust-language and helper copy were normalized across `fleet-monitoring-workspace.tsx`, `router-card.tsx`, `router-detail-workspace.tsx`, `route-state.tsx`, `draft-workspace.tsx`, `app/enrollment/page.tsx`, `app/rescue/page.tsx`, the route-level loading/not-found/error files, and `app/updates/page.tsx` so the shell, fleet, router detail, updates, drafts, enrollment, rescue, and route-state messaging now read more consistently in operator-grade Russian while preserving real product terms like `PassWall2`, `baseline`, `check-in`, `proxy`, and `direct mode` where they remain the truthful product language. Local LSP diagnostics are clean on all fifteen touched files; broader runtime/test/build proof remains intentionally unclaimed in this copy-only pass.
- A later 2026-04-16 `/updates` stabilization follow-up now confirms the current local fix path is code-valid and release-ready even though production proof is still pending: `apps/web/src/app/updates/page.tsx` stays a server entry, the heavy rollout editor is isolated behind the new client-only `src/components/updates-workspace-client-boundary.tsx`, `src/components/global-template-rollout-workspace.tsx` now uses deterministic timestamp formatting instead of locale-dependent output, and the full local `@vectra/web` gate is green again (`test --run`, `next typegen`, `typecheck`, `build`, plus clean LSP diagnostics on the touched files). The remaining work is no longer local code correction but a scoped guarded deploy of this exact `/updates` slice followed by sequential production browser QA on `/updates` for the previously observed React hydration/runtime failure.
- A later 2026-04-17 `/updates` continuation pass now closes the next local verification gap for the new rollout-groups/profiles/version-controller slice: `apps/web/src/server/api/routers/update.ts` no longer self-references `updateRouter.createCaller(...)` during router construction, so the typed `api.update.*` surface is restored for both RSC and client callers, `apps/web/src/server/vectra/presentation.test.ts` now reflects the widened router shape with `rolloutGroupId`, and the local `@vectra/web` gates are green again across `next typegen`, `typecheck`, `test --run`, and `build`. The same pass also uncovered and documented a repo/runtime mismatch that would otherwise look like an app bug: the active local dev DB (`paperclip`) was behind the new rollout-group schema even though `0006_sturdy_rollout_groups.sql` existed, because `packages/db/drizzle/meta/_journal.json` did not yet register `0006`. After fixing the journal entry and repairing the missing `vectra_router.rollout_group_id` on the active local DB, authenticated local `/updates` requests stopped failing with `column "rollout_group_id" does not exist`; the only remaining local QA gap is direct browser-tab interaction on `/updates`, which was blocked in this session by an external Chrome DevTools profile lock rather than by app/runtime errors.
- A later same-day local freshness repair now fixes the first concrete stale-data gap without pretending the control plane has full realtime semantics yet: router-detail save/apply flows in `apps/web/src/components/router-detail-workspace.tsx` now force `router.refresh()` consistently when they change server-derived router status/inventory, and router-affecting mutations across `router-detail-workspace.tsx`, `draft-workspace.tsx`, `import-review-actions.tsx`, `rescue-actions.tsx`, `update-version-drift-workspace.tsx`, `rollout-profiles-workspace.tsx`, and `global-template-rollout-workspace.tsx` now also invalidate `fleet.monitoring` so `/fleet` no longer waits only for the next 20-second poll to reflect queued jobs or visible router-state changes. Local LSP diagnostics are clean on all touched files; `next typegen`, `typecheck`, `test --run`, and `build` are green again. This pass intentionally does not add websocket/subscription-style realtime transport: fleet still uses polling and browser push still remains notifications-only, but the previously inconsistent refresh wiring is now substantially less stale under normal operator actions.
- A later same-day second-wave performance pass now removes one confirmed duplicate router-detail boot read and trims part of the fleet-monitoring overfetch without changing visible product behavior: `/routers/[routerId]` now server-loads a single `draft.editorSurface` model and feeds it into the client query as `initialData` instead of first paying for `fleet.byId` and then immediately fetching the editor surface again, while `apps/web/src/server/vectra/fleet-monitoring-data.ts` now pushes the obvious `healthIncidents.state = open` and `jobs.state in queued|delivered|running` filtering down into SQL instead of reading closed/completed rows only to discard them in memory. Local `next typegen`, `typecheck`, `test --run`, and `build` are green. This does not fully solve fleet slowness yet—the post-deploy production log still showed `fleet.monitoring` around 3.6s—so the next likely win remains latest-snapshot/query-shape reduction inside the fleet snapshot loader rather than more UI rewiring.
- A later same-night fleet-loader follow-up now closes the first backend hot path more decisively and fixes the temporary production SQL regression introduced during that optimization attempt: `apps/web/src/server/vectra/fleet-monitoring-data.ts` no longer reads full snapshot history into Node just to keep the first row per router, but instead asks PostgreSQL directly for latest-per-router inventory rows and still falls back to the old select/orderBy path for mocked test clients that do not implement `db.execute`. The initial raw-SQL rollout briefly broke `browser-push-monitor` on production because the first deploy still ran an older tar and the subsequent live SQL used an invalid `ANY/ALL` form; after rebuilding a fresh release tar from the current tree and correcting the query shape to `router_id in (...)`, production recovered cleanly with health/smoke green and the browser-push SQL error gone from the live web logs. The remaining fleet performance work is now no longer blocked by that SQL regression; the next pass can focus on whether the latest-per-router query materially lowered real `fleet.monitoring` latency or whether additional per-router aggregation still dominates.
- A later 2026-04-16 local fleet header-spacing follow-up now tightens only the oversized dead space at the top of `Парк` without widening scope beyond `apps/web/src/components/fleet-monitoring-workspace.tsx`: the fleet hero no longer uses `Panel`'s right-side `aside` for the stacked `Показано/Алертов/Снимок/Push` summary, and instead renders the same information as a compact in-flow strip above the triage cards and table. This keeps product behavior, routing, copy, and information architecture intact while removing the visually tall empty column that came from the header layout container choice; local LSP diagnostics are clean on the touched file.
- A later 2026-04-18 local smartphone-overflow follow-up now closes the immediate phone-usability complaint in the current `apps/web` workspace without widening scope into another redesign: root page-level horizontal clipping was removed from `src/styles/globals.css` and `src/components/app-shell-frame.tsx`, `fleet-monitoring-workspace.tsx` now prefers the existing router-card lane on phones while keeping the dense operations table from `lg` upward, the confirmed rollout-note `min-w-[260px]` blockers in `global-template-rollout-workspace.tsx` and `rollout-profiles-workspace.tsx` were made phone-safe, and `router-detail-workspace.tsx` received only narrow `min-w-0` containment around the summary/action/workspace wrappers. Local verification is green on `next typegen`, `typecheck`, and `build`; direct browser QA on the real local runtime (`next start` on `127.0.0.1:3300`) confirms the unauthenticated shell has no page-level horizontal overflow at `390x844`, static CSS/JS now load with `200`, and protected `/fleet` still redirects back to `/login`. The remaining explicit QA gap is an authenticated phone walkthrough of `/fleet`, router detail, and `/updates`, which was not claimed in this session.
- A later same-day authenticated deep mobile-QA follow-up now closes most of that remaining gap with real route-by-route phone evidence instead of another shell-only smoke: a dedicated local QA runtime was started on `127.0.0.1:3400` against the `paperclip` dev DB, a minimal approved router fixture was prepared for browser-driven router-detail coverage, and the authenticated `390x844` walkthrough now confirms no page-level horizontal overflow on `/fleet`, router detail (`basic-settings/main`, `node-list`, `node-subscribe`, `app-update`, `rule-manage`, `geo-view`), `/updates`, `/drafts`, `/rescue`, `/enrollment`, and `/install`. During that pass one new real regression was found and fixed in `apps/web/src/app/enrollment/page.tsx`: the enrollment/install guide still expanded beyond the viewport because wide bootstrap/code/address surfaces were leaking intrinsic width through local containers. Shared `apps/web/src/components/panel.tsx` was also tightened just enough to support containment on this route. Verification stayed green on `next typegen`, `typecheck`, and `build`, and the final fresh-runtime QA rerun confirmed clean `390px` width across the covered surfaces. Remaining limits are now explicit and narrower: this session proved route/tab-level reachability and absence of page-level horizontal overflow on the checked surfaces, but it did not exhaustively press every destructive or state-mutating action, and it did not cover disabled/unimplemented tabs such as `Other Settings`, `Access Control`, `Server-Side`, or the gated `Watch Logs` tab on the local fixture.
- A later same-session safe interaction pass now strengthens that mobile result from “surfaces open” to “core non-destructive operator actions are physically usable on a phone” for the checked local fixture. On the authenticated `390x844` runtime, router detail accepted live text input in `Комментарий к черновику`, `Сохранить только в панели` was tapped successfully from the phone viewport, and the panel emitted a real `POST /api/trpc/draft.save -> 200 OK` without introducing overflow or blocked tap zones; the same pass also confirmed live tab-bar interaction on router-detail core tabs plus interactive access to `/updates` working surfaces and the `/enrollment` `После своей подписки: вернуть привязки myshunt` disclosure/command section. This still stops short of a full destructive-flow proof: `Сохранить и применить на роутере`, router deletion, and bulk update actions were intentionally not executed, and `/updates` still deserves one follow-up pass that proves every subworkspace tab through stable element handles rather than rerender-sensitive snapshot refs.
- A later 2026-04-18 local router-detail micro-fix now closes one narrow but real operator annoyance without widening scope beyond `apps/web/src/components/router-detail-workspace.tsx`: changing `tab` / `section` inside the router detail workspace no longer uses `router.replace(...)` for those in-page console switches, and instead updates the same query params through route-local `window.history.replaceState(...)` while preserving the existing `buildRouterConsoleQuery(...)` semantics. This keeps the visible URL and deep-link behavior intact but avoids the page jumping back to the top when operators switch between sections like `DNS` and `Shunt Rule`. Local LSP diagnostics are clean on the touched file, direct ESLint on that file is clean, and the broader `@vectra/web` build still fails only on pre-existing unrelated issues in `src/server/api/routers/update.ts`.
- A later same-session correction now tightens that fix to the actual failing path after a real browser check disproved the first attempt: the scroll jump was not eliminated by URL replacement alone because the router-detail section switch still needed explicit scroll preservation at the moment the query-param change re-rendered the page. The final local fix keeps the same route-local `window.history.replaceState(...)` approach in `apps/web/src/components/router-detail-workspace.tsx` and now preserves `window.scrollY` across the in-page `section` switch itself; `apps/web/src/components/tab-bar.tsx` was left on its default link behavior because the jump was specific to this router-detail flow rather than a generic tab-bar issue. Local LSP diagnostics are clean on both touched files, and a fresh browser smoke on `127.0.0.1:3400` now shows stable `scrollY` across `DNS -> Shunt Rule -> DNS` with the URL still updating correctly.

## Dependencies

- `@vectra/contracts`
- `@vectra/db`

## Risks

- README в `apps/web` все еще не отражает фактический продуктовый surface area.
- Apply jobs теперь live-proven на AX3000T, но controller self-update, PassWall2 package update и firmware validation lanes ещё не прогнаны до конца на production-sensitive роутере.
- Исторические imported revisions, созданные до redaction remediation, теперь имеют локальный sanitation helper/test path, но production backfill нужно запускать только по отдельному maintenance окну.
- Interactive production browser smoke is now complete on the deployed UX-clarity slice (`Парк -> Установка -> Обновления -> router 1111111111`), but destructive flows were still intentionally not executed beyond a safe read-only terminal command.
- Current production UI now reflects the latest local 2026-04-14 web slice rather than only the older `0.1.11-r1` shell line; desktop/mobile browser smoke for this deployed build is complete and the health contour stayed green.
- The newest 2026-04-15 local control-plane refresh slice now has local green validation after dependency install plus local browser smoke against a real running app. The only runtime blocker discovered during QA was missing local Postgres readiness; that was resolved in-session by installing PostgreSQL 16, starting the local service, aligning the expected `paperclip` role/database to the actual dev `DATABASE_URL`, and applying migrations. Oracle review did not block the slice, but it did surface two concrete UX concerns that were then addressed locally in the same pass: hidden table overflow discoverability and subtle row-selection affordance in router-detail editable tables. The only remaining local limitation is fixture coverage: the local DB currently contains no router rows, so `/routers/[routerId]` was not browser-walked in this session even though the refreshed route still passes local `typecheck/test/build` and the fleet shell itself remains deploy-ready.
- Remaining risk is no longer deploy viability but polish: основной shell уже выровнен по кириллице/латинице и получил русские operator labels, но часть deep technical copy всё ещё осознанно остаётся английской (`Basic Settings`, `Node List`, package names, protocol names и некоторые PassWall-derived option labels), чтобы не ломать узнаваемость интерфейса.
- Исторические approved/imported состояния могут не содержать полный набор shunt extras (`default_node`, `${rule}_fakedns`, `${rule}_proxy_tag`) до нового reimport или расширения importer-side mapping; UI теперь умеет их редактировать, сохранять и применять, но старые импорты могут сначала показывать дефолты вместо полностью восстановленного состояния.
- В этом проходе controller-кнопка в `App Update` уже attended live-proven на cleanup-safe published `0.1.12-r7`: production AX3000T прошёл реальный reinstall через ту же кнопку, очередь вернулась к `очередь задач: 0`, а роутер сохранил `Controller 0.1.12-r7` без повторного появления `/etc/config/vectra-controller-opkg`. Непрогнанными пока остаются остальные live-кнопки из этой поверхности: `PassWall2`, `Xray`, `sing-box`, `Hysteria`, `Geoview`, а также attended execution для `Обновить подписки сейчас` и `Обновить GEOIP и GEOSITE`.
- Прямое side-by-side сравнение с живым LuCI PassWall UI пока упирается в HTTP auth на `http://192.168.1.1/cgi-bin/luci/admin/services/passwall2/client`: страница достижима, username `root` виден, но без пароля браузер не раскрывает реальные поля роутера.
- Прямой визуальный diff с LuCI теперь подтверждён именно для `Rule Manage`, `App Update` и `Node Subscribe`, но historical imported revisions всё ещё могут не показывать полный набор match extras до следующего controller package rollout или operator-guided reimport, потому что importer-side backfill пока только в локальном workspace.
- На production UI подтверждено только наличие delete affordance и успешный rebuild/redeploy; реальное удаление через live кнопку намеренно не нажималось, чтобы не снести активные или диагностически полезные роутеры без отдельного окна подтверждённой очистки.
- Новый action `Сохранить и применить на роутере` на production был проверен только по рендеру и текстам; во время smoke он не нажимался, потому что это создало бы реальную live apply-задачу на активный AX3000T.
- Новый install/bootstrap lane для `Установка` уже подтверждён и на production: generated script route и static baseline asset отдаются публично, authenticated `/enrollment` fetch возвращает нужные блоки, а опубликованный bootstrap-скрипт проходит `sh -n`; живой запуск bootstrap-скрипта на реальном роутере в этом проходе по-прежнему не выполнялся, чтобы не перетереть текущий PassWall runtime на AX3000T.
- Empty-router bootstrap now resolves the previously reported install blockers in code and on the published script route, but it is still not LAN-attended on a real clean AX3000T from the production panel; the current proof is local `typecheck/test/build` plus production `200` fetch of `/install/ax3000t-bootstrap.sh` with the new PassWall release URLs, `dnsmasq` replacement logic, persistent `/root/vectra-bootstrap-work`, and selective bundle extraction.
- `Watch Logs` intentionally остаётся bounded snapshot job, а не live tail: production proof на AX3000T теперь уже есть для `all` и `system`, но ещё нужен более широкий attended QA по источникам `controller`, `passwall`, `dnsmasq`, а также по empty-state/error-state copy и большим `TRUNCATED` выводам.
- Новый 2026-04-13 global baseline/fleet rollout слой уже развернут на production и browser-smoke подтверждает сам workspace `Обновления`, но любые реальные массовые save/queue actions по боевому парку всё ещё намеренно не выполнялись.
- После локальной `/updates` stabilization pass основной риск в этом lane больше не build/type correctness, а production-only runtime proof: текущий workspace локально зелёный и использует явный client boundary для тяжёлого rollout editor, но сам live `/updates` маршрут ещё нужно перепроверить на production после следующего guarded web release, чтобы закрыть ранее пойманный React hydration/runtime error без смешения этого шага с другими незавершёнными изменениями в workspace.
- 2026-04-08 install-shunt follow-up now also keeps the live AX3000T shunt remark `Маршрутизатор BloopCat` in the published baseline, serves a new public helper route `/install/ax3000t-myshunt-rebind.sh`, and teaches `Установка` how to restore `WorldProxy` / `YouTube` / `Special` / `Tiktok` by node remark after the operator imports a private subscription. This keeps the install baseline free of real node addresses while still giving the user a practical one-command path to recover the intended `myshunt` layout; local `@vectra/web` `test/build/typecheck` are green and production route checks returned `200`.
- A later 2026-04-13 bootstrap follow-up now also preserves user-private PassWall data during the primary controller install path on routers with an existing PassWall setup: the generated AX3000T bootstrap script merges prior `subscribe_list` sections and imported `nodes` back into the sanitized baseline before writing `/etc/config/passwall2`, so the install lane no longer wipes an existing subscription URL or imported proxy list just because Vectra is being added on top. This change is currently verified locally by green `@vectra/web` `test --run`, `build`, and post-build `typecheck`; it has not yet been LAN-attended on a real already-configured router.
- The latest 2026-04-13 follow-up now closes the remaining panel-side variant too: if an operator manually adds a new subscription on the router and then opens the Vectra router workspace, the next save/apply should no longer erase that subscription just because the active server revision predates it. The draft surface now carries live-only `subscribe_list` and imported node sections forward into the editable config instead of starting from the stale authoritative revision alone; this fix is now deployed with the 2026-04-14 web slice, but it still needs one attended validation on a router that gains a subscription directly in LuCI before the next apply from the panel.
- Production investigation now also exposes an ongoing release-lane drift: PostgreSQL `vectra_artifact` and live AX3000T snapshots already carry `vectra-controller-agent` / `luci-app-vectra-controller` up to `0.1.12-r7`, but the public signed OpenWrt feed metadata still points older. The web-backed controller update path remains capable of targeting the current direct artifacts via PostgreSQL metadata, but the external feed pointer still looks stale until the signed publish lane is rerun properly.
- The 2026-04-09 pilot-unlock hotfix is verified by code/tests and live deploy health, but not by an attended click-through on a real `netis,nx31` page after the release; the remaining confidence comes from the shared Filogic tuple, unit coverage, and the same production DB tuples that previously reproduced the disabled-update issue.
- Background notifications now have a real service-worker/push lane on supported browsers, but the attended production proof is still one step short of “human saw the native notification arrive”: the deployed evidence today is green `test/typecheck/build`, live VAPID-backed deploy, `vectra-push-sw.js` `200`, healthy web container, and migrated DB tables, not yet a browser session with permission granted and a deliberately triggered incident/offline event.
- Router terminal is now live-proven on `1111111111` for one safe read-only command, but it still needs broader attended QA for stderr/non-zero exit handling, longer output, and operator copy before wider use.
- The onboarding-clarity pass is now validated against production data by a real browser walkthrough, but it still lacks proof on more consequential live actions such as `Сохранить и применить на роутере` and the future global rollout buttons.
- Telegram visibility is now live-proven on `1111111111`, but it is still only as current as each router's latest snapshot and controller version: routers that have not yet received the Telegram-capable controller/LuCI release will still show `нет данных`, while updated routers can legitimately report `partial` when only some of `telegram.org`, `web.telegram.org`, `t.me`, or `api.telegram.org` are reachable.
- The mobile-first pass is now production-proven for the main shell routes, but `Черновики` and `Обновления` still remain inherently denser than the rest of the panel on narrow screens because they embed large editor surfaces and rollout forms by design.
- A later same-day text-density follow-up is now also production-proven: the repeated `Быстрый ориентир / Где делать что` block was collapsed into a denser operator-navigation strip across the shell, `Парк` router cards were reduced to status + next action + four compact facts instead of expanded diagnostics panels, and `Установка` / `Обновления` lost redundant explainer panels while keeping the same routes and backend behavior; local `@vectra/web` `test --run`, `build`, and `typecheck` are green, production `/api/health` stayed `200` after redeploy, and live browser smoke rechecked desktop `Парк` plus mobile `Обновления` / `Установка` / router detail without console messages.
- A final same-day cleanup closed the two remaining noisy routes too: production `/drafts` dropped duplicated explainer blocks, tightened router/meta preview into denser cards, and now associates labels with its select/input/textarea controls so mobile DevTools no longer reports form-field issues; production `/rescue` now uses the same operator workflow strip plus compact policy metrics and count badges instead of three narrative reminder cards. Local `@vectra/web` `build`, `test --run`, and post-build `typecheck` are green again, production `/api/health` stayed `200`, and mobile browser smoke reloaded both pages with no console messages.
- A final 2026-04-14 onboarding/rollout clarity follow-up is now deployed too: `apps/web` shortened the shared onboarding copy in `router-onboarding.ts`, simplified `Установка` into one primary bootstrap card plus a short next-step panel, made `Обновления` choose the operator task before opening heavy rollout editors, and reduced repeated explanatory copy in `Парк` router cards. Local `@vectra/web` `test --run`, `build`, and post-build `typecheck` are green, production `apps/web/src` was redeployed through the backed-up tar lane, `/api/health` stayed `200`, and a fresh browser smoke after hard reload confirmed updated copy plus clean desktop/mobile layouts on `Парк`, `Установка`, and `Обновления` with no console messages.
- A local 2026-04-15 UX cleanup pass now also tightens the remaining non-fleet operator surfaces without changing routes, jobs, or the fleet table-first direction: `operator-shell-header.tsx` keeps command/search plus section navigation but drops redundant summary chrome, `Обновления` now opens directly on the global baseline workspace instead of repeating task-choice framing, `Установка` defaults to command + next steps while pushing shunt-rebind and package/runtime detail behind disclosures, and both `Черновики` / `Восстановление` are reduced to narrower utility surfaces with less shell-level repetition. Local `@vectra/web` LSP diagnostics on the touched files are clean, and post-edit `typecheck`, `test --run`, and `build` are green.
- A later same-day responsive cleanup follow-up now closes the remaining mobile/tablet hierarchy gaps for the login shell and fleet top section too: `apps/web/src/components/operator-shell-header.tsx` stays compact through tablet widths on `/login` and `/fleet`, `apps/web/src/app/fleet/page.tsx` hides the extra `PageHeader` below `lg`, and `apps/web/src/components/fleet-monitoring-workspace.tsx` now keeps `?q=` search state in sync while putting the mobile fleet filter ahead of summary cards, with alerts and router cards still directly below. Local `@vectra/web` LSP diagnostics on all touched files are clean; `test --run`, `build`, `next typegen`, and post-typegen `typecheck` are green; and direct local browser QA confirmed the shorter login shell on phone plus preserved shell command routing into the expected unauthenticated `/fleet -> /login` redirect path.
- A final same-day density/composition cleanup pass now also removes the last obviously stacked chrome from the local operator UI without changing routes or the approved fleet/list direction: `operator-shell-header.tsx` drops the extra mobile section strip and redundant desktop reminder copy, `fleet-monitoring-workspace.tsx` trims the nested list-intro framing and removes leftover English helper badges, `router-detail-workspace.tsx` separates the top summary deck from the sticky action rail and collapses Telegram checks into one responsive disclosure instead of duplicated mobile/desktop blocks, `rescue/page.tsx` uses a denser native policy section instead of a full extra `Panel`, `rescue-actions.tsx` shortens the explanation copy, and `/drafts` now renders `DraftWorkspace` directly instead of wrapping an already heavy editor in one more shell panel. Local LSP diagnostics on all touched files are clean; `@vectra/web` `test --run`, `build`, `next typegen`, and post-typegen `typecheck` are green; and post-edit browser QA rechecked the local phone-width `fleet` and `rescue` surfaces successfully.
- A broader 2026-04-16 redesign pass now pushes the control plane further toward a dense operator console instead of a stack of explanatory surfaces. Shared chrome was flattened by slimming `operator-shell-header.tsx`, reducing `page-header.tsx` from a boxed hero to a thin route strap, lightening `panel.tsx`, and turning `action-strip.tsx` into a toolbar rather than another mini-card. Fleet was refocused around triage and the liked list direction in `fleet-monitoring-workspace.tsx` by cutting repeated orientation copy, reducing overview narration, shrinking right-rail/context prose, and demoting onboarding ceremony. Router detail was tightened in `router-detail-workspace.tsx` by reducing top-of-page ceremony, shortening draft validation copy, and removing repeated action explanations. `updates`, `drafts`, `enrollment`, and `rescue` now enter work faster with less route-level framing and fewer instructional wrappers. Local `@vectra/web` `test --run`, `build`, `next typegen`, and post-typegen `typecheck` are green, and browser QA on fresh local runtimes confirmed the denser shell plus the cleaned `fleet`, `updates`, `rescue`, and `drafts` surfaces without console warnings.
- A final live-data-specific pass on 2026-04-16 used the current production panel as the truth source before any deploy and confirmed the remaining clutter was not abstract “design taste” but real data density in `Парк`: with six live routers, the old production fleet still over-expanded rows/cards with repeated node/import/onboarding/diagnostic prose. The local follow-up therefore cut the live-data-heavy clutter specifically in `fleet-monitoring-workspace.tsx` by slimming per-router columns, removing extra alert/onboarding explanation copy, and reducing onboarding-card narration while keeping the approved list direction and route semantics intact. Local `@vectra/web` `test --run`, `next typegen`, post-typegen `typecheck`, `build`, and LSP diagnostics are green, so the remaining gap before rollout is deployment state rather than design uncertainty.
- A final benchmark-driven polish pass then compared the redesign direction against external operator/admin UI guidance (PatternFly, Cloudscape, Carbon, ServiceNow workspace patterns) and aligned the fleet surface more closely with a top-tier resource-management model: the primary table/list view now precedes the secondary overview/dashboard layer, destructive deletion was removed from mobile fleet cards, populated router cards were compressed again, and utility routes remained clean under fresh runtime QA. Final proof on a newly started local runtime (`3304`) confirmed no horizontal overflow and no console warnings on `fleet`, `updates`, `drafts`, and `rescue`, while the fleet heading/order now reflects a more credible table-first triage flow instead of a dashboard collage.
- A final ultrawork local UX pass then pushed the remaining two compositional weak points down to an acceptable floor without widening scope beyond the control-plane UI itself: `operator-shell-header.tsx` now behaves more like a route-aware utility bar (especially on login and router workspaces) instead of a persistent mini dashboard, while `router-detail-workspace.tsx` reduces top-of-page ceremony by making status a disclosure layer, shortening draft validation copy, and clarifying the current-state/edit/apply sequence. The final local proof run on fresh runtime `3305` stayed green with `@vectra/web` `test --run`, `next typegen`, post-typegen `typecheck`, `build`, clean diagnostics, and browser-verified lack of horizontal overflow plus no console warnings on final sampled routes (`fleet`, `drafts`).
- A later same-day redesign reset then replaced the previous iterative polish strategy with a true local-from-scratch visual pass driven by a new shared shell/page grammar instead of more local box-tweaks. `apps/web` now uses a stronger operator-shell frame (`layout.tsx`, `globals.css`, `operator-shell-header.tsx`), heavier reliance on explicit page archetypes, a redesigned table-first `fleet-monitoring-workspace.tsx`, a restructured `router-detail-workspace.tsx`, and updated primitives (`panel.tsx`, `page-header.tsx`, `action-strip.tsx`, `data-table.tsx`, `status-tile.tsx`, `tab-bar.tsx`) that unify spacing, emphasis, and disclosure rules. The last runtime reductions also removed the most distracting repeated copy from fleet cards/overview, normalized shell labels away from the older mixed-language helper badges, and kept all route/query semantics intact. Final local verification for this redesign reset is green: `@vectra/web` `test --run`, `next typegen`, post-typegen `typecheck`, `build`, clean LSP diagnostics on touched TS/TSX surfaces, and browser proof on fresh runtime `3309` showing no horizontal overflow and no console warnings on the critical redesigned fleet route.
- A final composition reset followed after direct browser feedback that the fullscreen fleet still felt chaotic and too sparse. The fix was not more polish but a stronger scene hierarchy inside `fleet-monitoring-workspace.tsx`: one dominant table-first work surface, one supporting alert rail, and all aggregate context/onboarding moved into a subordinate disclosure layer instead of competing standalone sections. After this reset, final browser QA on fresh runtime `3311` confirmed the new fleet holds the same structural hierarchy at desktop (`1440x960`), tablet (`834x1112`), and phone (`390x844`) with no horizontal overflow and no browser warnings, while still preserving the same actions, router links, search flow, and onboarding entry points.
- A final top-area correction then addressed the remaining first-screen duplication that the user pointed out directly: `operator-shell-header.tsx` was compressed from a hero-like explanatory block into a tighter navigation/toolbelt, so `fleet` now begins with the work surface itself instead of competing with a second large introduction layer. Proof on fresh runtime `3312` confirmed the page now resolves to a much cleaner first-screen hierarchy — the visible route headings collapse down to `Роутеры · Все роутеры`, `Плотная таблица парка`, and `Что требует внимания` — while the shell still preserves search, tabs, and logout behavior.
- A same-day 2026-04-16 production rollout now also closes the gap between local proof and live state for that redesign-reset slice: the seven atomic `main` commits covering the persistent shell header, shared primitives, fleet table-first reset, router-detail flow cleanup, utility-surface density pass, rescue warning fix, and guarded VPS deploy lane were pushed to GitHub and deployed to `router.vectra-pro.net` through the staged release-slice + `deploy-web-release.sh` path. Production web rebuilt successfully, `vectra-web` returned to `healthy`, the standard public contour stayed `307/200/200/200/400`, and authenticated route checks confirmed `303` on operator login plus `200` on both `/fleet` and the live router detail page for router `bdfdb919-5e06-4344-ad8b-67a16f3b6fcf`; the fleet HTML also now reflects the new first-screen hierarchy markers `Роутеры` and `Что требует внимания` on the deployed surface.

- A later same-day guarded production hotfix release now also serves that exact Xray updater fix from the public panel: `router.vectra-pro.net` was redeployed from a minimal patched release slice after one failed attempt exposed macOS AppleDouble `._*` files as a release-tar contamination risk, the clean retry rebuilt `vectra-web` successfully, and production `https://router.vectra-pro.net/api/health` returned `{"ok":true}` with container health back to `healthy`. The public `/install/ax3000t-bootstrap.sh` route now returns the expected script containing `download_size_kb`, `refresh_bootstrap_xray_runtime_via_builtin_updater()`, and the explicit `Проверяю, нужен ли runtime-апдейт Xray...` call, so the panel-distributed install lane is finally aligned with the LAN-proven Xray App Update behavior.
- A later 2026-04-17 public bootstrap hotfix now also removes the next real small-storage false-stop reported from a live OpenWrt router: `apps/web/src/app/enrollment/install-presets.ts` no longer hard-requires `Installed-Size` metadata for every feed prerequisite during storage-aware preflight. Feed prerequisites such as `libc` are now only checked for feed presence, while storage-budget parsing is limited to the runtime packages that the script actually sizes and installs. Production proof is currently panel-side rather than router-side: `https://router.vectra-pro.net/api/health` is green, and the live `/install/ax3000t-bootstrap.sh` route now exposes `require_feed_packages_with_storage()`, `feed_package_storage_budget_bytes()`, and the explicit `Не удалось определить storage budget...` fallback instead of aborting earlier on `Не удалось определить Installed-Size для OpenWrt пакета libc`. A fresh attended rerun on a clean or user-reported router is still the next proof point before calling this lane broadly field-proven.
- A final same-day guarded production bootstrap follow-up now also closes the next false-stop that surfaced immediately after that `libc` fix: the live public shell was still serving an older `require_vectra_package()` branch that hard-failed on `Installed-Size` for `luci-app-vectra-controller`, even though the current source no longer needs that gate. A minimal backport was synced into the guarded production release base, `vectra-web` was rebuilt successfully on the VPS, and public proof now shows the expected live markers together on `https://router.vectra-pro.net/install/ax3000t-bootstrap.sh`: `require_feed_packages_with_storage $REQUIRED_RUNTIME_PACKAGES`, `vectra_package_storage_budget_bytes()`, the updated `require_vectra_package()` without the old `Не удалось определить Installed-Size для пакета Vectra ...` branch, and the existing Xray runtime-convergence call. This is now production-served proof for both false-stop removals; the remaining gap is still one post-fix rerun on the affected real router.
- A later same-day guarded bootstrap follow-up now also fixes the next real field-only blocker that appeared after both storage-metadata fixes: on at least one user router, `opkg whatdepends xray-core` emitted header lines such as `What depends on ...` and `Root set:` in a shape that the live bootstrap parsed as fake external dependents, producing the false stop `Автоматический reclaim для xray-core запрещён: внешние зависимости -> Root What`. The generated shell now uses a dedicated `whatdepends_packages()` helper that only accepts indented package rows from `opkg whatdepends`, so the live route no longer treats those headers as package names. Local proof includes green `@vectra/web` `install-presets` tests plus a synthetic `whatdepends` sample that now yields only real package names; guarded production proof is live too: `vectra-web` rebuilt successfully, `https://router.vectra-pro.net/api/health` is green again, and the public `/install/ax3000t-bootstrap.sh` now contains `whatdepends_packages()`, `list_unexpected_dependents()`, and `package_depended_on_by_passwall_app()` wired through that filtered helper. The remaining open item is still the next user-side rerun on the affected router.
- A final same-day guarded bootstrap follow-up now also fixes the next production drift that explained why manual PassWall `App Update` could update `xray`, but the public bootstrap still aborted on `/overlay`: the live route was still serving an older contour that did not enter reuse-lane for an already working PassWall stack, so it kept planning a full managed package refresh for `xray-core` before ever reaching the built-in runtime updater. The guarded production release slice now restores reuse detection on the public shell via `passwall_runtime_ready_for_reuse()` plus `REUSE_EXISTING_PASSWALL_STACK`, skips heavy managed refresh in that lane, and then continues into the existing `refresh_bootstrap_xray_runtime_via_builtin_updater()` step. Public proof is live: `https://router.vectra-pro.net/api/health` is green and the served `/install/ax3000t-bootstrap.sh` now contains the reuse-lane markers `REUSE_EXISTING_PASSWALL_STACK`, `passwall_runtime_ready_for_reuse()`, `тяжёлый managed refresh будет пропущен`, and `existing working PassWall stack ... встроенному App Update`. The remaining proof gap is now the post-fix rerun on the user router, not another known panel-side mismatch between package refresh and App Update behavior.

## Next Review

- В attended change window прогнать оставшиеся реальные jobs с новой PassWall-parity поверхности: `Обновить подписки сейчас`, `Обновить GEOIP и GEOSITE`, `Обновить PassWall2/Xray/sing-box/Hysteria/Geoview` по отдельности.
- Решить, нужен ли importer-side backfill для исторических shunt extras, или достаточно operator-guided `reimport` для старых approved snapshots.
- Если пользователь захочет восстановить LuCI-parity и для старых imported snapshots без ручного пересохранения, выпустить следующий controller package с importer-side сохранением `network` и заново импортировать AX3000T.
- Свести остаточную mixed-language copy в router workspace к согласованному уровню русификации без потери PassWall-recognizable tab taxonomy.
- Пройти живой визуальный QA руками на production и решить, нужен ли ещё один pass по deep option labels внутри `Node Subscribe`, `Rule Manage` и expert JSON fallback, или текущий баланс русской оболочки и PassWall-англицизмов уже достаточно привычен.
- На VPS добавить/обновить firmware artifact + manifest и прогнать `sync-artifact-metadata.mjs --apply` уже для published firmware lane.
- Пройти живой controller/passwall/firmware update cycle против нового staged artifact contract.
- Если пользователю всё ещё нужен прямой визуальный diff с LuCI, открыть live PassWall screen повторно после операторской авторизации на роутере или получения пароля для HTTP auth.
- Подготовить отдельный операторский quickstart вместо T3-template README.
- Разобрать stale test routers, которые отдают страницу детали с вечным loading state, и решить: достаточно ли теперь операторского удаления, или нужен ещё server-side fallback empty-state для сломанного editor surface.
- В LAN-attended change window прогнать bootstrap с новой production `Установка`-страницы на тестовом или согласованном AX3000T, затем решить, нужен ли следующий шаг в виде fleet-template/batch-apply слоя для массовой рассылки эталона.
- After the empty-router bootstrap hotfix, run the production script exactly once on a clean or explicitly disposable AX3000T to confirm the real install path now survives missing PassWall feeds and the stock `dnsmasq` -> `dnsmasq-full` replacement without manual recovery.
- Прогнать attended `Watch Logs` с production панели для источников `controller`, `passwall` и `dnsmasq`, затем решить, нужен ли diagnostics surface ещё один polish-pass по copy, collapse/export affordance или обработке пустых/обрезанных snapshot outputs.
- При ближайшем безопасном change window провести первый контролируемый тест workspace `Обновления` на одном disposable или явно согласованном роутере: сохранить глобальный baseline, подготовить draft и при необходимости очередь apply, не трогая боевой парк.
- Продолжить ручной UX QA уже выкаченного onboarding-clarity flow и решить, нужны ли ещё подсказки/копирайт вокруг эталона, import review и массовой рассылки после реального использования оператором.
- Продолжить ручной visual QA уже выкаченного workflow-map/scope pass и решить, достаточно ли теперь очевидно разграничены локальные действия роутера и будущая массовая рассылка из `Обновления`, или нужен ещё один UX-pass по copy/подсказкам.
- Если понадобится ещё один UX-pass, смотреть уже не на верхнюю навигацию и карточки парка, а на самые тяжёлые глубокие поверхности: длинный baseline editor в `Обновления`, технические disclosures в `Установка` и предпросмотр применения на странице роутера.
- При следующем controller release обязательно доводить publish lane до конца: не только выкладывать `.ipk` и sync-ить `vectra_artifact`, но и заново выпускать подписанные `index.json` / `Packages` / `Packages.gz` / `Packages.sig`, иначе внешний feed будет выглядеть откатившимся даже при корректной работе web-backed update path.
- Provision a real Apple `Developer ID Application` signing identity plus notary profile for the helper build workstation, then rebuild and republish the macOS helper archives so `/install` can offer a Gatekeeper-friendly `.app` instead of the current unsigned bundle.
- When the next pilot-class router is on hand, do one attended production click-through on a live `pilot` device page (`App Update`, `Сохранить и применить`, `rescue`) to convert the current code-and-deploy proof into an explicit browser-level proof for the unlocked support gate.
- In the next attended production window, grant notification permission in a real operator browser, subscribe one client, and deliberately trigger at least one `offline` or `direct_mode` event to convert the new push lane from deploy-and-DB proof into explicit end-user delivery evidence.
- In the next safe terminal QA window, keep using the already-updated local router path and extend proof beyond `ubus call system board`: run one bounded stderr/non-zero command and one longer read-only output, then record whether any extra safety guard, truncation copy, or timeout handling is still missing before wider rollout.
- Roll the Telegram-capable controller beyond `1111111111` to at least one additional safe router, then verify that `partial` and `blocked` cases remain understandable in both `Парк` and router detail and decide whether any extra operator guidance is needed around target-specific Telegram failures.

2026-04-17 addendum:

- The local control-plane update lane now treats PassWall2 as a managed stack rather than an app-only button. `apps/web/src/server/api/routers/update.ts` now resolves deterministic package sets for `pilot/certified` Filogic routers, always emits explicit `packageArtifacts` plus `targetVersion`, `targetReleaseTag`, `originSource`, `fallbackPolicy`, and `updateScope`, and no longer queues scoped `Xray` jobs with null artifact/version fields.
- The operator-facing `App Update` workspace now reflects those semantics honestly: `apps/web/src/components/router-detail-workspace.tsx` renders real available target versions plus source labels for PassWall components, treats `PassWall2` as the managed-stack action, and surfaces the last fallback/drift outcome from `editor-surface` instead of the old `через package job` placeholder.
- The same source-of-truth shift now reaches artifact publishing and bootstrap defaults too: `apps/web/scripts/sync-artifact-metadata.mjs` can publish `passwall_package` and `passwall_bundle` metadata from the mirror manifest, `apps/web/src/app/enrollment/install-presets.ts` and `scripts/Sync-PasswallBootstrapMirror.{py,ps1}` now pin the current PassWall release to `26.4.10-1`, and a local dry-run confirmed the emitted bundle/package metadata now carries the correct `aarch64_cortex-a53` architecture instead of the earlier version-prefixed parse bug.
- Local verification for this web slice is green: `pnpm --filter @vectra/web test -- --run apps/web/src/server/api/routers/destructive-gating.test.ts apps/web/src/server/vectra/editor-surface.test.ts apps/web/src/app/enrollment/install-presets.test.ts apps/web/src/components/router-detail-workspace.test.ts`, `pnpm --filter @vectra/web typecheck`, `pnpm --filter @vectra/web lint`, and a dry-run `node apps/web/scripts/sync-artifact-metadata.mjs --passwall-mirror-dir <temp-mirror> --dry-run` all passed. This slice is still local-only and has not yet been exercised against a live router from the operator UI.
- A separate public install surface now lives outside login middleware at `apps/web/src/app/install/page.tsx`: the page shows one primary `Установить на роутер` action, live stage/status updates, copyable logs, and an honest mobile/manual fallback that reuses the existing public bootstrap asset instead of promising impossible browser-side SSH.
- The new desktop lane is backed by localhost-only Go helper `apps/install-helper`, which exposes only `/health`, `/scan`, `/install`, and `/events/{sessionId}`; it enforces allowlisted install origins plus per-tab session tokens, performs TOFU host-key pinning with mismatch failure, tries only user-approved local credential profiles before manual password entry, and runs only the fixed public bootstrap script. The same slice also adds rate limiting plus explicit pending-review audit metadata on public router registration, and local verification is green across `pnpm --filter @vectra/web test --run public-install public-install-rate-limit`, `pnpm --filter @vectra/web exec next typegen`, `pnpm --filter @vectra/web typecheck`, `pnpm --filter @vectra/web build`, `cd apps/install-helper && go test ./...`, and `cd apps/install-helper && go build ./...`.
- A same-day guarded VPS release now also serves that install surface on production at `https://router.vectra-pro.net/install`: staged sync through `deploy/scripts/deploy-web-release.sh`, fresh PostgreSQL backup under `/backups`, explicit `docker compose --env-file .env build web`, and `up -d web` produced a new `vectra-web` container with `○ /install` in the build output while keeping the standard public smoke contour at `307/200/200/200/400`. Browser smoke on the live page now confirms the honest no-helper branch (`helper не найден`, public fallback command, bootstrap-link CTA), and helper-side live API proof on the workstation confirms the SSH-unreachable branch returns event `router_not_found` with exact message `Роутер <ip> не отвечает по SSH.` rather than silently hanging.
- A later same-day production follow-up now also closes the last rollout gap for that public install lane instead of only the local/UI slice: after one first VPS attempt surfaced a stale payload mismatch, the release tar was re-uploaded, staging was re-verified on the VPS itself for `apps/web/src/app/install/page.tsx` plus all four `apps/web/public/install-helper/*.zip` artifacts, AppleDouble `._*` files were deleted before sync, and the guarded `deploy-web-release.sh -> docker compose build web -> up -d web` path was repeated successfully. Live proof is now stronger than the earlier fallback-only note: `https://router.vectra-pro.net/install` returns `200`, browser snapshot shows the simplified first screen with `helper: нужен helper`, one clear download CTA plus `Я уже запустил helper`, direct `200` responses exist for the published helper archives, and helper-side API proof still returns `router_not_found` with exact message `Роутер 10.255.255.1 не отвечает по SSH.` for an SSH-unreachable target.
- The same production helper lane now also has a clearer macOS packaging story: the published Apple Silicon zip was re-downloaded and inspected live, and it now contains `README.txt`, `Vectra Install Helper.app/Contents/MacOS/Vectra Install Helper`, the bundled `vectra-install-helper` binary, and `Info.plist` instead of a raw executable dump. The release script `apps/install-helper/build-release-artifacts.sh` now supports optional `MACOS_SIGN_IDENTITY` and `MACOS_NOTARY_PROFILE` for Developer ID signing plus notarization, but the current workstation still reports `security find-identity -v -p codesigning -> 0 valid identities found`, so the published macOS helper remains unsigned and will continue to trigger Gatekeeper warnings until a real Apple Developer ID certificate/profile is provisioned.

2026-04-18 update-delivery addendum:

- The local `App Update` surface now exposes version semantics explicitly instead of collapsing them into one ambiguous “version”: `PassWall2` shows managed-stack tag `26.4.10-1` separately from app package `26.4.10-r1`, while `Xray` now shows runtime-vs-package truth on the installed side and “built-in updater first / package target record” on the available side.
- `apps/web/src/server/vectra/editor-surface.ts`, `apps/web/src/lib/passwall-update-summary.ts`, and `apps/web/src/components/router-detail-workspace.tsx` now also surface operator-facing wording for built-in runtime convergence, runtime/package drift, storage-blocked package paths, and fallback success instead of the earlier generic placeholder copy. The same local slice adds a delivery warning next to update actions when the router is stale or when the control plane cannot confirm a DB write probe.
- Local verification for this web slice is green on `2026-04-18`: targeted Vitest suites, package `typecheck`, and `lint` all passed after the new delivery/version semantics landed.
- Live read-only router facts were rechecked on the current test device `192.168.99.1` (`Cudy WR3000E v1`, OpenWrt `24.10.5`): `luci-app-passwall2 = 26.4.5-r1`, `xray-core` package record still `25.10.15-r1`, runtime `Xray 26.4.15`, `/overlay` free about `14.7M`, `/tmp` free about `113.7M`, and router logs still contain the earlier `2026-04-18` check-in failures with server-side `No space left on device`.
- A later same-session guarded production hotfix now also deploys this control-plane slice rather than leaving it local-only. The release used the narrow backed-up file-copy lane for the touched production files plus `packages/contracts/src/schemas.ts`, `docker compose --env-file .env build web && up -d web` returned `vectra-web` to `healthy`, and the public smoke contour re-stabilized at `307/200/200/200/400` after the normal short `502` restart window.
- The deployed health probe now proves more than plain HTTP reachability: `https://router.vectra-pro.net/api/health` returns `{"ok":true,...,"checks":{"browserPushMonitor":true,"dbRead":true,"dbWriteProbe":true}}`, which closes the earlier blind spot where `/api/health` could stay green while router check-in writes were failing deeper in PostgreSQL.
- An attended production panel pass on `192.168.99.1` now proves the operator-facing version surface itself instead of only the source and deploy slices. After publishing and syncing PassWall mirror metadata, the panel data for this router now carries controller `0.1.12-r11`, PassWall managed-stack target `26.4.10-1`, PassWall app package target `26.4.10-r1`, and Xray package target `26.3.27-r1` alongside the already-installed runtime `Xray 26.4.15`; this is the first live production proof that the new split version semantics are visible to the operator rather than staying local-only.
- That same attended pass exposed a missing production rollout step before it was fixed. Initially, `update.artifacts` on production returned only controller and firmware rows, while the VPS still served the older `bootstrap/passwall2/26.4.5-1` mirror; because of that, `update.queuePasswallPackageUpdate` failed with a `500` validation error on empty `sha256` instead of queueing `Xray` or `PassWall2`. Uploading `bootstrap/passwall2/26.4.10-1/aarch64_cortex-a53` and running `sync-artifact-metadata.mjs --apply` on the VPS closed that gap without another source edit.
- Standalone `Update Xray` is now live-proven from the production panel: the backend queues `strategy = xray-built-in-first` with the expected release/package targets, router detail first shows the running state, and the final panel summary for this router is `success` with per-package `status = runtime-only-converged` and `pathUsed = not-needed` because runtime `26.4.15` was already newer than the mirrored package record.
- The remaining operator-facing truth gap is now narrower and explicit. `Update Controller` still leaves a false failure hint in router detail (`signal: killed`) even though both controller packages are already `0.1.12-r11`, and full `Update PassWall2` currently fails on this router with panel summary `opkg update: exit status 7` instead of yet reaching the expected success or storage-block wording.
- A later same-session production hotfix and attended rerun now close both of those stale operator gaps on the same router. `editor-surface.ts` now suppresses a stale controller failure when the installed version is already at or above the requested artifact, and after a one-time direct controller bootstrap to published `0.1.12-r13` the production panel now shows controller `0.1.12-r13 (актуально)` instead of the old false failure summary.
- The same hotfix also changes managed-stack job construction in production rather than only in tests: `apps/web/src/server/api/routers/update.ts` now drops unpinned recovery deps from the queued PassWall payload when the latest inventory already reports `dnsmasq-full` plus the required `kmod-nft-*` packages installed. On the live rerun for `192.168.99.1`, PostgreSQL shows the new `update_passwall_packages` payload contains only pinned artifacts and the router no longer spawns `opkg update` / `wget Packages.gz` before package work begins.
- The production `App Update` screen now reflects the live outcome honestly after that rerun. `PassWall2` shows installed app `26.4.10-r1`, `Xray` still shows runtime `26.4.15` vs package `25.10.15-r1`, and the last managed-stack attempt now surfaces the real blocker `v2ray-geoip: package path пропущен из-за места ...` instead of the earlier generic `opkg update: exit status 7`.
- A final same-session UI polish now also fixes the last cross-lane summary leak exposed by the attended rerun. `buildLastPasswallUpdateAttempt()` now ignores newer scoped `Update Xray` jobs when rendering the separate `Последняя попытка обновить PassWall-стек` block, so a later Xray `runtime-only-converged` no-op no longer overwrites the managed-stack storage-block summary on the live router page.
- A later same-session controller self-update follow-up now closes the remaining operator-path proof gap rather than leaving controller updates half-proven. `apps/web/src/lib/controller-update-jobs.ts` and `apps/web/src/server/api/routers/update.ts` now make `run_terminal_command` the default controller self-update lane for terminal-capable controllers (`>= 0.1.12-r1`) while preserving legacy `update_controller` only for older or unknown agents. The queued terminal payload pins both controller `.ipk` artifacts, verifies SHA-256 before install, suppresses package-driven restarts with the env-plus-sentinel guard, and changes tactic from pair install to split install if the combined `opkg install` path fails. Live production proof on `192.168.99.1` is now explicit too: the first panel reinstall exposed a checksum-preflight bug in that new shell payload, a same-session hotfix/redeploy corrected it, and the second `Переустановить 0.1.12-r13` from `router.vectra-pro.net` queued `run_terminal_command purpose=controller-self-update`, reached `accepted -> running -> success` in PostgreSQL, reinstalled both controller packages on the router, and kept the panel truth at `0.1.12-r13 (актуально)` with fresh post-restart check-ins and no manual SSH/bootstrap in the final mechanism.
- A later same-session fleet-monitoring hotfix now also closes the panel-side regression that made `/fleet` show `Telegram нет данных`, `Controller Не удалось определить`, and `PassWall2 неизвестно` even though PostgreSQL still had fresh snapshot truth. The root cause was the optimized raw-SQL path in `apps/web/src/server/vectra/fleet-monitoring-data.ts`: it selected `snake_case` snapshot columns through `db.execute(...)` but then read them as Drizzle-style `camelCase`, so the latest snapshot map was keyed under `undefined` and every fleet card lost its snapshot-derived versions and Telegram state. Local proof is green on targeted Vitest plus `build`, a new regression test now covers raw `execute()` rows with `snake_case` fields, and live production proof was completed by a narrow hot patch of the current Next bundle on the VPS followed by a `vectra-web` restart. The patched container state was then committed back into local VPS image tag `vectra-web:latest` as an emergency bridge until the next clean Docker rebuild, and authenticated `/fleet` browser snapshots now again show real values, including test router `OpenWrt` with `Telegram доступна`, controller `0.1.12-r13`, and PassWall2 `26.4.10-r1`.

2026-04-22 router rename local addendum:

- That earlier local rename slice is now corrected to target the real OpenWrt hostname instead of only a panel alias. `apps/web/src/server/api/routers/fleet.ts` no longer writes `displayName`; it validates an operator-supplied hostname, gates the action on controller terminal support, and queues a `run_terminal_command purpose=router-hostname-update` job that applies `uci set system.@system[0].hostname=...`, commits `system`, runs `reload_config`, reloads `/etc/init.d/system`, and then returns `ubus call system board`.
- Router detail header now reflects that real device path. `apps/web/src/components/router-detail-workspace.tsx` replaces the old `Имя в панели` copy with `Hostname роутера`, explains that the panel changes `system.@system[0].hostname` on the router itself, and warns that the task waits for the next check-in if the router is offline or if the controller is too old for the terminal lane.
- The same local web slice now keeps operator feedback honest after the job finishes. `apps/web/src/server/vectra/editor-surface.ts` classifies the new terminal purpose as `Смена OpenWrt hostname` in the management task log instead of mislabeling it as controller self-update, and router detail now prefers the router row hostname over the last snapshot hostname so the UI can reflect the successful rename as soon as the job result lands. Local verification is green on targeted Vitest, `@vectra/web lint`, `@vectra/web typecheck`, and `@vectra/web build`; this remains local-only until a real production rollout/browser smoke is done.

2026-04-22 router-detail App Update reboot shortcut addendum:

- The local router-detail source now exposes the router reboot action directly inside `App Update` instead of forcing the operator to leave the current router and switch to `/updates`. `apps/web/src/components/router-detail-workspace.tsx` now wires a dedicated `api.update.queueRouterReboot.useMutation`, shows a panel-native confirmation dialog, disables duplicate queueing while a reboot job is already pending, and surfaces the latest reboot summary alongside the existing App Update hints.
- The server path stays intentionally narrow and reuses the already-proven terminal reboot lane instead of inventing another controller protocol. `apps/web/src/server/api/routers/update.ts` now exposes single-router mutation `queueRouterReboot`, which delegates straight into the existing `enqueueRouterRebootJob()` / `router-reboot` payload path already used by bulk reboot in `/updates`.
- Local verification is green on targeted Vitest `src/components/router-detail-workspace.test.ts`, `pnpm --filter @vectra/web typecheck`, and `pnpm --filter @vectra/web build`. This is still local-only until the next web rollout, so the next acceptance step is to deploy the web slice and confirm the new App Update reboot button on live router detail without breaking the existing `/updates` bulk reboot flow.

2026-04-19 panel CLI addendum:

- The production operator surface now also has a repo-side CLI access lane: `apps/web/scripts/vectra-panel-cli.mjs` uses the same operator login cookie model and protected `tRPC` routes as the UI, while the root wrapper `scripts/VectraPanelCli.sh` exposes fast commands for `fleet`, `router`, `logs`, `terminal`, `update`, `rescue`, and raw `call <trpc.path>` usage without requiring manual browser work.
- That CLI lane now also answers the concrete coverage question instead of leaving it implicit: `catalog` enumerates the current operator `tRPC` surface (`draft`, `fleet`, `logs`, `notifications`, `rescue`, `terminal`, `update`) and the separate router-facing endpoints, `draft` and `notifications` now have first-class wrappers, and the router-facing lane is explicitly modeled as a different auth domain that requires `x-vectra-router-id` plus `x-vectra-router-token` rather than the operator cookie.

2026-04-20 deploy-parity addendum:

- A fresh read-only production parity check now closes the “is there still an undeployed commit?” question for the current web slice more concretely than the earlier narrative notes. Relative to `origin/main..HEAD`, all non-doc web/runtime files touched by the five product commits (`ae113f5`, `8471725`, `beaf05e`, `ae7e153`, `87c6cae`) are already present on the live VPS under `/opt/vectra-prorouter`: `16` changed files match `HEAD` exactly, while the remaining active trust/subscription presentation files (`fleet-monitoring-workspace.tsx`, `rollout-profiles-workspace.tsx`, `router-card.tsx`, `router-detail-workspace.tsx`, `updates-workspace-client-boundary.tsx`, `router-config-trust.ts`, `editor-surface.ts`, `editor.ts`, `presentation.ts`) already match the current local worktree instead of the older committed blob. This means there is no remaining undeployed committed web delta for production.
- Live runtime proof also matches the source-level parity result instead of leaving it at “files exist on the VPS.” `https://router.vectra-pro.net/api/health` currently returns the stronger `browserPushMonitor/dbRead/dbWriteProbe` payload, and a live `draft.editorSurface` query for router `VagrandRouter` now returns `workspaceRevisionId`, `subscriptionRuntime`, `configSourceMode = "live-import"`, and `requiresReimport = false`, which confirms that the newer subscription/runtime read model is active in the production app rather than only present in source.
- The remaining operational mismatch is now the inverse of the original concern: production is ahead of git on part of the current trust/subscription slice. New source files such as `draft-selection.ts`, `subscription-runtime.ts`, `subscription-audit.ts`, and `router-management-task-log.tsx` already exist on the VPS even though they are not yet committed in this workspace. The next close-out step for this area is therefore git parity and release hygiene, not another production web deploy.

2026-04-20 fleet-stability addendum:

- A same-day follow-up isolated the separate production instability as a fleet read-model payload issue rather than another subscription/source-of-truth bug. `apps/web/src/server/api/routers/fleet.ts` was still loading full `passwallDesiredRevisions` rows with embedded `config` JSON for every router and also scanned full snapshot history before collapsing it in Node; `apps/web/src/server/vectra/fleet-monitoring-data.ts` already had a faster latest-snapshot path, but still loaded live-import revisions as full rows even though config-trust only needs revision metadata (`origin`, `configDigest`, `createdAt`).
- The web slice now uses shared metadata-only revision loading in `apps/web/src/server/vectra/revision-metadata.ts`, `fleet.list` reuses the optimized latest-snapshot loader instead of walking all snapshot rows, and fleet surfaces now expose compact `latestDesiredRevision` metadata without shipping the full desired config. Local proof is green on new regression test `src/server/api/routers/fleet.test.ts`, existing `src/server/vectra/fleet-monitoring-data.test.ts`, and `pnpm --filter @vectra/web build`; the old repo-wide `tests/mobile-action-qa.spec.js` / missing Playwright tail is now fixed as well, so `pnpm typecheck` completes successfully from the workspace root.
- The narrowed production hotfix is now live too. Only `fleet.ts`, `fleet-monitoring-data.ts`, and the new `revision-metadata.ts` were backed up and copied into `/opt/vectra-prorouter`, `docker compose --env-file .env build web && up -d web` rebuilt `vectra-web` cleanly, and fresh production evidence now shows `fleet.monitoring` around `0.5s-1.5s`, `fleet.list` around `1.49s` instead of the earlier `14s+`, `vectra-web` `oom=false restart=0 health=healthy`, and container memory around `141MiB / 1.9GiB`. Live `fleet.list` on production now returns compact `latestDesiredRevision` metadata for `VagrandRouter` and peers, which is the intended shape reduction behind the stability fix.
- A later `2026-04-20` production audit now confirms the remaining subscription problem is runtime cleanup, not panel visibility. Using `scripts/VectraPanelCli.sh` against live `fleet.monitoring` and `draft.editorSurface` for all `8` routers, every router still reports `subscriptionRuntime.audits[*].status = drift`, `managedNodes = 0`, and only orphaned subscription nodes in the read-model. The largest live outlier is `AndreyVK`: `31` total nodes, `2` runtime subscriptions, and `30` orphan nodes while the currently audited subscribe payload resolves to `1` node; `VagrandRouter` still reports `17` total nodes and `16` orphan nodes, with the audit also showing a stale panel-draft footprint at `44` managed subscription nodes against only `1` current payload node. A deeper live check on `AndreyVK` also proves the duplicate-subscription symptom is still real on the router itself: two live subscription entries point to the same URL under different internal ids, so the panel is not inventing that duplication anymore. This means the next fix for the slice must actually reconcile/remove orphaned subscription runtime on routers rather than further reshaping the read-model.

2026-04-21 bulk reboot addendum:

- The local `/updates` slice now closes the missing operator ergonomics for mass actions instead of forcing row-by-row clicks: `apps/web/src/components/update-version-drift-workspace.tsx` now exposes `Выбрать все по фильтру`, `Снять выбор`, and a new destructive bulk action `Перезагрузить выбранные роутеры`.
- The backend side keeps this intentionally narrow and compatible with the current agent contract. `apps/web/src/server/api/routers/update.ts` now queues `queueBulkRouterReboot` through `run_terminal_command`, `packages/contracts/src/schemas.ts` now accepts `purpose = router-reboot`, and `apps/web/src/lib/router-reboot-jobs.ts` builds a delayed `/sbin/reboot` payload so the router can acknowledge success before the board actually goes down. `router-control.ts`, `editor-surface.ts`, and the local panel CLI catalog were updated too so reboot jobs stay exclusive in delivery, show up with a readable task-log label, and are discoverable from repo-side operator tooling.

2026-04-21 node-reference addendum:

- The local router workspace now stops depending on volatile PassWall node section ids when rebuilding the editable draft from live runtime. `apps/web/src/server/vectra/editor-surface.ts` now rebinds node references by stable node label/group whenever live subscription/runtime nodes rotate ids, so the panel keeps `basicSettings.main.selectedNodeId`, `socks.nodeId`, `autoswitchBackupNodeIds`, shunt `outboundNodeId`, and PassWall-specific extras such as `default_node`, `*_proxy_tag`, and subscription `to_node` aligned with the new live ids instead of silently falling back toward missing-target/`Close (Not use)` states.
- Local verification for this slice is green on `pnpm --filter @vectra/web test --run src/server/vectra/editor-surface.test.ts` and `pnpm --filter @vectra/web typecheck`. The new regression test explicitly proves the real failure contour: runtime node ids can rotate while labels stay stable, and the merged draft now keeps the same logical targets by label across selected node, socks, shunt rules, and extras instead of treating the rotated node as a brand-new disconnected target.
- Safe live verification is now also grounded on the current test router `OpenWrt` rather than only on synthetic fixtures. A fresh `scripts/VectraPanelCli.sh draft editor OpenWrt --json` snapshot showed that the router is currently reachable and approved, while the selected live node already remains `RoscomShunt`; because we did not want to mutate the router just to force an id rollover, the same captured live router data was then replayed through the real local merge function with a synthetic selected-node id rotation that preserved the router's actual label/group. The temporary Vitest probe passed, which confirms the new rebinding behavior on the real router topology without issuing writes, restarts, or subscription refreshes.
- The attended production rollout on `2026-04-21` then turned that live contour into an explicit server-side proof on the same safe router instead of stopping at the local replay. Because the VPS deploy root already held the source hash for the first node-rebinding pass, the first production action was just a background `docker compose --env-file .env build web && up -d web`; that rebuild exposed a remaining live gap rather than a green finish: `draft.editorSurface` for `OpenWrt` kept `selectedNodeId = RoscomShunt`, but `WorldProxy.outboundNodeId` in the draft collapsed to empty while the live router still reported opaque runtime tag `AuyH33PD`.
- The root cause was narrower than the original selected-node case. On this router, some shunt targets now resolve to opaque runtime tags that are not present in `nodes[]`, so node-label rebinding alone cannot recover them. `editor-surface.ts` now therefore falls back to the current live shunt/rule-manage `outboundNodeId` by stable shunt rule identity when node-based rebinding has no match, and a second regression test covers that exact contour.
- Final production proof on the same test router is now green after a one-file VPS hotfix plus second background rebuild. `scripts/VectraPanelCli.sh status` and `deploy/scripts/smoke-check.sh` stayed at the usual healthy `307/200/200/200/400` contour, while live `draft.editorSurface` for `OpenWrt` now reports `basicSettings.main.selectedNodeId = RoscomShunt`, `basicSettings.main.selectedNodeLabel = "Маршрутизатор BloopCat"`, `basicSettings.shuntRules[WorldProxy].outboundNodeId = AuyH33PD`, `ruleManage.shuntRules[WorldProxy].outboundNodeId = AuyH33PD`, and `fieldDiffs["Shunt[WorldProxy].outboundNodeId"].draftChanged = false`. That is the concrete production evidence that the operator workspace now preserves the logical shunt target on the safe test router instead of drifting toward empty/`Close (Not use)` state.
- Local proof for this slice is green on `pnpm --filter @vectra/web test --run src/server/api/routers/destructive-gating.test.ts src/server/vectra/router-control.test.ts src/server/vectra/editor-surface.test.ts` and `pnpm --filter @vectra/web build`.
- Live mechanism proof is now explicit on the safe test router `OpenWrt` (`192.168.99.1`, router id `e80b2f5b-d80a-4903-85e1-57d8ce747f15`) even before this local UI/API slice is deployed. The first attended terminal reboot attempt exposed a real BusyBox `sh` incompatibility in the initial shell shape (`&;` after a background reboot), the builder was corrected to a newline-separated payload, and the second attended run succeeded end-to-end: the reboot job reported pre-reboot `boot_id = c66e065a-1d3d-4033-8a8b-bcf9fc89708a`, a later post-reboot terminal check returned `boot_id = 3bbb25e8-0fb9-4884-9a7f-c3f4b4381731`, and fresh router check-in/snapshot timestamps resumed at `2026-04-20T22:16:42Z`. This proves the reboot mechanism itself is real on the current terminal lane; deployment is still required for the new `/updates` bulk button to exist on production.

2026-04-21 Xray runtime-target addendum:

- The production web/control-plane gap around Xray updates is now closed with live proof instead of only a source review. The root cause was not PassWall2 itself, but Vectra panel logic: `apps/web/src/lib/passwall-artifacts.ts`, `passwall-update-summary.ts`, `src/server/vectra/rollout-control.ts`, `src/server/api/routers/update.ts`, and `src/components/router-detail-workspace.tsx` were still comparing router runtime against the mirrored package artifact `xray-core 26.3.27-r1` instead of the upstream runtime target exposed by PassWall2 built-in updater metadata. On routers already running runtime `Xray 26.4.15`, that made the panel report “current” and skip the real built-in update path even though upstream `api.to_check('', 'xray')` still reported `remote_version = v26.4.17`.
- The production VPS deploy for this slice also exposed a concrete non-git drift risk in `/opt/vectra-prorouter`. A narrow backup-first sync of the Xray-related web files was not enough for the first `docker compose --env-file .env build web`: production source was missing `apps/web/src/lib/router-reboot-jobs.ts`, and its stale `router-detail-workspace.test.ts` / `destructive-gating.test.ts` no longer matched the current code. After explicitly copying those missing dependencies and rerunning the same bounded rebuild, `vectra-web` returned to `healthy` and public `/api/health` stayed at `200`.
- Attended production proof is now explicit on `2026-04-20/21`. After the web redeploy plus mirror refresh, live `update.versionDriftWorkspace` stopped showing `OpenWrt` as `xrayAvailable = 26.3.27-r1, xrayNeedsUpdate = false` and now shows `xrayAvailable = 26.4.17, xrayNeedsUpdate = true` together with `passwallAvailable = 26.4.20-1` and controller availability `0.1.12-r14`. After the controller was upgraded and a scoped `Update Xray` job completed, the same production workspace now reports `controllerInstalled = 0.1.12-r14`, `xrayInstalled = Xray 26.4.17 ...`, and `xrayNeedsUpdate = false` for test router `OpenWrt`.

2026-04-21 runtime-target components addendum:

- The same control-plane slice now generalizes runtime-target semantics beyond Xray instead of leaving `sing-box`, `hysteria`, and `geoview` on mirrored-package-only truth. `deploy/scripts/refresh-passwall-mirror.py` now publishes `runtimeTargets` for all four runtime-only PassWall components, `apps/web/src/server/api/routers/update.ts` now carries `runtimeTargetVersion` for any scoped runtime component job instead of only `xray-core`, and `apps/web/src/components/router-detail-workspace.tsx` now uses that published runtime target when deciding whether a component is already current on the router.
- Local verification for this follow-up stayed narrow and green: `pnpm --filter @vectra/web test --run src/components/router-detail-workspace.test.ts src/server/api/routers/destructive-gating.test.ts`, `pnpm --filter @vectra/web build`, and `python3 -m py_compile deploy/scripts/refresh-passwall-mirror.py` all passed after the generic runtime-target changes landed.
- Production proof for this narrower follow-up is metadata plus deploy-state proof, not four attended router updates. On the VPS, `/opt/vectra-prorouter/deploy/runtime/artifacts/bootstrap/passwall2/26.4.20-1/aarch64_cortex-a53/manifest.json` now exposes `runtimeTargets` for `xray-core = 26.4.17`, `sing-box = 1.13.9`, `hysteria = 2.8.1`, and `geoview = 0.2.5`; after the backup-first source sync, `docker compose --env-file .env up -d web` recreated `vectra-web` and `docker compose ps web` now shows the container as freshly recreated and `healthy`. The only attended router-side update proof in this window remains Xray on safe test router `OpenWrt`; the other runtime-only components now have current published metadata and deployed panel gating, but not yet their own attended update runs.

2026-04-21 subscription-runtime authority addendum:

- The local web slice now corrects the false `payload-mismatch` labeling on live PassWall subscription nodes. Fresh production facts from `scripts/VectraPanelCli.sh draft editor VagrandRouter --json` showed the exact mismatch: the router page already had `configSourceMode = live-import`, the live router import still contained `15` `add_mode=2` nodes in group `BloopCat`, but the server-side advisory audit fetched only `1` payload node and therefore pushed every live subscription node into the old orphan/error bucket even though those nodes were confirmed on the router itself.
- Upstream PassWall2 source review now explains why that fetch cannot be authoritative. `passwall2/luci-app-passwall2/root/usr/share/passwall2/subscribe.lua` downloads subscriptions with router-specific identity (`user_agent`, `x-device-os`, `x-ver-os`, `x-device-model`, `x-hwid`), so a plain server-side fetch of the same URL can legitimately return a different payload than the router receives. The local read-model now treats the live router import as authoritative for the node list, keeps server-side payload fetch only as advisory audit metadata, and splits the operator surface into three explicit layers: live subscription nodes from the router, panel-only nodes from panel draft/history, and local non-`add_mode=2` nodes still left inside a subscription group.
- Local verification for this slice is green on `pnpm --filter @vectra/web test --run subscription-runtime`, `pnpm --filter @vectra/web typecheck`, and `pnpm --filter @vectra/web build`. Deployment/browser proof on the affected production router pages is still the remaining step before the old `не совпала с payload` badges disappear from live UI.

2026-04-21 subscription-runtime rollout addendum:

- The attended production rollout for this slice is now complete. Because `/opt/vectra-prorouter` still carried unrelated live drift, the release stayed on the narrow backup-first lane: the previous production `subscription-runtime.ts`, `editor-surface.ts`, and `secrets.ts` were saved under `/opt/vectra-prorouter-backups/web-subscription-runtime-20260420-232943/`, only those three files were copied into `/opt/vectra-prorouter/apps/web/src/server/vectra/`, and `vectra-web` was rebuilt afterwards instead of syncing a broader web slice.
- The first foreground `docker compose --env-file .env build web` lost the SSH session during `next build` at `Collecting page data`, so the rebuild was repeated via a VPS-side background runner with log `/tmp/vectra-web-rebuild-20260421-064849.log` and status marker `/tmp/vectra-web-rebuild-20260421-064849.status`. That second run finished with `status=ok`, recreated `vectra-web`, and returned the container to `healthy`; public smoke stayed at the standard `307/200/200/200/400`, and `scripts/VectraPanelCli.sh status` still reports `dbRead=true` plus `dbWriteProbe=true`.
- Live production proof now matches the intended authority split instead of the earlier false-orphan state. `VagrandRouter` currently resolves `configSourceMode = live-import`, `manualNodes = 1`, `managedNodes = 21`, `panelOnlyNodes = 42`, and only `orphanNodes = 1`, with the previously problematic `🇫🇮 ⚡⚡ Финляндия Xhttp Gaming` now present inside `managedNodes` rather than the orphan/error bucket. The same post-deploy read model on `AndreyVK_Sochi` shows `managedNodes = 21` / `orphanNodes = 0`, and `AndreyVK` now shows `managedNodes = 42` / `orphanNodes = 0`, which closes the broader “many routers only show orphan subscription nodes” symptom at the control-plane layer.
- The remaining drift is now intentionally narrower and honest: the advisory payload audit on those routers still sees only `payloadNodeCount = 1`, so router pages continue to report `status = drift`, but real router subscription nodes stay in `managedNodes`, stale panel history stays in `panelOnlyNodes`, and only true local group leftovers remain in `orphanNodes`. The next step for this area is therefore cleanup/apply logic for panel-only duplicates and real router-local leftovers, not more authority/read-model work.

2026-04-21 subscription cleanup action addendum:

- The local operator workspace now adds that next cleanup/apply layer instead of stopping at diagnostics. `apps/web/src/components/router-editor-state.ts` gained a new pure helper `pruneNodes()` that removes selected node ids from the desired config and repairs the most important dependent references at the same time: main selected node falls back like the normal delete flow, SOCKS entries either retarget or drop when no fallback remains, shunt outbound references are cleared, and PassWall extras such as `default_node`, `to_node`, and `*_proxy_tag` are stripped when they still point at the removed node ids.
- `apps/web/src/components/router-detail-workspace.tsx` now exposes explicit operator actions in `Node List` for the two remaining runtime-drift layers. When only `panelOnlyNodes` remain, the workspace can now directly `Зафиксировать live runtime в панели` even if the editable draft has no manual unsaved changes, which persists the already-merged live-aligned draft as a fresh panel revision and removes the stale panel-only footprint from the working baseline. When `orphanNodes` remain, the workspace now offers `Подготовить cleanup draft`, which removes those true router-local leftovers from the next draft revision and pre-fills a cleanup note so the normal `Сохранить` / `Сохранить и применить` path can finish the router cleanup through the existing `apply_passwall_config` lane.
- Local verification for this new cleanup layer is green on targeted tests plus file-level lint: `pnpm --filter @vectra/web test --run src/components/router-editor-state.test.ts src/server/vectra/subscription-runtime.test.ts src/server/vectra/editor-surface.test.ts` and `pnpm --filter @vectra/web exec eslint src/components/router-editor-state.ts src/components/router-editor-state.test.ts src/components/router-detail-workspace.tsx` both pass. Repo-wide `@vectra/web typecheck` is currently still blocked by unrelated active errors in `src/components/update-launch-monitor-dialog.tsx`, `src/components/update-version-drift-workspace.tsx`, and `src/server/api/routers/update.ts`, so this cleanup slice is locally verified but not yet ready for production rollout until that separate update-surface drift is resolved or isolated.

2026-04-21 subscription cleanup rollout addendum:

- The cleanup UI is now deployed on production too. Because the live VPS root still carries unrelated source drift, the release stayed on the narrow file-to-file lane again: `router-detail-workspace.tsx` and `router-editor-state.ts` were backed up under `/opt/vectra-prorouter-backups/web-subscription-cleanup-20260421-074636/`, copied directly into `/opt/vectra-prorouter/apps/web/src/components/`, and `vectra-web` was rebuilt via the same VPS-side background runner pattern (`/tmp/vectra-web-rebuild-20260421-074709.log` + `.status`) that survives SSH disconnects during long `next build` runs.
- Production build proof is explicit rather than inferred from source timestamps. The attended VPS rebuild finished `status=ok`, `vectra-web` returned to `healthy`, public smoke stayed at `307/200/200/200/400`, and `scripts/VectraPanelCli.sh status` still reports `browserPushMonitor/dbRead/dbWriteProbe = true`. The running container bundle now also contains the new operator copy inside `/app/apps/web/.next/static/chunks/app/routers/[routerId]/page-*.js`, including `Subscription Cleanup`, `Нормализация draft и live runtime`, and `Подготовить cleanup draft`, which confirms the router detail page is serving the new cleanup actions from the live built artifact rather than only from source.
- This closes the operator loop for the current subscription-runtime slice: live router imports are already authoritative for listing subscription nodes, stale panel-only footprints can now be resaved away from the router page itself, and true router-local leftovers can now be removed from the next draft/apply without dropping into JSON mode. The remaining work is no longer UI rollout but operator proof of an attended cleanup on one agreed router plus the broader runtime cleanup policy for duplicates/local leftovers.

2026-04-21 updates live monitor addendum:

- The local `/updates` workspace now opens a dedicated live progress modal immediately after both single-router and bulk update/reboot actions instead of leaving the operator on the drift table with only background queueing. `apps/web/src/components/update-version-drift-workspace.tsx` now captures the exact `jobId` values returned by queue mutations, and the new `apps/web/src/components/update-launch-monitor-dialog.tsx` renders a per-launch overlay with automatic refresh, aggregate counters, per-router status cards, package-level detail chips, and router-reported error/stdout/stderr blocks.
- The backend side for that monitor stays intentionally narrow and compatible with existing job semantics. `apps/web/src/server/api/routers/update.ts` now exposes `update.launchProgress`, which resolves only the requested job ids back into normalized management-task items plus router display names; this makes the modal follow the exact launch the operator just triggered, including immediate queue failures for blocked or unsupported routers, instead of polling the whole updates workspace and guessing.
- Local verification for this slice is green on `pnpm --filter @vectra/web test --run src/server/api/routers/update-progress.test.ts`, `pnpm --filter @vectra/web typecheck`, `pnpm --filter @vectra/web lint`, and `pnpm --filter @vectra/web build`. This also closes the earlier local updates-surface typecheck drift around `update-launch-monitor-dialog.tsx`, `update-version-drift-workspace.tsx`, and `server/api/routers/update.ts`. Production rollout is still the remaining step before the new live monitor exists on deployed `/updates`.

2026-04-21 updates rollout addendum:

- The `/updates` release is now deployed on production too through the same narrow backup-first VPS lane. The previous live files were saved under `/opt/vectra-prorouter-backups/web-updates-monitor-20260421-080416/`, then only `apps/web/src/components/update-version-drift-workspace.tsx`, new `apps/web/src/components/update-launch-monitor-dialog.tsx`, and `apps/web/src/server/api/routers/update.ts` were copied into `/opt/vectra-prorouter`; final SHA-256 checks on the VPS match local source for all three intended files.
- The rebuild again used the safer host-side background runner instead of waiting on an interactive SSH session to survive `next build`. `/tmp/vectra-web-rebuild-20260421-080553.status` finished with `ok`, `vectra-web` returned to `healthy`, public smoke stayed at `307/200/200/200/400`, and `scripts/VectraPanelCli.sh status` still reports green `browserPushMonitor/dbRead/dbWriteProbe`.
- Live proof for the deployed surface is intentionally safe rather than destructive. Browser verification on production `https://router.vectra-pro.net/updates` now shows the `Контроллер версий` tab with the new bulk-action controls (`Выбрать все по фильтру`, `Перезагрузить выбранные роутеры`), and `scripts/VectraPanelCli.sh call update.launchProgress -- --input '{"jobIds":[]}'` now reaches the deployed procedure and returns the expected Zod validation error for empty `jobIds` instead of `procedure not found`. That is enough to confirm the new monitor path is live without queueing a real update or reboot on the park.

2026-04-21 subscription cleanup UX simplification addendum:

- A same-day local follow-up now simplifies the operator path on the router `Node List` instead of adding more cleanup branches. `apps/web/src/components/router-detail-workspace.tsx` now presents the node area through three simple counters (`Редактируете`, `На роутере`, `Разобрать`), keeps only the real live subscription list always open, and moves panel-only/router-local leftovers behind a disclosure so the primary working list is visually obvious.
- The same pass also collapses the cleanup actions into one primary sync button whose wording matches the situation: `Подготовить чистый draft` when local leftovers must be removed first, or `Синхронизировать панель с роутером` when the router list is already clean and the panel only needs a fresh revision. Short inline guidance now explains whether the action merely prepares a draft or immediately saves a new panel revision.
- The separate payload/live/panel audit is still available for debugging, but it is no longer always expanded in `Подписки`; it now lives behind a `Техническая диагностика` disclosure so the main operator flow stays focused on the live router list instead of advisory server-side comparison data.
- Local verification for this UX-only pass is green on `pnpm --filter @vectra/web exec eslint src/components/router-detail-workspace.tsx`, targeted runtime/editor Vitest, and `pnpm --filter @vectra/web build`.
- The simplification is now rolled out to production too through the same backup-first non-git VPS lane: only `apps/web/src/components/router-detail-workspace.tsx` was backed up under `/opt/vectra-prorouter-backups/web-subscription-cleanup-ux-20260421-112100/`, copied into `/opt/vectra-prorouter`, and rebuilt in the background with `status=ok`. Post-rollout proof is green on `docker compose --env-file .env ps web` (`healthy`), standard public smoke `307/200/200/200/400`, `scripts/VectraPanelCli.sh status`, and direct VPS source checks for the new `Синхронизация списка нод` / `Техническая диагностика` copy.

2026-04-21 subscription cleanup attended proof addendum:

- The simplified panel flow is now live-proven on a real router instead of remaining only a UI/deploy claim. On `AndreyVK_Sochi` (`routerId = 16129064-3a73-423d-818e-99ed9e73b61f`), the pre-action workspace showed `configSourceMode = live-import`, `managedNodes = 21`, `panelOnlyNodes = 13`, `orphanNodes = 0`, and `importReviewState = approved`, which made it the exact "panel drift only" case for the new single-button sync path.
- The attended action deliberately reused the router-authoritative draft instead of reconstructing anything from advisory payload data. `scripts/VectraPanelCli.sh draft editor AndreyVK_Sochi --json` was used to extract the current `.draftConfig`, and that exact live-aligned draft was persisted back into panel history with `draft save ... --note 'Sync panel draft to current live subscription nodes from router'`, producing revision `2a5ca24c-d939-4bed-8acc-aadc27408164`.
- Post-save proof closes the operator loop for this branch of the cleanup UX: fresh workspace/editor reads now show `latestDraftId = workspaceRevisionId = 2a5ca24c-d939-4bed-8acc-aadc27408164`, `managedNodes = 21`, `panelOnlyNodes = 0`, and `orphanNodes = 0`. This is the first attended proof that the simplified `Синхронизировать панель с роутером` path actually clears stale panel history without reintroducing the old "all live nodes are errors" failure mode.
- The remaining subscription-cleanup step is now narrower and more honest. The panel-sync branch is proven on a real router; what still needs attended proof is the other primary action, `Подготовить чистый draft`, on a router that truly has router-local leftovers or non-zero `orphanNodes`.
  2026-04-21 updates confirm-dialog addendum:
- The `/updates` bulk reboot lane no longer falls back to a browser-native `window.confirm(...)` popup. `apps/web/src/components/update-version-drift-workspace.tsx` now opens a site-native confirmation dialog that lists the selected router names, keeps the destructive copy inside the Vectra UI shell, and hands off into the already-deployed live launch monitor after confirmation instead of spawning a separate browser window.
- Local verification for this narrow UI hotfix is green on `pnpm --filter @vectra/web typecheck`, `pnpm --filter @vectra/web lint`, and `pnpm --filter @vectra/web build`.
- Production proof is now explicit on the safe test-router contour without queueing a new reboot. After a one-file backup-first VPS sync plus background rebuild, live `https://router.vectra-pro.net/updates` no longer raises the old native confirm for bulk reboot. With only test router `OpenWrt` selected in `Контроллер версий`, clicking `Перезагрузить выбранные роутеры` now opens an in-page modal with title `Поставить перезагрузку в очередь?`, router chip `OpenWrt`, and buttons `Отмена` / `Подтвердить перезагрузку`; dismissing the modal returns to the table without any system dialog. This closes the specific operator complaint about reboot confirmation opening outside the site shell.

2026-04-21 live UX audit addendum:

- A same-day live operator walkthrough now isolates a separate ergonomics problem that cuts across the current production web slice even where behavior is already correct. The review used the live logged-in panel plus `scripts/VectraPanelCli.sh` coverage and walked `/fleet`, `/drafts`, `/updates`, `/rescue`, `/enrollment`, and router detail on safe router `OpenWrt`; one additional router-detail navigation to `AndreyVK` also remained stuck on the verbose route-loading state for more than `20s`, which makes the loading contour itself part of the UX problem rather than a neutral transition.
- The dominant issue is not missing capability but stacked explanation layers. Shared chrome already shows the current section, command/search entry, and top-level tabs, yet most routes repeat another header paragraph and then add one or two onboarding/help panels before the real work surface begins. In practice this makes healthy screens read like documentation first and tooling second: `fleet-monitoring-workspace.tsx` puts `Triage-режим` and notification explainer cards above search and the operations table, `app/drafts/page.tsx` adds a full `Не основной путь` warning block before the JSON workspace, `app/updates/page.tsx` opens with both a three-step tutorial and a release-summary block before the active updates workspace, and `router-detail-workspace.tsx` layers a long summary narrative plus dense status cards before the editable PassWall tabs.
- The current routes also mix distinct operator modes at the same visual priority: live state, panel truth, trust/audit explanation, onboarding, diagnostics, and expert/reference material compete on one screen. `Rescue` and `Enrollment` are currently the clearest because they keep one primary job in view, while `Drafts` and `Updates` are the noisiest top-level sections and router detail remains the highest cognitive-load surface.
- The shared loading system amplifies the same pattern. `components/route-state.tsx` and the route-level `loading.tsx` files turn every transition into a mini explainer with summary, details, and checkpoint cards; the live stuck `AndreyVK` route made that especially visible, because the operator sees a long narrative about what the system intends to load instead of a compact skeleton plus retry or escape path.
- The next UX cut is now clearer than the broader product backlog. Keep the current information model, but compress the above-the-fold layer aggressively: reduce `operator-shell-header.tsx` and `page-header.tsx` to navigation plus one-sentence context, move help/trust/reference copy behind disclosures or secondary panels, default `/updates` to the active control workspace instead of tutorial cards, collapse `/drafts` warning copy into one short notice, slim router detail into a compact summary strip plus one explicit next action, and replace the current narrative loading states with minimal skeletons and a small status line.

2026-04-21 operator UX simplification implementation addendum:

- The approved safe UX cut is now implemented locally in source without changing auth, route topology, top-level tabs, tRPC contracts, or router-side save/apply semantics. Shared chrome is slimmer (`operator-shell-header.tsx`, `page-header.tsx`), and `route-state.tsx` now uses compact skeletons plus a timed retry/escape fallback instead of a long explainer loader.
- The top-level operator routes were simplified in the same presentation-only pass: `/fleet` now keeps search/filter/table/alerts first and demotes explanation behind smaller surfaces; `/drafts` opens directly into router selection, preview, and JSON editor with only a small inline expert notice; `/updates` now shows the active workspace immediately above the fold and moves release/reference material behind disclosure; `/rescue` demotes rescue policy behind disclosure; `/enrollment` trims helper copy so bootstrap and next steps stay visually dominant.
- Router detail now follows the intended three-layer contour in source: compact summary strip, one explicit next-safe-action surface for import/reimport/save/apply/recovery, then the PassWall tab workspace. Trust/service/destructive material was moved behind secondary disclosure, and the `AndreyVK` delay was narrowed to server-side `draft.editorSurface` preparation rather than client render.
- The blocking contour for that delay included secondary subscription audits inside `editor-surface.ts`, so the local source now applies a small budget to that audit path to stop a diagnostic read from holding the whole detail route open longer than the editing surface. Local verification for this UX workset is green on `pnpm --filter @vectra/web typecheck`, `pnpm --filter @vectra/web lint`, and `pnpm --filter @vectra/web build`.

2026-04-21 second UX simplification rollout addendum:

- The second safe UX pass is now deployed and live-proven on the heaviest operator surfaces instead of staying local-only. The production slice covers `/updates`, router detail, and `/fleet`, while keeping the same URLs, top-level tabs, `tab` and `section` deep links, command/search routing, save/apply/reimport semantics, and existing tRPC contracts. The change stayed presentation-only in the intended sense: hierarchy, disclosures, and first-paint density were simplified without introducing new workflows or persisted state.
- `/updates` now opens directly into the active workspace contour instead of landing on tutorial-heavy first paint. `UpdatesControlWorkspace` no longer renders the separate `Рабочие поверхности` hero wrapper, the baseline workspace reaches save/status/editor immediately, and the old page-level tutorial and release-summary blocks were demoted below the working surface after the route entry file itself was included in the rollout. Bulk actions, launch monitor, version-control workspace, and confirmation semantics remain unchanged.
- Router detail now follows the intended one-working-contour shape on live production too: compact summary strip, one explicit next-safe-action block, then the PassWall workspace. Unconfirmed changes and trust explanation are secondary disclosures, diagnostics and destructive actions no longer compete with save/apply on first screen, and `Watch Logs` remains reachable without dominating the viewport. The existing PassWall tabs, preview behavior, and save/apply rule that apply uses only the latest saved revision are unchanged.
- `/fleet` now starts with compact status, push/filter controls, and the router list instead of stacked trust/onboarding narrative. Empty alerts no longer occupy a heavy section, `Как читать парк` is collapsed by default, and the row/card presentation now relies on short operator cues for freshness, panel basis, trust, Telegram, rescue, and versions instead of repeating the same model in full prose above the table and inside each row.
- Local verification for the full second-pass workset is green on `pnpm --filter @vectra/web typecheck`, `pnpm --filter @vectra/web lint`, `pnpm --filter @vectra/web build`, and targeted Vitest (`34` files, `191` tests). A local router-detail smoke on `127.0.0.1:3052` remained blocked by workstation data drift in `draft.editorSurface` (`Unsupported state or unable to authenticate data`), but the same route and contour were browser-verified successfully on production, which narrows that failure to local decrypt/auth mismatch rather than a UI regression.
- Live browser smoke now closes the acceptance loop on production itself. Desktop and mobile checks confirm that `/updates` shows the workspace first, router detail keeps summary plus next action above the editor, and `/fleet` shows status/filter/list first with help collapsed. This shifts the next `Web control plane` UX step from rollout to narrow polish only, with the current production contour now serving as the baseline for future operator-facing changes.

2026-04-21 responsive hardening addendum:

- The local operator panel source now has a dedicated responsive-hardening pass instead of relying on scroll-tolerant desktop surfaces. Shared primitives in `apps/web/src/components` were tightened so shell/header/panel/action/dialog surfaces collapse safely below `lg`, and a new `mobile-records.tsx` layer provides the intended card/disclosure presentation for narrow viewports rather than leaving action-bearing tables as the only UI.
- The heaviest operator routes were moved onto that shared pattern in source. `/updates` now uses mobile cards and safer monitor/dialog containers for version drift and rollout controls; router detail now renders card/disclosure alternatives for node lists, subscriptions, app update, geo view, terminal history, and watch logs below `lg`; `/fleet`, `/login`, `/rescue`, `/enrollment`, and `/install` received the remaining shell/pre/code overflow fixes. One real tablet regression found during QA came from the shared `ActionStrip`: at `820px` it still switched to horizontal layout too early and blew router `App Update` out to `1451px`, so the primitive now stays vertical until `lg`, which matches the operator rule that notes and CTA stacks remain vertical on phone and tablet.
- The local QA proof for this slice is now explicit and maintainable. `apps/web/tests/mobile-action-qa.spec.js` became a route matrix with phone `390x844` plus tablet `820x1180`, hard overflow assertions on `documentElement.scrollWidth <= clientWidth`, and interaction helpers that skip disabled controls instead of timing out on valid read-only states. On a clean non-Turbopack `next dev` run at `http://127.0.0.1:3412`, the full matrix passed across `/login`, `/fleet`, router detail tabs, `/updates`, `/drafts`, `/rescue`, `/enrollment`, and `/install`; clean `.next` validation is also green on `pnpm --filter @vectra/web typecheck` and `pnpm --filter @vectra/web build`.
- The remaining gap is rollout proof, not local engineering proof. The live production panel was the source of the original responsive regressions, but this hardening pass is still local source only until the next VPS web deployment. The next acceptance step for this slice is a post-deploy phone/tablet walkthrough on production with the same route matrix.

2026-04-21 stabilization sweep closure addendum:

- The responsive slice is now closed as a production-proven stabilization pass rather than a local-only hardening branch. The final real regression discovered after rollout was narrower than the original route matrix: on production router detail for safe router `OpenWrt` at phone viewport `390x844`, `documentElement.scrollWidth` still reached `625` because the shared PassWall `TabBar` kept a `min-w-max` single row and the `UnconfirmedChanges` cards still let inner grid children leak width. `apps/web/src/components/tab-bar.tsx` now wraps tabs below `lg`, `router-detail-workspace.tsx` and `status-tile.tsx` now force `min-w-0` plus text breaking in the offending cards/grids, and `apps/web/src/components/tab-bar.test.ts` adds a narrow regression proof for that shared primitive.
- The stabilization gate for the deployed web slice stayed fully green from a clean runtime state: clean `.next` wipe, `pnpm --filter @vectra/web lint`, `pnpm --filter @vectra/web typecheck`, `pnpm --filter @vectra/web build`, root `pnpm test`, targeted Vitest on router detail/tab bar surfaces, deploy shell syntax checks, Python compile smoke for `deploy/scripts/refresh-passwall-mirror.py`, and a production-mode Playwright rerun of `apps/web/tests/mobile-action-qa.spec.js` against local `next start`.
- Production acceptance is now explicit on the live panel, not inferred from local QA. After the responsive web slice was rebuilt on the VPS, attended browser walkthroughs on phone `390x844`, tablet `820x1180`, and desktop `1440x900` confirmed `scrollWidth == clientWidth` and no `500` / hidden-primary-control regressions across `/login`, `/fleet`, safe-router detail tabs (`basic-settings/main`, `node-list`, `app-update`, `watch-logs`), `/updates`, `/drafts`, `/rescue`, `/enrollment`, and `/install`.
- The required safe live writes are also closed on the same safe router `OpenWrt` (`e80b2f5b-d80a-4903-85e1-57d8ce747f15`). First, `Синхронизировать панель с роутером` created fresh revision `b782da24-78e1-43c7-a0c6-a43468bcf58c`; second, the normal `Сохранить и применить на роутере` path queued real job `17e18740-3bfe-42ef-9f86-dba27938c357`, which completed `succeeded`. Fresh CLI/browser facts now agree: queue returned to `0`, `router.activeRevisionId = b782da24-78e1-43c7-a0c6-a43468bcf58c`, selected node stayed `RoscomShunt` / `Маршрутизатор BloopCat`, mode stayed `proxy`, and router services remained running.
- Residual risk for this slice is intentionally narrow. No new DB, API, or router-agent contract changes were needed to close the sweep, and the remaining `fleet` cue that `OpenWrt` still needs re-import is treated as existing trust-state/operator follow-up rather than as a regression from this stabilization pass.

2026-04-21 responsive tail-fix addendum:

- A final same-evening production QA pass found two narrower responsive regressions that were still real after the larger stabilization close-out: `/fleet` still overflowed at `1024x768` because the dense operations table became visible exactly at the `lg` breakpoint, and router detail `App Update` still leaked width both at `1024x768` and `1440x900` because the long managed-stack explanatory copy sat inside `ActionStrip` as non-shrinking inline items.
- The source fix stayed intentionally small and presentation-only. `apps/web/src/components/fleet-monitoring-workspace.tsx` now keeps the router-card lane through tablet-landscape and defers the dense fleet table until `xl`, while `apps/web/src/components/router-detail-workspace.tsx` now renders the `App Update` summary as a full-width wrapping block instead of two inline spans that could force the whole strip wider than the viewport.
- Regression proof was added next to the touched surfaces instead of only relying on manual memory. New/updated Vitest source checks now assert the fleet `xl` breakpoint split and the full-width wrapping `App Update` summary copy; the same pass also stayed green on `@vectra/web lint`, `@vectra/web typecheck`, and a clean-`.next` `@vectra/web build`.
- Production proof is explicit after rollout, not inferred from local source. A narrow backup-first VPS hotfix replaced only `fleet-monitoring-workspace.tsx` and `router-detail-workspace.tsx`, rebuilt `vectra-web`, and then passed a headless authenticated route matrix with `32/32` green checks across phone `390x844`, tablet `820x1180`, tablet-landscape `1024x768`, and desktop `1440x900` on `/fleet`, router detail (`basic-settings/main`, `app-update`), `/updates`, `/drafts`, `/rescue`, `/enrollment`, and `/install`, with `scrollWidth == clientWidth` throughout.

2026-04-22 worktree triage addendum:

- The final cleanup pass kept the validated responsive/operator slice intact and only removed hygiene debt around it. Root `.gitignore` no longer hides `apps/web/tests/mobile-action-qa.spec.js`, which means the mobile route matrix is now a real tracked regression harness instead of a local-only hidden file under a test path. The same cleanup also removed obvious local evidence artifacts from the workspace root and regenerated the repo map so web acceptance notes no longer sit next to screenshot/tar/debug residue.
- One last local responsive failure was reproduced honestly during the cleanup gate rather than dismissed as “already handled”: after login, `/fleet` still overflowed on phone `390x844`, and `/enrollment` still overflowed on tablet `820x1180`. The narrow source fixes stayed UI-only: `apps/web/src/components/operator-shell-header.tsx` now hides the chip/tab lanes at the intended breakpoints without leaking tablet width, and `apps/web/src/app/enrollment/page.tsx` now forces long `pre/code` payloads to wrap with `whitespace-pre-wrap`, `break-all`, and `[overflow-wrap:anywhere]`.
- The earlier fear that local `next start` was serving stale route code turned out to be a runtime false signal, not a lost web slice. After a clean `.next` rebuild and a single fresh `VECTRA_SECRETS_KEY=... pnpm --filter @vectra/web start -p 3400`, the current `/fleet` and `/enrollment` markup was served correctly and the production-mode Playwright route matrix passed again on both `390x844` and `820x1180`. Together with green `@vectra/web lint`, `typecheck`, `build`, and root `pnpm test`, this leaves no new reproduced web blocker in the current worktree beyond normal commit-slice cleanup.

2026-04-22 router-identical subscription preview addendum:

- The local router-detail subscription slice now stops treating a server-side `fetch(subscription.url)` as the source of truth for “how many nodes the subscription really gives now”. `apps/web/src/server/vectra/editor-surface.ts`, `subscription-runtime.ts`, `server/api/routers/update.ts`, and `components/router-detail-workspace.tsx` now consume only the latest successful router-side `inspect_subscriptions` preview whose digest matches the current live `subscribe_list` state; if that preview is missing, stale, or failed, the UI explicitly says the real router preview is unavailable instead of inventing a count from the server.
- The operator UX is also narrower and more honest now. Router detail shows three separate facts for subscriptions: `Подписка сейчас отдаёт`, `На роутере импортировано`, and `Разобрать`. Live imported subscription nodes stay authoritative in the main node list, panel-only drift remains separate, and cases where the router still holds more nodes than the current preview are labeled as cleanup/drift (`cleanup-needed`) rather than as a fake payload error.
- Local proof for this slice is green on `pnpm --filter @vectra/web lint`, `pnpm --filter @vectra/web typecheck`, and targeted Vitest `src/server/vectra/subscription-runtime.test.ts`, `src/server/vectra/editor-surface.test.ts`, and `src/components/router-detail-workspace.test.ts`. This is still a local source/worktree result only: no production rollout claim is made yet, and no live router preview run has been attended from the panel after this new read-only job type landed.

2026-04-22 PassWall managed-stack package-target hotfix addendum:

- A same-day production follow-up now closes the remaining `Web control plane` no-op bug behind the user report “panel says updated, but PassWall app stayed old”. Live job `c697cb43-b9b3-437f-a349-ed747f91ebf2` had already proved the mismatch precisely: bundle `targetVersion = 26.4.20-1`, but `update.launchProgress` still showed `packageTargetVersion = 26.4.10-r1`, so `luci-app-passwall2` was treated as already current and never moved off `26.4.10-r1`.
- Root cause was server-side artifact resolution in `apps/web/src/server/api/routers/update.ts`, not router-side delivery. `buildPasswallArtifactDescriptorMap()` merged the selected bundle metadata with all `passwall_package` rows, so older package rows from release `26.4.10-1` could overwrite the newer bundle-selected descriptors by package name. Local proof for the fix is explicit: targeted Vitest on `src/server/api/routers/destructive-gating.test.ts` and `src/server/api/routers/update-progress.test.ts` plus `pnpm --filter @vectra/web typecheck` were green after constraining overlay package rows to the selected bundle `releaseTag`.
- Production proof is now end-to-end on safe router `OpenWrt`. After the narrowed web redeploy, live job `623e35a0-0a03-4b6a-9f8f-0b85d7e85db3` queued `targetVersion = 26.4.20-1`, `packageTargetVersion = 26.4.20-r1`, and artifact URL `.../luci-app-passwall2_26.4.20-r1_all.ipk`; the job then completed `succeeded`, and fresh inventory snapshots now report `luci-app-passwall2 = 26.4.20-r1`.
- The remaining nuance for this surface is now narrower than package targeting. Live result semantics for already-ahead or runtime-only components are still somewhat confusing (`xray-core` and `geoview` remain `runtime-only-converged`, and `v2ray-geoip` can still read as `updated/not-needed` while the installed package record stays ahead of the mirror target), but the specific user-facing PassWall app no-op bug is now production-proven fixed.

2026-04-22 control-plane recovery incident addendum:

- The local control-plane backend now synthesizes router-reported recovery state into the existing incident model instead of requiring a schema or DB migration. `apps/web/src/server/vectra/router-control.ts` reads `recoveryPhase`, `lastRecoveryAction`, `awaitingOperator`, and grouped `panel/ru/foreign` reachability from the router check-in payload and maps them back into only the already-supported incident types: `server_unreachable`, `proxy_outage`, and `recovered`.
- The server-side behavior stays intentionally single-open-incident per router. When the router reports `controller_restart_wait`, the backend opens or refreshes `server_unreachable`; when the router reports `operator_attention` or another unresolved proxy-recovery phase with foreign reachability still not healthy, it opens `proxy_outage`; and when the synthetic recovery contour no longer asks for an open incident, the same path resolves the current open record instead of creating a parallel incident type.
- Local proof for this slice is green on the targeted server tests and the broader web verification lane already run from the current worktree: `pnpm --filter @vectra/web exec vitest run src/server/vectra/router-control.test.ts`, `pnpm --filter @vectra/web typecheck`, and `pnpm --filter @vectra/web build`. This remains source-only until the next panel rollout, so `Web control plane` stays at `pilot/high`; the next proof step is to deploy this slice and verify the new recovery incidents surface correctly in the existing production Rescue/Fleet/browser-push flow.

2026-04-22 control-plane recovery incident safety review addendum:

- A same-day deep safety review found that the new synthetic incident layer is not yet ownership-safe enough for rollout. `buildSyntheticRecoveryTransitions()` can synthesize `recovered` from any clean supervisor state while `applyIncidentTransitions()` resolves every open incident for the router, not only supervisor-owned `server_unreachable` / `proxy_outage`; in the current local source this can silently close unrelated open records such as `subscription_degraded` or manual/direct-mode incidents.
- The same resolution path also rewrites router status back to `active` or `pending` when it resolves incidents. Combined with the broad “resolve all opens” behavior, a clean control-plane recovery check-in can therefore clear another incident origin and also wipe a still-meaningful `direct` status even though the supervisor did not own that state.
- Current router-control tests cover only the happy synthetic open/resolve shapes for the new supervisor and do not yet lock the negative path above. Before deploy, this slice still needs explicit tests for “existing unrelated open incident survives supervisor recovery”, “manual direct-mode incident is not resolved by synthetic `recovered`”, and “sticky agent `operator_attention` does not keep reopening `proxy_outage` after a successful reconnect”.

2026-04-22 control-plane outage supervisor blocker-fix addendum:

- The local web backend now scopes synthetic control-plane recovery incidents to its own ownership instead of treating any open incident on the router as disposable. `router-control.ts` marks synthetic supervisor incidents with `metadata.origin = control-plane-recovery`, only synthesizes open/resolve transitions against those owned incidents, and skips synthetic open/resolve entirely when the currently open record belongs to a different origin such as `subscription_degraded`.
- The corresponding resolver path is now narrowed too. `applyIncidentTransitions()` no longer resolves every open incident on synthetic `recovered`; for supervisor-origin transitions it resolves only supervisor-owned `server_unreachable` / `proxy_outage` records and leaves unrelated incidents plus router status untouched.
- Local regression proof for this slice is green on `pnpm --filter @vectra/web test -- --run src/server/vectra/router-control.test.ts`. The targeted tests now cover the previously missing negative cases where a foreign open incident must survive a clean supervisor state and where the synthetic layer must not open a new supervisor incident on top of another active incident.

2026-04-23 full worktree rollout addendum:

- The current releasable web/contracts/db slice is now live on production rather than only synced on disk. A checksum dry-run against `/opt/vectra-prorouter` showed no remaining deployable file drift before rebuild, then the VPS rebuilt `vectra-web` from the already-synced current source tree and recreated the `web` container back to `healthy`.
- Current production health is explicit on every usual gate: `docker compose ps` shows `vectra-web` healthy after rebuild, `scripts/VectraPanelCli.sh status` returns green `browserPushMonitor/dbRead/dbWriteProbe`, and `deploy/scripts/smoke-check.sh` stays on the standard `307/200/200/200/400` contour. The matching DB migration boundary is also confirmed live: PostgreSQL already contains enum value `inspect_subscriptions`, so the current web slice is not hanging on an unapplied schema change.
- Panel-facing proof for the newly deployed source slice is concrete. `update.artifacts` now lists controller/LuCI `0.1.13-r4` at the top, and `update.versionDriftWorkspace` now reports `controllerAvailable = 0.1.13-r4` across current rows while installed routers still sit on `0.1.13-r3`. This means the current web/backend release is already serving the new controller line, the narrower synthetic-incident ownership logic, and the rest of the current `/updates` / router-control server slice from the live app rather than from an undeployed worktree.
- The remaining gap for this module is now router-side proof, not panel rollout. Features that depend only on panel/backend state are live, but the new router-originated paths in this same source line, such as live subscription preview and control-plane supervisor check-in semantics, still need one safe router on `0.1.13-r4` before they can be called production-proven end to end.

2026-04-25 router-detail DNS Clear IPSET/NFTSet addendum:

- `Web control plane` now restores the missing upstream PassWall2 DNS maintenance action on router detail. The `DNS` secondary tab in `apps/web/src/components/router-detail-workspace.tsx` now exposes `Clear IPSET/NFTSet`, reusing the controller terminal lane through new `update.queuePasswallClearIpsets` instead of requiring a separate LuCI visit.
- The queued command mirrors the upstream `flush_set` semantics: it sets `passwall2.@global[0].flush_set='1'`, commits `passwall2`, and restarts PassWall2 so the active iptables/nftables stop path clears temporary IPSET/NFTSet state. The task is labelled separately in the management log and treated as exclusive during router check-in delivery so it does not run in the same delivery batch as apply/update/reboot work.
- Local proof is green on `pnpm --filter @vectra/web test -- --run src/components/router-detail-workspace.test.ts src/server/api/routers/destructive-gating.test.ts src/server/vectra/editor-surface.test.ts src/server/vectra/router-control.test.ts`, `pnpm --filter @vectra/web typecheck`, `pnpm --filter @vectra/web lint`, and `pnpm --filter @vectra/web build`. No live router Clear IPSET/NFTSet job was queued during this local implementation pass.

## 2026-04-26 controller self-update incident follow-up

- Production web was hotfixed for the `0.1.13-r4` LuCI-loss incident: terminal controller self-update no longer reports partial LuCI reinstall failure as success, verifies both controller packages plus LuCI surface files before `exit 0`, and task-log summaries now surface actionable `opkg` errors such as `check_data_file_clashes` instead of green success copy. Local verification: `pnpm --filter @vectra/web test --run destructive-gating editor-surface` and `pnpm --filter @vectra/web typecheck`; production Docker build and health checks passed after redeploy.

2026-04-29 Vagrand subscription/control-plane preview addendum:

- Local source now closes the operator-facing false-apply contour exposed by `VagrandRouter`: router detail apply preview uses the fresh live import as the apply base when available, so a newly read live config no longer appears as a pending router apply just because the older authoritative revision differs.
- Managed subscription nodes are now inspectable from the router detail node list through read-only details. The UI exposes non-secret diagnostics such as protocol, endpoint, transport, TLS, REALITY/public-key presence, server name, gRPC mode, and extra key names without making subscription-produced nodes editable by default.
- Verification is local-only for this source slice: targeted Vitest on contracts/editor/subscription-runtime/router-detail and `pnpm --filter @vectra/web typecheck` are green. Production still runs the old panel until the next guarded web deploy.

2026-04-30 saved-draft/apply guardrail addendum:

- Local web slice now treats editable `operator_draft` revisions as applyable only when they are the newest non-superseded draft for the router; newer active/live import revisions hide stale drafts from `draft.workspace` and `draft.editorSurface`, and `draft.queueApply` rejects superseded or non-current revision ids before creating `apply_passwall_config` jobs.
- Router detail and the expert drafts workspace now make the apply semantics explicit: apply uses a saved panel revision and rewrites managed PassWall sections. Router detail also exposes a guarded `draft.discard` action for saved-draft-pending-apply revisions; queued apply jobs are cancelled only while still `queued`, while delivered/running apply remains a re-import case.
- Local proof: `pnpm --filter @vectra/web test --run draft-selection destructive-gating`, `pnpm --filter @vectra/web test --run editor-surface editor`, `pnpm --filter @vectra/web typecheck`, `pnpm --filter @vectra/web lint`, and `pnpm --filter @vectra/web build` are green. Production was only read-checked via `bash ./scripts/VectraPanelCli.sh fleet list`; no live apply/discard was executed.

2026-05-03 router-detail apply-preview UX addendum:

- Local router-detail UX now separates operator-visible field changes from the controller's technical full managed-section sync. The apply preview title changed to `Что реально изменится`, adds a factual diff count/list, hides duplicate `Rule Manage` ShuntRule mirror diffs when the matching `Basic Settings` ShuntRule field already changed, and relabels the large UCI command list as technical sync commands rather than real separate changes.
- This directly addresses the confusing ShuntRule target-edit case: changing one rule target can still produce a large `node_sync` command list because the controller rewrites managed PassWall2 sections safely, but the panel should now show the operator that the real edit is one/few field changes.
- Local proof: `pnpm --filter @vectra/web typecheck`, `pnpm --filter @vectra/web test --run router-editor-state editor`, `pnpm --filter @vectra/web lint`, and `pnpm --filter @vectra/web build` are green. Production deploy also completed through the guarded web release lane: `vectra-web` rebuilt/restarted healthy, standard smoke returned `307/200/200/200/400`, and deployed source/bundle markers contain the new apply-preview copy.

2026-05-11 Fleet RAM monitoring addendum:

- `apps/web` now promotes router RAM telemetry from inventory snapshots into the operator monitoring surface instead of leaving it buried in raw check-in data. The shared `router-memory` helper normalizes free RAM into `good`, `warning`, `critical`, or `unknown`, with warning below 64 MB/28% and critical below 48 MB/20%.
- Fleet monitoring now includes RAM in server snapshots, search, sorting, visible top chips including numeric `Мин. RAM`, alert cards, chart filters, desktop rows, and mobile router cards. Reachable routers with low RAM emit a `low_memory` alert so the panel shows OOM risk before PassWall/Xray is killed.
- Router detail now shows `RAM свободно` beside controller/link state and includes RAM in the App Update resource context. Local proof for this panel slice is green on full `@vectra/web` Vitest, `typecheck`, `lint`, `build`, and `git diff --check`; no live production deploy has been claimed in this note.

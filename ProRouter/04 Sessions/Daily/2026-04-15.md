---
type: session
date: 2026-04-15
tags:
  - session
---

# Session 2026-04-15

## Summary

- Continued the web control-plane redesign locally with a denser operator-shell / fleet-first / sticky-router-workspace slice.

## Completion Updates

### 00:14

- Summary: Installed GitHub CLI locally, authenticated `gh` against `github.com` for account `Waizor1` using the existing secure local token source, confirmed `ADMIN` access to private repo `Waizor1/Vectra-ProRouter` via GitHub API, and verified non-interactive HTTPS git access with `git ls-remote origin main` after `gh auth setup-git`.
- Modules: [[02 Modules/Knowledge Base and Runbooks]]
- Next: Use `gh` for repository operations such as PRs, workflow inspection, releases, and issue/comment handling without additional local setup.

### 00:27

- Summary: Re-audited local SSH access safely: confirmed `vectra-prod` remains the only configured server alias on this workstation, proved it resolves to and works with the dedicated `vectra_pro_vps` key in `BatchMode`, confirmed `tvpn_backend_github` as a separate GitHub SSH auth key, and recorded the raw mapping only in the gitignored local-access note.
- Modules: [[02 Modules/Knowledge Base and Runbooks]]
- Next: Treat `tvpn_backend_deploy` and `tvpn_frontend_deploy` as unmapped standalone deploy keys until a host-level mapping is explicitly recovered from deployment docs or operator input.

### 00:31

- Summary: Checked whether the workspace itself ties `tvpn_*` access material to Vectra or Strata naming. No repo evidence was found beyond the freshly added audit notes, so the current reading is narrowed to an operator hypothesis: `tvpn_backend_deploy` and `tvpn_frontend_deploy` probably belong to another project, likely Strata VPN, and should remain outside the confirmed Vectra access set until matched to real host documentation.
- Modules: [[02 Modules/Knowledge Base and Runbooks]]
- Next: Keep only `vectra-prod` and the dedicated Vectra VPS key in the confirmed server-access set for this project; recover `tvpn_*` host mapping later from external deployment notes if needed.

### 10:20

- Summary: Shipped a new local `apps/web` operator-shell refresh slice: densified shared UI primitives, pushed `Парк` harder toward a widescreen table-first control plane, and restructured the top of `router-detail-workspace.tsx` into a more rational split between summary/context and a sticky action rail while keeping existing routes, tabs, and mutations intact. LSP diagnostics on all touched TS/TSX files are clean.
- Modules: [[02 Modules/Web Control Plane]]
- Next: Install missing `apps/web` dependencies, rerun `npm exec --yes pnpm -- --filter @vectra/web typecheck`, `test --run`, and `build`, then decide whether to continue modularizing router detail below the command deck or move to production verification.

### 12:05

- Summary: Repaired local Sugar Memory availability for this workspace by tracing the timeout to the pipx `sugarai` runtime rather than repo config: MCP launch config was valid, but semantic/vector search in `sugar/memory/store.py` failed with `near "->": syntax error`, so the local runtime was patched to downgrade to FTS5 keyword search when sqlite-vec probing or vector queries fail. Real verification then passed through `sugar-memory_get_project_context`, `sugar-memory_search_memory`, `sugar-memory_store_learning`, and `sugar-memory_list_recent_memories`.
- Modules: [[02 Modules/Knowledge Base and Runbooks]]
- Next: If desired, upstream the Sugar fallback fix or replace the local pipx hotfix with an official package update so future reinstall/upgrade does not overwrite it.

### 12:32

- Summary: Hardened the repo-side follow-up around that fix and cleaned up the workstation docs for macOS use. Added `scripts/ensure-sugar-memory-local-fallback.py` plus `ai_docs/develop/features/sugar-memory-local-runtime.md` so the user-local Sugar patch can be checked/reapplied after a pipx reinstall, added `scripts/run-ps1.sh` as a shell wrapper for tracked `.ps1` helpers, updated the main docs/indexes from Windows-only `powershell` / `Get-Content` examples to `pwsh -NoProfile -File` with POSIX paths, and relaxed the live-router helpers to resolve `plink` / `pscp` as well as `plink.exe` / `pscp.exe` for Homebrew-style macOS installs.
- Modules: [[02 Modules/Knowledge Base and Runbooks]]
- Next: If full native macOS router automation is needed later, refactor the remaining PuTTY-oriented transport layer to support plain `ssh` / `scp` and OpenSSH-style host-key pinning in addition to the current PuTTY-compatible path.

### 13:18

- Summary: Shipped that second-stage transport refactor too. Added a shared `scripts/OpenWrtSshTransport.ps1` layer, wired `Get-OpenWrtRouterInventory.ps1`, `Manage-OpenWrtTmpProgramSession.ps1`, and `Invoke-VectraPostSysupgradeRestore.ps1` to support both native `ssh`/`scp` and the existing PuTTY lane, aligned all three helpers on the safer `sh -s` stdin execution pattern for multi-line remote shell blocks, and updated the docs/examples to prefer OpenSSH on macOS/Linux through `-Transport OpenSSH` plus `-OpenSshKnownHostsFile` and optional `-OpenSshIdentityFile` while keeping `RouterPassword` + `RouterHostKey` as the explicit fallback path.
- Modules: [[02 Modules/Knowledge Base and Runbooks]]
- Next: Run a live dual-transport smoke (`OpenSSH` + `PuTTY`) from a workstation with `pwsh` installed against a safe router, then narrow public guidance to the auth model we actually want operators to default to.

### 12:22

- Summary: Finished the local `apps/web` operator-control-plane refresh properly: installed missing workspace dependencies, fixed a small `router-console.ts` type-narrowing bug, reran `npm exec --yes pnpm -- --filter @vectra/web typecheck`, `test --run`, and `build` to green, then used Oracle review to tighten the two main UX risks by making horizontal table overflow explicit and selected rows in router-detail editors visually obvious. The only remaining build note is the same pre-existing lint warning in `src/server/api/routers/rescue.ts` about `import()` type annotations.
- Modules: [[02 Modules/Web Control Plane]]
- Next: Production-verify the refreshed shell/fleet/router-detail slice in a browser, then decide whether to continue decomposing `router-detail-workspace.tsx` or move to the next operator-surface target.

### 12:47

- Summary: Closed the last local QA gap for the refreshed `apps/web` control-plane slice. Post-implementation review initially failed strict sign-off only because browser QA could not get past `/fleet`: local `api/health` was `503`, Postgres was absent, and the running dev app was actually using `DATABASE_URL=postgres://paperclip:paperclip@localhost:5432/paperclip` from the shell environment rather than `apps/web/.env`. I resolved that by installing Homebrew `postgresql@16`, starting the local service, creating the expected `paperclip` role/database, running `db:migrate`, and re-running local browser smoke. After that, authenticated local navigation successfully rendered `/fleet`, command routing into `/drafts`, and the refreshed `Установка` / `Обновления` / `Восстановление` surfaces with no browser console errors and clean `200` network responses on the exercised routes.
- Modules: [[02 Modules/Web Control Plane]]
- Next: If desired, repeat the same browser smoke on production for the refreshed shell/fleet/router-detail slice; otherwise the next local follow-up is architectural cleanup of `router-detail-workspace.tsx` rather than further sign-off work.

### 16:18

- Summary: Finished the last deploy-readiness cleanup for the local web slice. Removed the remaining build/lint warning in `src/server/api/routers/rescue.ts` by switching the `db` reference to a proper type-only import, then reran `npm exec --yes pnpm -- --filter @vectra/web typecheck`, `test --run`, and `build` to full green with no warning residue. I also checked local fixture coverage after browser QA: the local `paperclip` database currently has zero `vectra_router` rows, so router-detail could not be browser-walked in this session, but that is now a data-gap rather than a code/runtime blocker for deploy readiness.
- Modules: [[02 Modules/Web Control Plane]]
- Next: Safe next move is production browser-walk of `/fleet`, `/drafts`, `/updates`, `/rescue`, `/enrollment`, and one real `/routers/[routerId]` record after deploy; local follow-up, if needed, is seeding/importing a router fixture for deeper router-detail smoke rather than more pre-deploy code work.

### 17:24

- Summary: Added a structural deploy-safety fix after the production runtime incident. The root cause was not web code itself but the non-git VPS deploy root sharing immutable release files and mutable live state under `deploy/runtime/*`; one bad sync touched `deploy/runtime`, production briefly came up against an empty Postgres data dir, and recovery required restoring runtime from `/opt/vectra-prorouter-backups/web-deploy-ready-20260415-140417`. To prevent recurrence, the repo now ships `deploy/scripts/deploy-web-release.sh`, which only allowlist-syncs releasable files, refuses any release payload containing `deploy/runtime`, rejects embedded `.env` files, and verifies `deploy/runtime/postgres/PG_VERSION` plus `deploy/runtime/artifacts` before a rebuild can proceed. `deploy/README.md` was updated to forbid whole-root `rsync --delete` into `/opt/vectra-prorouter` and to document the staging -> script-driven safe lane instead.
- Modules: [[02 Modules/Deployment Stack]] [[02 Modules/Web Control Plane]]
- Next: For the next attended production release, use only the staged release-slice + `deploy-web-release.sh` path and do not hand-roll rsync flags; if artifact publishing is needed, keep it in a separate runtime-artifacts lane rather than bundling it into web release sync.

### 16:58

- Summary: Added native macOS/Linux Python entrypoints for the repo's primary workflows, switched canonical runbooks from PowerShell to python3, and kept legacy .ps1 helpers as fallback only.
- Modules: [[02 Modules/Knowledge Base and Runbooks]]
- Next: Live-smoke the native OpenSSH router paths from a workstation with router access and Docker-backed DB harness when available.

### 17:00

- Summary: Implemented native macOS-first Python entrypoints for the documented router/planning/vault/bootstrap/DB workflows, updated repo docs and indexes to prefer python3 commands, and kept PowerShell helpers as legacy fallback.
- Modules: [[02 Modules/Knowledge Base and Runbooks]], [[02 Modules/Deployment Stack]], [[02 Modules/Shared Database]]

### 17:09

- Summary: Deployed the verified apps/web control-plane refresh to production through the documented VPS staging lane with a pre-deploy PostgreSQL dump plus file backup, rebuilt vectra-web cleanly, and restored healthy public router/operator health endpoints. Safety check confirmed the deploy does not mutate production router records or schema, but does cause a short shared vectra-web interruption for both operator UI and router-facing /api/router traffic while the container restarts; post-deploy smoke returned 307/200/200/200/400 and the production login shell reflects the new UI.
- Modules: [[02 Modules/Web Control Plane]], [[02 Modules/Deployment Stack]]

### 17:15

- Summary: Production incident and recovery: after the web deploy, routers disappeared from the panel because the release sync mistakenly replaced deploy/runtime with a slice that omitted runtime subdirectories, leaving production pointed at an empty Postgres data dir. Recovery was completed by stopping the stack, preserving the broken runtime under /opt/vectra-prorouter-backups/runtime-broken-20260415-141339, restoring deploy/runtime from the pre-deploy file backup /opt/vectra-prorouter-backups/web-deploy-ready-20260415-140417, and bringing the stack back up. Post-recovery checks confirmed vectra_router=6, vectra_router_inventory_snapshot=45411, public smoke 307/200/200/200/400, and authenticated production fleet rendering all six routers again.
- Modules: [[02 Modules/Web Control Plane]], [[02 Modules/Deployment Stack]]

### 17:20

- Summary: Hardened the VPS web deploy lane so future releases cannot overwrite deploy/runtime by mistake. Added deploy/scripts/deploy-web-release.sh as an allowlist-only sync path that rejects release payloads containing deploy/runtime or env files and checks protected runtime markers before rebuild; updated deploy/README.md to forbid whole-root rsync --delete into the non-git deploy root and document the new staged safe lane. Local dry-run verification confirmed normal release slices pass while any source containing deploy/runtime fails closed.
- Modules: [[02 Modules/Deployment Stack]], [[02 Modules/Web Control Plane]]

### 17:24

- Summary: Analyzed control-plane UX cleanup framing for operator-facing multi-router surfaces; identified product goal, duplication failure modes, preserved strengths, and top simplification/mobile priorities.
- Modules: [[02 Modules/Web Control Plane]]

### 17:40

- Summary: Implemented the next apps/web UX cleanup pass: simplified operator shell framing, removed duplicated workflow-map/explainer chrome from updates/enrollment/rescue/drafts, kept the fleet table-first direction intact, and tightened mobile-first information priority on the touched pages. Local LSP diagnostics plus @vectra/web typecheck, test --run, and build are green.
- Modules: [[02 Modules/Web Control Plane]]
- Next: Production-walk /updates, /enrollment, /rescue, /drafts, and one real /routers/[routerId] page to confirm the tighter framing against live data before any further router-detail refactor.

### 18:23

- Summary: Installed ast-grep as a root pnpm workspace devDependency and verified the CLI for local AST-based code navigation.
- Modules: `Tooling`

### 18:25

- Summary: Installed ast-grep globally with npm so the CLI is available across projects, and verified it via ast-grep --version.
- Modules: `Tooling`

### 18:33

- Summary: Completed the next cross-device web polish pass while keeping the liked fleet list intact. The work tightened operator shell framing, reduced duplicated page chrome on updates/enrollment/rescue/drafts, then went deeper into router detail and diagnostics: top summary noise was reduced, the draft-note/validation strip became a single adaptive block instead of separate mobile/desktop duplicates, status tiles were compacted, and Watch Logs / Terminal now read as denser serious operator tools across smartphone, tablet, and desktop. Local @vectra/web typecheck, test --run, build, and LSP diagnostics on the touched files are green.
- Modules: [[02 Modules/Web Control Plane]]

### 19:43

- Summary: Reviewed apps/web mobile UX cleanup for operator shell header and fleet workspace; found one blocking fleet search-state sync regression and one small duplication concern in header tab active logic.
- Modules: [[02 Modules/Web Control Plane]]

### 19:52

- Summary: Closed that follow-up completely in code. `apps/web/src/components/operator-shell-header.tsx` now stays compact through tablet widths on `/login` and `/fleet`, `apps/web/src/app/fleet/page.tsx` hides the extra `PageHeader` below `lg`, and `apps/web/src/components/fleet-monitoring-workspace.tsx` now syncs `searchInput` back to `initialSearchQuery` while moving the mobile fleet filter ahead of summary cards so the top flow is closer to filter -> alerts -> list. Local LSP diagnostics on all touched files are clean; `@vectra/web` `test --run`, `build`, `next typegen`, and post-typegen `typecheck` are green. Direct local browser QA also confirmed the shorter phone login shell and that header command `/fleet` still resolves into the expected unauthenticated `GET /fleet -> 307 -> /login` path rather than breaking route semantics.
- Modules: [[02 Modules/Web Control Plane]]

### 19:48

- Summary: Finished the mobile/tablet shell and fleet-top cleanup pass in apps/web: compacted operator shell through tablet widths on /login and /fleet, hid the extra fleet PageHeader below lg, moved mobile fleet priority closer to filter -> alerts -> list, and fixed ?q search-state sync in fleet-monitoring-workspace. Verified with clean LSP diagnostics plus @vectra/web test --run, build, next typegen, post-typegen typecheck, and direct local browser QA for phone login shell + shell command routing.
- Modules: [[02 Modules/Web Control Plane]]

### 23:44

- Summary: Closed the last local chrome-density gaps in `apps/web` without disturbing the liked fleet list direction. The pass removed redundant shell guidance in `operator-shell-header.tsx`, flattened the extra list-intro wrapper and leftover English helper chips in `fleet-monitoring-workspace.tsx`, split the router-detail top into a denser summary deck plus separate sticky action rail while collapsing duplicated Telegram markup into one responsive disclosure, reduced `rescue` from a panel-inside-panel policy block to a native dense section, shortened `rescue-actions` copy, and stopped double-wrapping the JSON expert editor on `/drafts`. Local LSP diagnostics on all touched files are clean; `@vectra/web` `test --run`, `build`, `next typegen`, and post-typegen `typecheck` are green; and local browser QA rechecked phone-width `/fleet` and `/rescue` after the edits.
- Modules: [[02 Modules/Web Control Plane]]
- Next: Run the same visual smoke on production routes with real router data before any deploy, with special attention to `/routers/[routerId]`, `/updates`, and `/drafts` where the heaviest operator surfaces still live.

### 23:44

- Summary: Finished the final local apps/web chrome-density cleanup: trimmed redundant shell guidance, flattened stacked fleet/router-detail framing, tightened rescue/drafts utility surfaces, and revalidated browser/render checks.
- Modules: [[02 Modules/Web Control Plane]]
- Next: Production-walk fleet, rescue, drafts, updates, and one real router detail page before any deploy.

### 00:58

- Summary: Completed a broader local redesign pass for `apps/web` aimed at the remaining "useless elements" rather than another small cleanup. Flattened shared operator chrome (`operator-shell-header.tsx`, `page-header.tsx`, `panel.tsx`, `action-strip.tsx`), pushed fleet harder toward a real triage console by removing more explanatory copy and onboarding ceremony from `fleet-monitoring-workspace.tsx`, tightened `router-detail-workspace.tsx` so the operator reaches context/actions/tabs faster with less helper prose, and compressed `updates`, `drafts`, `enrollment`, and `rescue` so they behave more like utilities than guided landing pages. Local LSP diagnostics on all touched files are clean; `@vectra/web` `test --run`, `build`, `next typegen`, and post-typegen `typecheck` are green; and browser QA on fresh local runtimes confirmed the thinner shell plus the cleaned `fleet`, `updates`, `rescue`, and `drafts` surfaces with no console warnings.
- Modules: [[02 Modules/Web Control Plane]]
- Next: Compare this denser local slice against production on real data and decide whether to deploy as-is or do one last live-data-specific reduction pass for `fleet`/router-detail before rollout.

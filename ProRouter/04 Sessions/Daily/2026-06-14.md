---
type: session
date: 2026-06-14
tags:
  - session
---

# Session 2026-06-14

## Summary

-

## Completion Updates

### 15:21

- Summary: Onboarded Cudy WBR3000UAX (id 12502d20) → renamed childsergeyavito, mirroring sergeyavito. Approve-import → rename → one bundled apply (rev 8af7b713): myshunt.default_node V1cdkgmh(DE proxy)→_direct [strand-safety], Special→NL(PiURmqEf), +DiscordVoiceUdp(PL 68S9lbGP)+rule, Google DoH DNS, +5 Kinopub→WorldProxy. Apply succeeded, NO strand (continuous check-ins), all 5 slots url_test_node=204, request-reimport reconciled compliance=compliant/live-import. Fleet 26.
- Modules: `Router Onboarding`

### 15:29

- Summary: 1111 (Xiaomi AX3000T, vectra-07bf0887f662): enabled zram swap to match fleet standard, addressing chronic low_memory (was hitting critical 36MB avail). Root cause of 1111 being tighter than siblings was NOT xray version (verified: no correlation across fleet — same xray 26.4.25 ranges 42-71MB free; newer cores show 25-86MB) but that the whole fleet runs zram swap (~117MB lz4) and 1111 had none. 1111 lacked it because it is on a NEWER kernel 6.6.127 (OpenWrt 24.10.6) while the fleet's pinned kmod-zram is built for 6.6.110 — kmods are vermagic-locked so the fleet package never applied. Installed the matching kmod-zram 6.6.127-r1 + zram-swap 32 from the official 24.10.6 filogic kmods feed (downloads.openwrt.org, all signature checks passed), enabled /etc/init.d/zram (S15zram boot symlink, survives reboot). Verified: /dev/zram0 116MB lz4 priority 100 active, swap used=0 (no pressure yet). Reversible: /etc/init.d/zram disable + opkg remove. NOTE: any other fleet router upgraded to 24.10.6 (kernel 6.6.127) will likewise lose zram until the matching kmod is installed.
- Modules: `Live Router Ops / 1111`

### 15:52

- Summary: Fleet-wide zram rollout: scanned all 26 routers (cat /proc/swaps + uname -r). Routers WITHOUT zram = every 24.10.6/kernel-6.6.127 unit (AndreyVK, DmitryGubenko, zhenya13911, AndreyVK_Sochi; 1111 fixed earlier) plus one outlier yuranrod-msk (24.10.4/6.6.110 — matching kmod was in its feed but package was simply never installed). Installed kmod-zram + zram-swap on 5 routers (AndreyVK, DmitryGubenko, zhenya13911, AndreyVK_Sochi, yuranrod-msk) via opkg (auto-resolves the kernel-matched kmod from each router's own distfeeds, same command for 6.6.110 and 6.6.127); all 5 verified Z=1 swap=116MB boot-enabled (S15zram). The other 18 routers already had zram. hh excluded (no-touch). NOT verified: sergeyavito (vectra-5c6f4d06a2bc) is not checking in since 10:13Z (status active but stale check-in, queuedJobCount=1, terminal job undelivered) — separate sync-gap; peer-pattern (24.10.4, 55MB avail) implies it likely has zram; revisit when it reconnects. Confirms the kmod-zram-vermagic-vs-kernel rule: any router upgraded to 24.10.6 loses zram until the 6.6.127 kmod is installed.
- Modules: `Live Router Ops / Fleet`

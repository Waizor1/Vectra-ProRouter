---
type: session
date: 2026-06-05
tags:
  - session
---

# Session 2026-06-05

## Summary

-

## Completion Updates

### 12:34

- Summary: r30 fleet rollout complete: 18/20 active routers on 0.1.13-r30 (excludes hh policy + NetisNX offline). PR #29 (controller version-mismatch bail-out + panel stuck-job-janitor) merged after 5-agent review (code-reviewer + security-reviewer + critic + architect + verifier — all CLEAN after must-fix iteration; critic caught dedupeKey leak others missed). PR #30 (build-script chmod follow-up) merged. r30 IPK published, web rebuilt with janitor wired into /api/health (verified 'stuckJobJanitor: true'). Deleted dead totchto duplicate record (07c2425e r26 silent 8d). 2 stragglers stay healthy on r29 thanks to r30 guards rejecting cleanly: DmitryGubenko (RAM 41<48 transient), dmitry-filicity (wget Operation not permitted — local network). Canary ar-filicity stable on r30 8+ min, zero bail-outs observed. Both layers of defense now live: controller-side bail-out after 6 retries (~225s) + panel-side janitor every 10 min with 1h staleness threshold. Total: 183 go + 91 web tests passing (+6 new regressions).
- Modules: `Router Fleet Operations`

### 13:16

- Summary: r30 fleet rollout 100%: 21/21 active routers on 0.1.13-r30 (NetisNX still 22d offline, physically unreachable). Final 2 stragglers landed via per-router recovery: DmitryGubenko (RAM 41<48 transient) — passwall stop+drop_caches freed 50MB window; dmitry-filicity (wget Operation not permitted) — root cause local dnsmasq failed to resolve api.vectra-pro.net specifically (other domains work), workaround: persistent /etc/hosts entry 72.56.14.52 api.vectra-pro.net. Both techniques safe under r30 guards (failed attempts left routers healthy on r29). All defenses now live across fleet: PendingJobResultRetryCount bail-out after 6 retries (~225s) + panel stuck-job-janitor 10-min sweep + dedupeKey clearing on cancel. totchto-style 9-day silence loop no longer possible. Followup r31: investigate why dnsmasq on dmitry-filicity fails for api.vectra-pro.net only (possible passwall2 routing interaction).
- Modules: `Router Fleet Operations`

### 19:08

- Summary: avfilicity (netis NX31, 94d5c9f3) onboarding complete + STRAND incident resolved. Approved import, renamed OpenWrt->avfilicity, selected myshunt, grafted DiscordVoiceUdp->oRjVbnav, route-policy compliant. ROOT CAUSE of strand: apply #1 left myshunt.default_node=DSAdJuW2 (proxy server) + mainSwitch=true -> on netis NX31 the catch-all incl. control-plane routed through the proxy -> router silent ~35min (no inbound recovery). FIX (operator rule): default_node=_direct (only explicit slots proxy). evaluateFleetRoutePolicy does NOT check default_node so it still showed 'compliant' (compliance != safety); confirmed vs correct sibling sergeyavito. RECOVERY: enter_direct_mode -> rescue reconnect -> rebuild draft from fresh import (panel rejected stale draft) -> apply default->direct + Google DoH. No re-strand; final verified active/approved/live-import/compliant, services running, url_test_node=204 on all 5 slots (DE/RU/NL/BY/PL). Lessons stored: feedback_shunt_default_node_direct + feedback_safe_live_apply_onboarding (+ICM/Sugar).
- Modules: `Router Fleet Operations`

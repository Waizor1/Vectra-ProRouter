---
type: module
path: router/vectra-controller-agent
stage: pilot
confidence: high
last-reviewed: 2026-05-15
tags:
  - module
  - go
  - openwrt
---

# Router Agent

## Confirmed

- 2026-05-15 onboarding/runtime-version gate: the controller agent now publishes an additive `controllerRuntimeVersion` inventory field from the running binary ldflag, so the web r23 onboarding gate no longer trusts opkg/package metadata that may have been unpacked before the old process restarts. The standard OpenWrt package Makefile now injects the same runtime-version ldflag as the manual feed builder, and controller update job success is runtime-confirmed: legacy `update_controller` and terminal self-update jobs keep their pending result until the restarted process reports the expected `ControllerRuntimeVersion`. Local Go coverage pins the runtime signal behavior, the package Makefile ldflag, terminal self-update target tracking, compat self-update storage/resource guarding plus direct-mode auto-resume parity, and the no-false-green update-result path while keeping installed `controllerVersion` separate.

- 2026-05-15 optimization baseline controller job: the agent now implements typed `collect_optimization_baseline` as a read-only diagnostic. It refreshes inventory, returns resources/service health/safety/package/binary/rules facts, scans Xray-like processes for RSS/VM/thread counts, reads conntrack count/max, captures a small PassWall global config surface, and can include bounded logs plus fleet route verification. The job is classified under the diagnostic resource guard rather than storage/heavy write gates; post-review it also skips its own diagnostic shell during process scans so embedded match patterns do not appear as fake Xray processes, and red route-smoke inside the diagnostic payload is surfaced as a warning that makes `ok=false` instead of looking like a clean baseline. The web history now preserves the agent resource-guard failure payload when the diagnostic is blocked before collection. Local proof is green on full `go test ./... -count=1` and `go vet ./...`.

- 2026-05-15 automated onboarding r23 rollout closeout: controller/LuCI `0.1.13-r23` is now the live floor for typed onboarding jobs, the router-side agent stayed compatible with the new `ensure_passwall_runtime` and `verify_passwall_routes` path, and `yuranrod-msk` completed onboarding with green five-slot route proof while preserving the Discord UDP tuning and service health.

- 2026-05-14 automated onboarding backend MVP: ADR-0002 and `ai_docs/develop/features/router-automated-onboarding-workflow.md` define the controller-side role for unattended onboarding, and the web/backend now has a feature-flagged state machine that advances from router `register`, `check-in`, and `job-result`. The controller implements typed `verify_passwall_routes`: it verifies PassWall main switch, selected shunt binding intent, route/rule/node extras, and five `url_test_node=204` smokes before the web run can move to final re-import. The scorer accepts the proven RU-entry Netherlands `Special` fallback so low-storage routers like the recent Cudy/YuranRod path are not forced back to a dead plain-NL node. The controller now also implements typed `ensure_passwall_runtime` for known minimal runtime repairs: compact geodata and `dnsmasq-full`, classified as storage-gated, with `dnsmasq-full` package staging before base `dnsmasq` removal, `/etc/config/dhcp` preservation, PassWall restart, and post-repair inventory returned to the web gate.

- 2026-05-13 controller `0.1.13-r22` resource-guard adjustment: heavy non-storage jobs now use a 40 MB RAM floor so `refresh_subscriptions`/apply-style operational refreshes can run during low-but-usable RAM windows; storage/package/firmware jobs keep the conservative 64 MB floor. This was driven by Kirill-MSK, where the saved subscription-only revision was initially blocked at 37 MB but later ran cleanly after r22. Local proof: targeted Go tests cover allowing subscription refresh at 48 MB while still blocking storage updates below 64 MB; live proof: Kirill-MSK installed controller/LuCI `0.1.13-r22`, applied the saved subscription URL replacement, re-imported to `live-import`, and finished with `requiresReimport=false`, `digestMismatch=false`, `pendingChanges=0`, `alertKinds=[]`, and green route smoke across WorldProxy/YouTube/Special/Tiktok/DiscordVoiceUdp.

- 2026-05-13 `Kirill-MSK` onboarding: router `297477dd-f91b-40b3-92bf-e63f059fceba` (`kirill-msk`, Xiaomi Mi Router AX3000T, OpenWrt `24.10.4`, controller/LuCI `0.1.13-r21`) is now `active/approved` in `live-import` with `requiresReimport=false`, `digestMismatch=false`, `pendingChanges=0`, and `fleetPolicyCompliance=compliant`. The applied baseline selects `myshunt`, uses Google DoH (`8.8.8.8`, `https://dns.google/dns-query`), and binds `WorldProxy=lxa8ZMO1`, `YouTube=K4ORjBlk`, `Special=HSRcsffK`, `Tiktok=epeYY0jj`, `DiscordVoiceUdp=aDlno6jU`; Discord UDP rule/tuning is present on the router (`19294-19344,50000-50100`, `mux=1`, `mux_concurrency=-1`, `xudp_concurrency=16`). Router-side proof: all five `url_test_node` checks returned `204`, socks smoke returned Telegram `200` through WorldProxy and `204` through YouTube/Special/Discord targets, services stayed running, `/overlay` had about `28-29 MB` free, and `MemAvailable` recovered to about `69-72 MB` during smoke. Fresh monitoring after closeout still reports `stable`/`fresh`, but flags warning-level `low_memory`/`router_safety` at about `64 MB` available RAM; treat RAM headroom as a watch item, not a config/apply blocker.

- 2026-05-13 `totchto-filiciy` onboarding follow-up: new router `totchto-filiciy` (`Cudy WR3000E v1`, OpenWrt `24.10.4`) is now `active/approved` in `live-import` with `queuedJobs=0`, `selectedNode=myshunt`, healthy `passwall/dnsmasq/controller/passwallServer`, and green router-side smoke on the current applied bindings (`WorldProxy=5t41GjFB`, `YouTube=OimBMZcM`, `Special=LrFNLcHV`, `Tiktok=aPQVHIgJ`, `DiscordVoiceUdp=O9r7ieQq`). Important nuance: the fleet policy surface still flags `Special` because the canonical plain-NL candidate `8tAeOzws` returned `000`, while the live-good RU-entry NL fallback `LrFNLcHV` returned `204`; for this router, keeping the proven fallback is the safe state and normalizing back to `8tAeOzws` would knowingly break the slot. Controller/LuCI was then safely advanced to `0.1.13-r21`; the guarded self-update job succeeded and both `vectra-controller-agent` and `luci-app-vectra-controller` report `0.1.13-r21`. Important post-r21 finding: fleet-policy self-heal initially moved `Special` back to the dead plain-NL `8tAeOzws`, so the live stopgap disables `8tAeOzws` and keeps `Special=LrFNLcHV`. Final route proof after re-import/check-ins: `WorldProxy=5t41GjFB`, `YouTube=OimBMZcM`, `Special=LrFNLcHV`, `Tiktok=aPQVHIgJ`, and `DiscordVoiceUdp=O9r7ieQq` all returned `204`; `8tAeOzws` stayed `000`. Current watch item is low `/overlay` headroom (`~11-12 MB free`) and the need for a durable policy exception or live-health-aware fallback before a future subscription refresh can re-enable the dead candidate.

- 2026-05-13 controller r21 rollout closeout: the r21 fleet-policy self-heal package is live on 14/15 active routers after selective safe retries. The only router left on r20 is `denisvitalevichtescha`; its latest snapshots show critical low RAM around 29 MB, and the r20 resource guard correctly blocks `controller-self-update` terminal jobs before any router-side commands run. `AndreyVK` and `Vasily_Filicity` initially failed for the same empty-result reason (`run_terminal_command` blocked below the 64 MB storage floor) and then upgraded successfully once `MemAvailable` recovered. Route-policy compliance remains green on every non-`hh` router, so the remaining r20 holdback is an update-safety/resource issue, not active server-package drift.

- 2026-05-12 fleet route policy self-heal: the controller source now has a canonical fleet route policy layer for the standard non-`hh` server package (`WorldProxy` RU-entry Germany, `YouTube` RU Russia, `Special` Netherlands, `Tiktok` Belarus, `DiscordVoiceUdp` RU-entry Poland with UDP/mux/xudp tuning). After check-in and after `refresh_subscriptions`, non-exempt routers can reconcile live `myshunt` bindings by semantic target instead of volatile subscription node id, while preserving subscription `auto_update` and not rewriting subscription URLs. `hh` is a no-touch exception. Local proof: new `internal/passwall` regression tests cover fleet-policy restoration, Discord tuning preservation, refreshed node ids, and the `hh` no-op path; full `go test ./... -count=1` is green.

- 2026-05-12 controller/web `0.1.13-r20` rollout closeout: after review, the web queueing path now keeps direct-rescue routers on the normal `controller-self-update` purpose so legacy agents can still run their proxy-resume result handling, while terminal self-update commands perform a live RAM/overlay/tmp preflight before staging or `opkg install`. Router-agent release r20 is installed on all fresh online routers; stale offline `testrouter` remains the only controller-version outlier. Live post-rollout evidence shows open incidents `0`, direct routers `0`, and no `proxy_runtime_missing` active outage; current fleet warnings are low-resource and retained dmesg OOM evidence rather than PassWall service-down state.

- 2026-05-12 controller `0.1.13-r18` runtime watchdog: after `1111111111` lost proxy service because the kernel OOM-killed `xray` while `/etc/init.d/passwall2 running` stayed green, the inventory safety layer now reports critical `proxy_runtime_missing` when PassWall is enabled/running but the selected-node runtime is gone. For `xray` and `sing-box`, the detector scans `/proc/*/cmdline` for the active PassWall global config `/tmp/etc/passwall2/acl/default/global.json` instead of trusting broad `pidof`, so DNS/helper processes cannot hide a dead transparent-proxy runtime; `hysteria` still uses a bounded `pidof` process check. Local rescue consumes this event before HTTP probes or `url_test_node`, restarts `/etc/init.d/passwall2`, and records the watchdog reason/count under the existing cooldown. Local proof: `cd router/vectra-controller-agent && go test ./... -count=1` and `git diff --check`. Production proof: signed feed and artifact metadata now expose `0.1.13-r18`, canary router `1111111111` installed controller/LuCI r18 successfully, PassWall/controller/dnsmasq remain running, and the online active fleet converged to r18 except stale queued `testrouter`.
- 2026-05-12 OpenWrt-aware router safety guard: inventory now emits bounded `safetyEvents` from cheap OpenWrt primitives (`/proc/meminfo`, `df`, `/etc/init.d/* running/enabled`, cached `logread -l 160` and `dmesg | tail -n 160`) so low RAM, low `/overlay`, low `/tmp`, degraded PassWall/dnsmasq/passwall_server, and OOM/crash mentions become explicit controller facts without steady-state heavy probing. Diagnostics are memory-gated (`MemAvailable >= 64 MB`), cached for 10 minutes, time-limited to 2s per command, and capped at 12 events. The job executor also runs a pre-job resource guard before heavy router-side work: storage/package/firmware jobs, rule/subscription refreshes, heavy `apply_passwall_config`, heavy `run_rescue_repair`, generic terminal diagnostics, and log collection are blocked with `router_resource_guard` when RAM/tmp/overlay are unsafe or unknown; router reboot and hostname update remain allowed. Local proof: `cd router/vectra-controller-agent && go test ./cmd/vectra-controller-agent ./internal/inventory -count=1` and later full-module verification.

- 2026-05-12 router-safe service monitoring: the controller agent no longer treats Telegram/YouTube HTTPS probes as a normal every-check-in inventory task. Probes are cached for `30m`, require PassWall enabled/running, and require `MemAvailable >= 128 MB`; unknown or lower memory omits service reachability from the snapshot so the panel shows a blind spot instead of spending RAM/CPU on service checks. Local proof: `cd router/vectra-controller-agent && go test ./... -count=1`.

- 2026-05-11 controller `0.1.13-r17` PassWall watchdog: after the VagrandRouter OOM case proved that `xray`/PassWall can die while the controller stays alive, the local rescue evaluator now tries a bounded `/etc/init.d/passwall2 restart` before incrementing proxy-failure/direct-fallback state. The trigger covers enabled PassWall with `passwall2` service `stopped`/`degraded` and conclusive selected proxy-node failure; the watchdog is skipped while control-plane recovery owns PassWall, uses the existing rescue cooldown/warmup window, and persists `last_passwall_watchdog_at`, reason, and restart count into state/status. Local proof: `cd router/vectra-controller-agent && go test ./... -count=1`; live proof: Vagrand and 13 fresh online active routers report controller/LuCI `0.1.13-r17`, with Vagrand services running and open incidents `0`.

- 2026-05-11 controller `0.1.13-r16` low-memory OOM hardening: after VagrandRouter killed `xray` under memory pressure, the agent inventory path stopped forking `opkg status` for package versions and now reads `/usr/lib/opkg/info/*.control` plus `/usr/lib/opkg/status` directly. PassWall section counts no longer use shell `grep|wc`, and expensive binary version plus Telegram/YouTube HTTPS probes are skipped when `MemAvailable` is below 64 MB. Local proof: `cd router/vectra-controller-agent && go test ./...`; live proof on Vagrand: `opkg_seen=0` during 75s burn-in, `xray` PID unchanged, `render_uses_opkg=0`, `luci_uses_opkg=0`, and no post-r16 OOM beyond the original `2026-05-11 18:21:13` log lines.

- 2026-05-11 `sergeyavito` onboarding: live controller `0.1.13-r13` applied and re-imported the new router's managed PassWall2 config cleanly. Final UCI proof on the router shows `passwall2.@global[0].node=myshunt`, Google DoH under `vectra_global`, `myshunt.WorldProxy=oM9d10wx`, `myshunt.YouTube=WbD7auCi`, `myshunt.Special=6rjcPYsb`, `myshunt.Tiktok=ZVtBTe1d`, `myshunt.DiscordVoiceUdp=hN0zxXEY`, and `DiscordVoiceUdp` UDP `19294-19344,50000-50100` with `hN0zxXEY` `mux=1`, `mux_concurrency=-1`, `xudp_concurrency=16`. Router-side validation returned `url_test_node=204` for all five selected nodes, Telegram `200` on all four endpoints, and YouTube `204` on all three probes.
- 2026-05-11 controller `0.1.13-r13` shunt self-heal: live check showed `1111111111` route slots changed after PassWall2 native subscription auto-update, not after a new operator apply. Product decision: keep subscription `auto_update=1` and make the controller restore Vectra-selected route intent. The agent now persists the latest desired revision and runs `ReconcileShuntBindings` before check-in import plus after `refresh_subscriptions`, matching volatile subscription nodes by stable label/transport/port/address and reapplying route-local mux/XUDP extras. Published signed feed `0.1.13-r13`, installed on `1111111111`, and proved with a bounded drift injection: `WorldProxy` was manually set to `7JuG36XG`, then the next controller check-in restored it to `K4YF9ZXV` with `passwall shunt self-heal restored 1 binding(s)` while `vectra_sub_subscribe_list1.auto_update=1` stayed enabled.
- 2026-05-08 `1111111111` ShuntRule correction: live investigation found the router was the only normal-fleet router whose `DiscordVoiceUdp` target had drifted from RU-entry Poland to RU-entry Germany. Earlier same-day safe-probe/cleanup applies had temporarily put several route slots on unintended country nodes; a later operator draft restored `WorldProxy`/`YouTube`/`Special`/`Tiktok` to the fleet baseline but left `DiscordVoiceUdp` bound to the WorldProxy Germany node. The correction restored `myshunt.DiscordVoiceUdp` to the local RU-entry Poland node and preserved the Discord UDP port/tuning. Final proof: panel `approved`, queue `0`, `live-import`, `requiresReimport=false`, no unconfirmed changes, and all five ShuntRules now match the normal non-`hh` baseline.
- 2026-05-08 Stewe onboarding: new live router `stewe` (`netis NX31`, OpenWrt `24.10.5`, controller `0.1.13-r12`) was configured to match the 2026-05-07 non-`hh` ShuntRules/DiscordVoiceUdp baseline. The workflow first cleared stale-authoritative state with re-import, then applied Google DoH and the known-good route bindings (`WorldProxy` RU-entry Germany, `YouTube` RU-entry Russia, `Special` Netherlands, `Tiktok` Belarus, `DiscordVoiceUdp` RU-entry Poland). As with the fleet rollout, panel apply needed a bounded router-side UCI remap for actual `myshunt` bindings and `DiscordVoiceUdp` section creation; final proof is queue `0`, panel `live-import`/`requiresReimport=false`, no unconfirmed changes, healthy services, Telegram `4/4`, YouTube `3/3`, and Discord node `url_test_node=204` with `mux=1`, `mux_concurrency=-1`, `xudp_concurrency=16`.
- 2026-05-08 non-HH Discord/ShuntRules fleet rollout: the live `1111111111` baseline was applied to the eight online approved non-`hh` routers. Final router-side checks confirmed `WorldProxy`/`YouTube`/`Special`/`Tiktok` route bindings plus dedicated `DiscordVoiceUdp` UDP ports `19294-19344,50000-50100` on RU-entry Poland with `mux=1`, `mux_concurrency=-1`, `xudp_concurrency=16`, and `url_test_node=204` on every target; `hh` and stale `testrouter` stayed untouched. Several routers had subscription section-id rotation after the panel apply, so the rollout was completed with bounded UCI remapping followed by reimport until the panel returned to `live-import` and `requiresReimport=false`.
- 2026-05-08 controller `0.1.13-r12` rollout: the subscription section-name idempotency fix is published in the signed stable feed, synced into production artifact metadata, and installed on all 10 online active routers; `testrouter` is still stale/offline with pre-existing queued jobs. `update.versionDriftWorkspace` reports the online fleet on `0.1.13-r12` with no queued controller updates.
- 2026-05-08 subscription apply idempotency: controller apply now renders subscription sections through `subscriptionSectionID()` instead of blindly prepending `vectra_sub_`, so configs imported from prior Vectra applies like `vectra_sub_vectra_sub_...subscribe_list1` converge back to `vectra_sub_subscribe_list1` on the next apply. Local proof is green on `cd router/vectra-controller-agent && go test ./...`.
- 2026-05-08 upstream PassWall parameter parity: controller source still preserves new PassWall2 params as extras on apply. Added local `internal/passwall` tests for node `mkcp_mtu` / `tls_pinSHA256` and subscription domain-resolver extras so future controller package builds do not silently drop these 26.4.20+/26.5.1+ fields.
- 2026-05-08 apply/preview parity follow-up: local controller tests remain green after the web/contract parity patch; the source controller remains the apply-side authority for safe UCI ids and shunt binding precedence. Controller package rollout is still a separate later step; production web now mirrors those controller rendering rules in preview.
- 2026-05-08 Shunt binding apply hardening: controller source now renders explicit ShuntRule bindings as authoritative for shunt nodes and skips duplicate stale extras keys matching rule ids. Existing production controllers still rely on the web-side normalization until the next controller package rollout, but the source-level bug is covered by `internal/passwall` tests.
- 2026-05-07 live Discord voice test on router `1111111111`: inspected local `zapret-discord-youtube-1.9.8c.zip` without executing it, found its Discord voice path relies on UDP `19294-19344,50000-50100` plus Discord/STUN detection and a fuller Discord host/IP surface, then applied a bounded live UCI test patch. First pass added explicit Discord domain rules from the archive, added the two Discord IP ranges that were missing below the existing `66.22.192.0/18` coverage (`66.22.176.0/24`, `66.22.188.0/22`), restored node-local `mux=1`, `mux_concurrency=-1`, `xudp_concurrency=16` on current `WorldProxy` outbound `OPIqwEuA`, committed `/etc/config/passwall2`, and restarted PassWall2. When the user reported voice still failed, the deeper fix added a dedicated `DiscordVoiceUdp` shunt rule for UDP ports `19294-19344,50000-50100` and moved only that UDP rule to the current Poland/gRPC candidate `dqnJE64Y` with `mux=1`, `mux_concurrency=-1`, `xudp_concurrency=16`; general `WorldProxy` stayed on `OPIqwEuA`. Post-change proof: `xray=running`, `url_test_node dqnJE64Y = 204:0.208831`, `url_test_node OPIqwEuA = 204:0.176172`, Discord API probe `200:0.228351`, and generated Xray routing contains `DiscordVoiceUdp` with `port="19294-19344,50000-50100"` to `dqnJE64Y`. The user then confirmed that Discord voice started working, so this is the current known-good contour; sync/reimport into panel source of truth is still needed before any future broad apply.
- 2026-05-05 DmitryGubenko direct-mode follow-up: live triage showed the router was fresh and controller `0.1.13-r10` was running, but control-plane recovery had left it in `direct`/`operator_attention` after a failed service-level foreign probe even though concrete PassWall2 shunt proxy nodes still answered `url_test_node`. Controller `0.1.13-r11` adds a recovery guard that probes concrete shunt outbound slots (`WorldProxy`, `YouTube`, `GooglePlay`, `Proxy`, `ProxyGame`, `Tiktok`, `Special`, `default_node`) before disabling PassWall again after a retry; if any concrete node responds, the agent keeps proxy mode and clears the recovery lane back to idle instead of requiring manual operator reconnect. Local proof is green on targeted rescue/control-plane tests and `cd router/vectra-controller-agent && go test ./... -count=1`; production proof now includes public stable feed `0.1.13-r11`, metadata sync, successful DmitryGubenko install with `rescue_mode=proxy`, `recovery_phase=idle`, node tests returning `204`, and fleet controller rollout to all fresh active routers except stale `testrouter`.
- 2026-05-04 controller state self-heal release: `router/vectra-controller-agent/internal/state` now treats empty/corrupt `state.json` as recoverable instead of fatal. `Save()` writes an fsynced atomic `state.json` plus `state.json.last-good`; `Load()` backs up unreadable bytes to `state.json.corrupt-*`, restores `.last-good` when present, salvages top-level identity/token fields from truncated JSON when possible, and otherwise starts with a fresh state so the service can re-register rather than crash-loop. Fresh local proof is green on `cd router/vectra-controller-agent && go test -count=1 ./...`.
- Controller/LuCI `0.1.13-r10` is production-published and live-installed on the recovered AndreyVK router `a0c9c42e-1934-40ce-8e02-d1cce12f278e`: the public signed feed and panel metadata both advertise `0.1.13-r10`, the controller self-update terminal job `6ba9ee84-4448-404c-a176-7844fbe91989` succeeded, latest snapshot reports `vectra-controller-agent=0.1.13-r10`, `luci-app-vectra-controller=0.1.13-r10`, controller/passwall/dnsmasq/passwallServer running, Telegram `4/4`, YouTube `3/3`, and a read-only terminal check confirmed `/etc/vectra-controller/state.json.last-good` exists with `0600` permissions.
- Это отдельный Go-модуль `vectra-controller-agent` c `go 1.22.0`.
- В README зафиксированы зоны ответственности:
  - `cmd/vectra-controller-agent`
  - `internal/controlplane`
  - `internal/rescue`
  - `internal/passwall`
  - `openwrt/`
- OpenWrt runtime-файлы (`/etc/config`, `/etc/init.d`, `uci-defaults`, `render-config.sh`) лежат отдельно под `openwrt/files`.
- На pilot VPS агент реально cross-build'ится в статический `linux/arm64` бинарь через `GOTOOLCHAIN=local CGO_ENABLED=0 GOOS=linux GOARCH=arm64`.
- Из этого бинаря и runtime-файлов теперь реально собирается `.ipk` для `aarch64_cortex-a53`, опубликованный в signed feed на `api.vectra-pro.net`.
- Bootstrap-конфиг теперь явно разделяет:
  - `control_url=https://api.vectra-pro.net` для router-facing REST
  - `panel_url=https://router.vectra-pro.net` для operator/UI ссылки
- Agent runtime сохраняет оба URL в status/config surface и остаётся совместимым с legacy-конфигом, где задан только `panel_url`.
- По коду подтверждено, что именно `vectra-controller-agent` содержит rescue/direct-mode path, который может записывать `set passwall2.@global[0].enabled='0'`, коммитить `passwall2` и делать `/etc/init.d/passwall2 restart`.
- Published agent package install path подтверждён по содержимому `.ipk`: `conffiles` включает `/etc/config/vectra-controller`, а `postinst` запускает `uci-defaults`, `enable` и `restart` сервиса.
- На живом `Xiaomi AX3000T stock-layout` пакет реально установлен и пережил controlled `opkg --force-reinstall` с published `.ipk` напрямую с `api.vectra-pro.net`.
- Live SHA-check подтвердил, что на роутере раньше действительно работал старый бинарь, а не только старый лог; после forced reinstall `/usr/sbin/vectra-controller-agent` совпал по SHA с текущим артефактом из production feed.
- Исправление multiline UCI parser path реально подтвердилось на живом роутере: после установки нового binary router перешёл из `awaiting_import` в `import_review`, а baseline import больше не падал на `invalid uci option line: domain:ozon.ru`.
- Live pilot chain `install -> register -> check-in -> import_review -> approve -> approved` уже реально пройдена против `router.vectra-pro.net` / `api.vectra-pro.net`.
- Rescue state теперь сохраняется в persisted state, а не живёт только в памяти процесса.
- Local rescue loop теперь использует реальные probes:
  - server reachability через `GET /api/health` на control plane
  - proxy-path probe через `/usr/share/passwall2/test.sh url_test_node <selected-node>`
  - temporary direct-path verification перед local fallback, если policy требует подтверждённый direct path
- Agent runtime теперь умеет различать server outage и proxy outage на уровне входных сигналов rescue state machine; недоступность сервера сама по себе больше не является основанием выключать PassWall2.
- Inventory collector расширен до human-usable live surface:
  - `selectedNodeLabel`
  - `layoutFamily`
  - `openwrtDescription`
  - `rulesAssets`
  - resource snapshot по RAM/swap/overlay/tmp
  - explicit service health по `vectra-controller`, `passwall2`, `passwall2_server`, `dnsmasq`
- Apply receipts нормализованы сильнее:
  - exact `uciCommands`
  - `operationResults`
  - `commandResults`
  - `postApplyImportDigest`
- Artifact-driven lanes теперь реально исполняются на agent side:
  - controller/passwall package jobs умеют скачать artifact/feed metadata, stage `.ipk` в temp dir, проверить SHA-256 и использовать `Packages`/`Packages.sig` при наличии usign verification path
  - firmware validation job больше не привязан только к legacy `imagePath`; поддержан `artifactUrl -> stage -> verify -> validationCommand/sysupgrade -T` path с сохранением legacy fallback
- Текущий workspace теперь кодирует attended PassWall2 post-sysupgrade recovery как штатное поведение `update_passwall_packages`:
  - дефолтный package set расширен до `luci-app-passwall2`, `xray-core`, `sing-box`, `hysteria`, `geoview`, `v2ray-geoip`, `v2ray-geosite`, `dnsmasq-full`, `chinadns-ng`, `kmod-nft-socket`, `kmod-nft-tproxy`, `kmod-nft-nat`
  - после feed-based и staged install agent теперь штатно вызывает `lua /usr/share/passwall2/rule_update.lua log geoip,geosite`, поэтому актуальные `geoip_url`/`geosite_url` из UCI и web panel остаются source of truth, а не хардкод в контроллере
  - затем agent делает discrete runtime recovery command `/etc/init.d/passwall2 running >/dev/null 2>&1 && /etc/init.d/passwall2 restart || /etc/init.d/passwall2 start`, без SSH stdin-tail assumptions
  - inventory/config surface теперь дополнительно показывает `dnsmasq`, `dnsmasq-full`, `chinadns-ng`, `v2ray-geoip`, `v2ray-geosite` и nft runtime-kmods, что упрощает post-sysupgrade диагностику
- Controller package `0.1.11-r1` with this recovery logic is now published in the production signed feed and mirrored into production PostgreSQL metadata; live AX3000T runtime itself still remains on `0.1.10-r1` until the next queued controller/package install cycle.
- Локальный `go test ./...` по модулю по состоянию на 2026-04-06 проходит успешно.
- По коду подтверждено, что agent уже умеет import/apply typed PassWall2 state, rescue-aware polling, staged package update jobs и firmware validation по artifact contract с legacy compatibility.
- Live recovery after controller loss уже подтверждён на AX3000T: при локальном доступе по LAN агент оказался установлен как `0.1.5-r1`, был поднят через init script, resumed outbound polling to `api.vectra-pro.net`, успешно получил `update_controller` и `reconnect`, очистил local rescue reason и перевёл router back into `proxy` + `approved`.
- На живом роутере локальный runtime теперь подтверждает post-recovery steady state:
  - `service_state=running`
  - `rescueMode=proxy`
  - `passwallEnabled=1`
  - `importState=approved`
  - `jobsAvailable=0`
  - `lastRescueReason=''`
- В текущем локальном workspace добавлен ещё один agent-side hardening path:
  - `internal/config/config.go` теперь корректно merge-ит partial `rescue_policy` и не затирает defaults нулевыми значениями JSON
  - `render-config.sh` реально рендерит rescue policy в runtime `config.json`
  - `internal/controlplane/client.go` теперь включает server response body в non-2xx error, что уже помогло на живом AX3000T диагностировать production `400`
- Первый live authoritative apply/rollback cycle теперь реально подтверждён на AX3000T:
  - draft `#20` применился на роутере и сменил `uci get passwall2.@global[0].loglevel` с `error` на `warning`
  - draft `#22` тем же путём вернул `loglevel` обратно в `error`
  - в обоих случаях роутер сохранил `enabled=1`, `node=myshunt`, `rescueMode=proxy`, `publicReachable=true`
- В apply receipts на обоих live jobs остаётся повторяющийся stderr-шум от `lua /usr/share/passwall2/subscribe.lua start all` (`Broken pipe` / `head: standard output: I/O error`), хотя итоговый job status и router health остаются успешными. Это уже не blocker для apply lane, но это реальный noise/risk, который стоит сузить в runtime path.
- Server-side production bug, который после apply возвращал router в `out_of_sync`, закрыт парным web fix: post-apply import теперь auto-promote-ится обратно в `approved`, если check-in пришёл с известным `appliedRevisionId`. Повторный rollback apply уже подтвердил этот steady-state на живом роутере.
- Live sysupgrade identity-loss root cause был закрыт и live-validated в controller package `0.1.9-r1`:
  - в agent package добавлен `/lib/upgrade/keep.d/vectra-controller`
  - keep rule сохраняет весь `/etc/vectra-controller`, а не только один файл
  - на живом AX3000T `sysupgrade -l` теперь показывает `/etc/vectra-controller/state.json`
  - backup-only proof через `sysupgrade -b /tmp/...tar.gz` подтвердил, что `etc/vectra-controller/state.json` реально попадает в архив, а не только в list surface
- Full live sysupgrade proof теперь пройден повторно на том же AX3000T уже после keep.d fix:
  - перед flash backup tar `C:\Users\user\Downloads\vectra-pre-sysupgrade-20260407b.tar.gz` включал `etc/vectra-controller/state.json`, `etc/opkg/customfeeds.conf` и opkg keys
  - после реального `sysupgrade` на тот же OpenWrt `24.10.6` файл `/etc/vectra-controller/state.json` сохранил тот же `router_id = bdfdb919-5e06-4344-ad8b-67a16f3b6fcf`
  - controller packages, LuCI package и PassWall2 app packages ожидаемо не пережили flash как установленные пакеты, но после reinstall агент не зарегистрировал новый router id
- Live router после post-firmware reinstall и последующего controller refresh снова приведён в steady state:
  - current live runtime `vectra-controller-agent = 0.1.10-r1`
  - current live runtime `luci-app-vectra-controller = 0.1.10-r1`
  - `luci-app-passwall2 = 26.4.5-r1`
  - `xray-core = 26.3.27-r1`
  - `geoview = 0.2.5-r1`
  - current physical router id `bdfdb919-5e06-4344-ad8b-67a16f3b6fcf` снова переведён из `import_review` в `active + approved`
  - active authoritative revision для него: `a02ee206-3ff6-40db-b23e-c036a48463be`
  - live check-in после approval уже подтвердил `import_state=approved`, пустой `last_rescue_reason` и прежний config digest
  - старый потерянный router id `a1c21287-d35f-4d7c-b8e0-a735b699898b` сохранён для истории, но переведён в `offline`
- Локальный follow-up по PassWall `Rule Manage` parity теперь также закрывает importer-side hole для shunt match extras: `internal/passwall/import.go` больше не отбрасывает `network` у `shunt_rules`, тесты дополнительно фиксируют round-trip `protocol`, `inbound`, `network`, `source`, `port`, `invert`, и `go test ./...` по модулю снова зелёный.
- 2026-04-08 local follow-up now also adds a new read-only diagnostics job type `collect_router_logs`: agent-side `executeJobs()` can request bounded `logread` snapshots for `all`, `controller`, `passwall`, `dnsmasq`, or raw `system`, normalize line limits, truncate oversized outputs, and report structured snapshot payloads back through the existing `job-result` channel. The shared contract fixtures and local `go test ./...` are green with this new job type.
- 2026-04-08 live production validation then closed a latent controller self-update bug on the AX3000T: controller and LuCI packages were already at `0.1.12-r1`, but the running process still pointed at `/usr/sbin/vectra-controller-agent (deleted)`, so the first `collect_router_logs` attempt failed with the old binary behavior. After a manual service restart restored the current executable, release `0.1.12-r2` added a restart-or-start `postinst`, was published to the stable feed, installed on the router, and proved that `opkg` upgrade now hands off directly to a live non-deleted binary without a manual restart.
- Live AX3000T proof now also exists for the diagnostics lane itself: `collect_router_logs` succeeded from the operator panel for both `all/200` and `system/50`, reported structured snapshot payloads through `job-result`, and those snapshots rendered in the production `Watch Logs` tab.
- 2026-04-09 live triage on router `AndreyVK` (`netis,nx31`, router id `a0c9c42e-1934-40ce-8e02-d1cce12f278e`) shows a new controller self-update regression outside the AX3000T proof path: latest snapshots still report `controllerVersion = unknown`, `packageVersions["vectra-controller-agent"]` is empty, and every recent `update_controller` job to `0.1.12-r2` failed. The decisive live evidence is the terminal `opkg` stdout: installation reaches `Configuring vectra-controller-agent.` and then dies with `signal: killed`. In local source this matches a restart race: `update_controller` already persists a pending success result and schedules a delayed restart itself, but package `0.1.12-r2` also restarts the service from `postinst`, so the running agent can be killed mid-`opkg install` before it finishes the self-update flow. The local fix now suppresses `postinst` restart only for agent-driven self-update installs via `VECTRA_SKIP_POSTINST_RESTART=1`, while keeping manual `opkg install` restart behavior intact; `go test ./...` is green, but this fix is not yet published to the feed or deployed to live routers.
- A same-night follow-up now also closes that live `AndreyVK` regression for real in published release `0.1.12-r4`: the agent self-update lane no longer relies only on an environment variable inside `postinst`, but also drops a temporary `/tmp/vectra-skip-postinst-restart` sentinel before `opkg install`, clears stale sentinels on startup, and teaches the controller init script to ignore package-driven `start/reload` when `VECTRA_SKIP_POSTINST_RESTART=1` is set. The same release also fixes `Watch Logs -> Controller` on the agent side by replacing the broken literal `logread -e 'vectra-controller|vectra'` filter with a real `grep -E` pipeline. Local `go test ./...` stayed green, controller/LuCI `0.1.12-r4` were built and published as direct artifacts on production, production metadata was synced to PostgreSQL, a live `update_controller` job on `AndreyVK` finished `success`, and subsequent snapshots now stably report `controllerVersion = 0.1.12-r4` with both controller packages present. A post-fix live `collect_router_logs` check on `source=controller` now also returns the previously hidden failure lines instead of an empty snapshot.
- A later same-day read-only check on the live AX3000T `1111111111` (`bdfdb919-5e06-4344-ad8b-67a16f3b6fcf`) surfaced a different post-update edge case on top of that fix: production `update_controller` to `0.1.12-r4` still ended with `signal: killed`, but the router did not stay on the old package state. Instead, the current live binary and package payload files on the router already match the new self-update generation (`VECTRA_SKIP_POSTINST_RESTART` and `/tmp/vectra-skip-postinst-restart` strings are present in the binary, and `/usr/lib/opkg/info/*vectra*` control/postinst files are `0.1.12-r4`), while `/usr/lib/opkg/status` leaves both `vectra-controller-agent` and `luci-app-vectra-controller` in `Status: install ... not-installed`. This explains the new production symptom precisely: the router keeps polling successfully with a live controller process, but inventory still resolves `controllerVersion = unknown` because both `render-config.sh` and `internal/inventory/collector.go` currently trust `opkg status <pkg>` as the only authoritative version source.
- A same-night live hotfix on that same AX3000T now proves the immediate recovery path too: replacing `/usr/libexec/vectra-controller/render-config.sh` with the local control-file fallback logic and restarting the service makes `/var/run/vectra-controller/config.json` and `/var/run/vectra-controller/status.json` report `controllerVersion = 0.1.12-r4` again even while `opkg status vectra-controller-agent` remains empty. This confirms the router-side version source problem separately from the package install journal state.
- A 2026-04-10 follow-up now also proves the permanent visibility fix in published release `0.1.12-r5`: controller/LuCI `0.1.12-r5` were built, copied into the production artifact path, synced into PostgreSQL metadata, and then manually reinstalled on the live AX3000T `1111111111` through the same direct `.ipk` URLs that the operator UI advertises. After that reinstall, both `opkg status vectra-controller-agent` and `opkg status luci-app-vectra-controller` returned normal `install user installed`, `/var/run/vectra-controller/config.json` and `/var/run/vectra-controller/status.json` both reported `controllerVersion = 0.1.12-r5`, and production `vectra_router_inventory_snapshot` already stores the latest snapshot for `bdfdb919-5e06-4344-ad8b-67a16f3b6fcf` as `0.1.12-r5`.
- A later same-day attended operator check now also proves the actual web-backed self-update path on that AX3000T end-to-end: clicking `App Update -> Controller -> Переустановить 0.1.12-r5` on `router.vectra-pro.net` created production job `e4e664d8-39f8-4076-ba0c-10dc30e26bfd`, PostgreSQL marked it `succeeded`, the router page returned to `очередь задач: 0` while still showing `Controller 0.1.12-r5`, and a follow-up read-only LAN check confirmed `opkg status vectra-controller-agent` plus runtime `controller_version` stayed at `0.1.12-r5`. The only leftover side effect from that successful UI-triggered reinstall is the standard opkg shadow conffile `/etc/config/vectra-controller-opkg`.
- A later 2026-04-10 cleanup pass on the same AX3000T removed that leftover shadow conffile live without touching package state: `/etc/config/vectra-controller-opkg` was backed up under `/root/vectra-cleanup-20260410-024806/` and then deleted, while both Vectra packages stayed at `Status: install user installed` and `/var/run/vectra-controller/status.json` still reported `controller_version = 0.1.12-r5` with the service running. In the local source tree, the next controller package now also stops persisting auto-discovered inventory fields in the package-owned UCI conffile, derives board/version facts at render time instead, and removes stale `vectra-controller-opkg` during `uci-defaults` migration so this shadow should not reappear after the next release.
- A later 2026-04-10 publish closes that durability loop for real in release `0.1.12-r7`: first `0.1.12-r6` proved the live operator self-update path still recreated `/etc/config/vectra-controller-opkg`, because package `postinst` returned on `VECTRA_SKIP_POSTINST_RESTART` before running `/etc/uci-defaults/90_vectra_controller_defaults`. The `r7` follow-up reorders that `postinst` flow so the migration/cleanup runs before the restart-skip exit, and controller/LuCI `0.1.12-r7` were then built, published as direct production artifacts, synced into PostgreSQL metadata, and installed through the real `App Update` button on the live AX3000T. Final LAN verification now shows `vectra-controller-agent = 0.1.12-r7`, `luci-app-vectra-controller = 0.1.12-r7`, `/var/run/vectra-controller/status.json` reporting `controller_version = 0.1.12-r7`, `/etc/config/vectra-controller-opkg` absent, and `/etc/config/vectra-controller` reduced to stable operator-managed settings only.
- A 2026-04-14 local investigation on live router `AndreyVK_Sochi` (`16129064-3a73-423d-818e-99ed9e73b61f`) found that the operator-visible `0 подписок` state is not just a stale UI badge. Production DB history shows `apply_passwall_config` on revision `d79a3fb1-5256-4c7a-8bdc-393280e54b80` did send a real `subscribe_list` section with the preserved secret URL, but agent-side `internal/passwall/apply.go` rendered the imported anonymous subscription id `@subscribe_list[0]` into the invalid UCI section name `vectra_sub_@subscribe_list[0]`. The post-apply live reimport then came back as revision `3624770b-9be9-4125-a7bf-127a3215ada3` with `0` `subscribe_list` sections, which explains why the router summary says `0 подписок` while the web draft still shows one stale subscription row. Local fix: `safeID()` now sanitizes all non-`[A-Za-z0-9_]` characters and regression test `TestExecutorApplySanitizesImportedSubscriptionSectionIDs` is green; this fix is not yet published as a controller package or deployed to routers.
- A same-day 2026-04-14 terminal follow-up added a new bounded router-terminal lane in source and published artifacts `0.1.12-r9`, and a later production follow-up advanced the safe-router path to `0.1.12-r10`: shared contracts include `run_terminal_command`, the agent executes one-shot shell commands with timeout, exit-code capture, stdout/stderr truncation, and timeout reporting, Go regression coverage is green locally, production direct artifacts plus PostgreSQL metadata now expose the newer controller/LuCI release, `Watch Logs -> Terminal` was already live-proven on router `1111111111`, and that same router has since been upgraded through the operator UI to `0.1.12-r10`.
- A later 2026-04-14 diagnostics follow-up is now live-proven on one production router rather than source-only: `internal/inventory/collector.go` probes Telegram reachability via the existing Go HTTP prober against `https://telegram.org/`, `https://web.telegram.org/`, `https://t.me/`, and `https://api.telegram.org/` with a `3s` timeout per request, caches the aggregated result in-process for `5m`, summarizes it as `reachable / partial / blocked`, and publishes both the aggregate and the per-target checks in `inventory.TelegramReachability`; local `go test ./...` is green with the probe coverage. After publishing controller/LuCI `0.1.12-r10` and upgrading router `1111111111`, production snapshots first reported Telegram `partial` because `t.me` timed out and then stabilized at `reachable` `4/4`, which confirms the probe works on a live router.

## Risks

- 2026-05-13 Kirill-MSK subscription URL replacement risk is closed by r22: the original apply job `80d84b6b-f5cc-4b1c-8c28-a0d29d57374f` correctly failed at `MemAvailable=37 MB` under the old 64 MB heavy-job floor, but controller/LuCI `0.1.13-r22` lowers only the heavy operational refresh floor to 40 MB and keeps package/storage updates at 64 MB. Retry apply job `e3ec68e2-2c55-40ad-bb87-a99654c954f4` succeeded, live re-import converged with no drift/pending changes, and route smoke is green. Remaining watch item is ordinary RAM headroom during future auto-refreshes, not the saved subscription replacement.

- 2026-05-13 `Artem-Lutfulin` onboarding remains blocked on router reachability, not panel setup: router `cd9ba0ed-c96b-4e55-8952-9e4621fe659b` (`NetisNX`, `netis,nx31`, OpenWrt `24.10.4`, controller `0.1.13-r21`) was approved from its first live import, the real hostname update to `artem-lutfulin` is queued, and the fresh personal subscription URL is stored only in operator draft `4b14d5b2-2f95-4303-b930-95052485284d` with apply job `8c28c31f-2da1-4e48-ad89-ae3c64aad808` queued as subscription-only refresh/restart. Follow-up polling from `2026-05-13T18:22Z` to `18:32Z` still showed no check-in after `2026-05-13T17:43:16Z`; monitoring reports `offline`/`Нет связи`, and valid subscription-managed route nodes are not imported yet. A bounded heartbeat will continue the finish lane when the router reconnects: drain queued jobs, re-import, resolve/probe current subscription nodes, apply the Google DoH + `myshunt`/ShuntRules/`DiscordVoiceUdp` baseline, and verify bindings plus smoke.

- Packaging lane сейчас обходит `golang/host` внутри official OpenWrt `24.10.4` SDK, потому что host toolchain на данном VPS оказался нестабилен.
- Pure SDK compile lane для LuCI side всё ещё не закрыт: на VPS он упирается в `ucode/module.h` при сборке `lucihttp`, поэтому release feed для pilot/stable preview остаётся на manual packaging lane.
- Controlled direct/reconnect operator path уже валидирован live на AX3000T: manual direct job выключал `passwall2.@global[0].enabled`, reconnect job возвращал `enabled=1`, очищал rescue reason и оставлял router в `active|approved`.
- Автоматический local proxy-failure rescue path всё ещё требует отдельного failure-injection теста; validated пока именно operator-triggered direct/reconnect, а не полный outage сценарий.
- Даже после удаления LuCI-обвязки пользователь может воспринимать проблему как "остатки контроллера", если на роутере до перезагрузки продолжает жить уже запущенный процесс `vectra-controller-agent`; ребут в таком случае естественно убирает симптом без дополнительных изменений.
- Controller self-update lane уже прошла live validation для staged controller package update, но текущий финальный recovery после firmware flash выполнялся manual reinstall route, а не через self-update job.
- Firmware lane уже прошла реальный manual sysupgrade до OpenWrt `24.10.6`, а identity persistence root cause закрыта и повторно проверена в `0.1.9-r1`; post-flash PassWall2 recovery logic уже опубликована в controller `0.1.11-r1`, но этот automated queue-driven restore всё ещё не validated live на AX3000T.
- Для текущего live-router нет inbound-доступа с VPS по `22/80/443` на последнем seen IP `5.228.192.149`, поэтому удалённо добить пакет/рестарт напрямую нельзя; фактический recovery теперь зависит от следующего outbound check-in или локальной ручной переустановки/рестарта контроллера.
- apply lane сейчас live-proof; benign stderr noise по `subscribe.lua start all` сузили на стороне Vectra command normalization, но стоит оставить это в regression tests.
- Для release-grade firmware lane всё ещё нужен полный live proof уже опубликованного controller build: queued `update_passwall_packages` / reinstall path должен быть прогнан на AX3000T без ручного SSH между install и runtime recovery.
- Этот importer-side shunt-extras fix пока существует только в локальном workspace: без следующего controller package release historical approved/imported snapshots на production всё ещё могут не содержать `network` и часть match extras до reimport.
- Новый router-log lane остаётся snapshot-only: streaming/tailing по-прежнему нет, но first live proof на AX3000T уже закрыт, поэтому дальше риск смещается с "заработает ли вообще" на ширину source coverage, пустые noisy outputs и читаемость operator UX для больших snapshot payloads.
- The controller self-update bug itself is now closed in the direct-artifact production lane via `0.1.12-r4`, and the later conffile-cleanup durability fix is now live in published `0.1.12-r7`; however, the public signed `Packages` / `Packages.sig` feed metadata still has not been republished with the original long-lived usign key. Web-backed update jobs already target the correct `r7` artifacts from PostgreSQL; external feed-name installs remain a separate signing/ops follow-up.
- The real cleanup-safe proof currently exists on the AX3000T self-update lane only. `0.1.12-r7` is live-validated there, but a second attended reinstall on another hardware tuple would still increase confidence that the reordered `postinst` migration behaves identically outside this device.
- A 2026-04-10 production recheck from the VPS now closes the operator-visible loop completely for the current AX3000T baseline: external `GET /api/health` and router-authenticated `POST /api/router/check-in` are healthy, Caddy logs show repeated `200` responses for router `bdfdb919-5e06-4344-ad8b-67a16f3b6fcf`, and PostgreSQL now stores the latest snapshot for `1111111111` with `controllerVersion = 0.1.12-r7`.
- The new subscription-loss fix is still local-only. Until a new controller package is published and installed on affected routers, any panel-driven `apply_passwall_config` that targets imported subscription IDs like `@subscribe_list[0]` can still delete the live `subscribe_list` section and fail to recreate it, even though the draft row remains visible in the web UI.
- The new terminal lane is no longer release-only: one safe AX3000T now proves read-only command execution end-to-end, but broader runtime coverage is still missing for stderr and non-zero exits, longer outputs, and BusyBox variability on other router classes.
- The new Telegram reachability probe is no longer source-only: one production router on `0.1.12-r10` already reports real `partial` and later `reachable` snapshots, but wider rollout is still needed to learn how often other networks or providers block only `web.telegram.org` or `api.telegram.org`, whether the current `3s` timeout is still right outside this AX3000T path, and how operators should react to recurring partial states.

## Next Review

- Прогнать автоматический proxy failure -> local direct fallback -> stable recovery на AX3000T и зафиксировать real-world timing/cooldown.
- Поставить в очередь уже опубликованный `0.1.11-r1` `update_passwall_packages`/controller update на AX3000T и подтвердить live recovery `dnsmasq-full/chinadns-ng/nft` + custom geodata refresh без ручного SSH.
- Решить, нужен ли отдельный controller package release только ради importer-side shunt extras backfill, или операторского `reimport` достаточно для текущего AX3000T.
- После этого обновить note уже не как pilot, а как certified stable lane для `xiaomi,mi-router-ax3000t` stock-layout.
- Расширить live `collect_router_logs` проверку на AX3000T до источников `controller`, `passwall` и `dnsmasq`, затем решить, нужны ли дополнительные нормализации/подсказки для пустых или шумных логов.
- Decide whether to republish the public signed OpenWrt feed metadata with the original usign key so external feed consumers can also see `0.1.12-r7`, while keeping the already-proven PostgreSQL-backed direct-artifact lane as the primary operator update path.
- If a safe second device is available, rerun one more attended `App Update -> Controller` reinstall outside the AX3000T so the `0.1.12-r7` cleanup proof is no longer single-device only.
- Extend the attended production update proof beyond controller: run the remaining `App Update` package buttons (`PassWall2`, `Xray`, `sing-box`, `Hysteria`, `Geoview`) from the operator UI and record which jobs stay clean versus which still need router-side recovery work.
- Publish the new controller package with the `safeID()` subscription fix, install it on `AndreyVK_Sochi`, then re-apply the preserved draft revision so the stored subscription URL is restored on the live router without re-entering the secret manually.
- Extend live terminal proof beyond `1111111111`: run one bounded stderr or non-zero command and one longer read-only command, then verify that the resulting `run_terminal_command` payload still renders cleanly in production `Watch Logs`.
- Upgrade at least one additional safe router to the Telegram-capable controller/LuCI release and verify that its snapshot carries both the aggregate status and the per-domain Telegram checks, then decide whether any extra retry/help copy is needed for persistent `partial` cases.

2026-04-17 addendum:

- The local source tree now carries a dedicated PassWall update executor in `cmd/vectra-controller-agent/passwall_update.go` instead of routing these jobs through the older generic staged/feed install path. The new lane sorts managed-stack packages deterministically, keeps `luci-app-passwall2` last, evaluates tmp/overlay headroom from artifact metadata before the first mutating step, and returns per-package result objects rather than one flat command summary.
- Scoped runtime component jobs (`xray-core`, `geoview`, `sing-box`, `hysteria`) now share the same artifact-driven payload format as managed-stack updates. The executor keeps package-path as the preferred route, then falls back adaptively to the built-in PassWall component updater and, for `xray-core`, a direct binary refresh extracted from the target `.ipk` when package convergence is blocked by storage or package-state drift.
- Post-update convergence now matches the approved plan more closely: runtime components are judged by `runtimeVersion`, package-managed pieces by `packageVersion`, `rule_update.lua log geoip,geosite` plus PassWall restart/recovery always run after the stack update, and the result payload now reports `pathUsed`, `driftDetected`, and per-package failure text honestly instead of claiming a clean package upgrade when only runtime drift was resolved.
- This controller-agent slice has only static review in the current workspace. `go` is not installed locally, so `go test ./...` and compile validation were not runnable here; the next required proof is a published artifact plus attended `Update Xray` / `Update PassWall2` on a safe `pilot/certified` Filogic router, including a low-overlay fallback case.

2026-04-18 update-delivery addendum:

- The PassWall executor in `cmd/vectra-controller-agent/passwall_update.go` now follows the approved split semantics instead of one generic package-first path: standalone `xray-core` updates honor `strategy = xray-built-in-first`, try the built-in PassWall updater before package install, and only then fall back to the direct Xray binary payload when runtime convergence is still blocked.
- Per-package result modeling is now stronger and compile-verified: the agent emits package/runtime before/after fields, separate package/runtime targets, explicit `package-updated` / `runtime-updated` / `runtime-only-converged` / `storage-blocked` states, and treats `storage-blocked` as a terminal unsuccessful package result rather than a silent success path.
- The earlier note that `go` was unavailable in this workspace is now stale. On `2026-04-18`, local `go test ./...` passed for the full `router/vectra-controller-agent` module after the update-delivery refactor and test refresh.
- A later same-session production publish now moves this lane beyond local compile proof: controller/LuCI `0.1.12-r11` were built in an isolated VPS temp workspace against the OpenWrt `24.10.6` Filogic SDK, copied into the public direct-artifact path under `api.vectra-pro.net/artifacts/openwrt/stable/aarch64_cortex-a53/`, and inserted into PostgreSQL `vectra_artifact` metadata through an explicit spec-based sync so the stale signed feed index did not block operator-visible availability.
- Read-only validation on the live test router `192.168.99.1` still shows controller runtime `0.1.11-r1`, so the new PassWall executor is published but not yet installed on that device. The same read-only check now also shows `last_check_in_at = 2026-04-17T23:26:01Z`, which is enough to prove server delivery recovered before any attended update job is queued to this router.
- A later same-session attended update pass now proves the published agent on the real test router rather than leaving this note at pre-install state. `Update Controller` from the production panel upgraded `vectra-controller-agent` and `luci-app-vectra-controller` on `192.168.99.1` to `0.1.12-r11`, and subsequent inventory snapshots report `controllerVersion = 0.1.12-r11`; however, the same surface still records a false controller failure summary with `signal: killed`, so the remaining agent-side gap here is result-reporting accuracy after self-update, not package convergence itself.
- Standalone `update_passwall_packages` for `xray-core` is now also live-proven on that router with the published `0.1.12-r11` agent. The queued job kept `strategy = xray-built-in-first`, saw runtime `Xray 26.4.15` already ahead of package target `26.3.27-r1`, and returned `status = runtime-only-converged`, `pathUsed = not-needed`, and `driftDetected = true` exactly as the new executor and payload model intended.
- The managed-stack path still lacks the clean success-or-storage-block proof on `192.168.99.1`. The production panel now queues the correct managed payload and the router agent accepts it, but the live result currently fails before package convergence with `opkg update: exit status 7`; a same-window read-only router probe showed about `14.9M` free on `/overlay` and no lingering install transaction afterward, so the next agent/runtime investigation target is package-feed refresh instability on this router rather than only overlay accounting.
- A later same-session attended bridge now moves the published agent beyond `0.1.12-r11` on the real test router instead of leaving it there. Because the older live agent still used unconditional `opkg update` in its own staged controller reinstall lane, the test router had to be bootstrapped once by direct `.ipk` install; after that bridge, fresh inventory and production UI both confirm controller/LuCI `0.1.12-r13` on `192.168.99.1`.
- The core agent-side update executor fix is now live-proven on that router, not only compile-proven. With controller `0.1.12-r13` in place, a production `Update PassWall2` rerun accepted a managed payload that contained only pinned artifacts, the router no longer started `opkg update` or `wget Packages.gz`, and the final result payload came back from the agent with detailed per-package states instead of one flat failure string.
- The remaining managed-stack gap is now narrower and explicit. On the live rerun, the agent updated `luci-app-passwall2` to `26.4.10-r1`, treated `xray-core` and `geoview` as `runtime-only-converged`, reported `v2ray-geoip` as `storage-blocked` because overlay headroom was insufficient, and flagged `v2ray-geosite` as a package-path failure because OpenWrt refused the target package as a downgrade-like version. The next agent/runtime review point is therefore not “why does the lane still die before work starts” but “how should the managed-stack lane classify and present mixed partial convergence on low-overlay routers”.

2026-04-21 update-reboot investigation addendum:

- A code-grounded review now narrows the old symptom “PassWall package update from the panel lands, but PassWall only works again after reboot” to the operator `update_passwall_packages` lane, not to upstream PassWall2 LuCI `App Update`.
- Upstream PassWall2 LuCI still does not auto-update the `luci-app-passwall2` package: `check_passwall2` only reports that self-update is unsupported, while the `App Update` page drives `update_<component>` endpoints for runtime binaries such as `xray`, `sing-box`, `hysteria`, and `geoview`. That upstream component path already stops/restarts `passwall2` around the binary move, so it does not match the observed “package updated, then only reboot helps” contour.
- The historical operator-side bug is missing post-install convergence. Older controller builds could finish the package replacement but leave PassWall without the follow-up `lua /usr/share/passwall2/rule_update.lua log geoip,geosite` and discrete `/etc/init.d/passwall2 restart || start`, so runtime stayed half-recovered until the next boot rebuilt transient state.
- Current source keeps that repair explicit in `runPasswallPostInstallRepair()` and the current controller package line remains on `0.1.12-r13`, so if the symptom is still seen on a router today, the first thing to verify is whether that router is still running a pre-`0.1.11-r1`/pre-repair controller build rather than the current update executor.

2026-04-21 terminal reboot proof addendum:

- Live terminal coverage on the safe test router `OpenWrt` (`192.168.99.1`, controller `0.1.12-r13`) now extends beyond read-only board/info commands into a real restart-safe control action. The first attended reboot attempt through `run_terminal_command` failed immediately with BusyBox `sh: syntax error: unexpected ';'`, which exposed a genuine shell-shape incompatibility in the initial delayed reboot payload (`&;` after a background command) before any router-side reboot occurred.
- After correcting the payload to a newline-separated shell form, the same router proved the reboot mechanism end-to-end through the existing agent terminal lane without any router-side code change: the accepted job reported pre-reboot `boot_id = c66e065a-1d3d-4033-8a8b-bcf9fc89708a`, the delayed `/sbin/reboot` executed, a later queued `cat /proc/sys/kernel/random/boot_id` returned `3bbb25e8-0fb9-4884-9a7f-c3f4b4381731`, and fresh panel snapshots/check-ins resumed at `2026-04-20T22:16:42Z`. This is now explicit live proof that the current terminal lane can safely drive a router reboot when the payload is shaped to return before the board actually goes down.

2026-04-21 Xray built-in updater addendum:

- The standalone Xray executor gap is now closed in both source and live runtime. `cmd/vectra-controller-agent/passwall_update.go` now keeps the explicit `runtimeTargetVersion` for `strategy = xray-built-in-first`, resolves status/completion against the effective runtime target instead of only the mirrored package version, and applies that same effective target across built-in updater validation, runtime-only convergence, and direct-binary fallback checks. The new `package_update_test.go` coverage locks that behavior in at the agent layer.
- A new direct-artifact controller publish `0.1.12-r14` is now live-proven on the safe test router `OpenWrt` (`192.168.99.1`, router id `e80b2f5b-d80a-4903-85e1-57d8ce747f15`). Production panel `Update Controller` queued job `dd8bf2b3-b4d7-4268-accb-37e1434afb33`; the router accepted the paired reinstall, restarted cleanly, and the next production snapshot reported `controllerVersion = 0.1.12-r14`.
- The same router then attended the real proof for the Xray bugfix rather than only a code-path review. Production `Update Xray` queued job `0ad9a336-979b-4773-91a6-3883da9b37f1` with `strategy = xray-built-in-first`, `packageTargetVersion = 26.3.27-r1`, and `runtimeTargetVersion = 26.4.17`. The job completed `succeeded`, the next router snapshot moved `binaryVersions.xray` from `Xray 26.4.15 ...` to `Xray 26.4.17 ...`, while `packageVersions["xray-core"]` stayed at `25.10.15-r1`. That exact runtime/package split is the expected live signature of the PassWall2 built-in updater path rather than an `opkg` package reinstall, and it closes the earlier false “already current” short-circuit on routers whose runtime was newer than the mirrored package but still behind upstream Xray.

2026-04-22 stabilization no-op addendum:

- The whole-repo triage cleanup reran the current router-agent proof lane from the existing worktree instead of trusting stale memory. `cd router/vectra-controller-agent && go test ./...` passed again during the same sweep that revalidated the web and deploy contours.
- No new router-agent regression was reproduced by those checks, and no new router-side bug surfaced from the local responsive/hygiene fixes. For this cleanup pass, the evidence supports leaving the current agent slice untouched rather than inventing another Go change just to reshape commit boundaries.

2026-04-22 subscription preview parity addendum:

- The local agent now carries a new read-only job type `inspect_subscriptions` whose only purpose is to answer the operator question “what does PassWall on this router receive right now from the live subscription list?” without mutating UCI or triggering a real re-import. `cmd/vectra-controller-agent/main.go` accepts the new job type, contracts/fixtures now cover it, and the result payload returns structured per-subscription preview fields such as `subscriptionKey`, `subscriptionId`, `urlHash`, `accessMode`, `userAgent`, fetch state, payload counts, payload fingerprints, and `checkedAt`.
- The router-side implementation is intentionally PassWall-synced rather than a fresh Go/TS reimplementation. `internal/passwall/subscription_preview.lua` loads the live router `/usr/share/passwall2/subscribe.lua`, suppresses its normal logging, reuses the current runtime helpers (`curl`, `parse_link`, and live `subscribe_list` UCI data), and emits a dry-run JSON summary. This keeps header generation and direct/proxy/auto behavior anchored to the same PassWall fetch path that uses `user_agent`, `x-device-os`, `x-ver-os`, `x-device-model`, and `x-hwid`.
- Local engineering proof for the agent side is green on `cd router/vectra-controller-agent && go test ./...`. The remaining gap is live proof, not compile proof: this new helper has not yet been attended on a real router after landing, so the next operational step for this module is a safe read-only `inspect_subscriptions` run against a known problematic subscription/router pair rather than more source reshaping.

2026-04-22 tcping inventory false-failure addendum:

- Live triage on router `DmitryGubenko` (`35ab62ff-71bc-4364-b383-a724c98d4cf6`, `netis,nx31`) now closes one new operator-visible managed-stack failure as a controller-agent bug rather than a router/package problem. Two production `update_passwall_packages` jobs against PassWall mirror `26.4.20-1` both failed with summary `tcping: tcping did not reach target through package path`, even though the same router stayed reachable, kept `controller/passwall/dnsmasq` at `running`, and reported healthy storage headroom (`overlayFreeMb ~= 61`, `tmpFreeMb ~= 117`).
- Read-only terminal proof on that live router is decisive: `opkg status tcping` returns `Version: 0.3-r1` with `Status: install user installed`, `/usr/bin/tcping` exists, and `/usr/lib/opkg/info/tcping.{control,list}` are present. The job payload itself also shows `opkg` stdout `Package tcping (0.3-r1) installed in root is up to date.`, so the package path reached the router and did not fail on download, storage, or install.
- The local root cause is now fixed in source. `internal/inventory/collector.go` previously omitted `tcping` from `passwallInventoryPackages`, while the PassWall managed-stack executor still uses a fresh post-install inventory snapshot to verify package-only components. That mismatch made `packageVersionAfter` stay empty for `tcping`, so the executor produced a false failure even after successful `opkg` convergence. The local fix adds `tcping` back into tracked package inventory and locks it with a regression test in `internal/inventory/collector_test.go`; targeted verification is green on `go test ./internal/inventory ./cmd/vectra-controller-agent`.
- This fix is still local-only until the next controller package publish/install. Until then, routers can continue to show the same false managed-stack failure when `tcping` is the only remaining package under evaluation, even though the router-side package state itself is already correct.

2026-04-22 controller version target addendum:

- The local package sources for the controller pair now target the next release line instead of the stale mixed `0.1.12-r13` / published-`0.1.12-r14` contour. `router/vectra-controller-agent/openwrt/Makefile` and `router/luci-app-vectra-controller/Makefile` now resolve to `PKG_VERSION = 0.1.13` with `PKG_RELEASE = 1`, which means the next build artifacts will be named and reported as `0.1.13-r1`.
- The web-side regression fixtures that model the current controller artifact/source version are aligned to the same target in local source. Targeted expectations in `update-progress.test.ts`, `destructive-gating.test.ts`, `router-control.test.ts`, `editor-surface.test.ts`, `fleet-monitoring-data.test.ts`, and `fleet.test.ts` now expect `0.1.13-r1` instead of the older `0.1.12-r13/r14` current-version placeholders.
- Verification for this bump is local-only by design: `pnpm --filter @vectra/web test -- --run src/server/api/routers/update-progress.test.ts src/server/api/routers/destructive-gating.test.ts src/server/vectra/router-control.test.ts src/server/vectra/editor-surface.test.ts src/server/vectra/fleet-monitoring-data.test.ts src/server/api/routers/fleet.test.ts` passed (`36` files / `196` tests through the current vitest selection). No publish, direct artifact sync, or live router install is claimed yet for `0.1.13-r1`.

2026-04-22 controller 0.1.13-r1 publish addendum:

- That publish gap is now closed for the production operator path. `vectra-controller-agent_0.1.13-r1_aarch64_cortex-a53.ipk` and `luci-app-vectra-controller_0.1.13-r1_all.ipk` were built on the VPS from the current local source slice, copied into the production direct-artifact directory, and inserted into live panel metadata through the spec-based sync lane.
- Current live proof is intentionally panel-facing rather than router-install-facing. Production `update.artifacts` now exposes `0.1.13-r1` as the newest controller pair, and `update.versionDriftWorkspace` now offers `controllerAvailable = 0.1.13-r1` for the safe test router `OpenWrt`, while that router itself still reports `controllerInstalled = 0.1.12-r14` until an attended controller update is actually queued and completed.
- This also turns the earlier `tcping` inventory fix from “local-only” into “published but not yet installed everywhere” for future routers that take the next controller update. The remaining operational proof for this module is narrower now: run one attended controller update to `0.1.13-r1` on a safe router and then re-test the old `tcping` false-failure contour on `DmitryGubenko` or an equivalent target.

2026-04-22 low-overlay geodata false-failure addendum:

- The local agent now also closes the second false managed-stack failure that stayed after the earlier `tcping` fix. `cmd/vectra-controller-agent/passwall_update.go` no longer requires exact string equality for package-managed geodata when the router already carries a newer package record than the mirrored target; non-runtime package convergence now accepts installed version `>=` target, and the numeric comparator explicitly normalizes mixed geodata timestamp formats such as `YYYYMMDDHHMM(.release)` versus `YYYYMMDDHHMMSS-rN`.
- This is the precise source-side fix for the operator-visible low-overlay contour where `v2ray-geoip` could be marked `storage-blocked` and `v2ray-geosite` could fail with `did not reach target through package path` even though the router was already ahead of the panel target and only needed the normal `rule_update.lua` + restart repair. New regression coverage locks the datestamp comparison itself plus the early no-command exit for `v2ray-geosite` when the installed package is newer; `cd router/vectra-controller-agent && go test ./...` is green after the patch.
- The fix is still local-only until the next controller package install reaches a real router. The next operational proof is now narrower than “retest PassWall updates in general”: upgrade one safe router to the next controller build, rerun `Update PassWall2` on a low-overlay router that already has newer `v2ray-geo*` package metadata than the mirrored target, and confirm the old false `storage-blocked` / `did not reach target` pair disappears without masking genuine storage failures for truly outdated packages.

2026-04-22 controller 0.1.13-r2 geodata-fix publish addendum:

- That “local-only until next controller build” gap is now closed for distribution. Controller/LuCI `0.1.13-r2` were built in an isolated VPS temp workspace from the current router source slice after bumping only `VECTRA_RELEASE` to `2`, using the same live OpenWrt `24.10.6` Filogic SDK and the current published `controller-r9` signing key so the public `vectra.pub` did not rotate again.
- This release is specifically the geodata false-failure follow-up on top of the already-published `0.1.13-r1` line. The router-agent source fix for mixed `v2ray-geo*` datestamp comparison is now carried by the public artifacts `vectra-controller-agent_0.1.13-r2_aarch64_cortex-a53.ipk` and `luci-app-vectra-controller_0.1.13-r2_all.ipk`, and both the signed feed plus production `update.artifacts` now expose `0.1.13-r2` instead of `0.1.13-r1`.
- Live proof is release-lane proof, not router-install proof. Public `index.json` now reports `version = 0.1.13`, `release = 2`, both `.ipk` URLs return `HTTP 200`, and `update.versionDriftWorkspace` now resolves only `controllerAvailable = 0.1.13-r2` across current rows. The remaining next step for this module is now the attended low-overlay router install/retest, not another publish step.

2026-04-22 router hostname update addendum:

- The local agent/controller contract now carries a dedicated terminal purpose for router hostname changes instead of treating them as an untyped shell command. `router/vectra-controller-agent/cmd/vectra-controller-agent/main.go` now recognizes `purpose = router-hostname-update` and echoes `hostnameAfter` into the job result payload when the terminal command succeeds, which gives the control plane a trustworthy signal that the real OpenWrt rename completed on-device.
- This keeps the hostname path aligned with the current controller architecture: no SSH side channel, no DB-only alias, and no special one-off router API. The web panel queues the same existing `run_terminal_command` job lane the agent already executes, but the agent now returns just enough typed result metadata for the panel to update router identity state immediately after the successful job result instead of waiting only for a later inventory snapshot.
- Local proof for the agent side is green on `cd router/vectra-controller-agent && go test ./...`. This remains source-only until a controller package carrying this terminal-purpose result metadata is published/installed on a real router and one attended hostname rename is verified end-to-end from the panel.

2026-04-22 low-overlay geodata r2 follow-up addendum:

- A same-day live retest now shows the earlier `0.1.13-r2` geodata note was incomplete rather than fully closed. Safe router `OpenWrt` is already on controller/LuCI `0.1.13-r2`, but production `update_passwall_packages` job `861650de-3294-4b72-b371-d590babacc30` still failed with the old operator contour: `v2ray-geoip` stayed `storage-blocked` on about `14 MB` free overlay, and `v2ray-geosite` still came back as `did not reach target through package path` even though `opkg` itself reported `Not downgrading package v2ray-geosite on root from 20250916122507-r1 to 202603292224.1.`.
- The new local source fix now closes both remaining gaps more accurately than the earlier timestamp-only compare. `cmd/vectra-controller-agent/passwall_update.go` now compares package versions with `opkg`-style ordering for package-managed packages instead of the previous date-normalized heuristic, and after a successful `lua /usr/share/passwall2/rule_update.lua log geoip,geosite` repair it reconciles `v2ray-geoip` / `v2ray-geosite` as rule-managed convergence instead of leaving them as terminal package-path failures on low-overlay routers.
- Local verification is explicit and green: `cd router/vectra-controller-agent && go test ./...` passes after new regression coverage for mixed geodata version ordering plus rule-managed reconciliation of `storage-blocked` / false-failed `v2ray-geo*` results. This slice is still local-only until the next controller publish/install reaches a real router. The next operational step is now concrete: publish the next controller package from this source state and rerun `Update PassWall2` on a safe low-overlay router, expecting the old paired `storage-blocked` / `did not reach target` geodata contour to disappear from the final job summary.

2026-04-22 controller 0.1.13-r3 low-overlay live proof addendum:

- That next publish/install step is now closed on a real router rather than left as an intention. Controller/LuCI `0.1.13-r3` were published to the stable signed feed and production metadata, safe router `OpenWrt` (`e80b2f5b-d80a-4903-85e1-57d8ce747f15`) accepted controller self-update job `693bfb91-ae47-49da-84e3-cdf57f38a4e9`, and the next inventory snapshot reported `controllerVersion = 0.1.13-r3` with both controller packages present as `0.1.13-r3`.
- The only controller self-update stderr on that live proof was the expected opkg shadow-conffile warning for `/etc/config/vectra-controller`, which produced `/etc/config/vectra-controller-opkg` but did not block package convergence or service health. Fresh router inventory after the restart still showed `controller`, `passwall`, `passwallServer`, and `dnsmasq` all at `running`.
- The decisive low-overlay retest is now live-proven too. Production `Update PassWall2` job `c697cb43-b9b3-437f-a349-ed747f91ebf2` on the same router finished `succeeded` with no terminal geodata failure, even though overlay headroom stayed around `14 MB` and the installed package records still reflected the same ahead-of-target contour as before. Final `packageResults` now report `v2ray-geoip -> status=updated, pathUsed=not-needed, driftDetected=true` and `v2ray-geosite -> status=already-current, pathUsed=not-needed, driftDetected=false`, which is the exact operator-visible symptom this controller release was meant to fix.
- One narrower follow-up remains separate from this bugfix: the live managed-stack payload on that job still resolved `v2ray-geoip`, `v2ray-geosite`, and `luci-app-passwall2` artifacts from the older `26.4.10-1` mirror while the bundle release tag stayed `26.4.20-1`. The new `0.1.13-r3` agent handled that mixed payload honestly and no longer surfaced false geodata errors, but the artifact-resolution mismatch itself is still a deploy/web follow-up rather than part of the closed low-overlay status bug.

2026-04-22 second AX3000T r3 proof addendum:

- A later same-day live retest now proves that the remaining red contour on router `1111111111` (`bdfdb919-5e06-4344-ad8b-67a16f3b6fcf`) was not a new web-side regression after the PassWall package-target hotfix, but simply another router still running the older controller line. Before intervention, this certified Xiaomi AX3000T still reported `vectra-controller-agent = 0.1.12-r13`, `luci-app-passwall2 = 26.4.20-r1`, and latest failed managed-stack job `0e6d3ea8-b812-4ba6-80c9-26e04d1cb7a0` showed the exact older agent contour: `v2ray-geosite did not reach target through package path` plus false `tcping did not reach target through package path`, even though stdout already contained `Package tcping (0.3-r1) installed in root is up to date.` and `Not downgrading package v2ray-geosite ...`.
- The attended fix path stayed entirely within the already-proven operator contour. Controller self-update job `4d5dc9b3-2e56-46d3-9052-41bcdccc758c` ran through `run_terminal_command purpose=controller-self-update`, moved the router from `0.1.12-r13` to `0.1.13-r3`, and the next inventory snapshot confirmed `controllerVersion = 0.1.13-r3` with the router still `active/approved`.
- The decisive proof is the immediate rerun of the same managed-stack lane on that same router, not a cross-router inference. Follow-up PassWall job `75a9f5e5-cb33-4a28-a264-544012224bb7` then finished `succeeded`; final `packageResults` now report `tcping -> already-current/not-needed`, `v2ray-geosite -> already-current/not-needed`, `v2ray-geoip -> already-current/not-needed`, and no terminal failure summary remained.
- This materially strengthens confidence in the current `0.1.13-r3` agent slice. The `tcping` inventory fix plus the geodata reconciliation logic are now live-proven not only on safe router `OpenWrt`, but also on a second certified AX3000T that had still been reproducing the old false-failure contour while stuck on `0.1.12-r13`.

2026-04-22 control-plane outage supervisor addendum:

- The local agent source now has a dedicated control-plane outage supervisor above the existing proxy-path rescue. A new persisted `controlPlaneRecovery` block tracks last successful control-plane contact, outage window, current recovery phase, controller-restart timestamp, auto-reboot budget timestamp, PassWall retry timestamp, grouped `panel/ru/foreign` probe status, and the last recovery action reason.
- The recovery logic is intentionally bounded against reboot loops and panel-maintenance false positives. After one full hour of continuous `control_url` outage, the agent now classifies the router into `panel-only outage`, `foreign blocked`, or broader WAN degradation, allows only one local `vectra-controller` restart per outage window, budgets automatic router reboot to at most one action per `12h`, and leaves the router in stable direct mode with `operator_attention` if foreign reachability still does not recover after the single post-reboot PassWall retry.
- This implementation pass also closed two late state-machine gaps in source: `controller_restart_settle` is now honored through `max(2*poll_interval, 90s)` before re-entering normal monitoring, and `direct_settle` now starts a fresh settle window even when the router was already in direct mode before the control-plane supervisor took over. Local proof is green on `cd router/vectra-controller-agent && go test ./...`, with explicit unit coverage for controller-restart-only, direct fallback, reboot cooldown, PassWall retry, operator-attention escalation, grouped probe classification, persisted-state round-trip, and the two hardening cases above.
- This keeps `Router Agent` at `pilot/high` because the supervisor is still source-only in the current workspace and has not yet been published or exercised on a real router. The next operational step is narrow: publish the next controller package with this state machine, install it on one safe router, and run attended drills for both “panel-only outage” and “foreign blocked after one hour” before treating the contour as live-proven.

2026-04-22 control-plane outage supervisor safety review addendum:

- A same-day deep code review against the local supervisor slice found multiple production blockers before rollout, not polish debt. The most severe issue is that grouped `panel/ru/foreign` probe results are cached for `5m` under a key that ignores rescue mode, while `direct_settle = 45s`, `passwall_warmup = 75s`, and `post_reboot_settle = 4m`; this means the supervisor can evaluate reboot or operator-attention branches on stale pre-direct or pre-reboot reachability instead of on a fresh post-action probe.
- A second blocker is sticky recovery ownership after a successful operator reconnect or controller self-update auto-resume. `resumeProxyMode()` clears the PassWall rescue state but does not clear `controlPlaneRecovery`, so routers can remain stuck in `operator_attention`, keep reporting `awaitingOperator = true`, and continue skipping the older quick-rescue path even after proxy mode was restored successfully.
- Branch ownership is also still too broad for safe rollout. Current source allows `ru blocked` alone to enter the direct/reboot contour, and it also treats routers that were already in direct mode before the supervisor took over as eligible for the same reboot budget. Both behaviors exceed the intended “safe-path only after foreign blocked” boundary and need a narrower owner/entry gate before the slice should be published.
- Current local tests remain useful but incomplete for this contour. The worktree still lacks regression coverage for cache invalidation across direct/reboot/PassWall transitions, reconnect clearing `controlPlaneRecovery`, RU-only outage staying non-destructive, and preserving unrelated open incidents when the synthetic recovery backend resolves supervisor-owned alerts.

2026-04-22 control-plane outage supervisor blocker-fix addendum:

- The local agent slice now closes the concrete supervisor blockers found in the same-day safety review rather than only recording them. `controlplane_recovery.go` now invalidates grouped reachability cache on every recovery action that changes network shape or ownership (`enterDirectModeForRecovery`, `resumeProxyMode`, controller restart scheduling, reboot scheduling, and successful control-plane contact), so `direct_settle`, `passwall_retry_wait`, and post-reboot checks no longer reuse stale pre-action probe groups across shorter settle windows.
- Recovery ownership is also narrowed to the intended contour. RU-only panel outages no longer enter direct/reboot recovery, and routers that were already in direct mode before the supervisor takes over now stay in monitoring instead of being treated as supervisor-owned `direct_settle` candidates for later reboot escalation.
- Sticky supervisor state after a successful reconnect is explicitly cleared now, but only on the manual/terminal auto-resume paths that actually restore proxy mode for the operator. A new ownership reset helper clears `controlPlaneRecovery` back to `idle` while preserving the reboot cooldown timestamp, which keeps the older quick-rescue path available again without discarding the 12h reboot budget.
- Local regression proof for these fixes is green on `cd router/vectra-controller-agent && go test ./cmd/vectra-controller-agent ./internal/config ./internal/state ./internal/recovery ./internal/rescue`. New targeted coverage now locks the negative paths that were previously missing: stale cache invalidation after direct fallback, RU-only outage staying non-destructive, already-direct routers not entering supervisor-owned settle, and explicit clearing of sticky `operator_attention` ownership.

2026-04-22 DmitryGubenko live VPN complaint addendum:

- A later same-day read-only incident check on live router `DmitryGubenko` (`35ab62ff-71bc-4364-b383-a724c98d4cf6`, `netis,nx31`) shows that the complaint "VPN does not work on the router" is not reproducible at the PassWall runtime layer anymore. Fresh production snapshots keep the router in `active/approved`, report `controller/passwall/dnsmasq/passwallServer = running`, keep `passwallEnabled = true` with selected node `myshunt`, and show the converged stack `vectra-controller-agent = 0.1.13-r3`, `luci-app-passwall2 = 26.4.20-r1`, `xray = 26.4.17`.
- The new read-only runtime proofs are explicit rather than inferred from stale panel state. Production `run_terminal_command` job `c4ba8392-1bc5-45ac-8b81-c0117a09700f` returned `rc=0` for three real PassWall node tests (`M0OjiPBJ`, `dqnDBqfQ`, `H25TQVlY`), and fresh router snapshots right after that check show `telegramReachability = reachable` with `4/4` successful HTTPS probes to `telegram.org`, `web.telegram.org`, `t.me`, and `api.telegram.org`.
- Current logs do not point to a live proxy-core outage. `collect_router_logs source=passwall` returned an empty PassWall/Xray error slice, while `collect_router_logs source=system` plus targeted `logread` show only service restarts around the earlier same-day update window and client-side LAN/Wi-Fi noise: repeated `dnsmasq-dhcp` `DHCPNAK` for host `64:d2:c4:b0:77:96` still requesting old address `192.168.0.147` while the router offers `192.168.98.105`, and repeated `hostapd` `did not acknowledge authentication response` for station `a8:80:55:ad:81:0e`.
- A follow-up same-day read-only L2 pass makes the access-layer diagnosis materially stronger. `ubus call network.interface dump` still shows clean separation in intended config (`lan = br-lan = 192.168.98.1/24`, `wan = eth1 = 192.168.0.182/24`, upstream DHCP server `192.168.0.1`), and dnsmasq confirms only one LAN DHCP pool (`192.168.98.100-249`) with `no-dhcp-interface=eth1`. But later terminal jobs also show the upstream gateway MAC `cc:2d:21:06:17:20` for `192.168.0.1` present on both `eth1` and `br-lan`, and `brctl showmacs br-lan` places that MAC on bridge port `3`, which `brctl showstp br-lan` maps to `lan3`.
- The same bridge-table pass also closes one ambiguity about the complaining client. Sanitized UCI reads show a very simple local topology (`br-lan` over `lan1/lan2/lan3`, both SSIDs bridged to `lan`, no guest/VLAN/relay contour in config), and `brctl showmacs br-lan` maps the earlier DHCP-noisy client `64:d2:c4:b0:77:96` to the same bridge port `3` as the upstream `192.168.0.1` MAC. By contrast, active 2.4 GHz client `1c:57:dc:62:df:16` lands on port `4` (`phy0-ap0`) and active 5 GHz clients such as `3a:7d:64:40:54:b0` and `66:33:b9:33:8c:42` land on port `5` (`phy1-ap0`).
- That means the upstream `192.168.0.x` segment is not just "remembered" by clients; it is physically visible inside the OpenWrt LAN bridge on `lan3`. The same capture window shows the earlier problem client `64:d2:c4:b0:77:96` already holding the correct lease `192.168.98.105`, while the weak 2.4 GHz station `a8:80:55:ad:81:0e` is still associated at unusably low SNR (`-88 dBm`, negative SNR margin). The best current reading is therefore: PassWall runtime healthy, but Dmitry's local topology likely leaks the upstream Tenda `192.168.0.x` network back into `lan3`, creating a real second-gateway / wrong-segment path that can make clients appear to "lose VPN" even when the proxy stack is fine.
- This changes the operational reading of the Dmitry incident. The earlier morning red contour was real but already explained by the older false managed-stack failures on `tcping` and geodata; after controller `0.1.13-r3`, successful PassWall package jobs, and the later reboot, the current remaining evidence points at client reconnect, stale lease, or Wi-Fi association churn rather than a still-broken PassWall dataplane on the router itself.

2026-04-22 DmitryGubenko Wi-Fi isolation workaround addendum:

- The same-night live mitigation is now also complete, and it stayed inside the lowest-risk boundary for this incident. Instead of rewriting WAN or the existing wired bridge while the agent depended on the router remaining reachable, the current production SSIDs `Netis_2.4G` and `Netis_5G` were moved onto a dedicated Wi-Fi-only logical interface `wifi_clean`.
- The applied network shape is explicit in recorded terminal jobs, not inferred from intent. `network.wifi_clean` now serves `192.168.99.1/24`, `dhcp.wifi_clean` advertises `192.168.99.100-249`, both default radio AP sections point at `wifi_clean`, and the interface is backed by an explicit empty bridge device `br-wifi_clean` via `network.wifi_clean_dev`, which was required to make the SSID-side L3 interface come up cleanly.
- Change safety was handled conservatively. Before the first mutating step, the router saved copies of `/etc/config/network`, `/etc/config/wireless`, `/etc/config/dhcp`, and `/etc/config/firewall` under `/tmp/vectra-wifi-seg/backup/` and armed an automatic rollback timer. After the bridge-backed `wifi_clean` fix was verified, the rollback was explicitly cancelled and no rollback process remained.
- Final live verification is strong enough to treat the workaround as active. Terminal history now shows `ifstatus wan` still up on `eth1` with `192.168.0.182/24` and gateway `192.168.0.1`, `ifstatus wifi_clean` up on `br-wifi_clean` with `192.168.99.1/24`, and `ubus call network.wireless status` reporting both radios up with both production SSIDs bound to `wifi_clean`. The panel-side router record also kept fresh check-ins through `2026-04-22T21:29:49.711Z`, so controller connectivity survived the live change.
- This closes the requested Wi-Fi-side workaround, but not the underlying wired topology defect. `logread` still shows `dnsmasq-dhcp: no address range available for DHCP request via br-lan`, which matches the earlier proof that the upstream `192.168.0.x` segment leaks into `br-lan` through `lan3`. The current operational reading is therefore narrower and more useful: Wi-Fi clients should now receive clean `192.168.99.x` leases from the isolated SSIDs, while any remaining wrong-segment behavior on the wired side still requires physical cabling or downstream-switch correction on `lan3`.

2026-04-23 controller 0.1.13-r4 publish addendum:

- The current router-agent source slice is now published as controller/LuCI `0.1.13-r4` rather than remaining a local-only worktree. The VPS build used the same OpenWrt `24.10.6` Filogic SDK plus current live signing key lane, `deploy/scripts/sync-runtime-artifacts.sh` safely synced the resulting signed feed into `deploy/runtime/artifacts/openwrt/stable/aarch64_cortex-a53/`, and production metadata now contains fresh controller artifact rows for both `vectra-controller-agent` and `luci-app-vectra-controller` at `0.1.13-r4`.
- Public and panel-facing proof are both explicit. External `index.json` now reports `version = 0.1.13`, `release = 4`, both new `.ipk` URLs return `HTTP 200`, and production `update.versionDriftWorkspace` now resolves `controllerAvailable = 0.1.13-r4` across live routers that are still on `0.1.13-r3`.
- This means the next controller line carrying the current source state is now in the live publish lane, including the control-plane outage supervisor with blocker fixes, the router-hostname terminal result metadata, and the router-side subscription-preview job support. What is still missing is router-install proof for `r4`: no safe router has been upgraded to this exact line yet, so the new outage contour and preview/hostname paths remain published-but-not-live-proven.
- The next operational step for `Router Agent` is now narrower and concrete: install `0.1.13-r4` on one safe router, then run attended drills for `panel-only outage`, `foreign blocked after one hour`, router-side subscription preview, and one real hostname change from the panel.

2026-04-24 router `1111111111` Telegram triage addendum:

- A same-day read-only triage on certified router `1111111111` (`bdfdb919-5e06-4344-ad8b-67a16f3b6fcf`, Xiaomi AX3000T) does not reproduce a persistent router-wide Telegram outage. Fresh production snapshot at `2026-04-23T22:49:04.917Z` still shows `controller/passwall/dnsmasq/passwallServer = running`, `passwallEnabled = true`, `selectedNodeId = myshunt`, controller `0.1.13-r3`, PassWall app `26.4.20-r1`, and `telegramReachability = reachable` with `4/4` successful HTTPS probes to `telegram.org`, `web.telegram.org`, `t.me`, and `api.telegram.org`.
- The current path for Telegram on this router is more specific than "whatever the main node is". Read-only panel config for the live approved revision shows `geosite:TELEGRAM` inside shunt rule `WorldProxy`, and that rule currently points at node `2VQPbYRq` labeled `🇩🇪⚡Германия YouTube 🚫Ad🚫` (`vless`, `tcp`, `ger1.nfnpx.online:443`). This means Telegram traffic is intentionally steered through a separate Germany path rather than the generic default route.
- Live on-device DNS now confirms the shunt/FakeDNS contour explicitly. Read-only terminal job `82b686e4-04ea-4ff1-9d71-d37cfbbc6bce` returned `passwall2.@global[0].enabled = 1`, `node = myshunt`, and local resolver answers for `telegram.org`, `web.telegram.org`, and `api.telegram.org` as `198.18.x.x` FakeDNS addresses through `127.0.0.1:53`, which is expected for the current transparent-proxy design rather than evidence of a DNS failure by itself.
- Current logs do not point to an active proxy-core fault on this router. Fresh `collect_router_logs source=passwall` job `1a91f751-da96-4acc-a586-9c5768e01989` completed successfully but returned an empty PassWall/Xray error slice, which is consistent with the green runtime snapshot and does not support a standing PassWall crash or restart loop.
- Operational reading should therefore stay narrow. The symptom "Telegram Desktop on macOS gets stuck at updating until the app is restarted" is not currently reproducible as a live router-wide Telegram block on `1111111111`; the best current hypothesis is either intermittent instability on that dedicated Telegram `WorldProxy -> Germany` route or a client-side stale long-lived session on macOS over the existing FakeDNS/tproxy path. The next high-signal proof should be captured during the failure window itself from the affected Mac plus one immediate router-side Telegram/domain/node check, not another broad PassWall update.

## 2026-04-26 controller/LuCI package repair release

- Controller package release `0.1.13-r5` was built from a clean `HEAD` source slice, not from the unrelated local auto-rescue/Telegram dirty tree. The release intentionally carries the `0.1.13` controller agent code forward with new immutable package release metadata and clean packaging, then restored affected routers through the terminal self-update lane. Live fleet proof after rollout: `VagrandRouter`, `AlekseyHorev`, `1111111111`, `AndreyVK`, `Vasily_Filicity`, `testrouter`, and `DmitryGubenko` all report `vectra-controller-agent=0.1.13-r5`, `luci-app-vectra-controller=0.1.13-r5`, and controller service `running`; `AndreyVK_Sochi` was intentionally left on working `0.1.13-r3` because its previous `0.1.13-r4` terminal job failed with `Operation not permitted`.

## 2026-04-29 VagrandRouter PassWall no-proxy incident

- Live incident check on certified `VagrandRouter` (`f0f2437a-7786-4a3b-be38-5c02af2f4461`, Xiaomi AX3000T/OpenWrt `24.10.5`) found the panel-side symptom was real: snapshots before the fix showed `telegramReachability = blocked` for `telegram.org`, `web.telegram.org`, `t.me`, and `api.telegram.org`, and router-local curl to `https://telegram.org/` timed out.
- Root cause was not controller/API reachability or resource pressure. PassWall2 was enabled on `myshunt`, but the transparent proxy core was absent: `xray` was not running. `/tmp/log/passwall2.log` showed repeated `global.json` startup failures on shunt outbound `YouTube:🇷🇺⚡Россия YouTube 🚫Ad🚫` with Xray REALITY config error `empty "password"`; PassWall2 then entered `Running in no proxy mode`.
- Containment stayed minimal and backed up router config first: `/etc/config/passwall2` was copied to `/root/vectra-vagrand-passwall-hotfix-20260429-014205/passwall2.before`, then only `passwall2.myshunt.YouTube` was changed from broken node `2UNaVJtF` to already-tested working node `C9sSnDgU` (`🇫🇮 ⚡Финляндия YouTube 🚫Ad🚫`), followed by `/etc/init.d/passwall2 restart`.
- Validation after the restart was green: `xray run -c /tmp/etc/passwall2/acl/default/global.json` was present, PassWall2 loaded nftables rules instead of no-proxy mode, router-local Telegram curl returned `HTTP 200`, and the next production snapshot at `2026-04-28T22:42:51Z` reported `telegramReachability = reachable` with all four Telegram probes successful.
- Follow-up risk: the broken subscribed Russia YouTube node still exists in the subscription as a selectable node. If a later subscription/apply remaps `myshunt.YouTube` back to `2UNaVJtF`, the same `empty password` Xray failure can return. Prefer a durable panel/draft or subscription-side mapping update after confirming the intended replacement route.

2026-04-30 DmitryGubenko direct-mode repair addendum:

- A live follow-up recovered `DmitryGubenko` (`35ab62ff-71bc-4364-b383-a724c98d4cf6`) from the 2026-04-29 control-plane-recovery direct-mode incident by queueing a normal `reconnect` job (`cd755301-07b4-4ee9-97ce-45ffd0996d8f`). The job completed `success`, set PassWall back to proxy mode, cleared the active rescue state, and resolved the open `proxy_outage` incident.
- Current production validation is green: fleet monitoring now shows `statusLabel = Прокси-режим`, `passwallEnabled = true`, `operationalState = stable`, no direct-mode alert, and Telegram reachability `4/4` successful for `telegram.org`, `web.telegram.org`, `t.me`, and `api.telegram.org`. A router-local terminal probe also fetched `api.vectra-pro.net`, `telegram.org`, and `youtube.com` successfully through FakeDNS (`198.18.x.x`) on `myshunt`.
- The comparison window is router-specific, not a global outage. Between `2026-04-29 03:45` and `05:25 UTC`, all other live routers kept Telegram reachable with zero blocked snapshots, while Dmitry alone switched from Telegram reachable to blocked at `03:57:44 UTC`, then entered direct at `05:16:43 UTC` after the one-hour control-plane/proxy recovery threshold. The distinguishing topology remains Dmitry's double-NAT/local upstream path (`eth1 192.168.0.182/24 via 192.168.0.1`) plus the previously documented lan3/Tenda contamination risk, not package/version drift.

2026-04-30 DmitryGubenko direct-mode auto-retry source addendum:

- The controller-agent source now closes the manual-reconnect gap exposed by DmitryGubenko's 2026-04-29 direct-mode incident. In `PhaseDirectSettle`, once the control plane is reachable again and RU/local WAN probes are reachable, the agent no longer stops immediately at `operator_attention`; it performs one automatic `resumeProxyMode()` PassWall retry and moves to `passwall_retry_wait` before asking for operator action.
- The retry is deliberately bounded to one attempt per outage window using `LastPasswallRetryAt >= OutageStartedAt`. If that retry has already happened, or if PassWall is already enabled, or if RU reachability is not green, the old safe operator-attention behavior remains in place.
- Local proof is green on `cd router/vectra-controller-agent && go test ./cmd/vectra-controller-agent ./internal/config ./internal/state ./internal/recovery ./internal/rescue` and full `cd router/vectra-controller-agent && go test ./...`. New regression coverage locks both the desired Dmitry-style auto-resume path and the no-second-retry operator-escalation path.
- This is source-only until the next controller package is published and installed on routers. Operationally, it means the next Dmitry-like control-plane recovery should self-try proxy mode once instead of waiting for a manual panel reconnect, while still failing closed to operator review if the proxy path remains broken.

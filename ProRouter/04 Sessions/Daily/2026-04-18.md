---
type: session
date: 2026-04-18
tags:
  - session
---

# Session 2026-04-18

## Summary

- Closed the local `apps/web` smartphone overflow regression with a minimal responsive fix rather than another redesign.
- Verified the touched web slice with `next typegen`, `typecheck`, and `build`, then re-ran mobile browser QA against a correct local `next start` runtime on `127.0.0.1:3300`.
- Confirmed that the unauthenticated operator shell no longer creates page-level horizontal overflow at `390x844` and that protected `/fleet` still redirects back to `/login`.

## Completion Updates

### 01:36

- Summary: Deployed the second-wave guarded web optimization slice to production: router detail now boots from a single editor-surface read instead of a duplicate fleet-byId plus editor-surface path, fleet monitoring now filters open incidents and active jobs in SQL, the VPS rebuild succeeded after clearing exhausted Docker build cache, and the public contour recovered to 307/200/200/200/400 with both /api/health and /healthz back at 200. Live logs still show fleet.monitoring around 3.6s, so the next optimization target is the fleet snapshot query shape itself.
- Modules: `Deployment Stack, Web Control Plane`
- Next: Have the user compare /fleet and router detail responsiveness on production; if fleet is still the main pain, optimize latest-snapshot and per-router fleet summary queries next rather than expanding more UI surfaces.

### 01:44

- Summary: Researched external best practices for minimal mobile overflow fixes in Next.js/React dashboards, focusing on safe diff patterns for shell overflow and wide tables/cards/workspaces.
- Modules: [[02 Modules/Web Control Plane]]

### 01:48

- Summary: Investigated live VPS pressure on 72.56.14.52, confirmed RAM was healthy but root disk was at 91% due to web-release backups accidentally capturing deploy/runtime/postgres, pruned older large rollback directories to restore ~15G free, and fixed deploy-web-release.sh locally plus on the VPS to exclude deploy/runtime from future backups.
- Modules: [[02 Modules/Deployment Stack]]
- Next: After the next web release, confirm new web-release backup dirs stay small and no longer contain deploy/runtime/postgres; if growth resumes, add an explicit rollback-backup retention policy.

### 01:58

- Summary: Ran a deeper live disk audit on 72.56.14.52 after the initial cleanup: confirmed the remaining heavy footprint is mostly /opt build inventory (three OpenWrt SDK trees plus three SDK archives), /var/lib/docker, /var/log journal, and root Go caches rather than another hidden runtime leak; also removed stale Vectra release payloads from /tmp, dropping /tmp from 286M to 72K while root usage moved from 63% to 62%.
- Modules: [[02 Modules/Deployment Stack]]
- Next: Decide whether the VPS should keep all three OpenWrt SDK trees and archives locally; if not, a single current-SDK policy will reclaim more space than further log or cache tuning.

### 02:12

- Summary: Closed the local smartphone overflow complaint in `apps/web` with a minimal responsive fix: removed root horizontal clipping from the shell, switched `Парк` to the existing router-card lane on phones while keeping the dense table from `lg`, made the known rollout note `min-w-[260px]` inputs phone-safe, and added only narrow `min-w-0` containment in router detail. Local verification is green on `next typegen`, `typecheck`, and `build`; browser QA against the correct local runtime on `127.0.0.1:3300` confirms no page-level horizontal overflow on the unauthenticated shell at `390x844`, successful static asset loads, and the expected protected-route redirect from `/fleet` to `/login`.
- Modules: [[02 Modules/Web Control Plane]]
- Next: Run one authenticated phone walkthrough of `Парк`, router detail, and `Обновления` before making a stronger claim about the full operator workspace mobile experience.

### 02:08

- Summary: Implemented PassWall/Xray update-delivery semantics across web, contracts, health probing, and controller-agent; verified green locally and rechecked live router/prod facts read-only.
- Modules: [[02 Modules/Web Control Plane]], [[02 Modules/Router Agent]], [[02 Modules/Shared Contracts]], [[02 Modules/Deployment Stack]]
- Next: Deploy the web+agent slice; Re-run attended Update Xray and Update PassWall2 from the operator UI after deploy

### 02:08

- Summary: Closed the local apps/web smartphone overflow regression with a minimal responsive fix and verified no page-level horizontal overflow on the correct 390x844 local runtime.
- Modules: [[02 Modules/Web Control Plane]]
- Next: Run one authenticated phone walkthrough of Fleet, router detail, and Updates before claiming full operator-workspace mobile proof.

### 02:19

- Summary: Completed the fleet.monitoring backend hot-path pass: router detail now boots from a single editor-surface read, fleet monitoring no longer reads full snapshot history just to keep the newest row per router, and the production SQL regression from the first raw-SQL rollout was resolved by rebuilding and redeploying a fresh corrected tar. Production is healthy again at 307/200/200/200/400 with the previous browser-push ANY/ALL SQL error gone from live logs.
- Modules: `Web Control Plane, Deployment Stack`
- Next: Have the user compare live /fleet responsiveness again; if fleet is still the main bottleneck, profile remaining per-router aggregation work inside loadFleetMonitoringSnapshot rather than expanding more UI surfaces.

### 02:27

- Summary: Deployed the PassWall/Xray update-delivery slice to production, published controller/LuCI 0.1.12-r11 as direct artifacts with PostgreSQL metadata, and confirmed the new /api/health dbWriteProbe plus recovered check-in delivery on test router 192.168.99.1.
- Modules: [[02 Modules/Web Control Plane]], [[02 Modules/Router Agent]], [[02 Modules/Shared Contracts]], [[02 Modules/Deployment Stack]]
- Next: Run an attended operator-UI update flow on 192.168.99.1: update controller to 0.1.12-r11, then verify Update Xray and Update PassWall2 behavior end-to-end.

### 02:32

- Summary: Ran authenticated 390x844 mobile QA across fleet, router detail core tabs, updates, drafts, rescue, enrollment, and install on fresh local runtime; found and fixed the remaining enrollment overflow regression.
- Modules: [[02 Modules/Web Control Plane]]
- Next: For deeper-than-layout confidence, run a second mobile pass that executes safe interaction states inside updates editors, enrollment disclosures, and non-destructive router-detail actions.

### 11:41

- Summary: Reviewed the oversized working tree, separated product changes from local runtime noise, tightened root .gitignore for local QA/MCP/helper artifacts, and taught Sync-ProRouterVault.py to honor gitignore so Repo Map stops reintroducing that noise.
- Modules: [[02 Modules/Knowledge Base and Runbooks]]
- Next: Split the tracked diff into focused commits: web/install+updates, scripts/docs, and ProRouter session memory; decide whether apps/web/public/install-helper/*.zip should stay versioned or move to a release/build artifact lane before the final commit.

### 11:49

- Summary: Extended the authenticated 390x844 mobile QA from layout checks into safe live interactions: router detail note entry and draft.save succeeded, updates remained reachable, and enrollment disclosures expanded cleanly without blocked zones.
- Modules: [[02 Modules/Web Control Plane]]
- Next: If stronger proof is needed, run one more mobile pass for save+apply, stable updates subworkspace clicks, and other non-destructive-but-stateful actions.

### 12:18

- Summary: Fixed the router-detail section-switch annoyance where changing inner sections like `DNS` and `Shunt Rule` kicked the page back to the top. The first local attempt only swapped the query-param update path to `window.history.replaceState(...)`, but a real browser smoke disproved it; the final correction now also preserves `window.scrollY` during the route-local section switch in `apps/web/src/components/router-detail-workspace.tsx` while keeping the same visible URL/deep-link behavior. Local LSP diagnostics are clean, and a fresh browser check on `127.0.0.1:3400` confirms stable `scrollY` across `DNS -> Shunt Rule -> DNS` instead of the old jump-to-top.
- Modules: [[02 Modules/Web Control Plane]]
- Next: No broader navigation cleanup is justified from this fix alone; only revisit shared tab behavior if the same jump is reproduced outside router detail.

### 11:51

- Summary: Verified the production operator update flow on test router 192.168.99.1: controller packages now converge to 0.1.12-r11 from the panel, published/synced PassWall mirror metadata 26.4.10-1 so new versions became visible in the UI, standalone Xray update now returns runtime-only-converged through xray-built-in-first semantics, and full PassWall2 currently fails with opkg update exit status 7 on the router.
- Modules: [[02 Modules/Web Control Plane]], [[02 Modules/Router Agent]], [[02 Modules/Deployment Stack]]
- Next: Investigate the false controller self-update failure hint after real convergence and debug why managed-stack Update PassWall2 dies on opkg update exit status 7 on 192.168.99.1 instead of reaching a final success or storage-block result.

### 12:37

- Summary: Closed the remaining same-day production update-delivery gaps on test router `192.168.99.1`: manually bootstrapped controller/LuCI to published `0.1.12-r13`, deployed a web hotfix that suppresses stale controller self-update failures after convergence and stops queued PassWall managed-stack jobs from re-adding already-installed unpinned recovery deps, then re-ran operator `App Update` live. The new panel job now queues only pinned PassWall artifacts with no router-side `opkg update`, `Update Xray` still succeeds as `runtime-only-converged`, and `Update PassWall2` now returns an honest mixed result instead of a silent transport failure: `luci-app-passwall2` moved to `26.4.10-r1`, `xray`/`geoview` stayed runtime-current, `v2ray-geoip` surfaced an overlay storage block, and `v2ray-geosite` surfaced a target-version refusal. The production UI now reflects those truths directly with controller `0.1.12-r13`, PassWall app `26.4.10-r1`, and a concrete storage-block summary instead of the old generic `opkg update` error.
- Modules: [[02 Modules/Web Control Plane]], [[02 Modules/Router Agent]], [[02 Modules/Deployment Stack]]
- Next: Decide whether the managed-stack lane should treat partial convergence with runtime-current components plus one storage-block as a softer warning state instead of full failure, and whether the `v2ray-geosite` target selection should normalize version ordering to avoid the downgrade-like refusal on routers that already carry a differently-versioned package record.

### 12:46

- Summary: Closed one last production UI truth bug after the attended reruns: a later scoped `Update Xray` success had started overwriting the separate `Последняя попытка обновить PassWall-стек` block with `xray-core via not-needed`. `editor-surface.ts` now ignores scoped `update_passwall_packages` jobs for that stack-specific summary, targeted tests/lint are green again, the narrowed web redeploy is healthy, and a fresh live reload on `192.168.99.1` now keeps the managed-stack storage-block summary visible while preserving controller `0.1.12-r13`, PassWall app `26.4.10-r1`, and Xray runtime/package drift truth in the same panel view.
- Modules: [[02 Modules/Web Control Plane]], [[02 Modules/Deployment Stack]]
- Next: If operator polish continues in this area, split stack-level and scoped-component last-attempt surfaces completely so `Update Xray` and `Update PassWall2` each keep their own persistent attempt history instead of sharing one generic banner lane.

### 12:39

- Summary: Bootstrapped test router controller to 0.1.12-r13, deployed the web hotfix for stale controller results plus installed-recovery-dep filtering, and live-verified Update Xray plus Update PassWall2 from the production panel on 192.168.99.1.
- Modules: [[02 Modules/Web Control Plane]], [[02 Modules/Router Agent]], [[02 Modules/Deployment Stack]]

### 12:47

- Summary: Fixed the live App Update summary leak so scoped Xray jobs no longer overwrite the separate PassWall stack attempt banner; redeployed web and re-verified the corrected production page on 192.168.99.1.
- Modules: [[02 Modules/Web Control Plane]], [[02 Modules/Deployment Stack]]

### 12:55

- Summary: Split the oversized working tree into four focused commits: repo hygiene/gitignore, public install helper plus bootstrap hardening, rollout groups/updates control, and docs plus cross-platform helper tooling.
- Modules: [[02 Modules/Knowledge Base and Runbooks]], [[02 Modules/Web Control Plane]], [[02 Modules/Shared Database]]
- Next: Push the branch and, if needed, open a PR with the four-commit split preserved.

### 13:08

- Summary: Applied the smallest safe local UI fix for router-detail tab switching in `apps/web`: `tab` / `section` changes on `/routers/[routerId]` now keep the existing query-param/deep-link semantics but update the URL through route-local history replacement instead of client-router replace, so switching sections like `DNS` and `Shunt Rule` no longer jumps the page back to the top. `apps/web/src/components/router-detail-workspace.tsx` has clean LSP diagnostics and file-level ESLint; broader `@vectra/web` build/typecheck remain blocked by pre-existing unrelated issues around `.next/types` generation and `src/server/api/routers/update.ts`.
- Modules: [[02 Modules/Web Control Plane]]
- Next: If stronger proof is needed, run a local browser smoke on a real router-detail page and confirm the scroll position stays stable across `Basic Settings` sub-tabs and primary PassWall tabs without widening the fix beyond this route.

### 13:13

- Summary: Fixed local apps/web router-detail tab switching so tab/section query-param updates preserve scroll position instead of jumping to the top.
- Modules: [[02 Modules/Web Control Plane]]

### 13:19

- Summary: Fixed the router-detail inner-section jump so DNS/Shunt Rule switching now preserves scroll position while keeping the same URL behavior.
- Modules: [[02 Modules/Web Control Plane]]
- Next: No wider tab-bar change is needed unless the same scroll-jump is reproduced outside router detail.

### 13:33

- Summary: Made controller self-update panel-driven by default for terminal-capable agents, hotfixed the checksum preflight bug in the new terminal payload, redeployed production web, and live-verified on 192.168.99.1 that 'Переустановить 0.1.12-r13' queues run_terminal_command purpose=controller-self-update and completes success without a manual SSH bridge.
- Modules: [[02 Modules/Web Control Plane]], [[02 Modules/Deployment Stack]]

### 21:23

- Summary: Fixed the production /fleet panel regression where latest snapshot data disappeared from router cards: fleet-monitoring-data now hydrates raw execute() snapshot rows correctly, local regression tests/build are green, and live /fleet again shows Telegram/controller/PassWall versions for the test router after a production hot patch and vectra-web restart.
- Modules: [[02 Modules/Web Control Plane]], [[02 Modules/Deployment Stack]]

---
type: session
date: 2026-06-30
tags:
  - session
---

# Session 2026-06-30

## Summary

-

## Completion Updates

### 00:22

- Summary: On-device smoke test of the hardened controller = 9/9 PASS on the real Xiaomi AX3000T 1111111111 (production controller 0.1.13-r30 untouched). Method: cross-compiled aarch64 binary staged to VPS artifacts tmp path, fetched + run from /tmp via panel terminal.queueCommand (no direct SSH); binary dialed only TEST-NET 192.0.2.1 so the real panel was never contacted (no duplicate registration); additive nft carve-out applied->verified->removed. Validated on real hardware: binary executes on aarch64/musl; EnsureIdentity wrote identity+ed25519 keypair; credential-floor invariant (no mirror without token); T3 carve-out installs at priority mangle-10 (=-160, ahead of PassWall -151) and coexists with live passwall2; SO_MARK 0x564354 proven end-to-end (carve-out accept counter 0->8 = control-plane routed DIRECT past tproxy on real kernel); carve-out removed cleanly. VPS staging cleaned up. T2 reauth covered by unit+interop+panel tests. Branch feat/controller-reliability-hardening canary-ready; .ipk build/publish + full canary install pending operator sign-off.
- Modules: [[02 Modules/Router Agent]]

### 12:06

- Summary: Controller reliability hardening FINISHED + canary-validated end-to-end on real AX3000T 1111111111 (r32, prod fleet untouched stable=r30). Canary on r31 surfaced 2 robustness defects, both fixed in r32 and re-proven in production: (D1 low-RAM self-update guard exit72 at ~46MB<64MB floor) job_safety now ALWAYS pre-drops caches for update_controller jobs + render-config defaults pre_drop_caches=1 -> r31->r32 self-update passed the guard automatically; (D2 carve-out not applied on self-update because it replaces files w/o postinst and restart!=boot) carve-out now applied in init.d start_service -> confirmed present after self-update AND auto-reappears after flush+restart (definitive on-device proof). r32 live: T1 identity mirror present, T3 carve-out priority -160 + SO_MARK counter incrementing (control-plane DIRECT past tproxy in production), continuous check-ins, no strand, passwall intact, compliance compliant, 0 incidents. Built/published to BETA channel only (stable r30 untouched, fleet 100% safe), per-router queueControllerUpdate(channel=beta). Full validation green (go test/vet/-race both platforms, shell carve-out 41 + watchdog 35). Pending operator go: promote r32 to stable + staged fleet rollout (low-RAM routers need uci pre_drop_caches=1 pre-step before first update).
- Modules: [[02 Modules/Router Agent]]

### 20:41

- Summary: FINALIZED the hardened controller end-to-end. (1) Committed branch feat/controller-reliability-hardening (ad6e064, 39 files). (2) Deployed the panel T2 half to prod (deploy-web-release.sh release-slice rsync w/ auto-backup + docker compose build/up; additive, no migration; web Up healthy, all health checks pass) — T2 keypair-recovery now PROVEN end-to-end against prod: throwaway router register w/ valid recoveryProof -> 201 (token re-issued), forged proof -> 403, throwaway deleted. (3) Kept stable=r30 as rollback anchor, rolled fleet via channel=beta(r32). Rollout RESULT: 23/27 routers on r32 (all online board families: AX3000T, netis NX31 incl strand-history boards, Cudy WBR3000UAX/WR3000H/WR3000E), ZERO strands (every status active except hh=its own pre-existing direct). r30 remainder: hh (no-touch, excluded); AlexanderBabkin + childsergeyavito (OFFLINE pre-rollout, updates queued, auto-land on return); DmitryGubenko (tight-RAM netis ~44MB, self-update guard (separate hardcoded ~64MB floor, NOT the job_safety storage floor) refuses, pre_drop_caches insufficient -> needs reboot-window or zram). Per-router rollout = uci pre_drop_caches=1 + restart, then queueControllerUpdate(beta), verified r32 + carve-out auto-applied + identity mirror + check-ins continuous. Rollback ready: channel=stable->r30.
- Modules: [[02 Modules/Router Agent]]

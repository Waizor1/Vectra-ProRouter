---
type: dashboard
updated: 2026-05-08
tags:
  - dashboard
  - status
---

# Stage Board

## 2026-05-08 PassWall parity follow-up

- Followed up on the same-class PassWall parity gaps surfaced during the ShuntRule incident. Production web now exposes node-local Xray/Mux/XUDP extras (`mux`, `mux_concurrency`, `xudp_concurrency`, `packet_encoding`) in the Node editor/read-only runtime node details, and `draft.editorSurface` now registers dynamic PassWall extras for nodes, SOCKS, subscriptions, global/rule/app extras, and non-standard shunt-rule extras so they appear in field diffs instead of being invisible panel state. Contract preview parity now also matches controller apply for `dns_redirect` and controller-style UCI safe section ids such as imported `@subscribe_list[0]`. Verification: targeted Vitest (`router-editor-state`, `editor`, `passwall-contracts`), `@vectra/web typecheck`, `@vectra/web lint`, `@vectra/web build`, full controller `go test ./...`, production Docker rebuild/restart, smoke-check, panel health, production source markers, and live `draft.editorSurface` proof that `extras.xudp_concurrency`/`extras.mux_concurrency` fields are now registered on router `1111111111`.

## 2026-05-08 Shunt target apply fix addendum

- Confirmed the operator symptom where an `apply_passwall_config` job finished successfully but server selections did not change on router `1111111111`. The failed apply receipt had `ShuntRule.outboundNodeId` updated to `6ee5vwyo`, while `myshunt.extras.WorldProxy` still held `OPIqwEuA` and `myshunt.extras.DiscordVoiceUdp` still held `dqnJE64Y`; existing controller apply rendered both and the later stale extra won. Production web now normalizes ShuntRule bindings before validation/save/apply and keeps `ruleManage.shuntRules` mirrored, so changing a ShuntRule target also updates the selected shunt node binding stored in extras. Preview parity was tightened to render shunt node bindings plus node/socks/subscription/global extras, and controller source was hardened so future controller builds skip stale shunt-binding extras after explicit ShuntRule bindings. Verification: targeted web Vitest, `@vectra/web typecheck`, `@vectra/web build`, Go passwall package test, full controller `go test ./...`, production Docker rebuild/restart, smoke-check, panel health, and browser route smoke.

## 2026-05-08 ShuntRules panel recovery addendum

- Production panel hotfix restored operator visibility/editability for PassWall2 ShuntRule extras that were already imported but hidden from Basic Settings: `protocol`, `inbound`, `network`, `source`, `port`, and `invert`. The operation preview now renders extras into UCI commands too, so Discord voice settings such as `DiscordVoiceUdp.inbound=tproxy`, `network=udp`, and `port=19294-19344,50000-50100` are no longer invisible in the panel. A second narrow route hotfix makes `/routers/1111111111?...` resolve to the UUID router page instead of failing with `Invalid uuid`, preserving the current tab/section query. Verification: targeted `passwall-contracts` Vitest, `@vectra/web typecheck`, `@vectra/web build`, production Docker rebuild/restart, smoke-check, panel health, and headless browser proof that Basic Settings → Shunt Rule shows `DiscordVoiceUdp`, `Inbound Tag`, `UDP`, and the Discord voice port range. Do not resume Discord fleet rollout until the volatile subscription-node target/source-of-truth mapping is normalized.

## 2026-05-07 Discord voice `1111111111` test addendum

- Live test patch applied on router `1111111111` after inspecting `/Users/waizor/Downloads/zapret-discord-youtube-1.9.8c.zip`: the archive's Discord voice fix points to UDP `19294-19344,50000-50100` with Discord/STUN handling, plus a fuller Discord host/IP surface. Router `WorldProxy` already had `geosite:DISCORD` and broad `66.22.192.0/18`, but was missing `66.22.176.0/24` and `66.22.188.0/22`; those ranges and explicit Discord domains were added to the live `WorldProxy` rule. First validation was green for `OPIqwEuA` (`xray=running`, `url_test_node=204:0.322123`, Discord API `200:0.298844`, generated Xray `xudpConcurrency=16`), but the user still reported voice failure, so the deeper live patch added a dedicated `DiscordVoiceUdp` shunt rule for UDP ports `19294-19344,50000-50100` and routed only that rule to the Poland/gRPC candidate `dqnJE64Y` with `mux=1`, `mux_concurrency=-1`, `xudp_concurrency=16`; general `WorldProxy` remains `OPIqwEuA`. Post-change proof: generated Xray contains `DiscordVoiceUdp` with the port rule to `dqnJE64Y`, `url_test_node dqnJE64Y=204:0.208831`, `url_test_node OPIqwEuA=204:0.176172`, and Discord API `200:0.228351`. The user confirmed Discord voice started working after this second patch. Next step: sync/reimport the live config into panel source of truth so a later apply does not overwrite the working contour.

## 2026-05-07 live DNS rollout addendum

- Live operator rollout moved the online non-`hh`, non-`1111111111` routers to the DNS contour tested on `1111111111`: Google DoH (`remote_dns_protocol=doh`, `remote_dns=8.8.8.8`, `remote_dns_doh=https://dns.google/dns-query`, `remote_dns_detour=direct`) with `dns.google` host pins. Applied targets: `AlekseyHorev`, `Vasily_Filicity`, `DmitryGubenko`, `AndreyVK`, `artempushkino`, `AndreyVK_Sochi`, `sairoutermsk`, and `VagrandRouter`; `hh` stayed excluded/no-touch, and stale `testrouter` was not queued because it already had 3 pending jobs and last checked in on 2026-05-01. Verification: panel health green, apply queues drained back to the stale `testrouter` jobs only, editor/live DNS surfaces match the source contour on all eight targets, router-terminal UCI checks match on all eight targets, and `url_test_node` returned `204` on all eight target WorldProxy nodes. Fleet monitoring later showed Telegram/YouTube reachable on all online routers except a stale/partial YouTube snapshot on `AlekseyHorev`; direct terminal YouTube probes there were mostly green with one transient timeout.

## 2026-05-06 bootstrap PassWall start guard addendum

- Source-only enrollment hardening now covers the router install failure where `opkg install vectra-controller-agent` reached the signed Vectra feed but failed after the bootstrap had already started PassWall2 while `myshunt` still had `Default/default_node` on a non-server fallback. The generated AX3000T bootstrap now binds `myshunt.default_node` to the same unique server remark used for `WorldProxy`, installs `vectra-controller-agent`/LuCI before any PassWall2 service start, and leaves PassWall2 disabled instead of starting it when the selected node or shunt default is `_direct`, `_default`, `_blackhole`, missing, or another shunt. Local proof is green on targeted `install-presets` Vitest, `@vectra/web typecheck`, `@vectra/web build`, and scoped diff check. The guarded web slice was deployed to production through the release-slice lane; VPS `vectra-web` rebuilt and returned healthy, public smoke is `307/200/200/200/400`, live `/install/ax3000t-bootstrap.sh` contains the controller-before-PassWall start order plus start guard, and live `/install/ax3000t-myshunt-rebind.sh` binds `default_node` as well. No live-router apply was executed.

## 2026-05-06 Google DoH default addendum

- Source defaults for new AX3000T/global rollout DNS now use Google DoH (`remote_dns_protocol=doh`, `remote_dns=8.8.8.8`, `remote_dns_doh=https://dns.google/dns-query`, `remote_dns_detour=direct`) with `dns.google` host pins instead of the prior Google UDP profile. Validators now reject drift away from this Google DoH baseline and still reject legacy Yandex DoH pins. No live router apply was executed in this local change; router `hh` is explicitly special-case/no-touch and must be excluded from any future DNS rollout unless the operator asks otherwise.


## 2026-05-03 hotfix addendum

- Production router enrollment recovered for the user-side stock `OpenWrt` install. Root cause was shared contract drift: Go importer/apply supported PassWall2 `socks` nodes, while `packages/contracts` rejected `protocol: "socks"` in imported baselines, causing repeated HTTP `400` on `/api/router/register` before a router row was created. The web/contracts slice was rebuilt and deployed; the same source then returned HTTP `201`, and the new router is visible as `OpenWrt` in `pending/import_review`.
- Router-detail UX now also separates real operator field changes from technical full managed-section sync in the apply preview, and this web slice was deployed to production via the guarded release lane on 2026-05-03. Production health/smoke is green; deployed source and bundle contain the new preview copy.

## 2026-05-04 bootstrap-preflight addendum

- A production web hotfix now addresses the Cudy/WR3000 install stop where OpenWrt `24.10.4` reported `В доступных OpenWrt feeds не найден обязательный пакет: libc`. The generated bootstrap script now treats already-installed OpenWrt base packages as satisfied before looking for them in feed availability, which matches `libc` being a base image package rather than a normal feed-advertised package. Local proof is green on targeted `install-presets` Vitest, `@vectra/web typecheck`, and `@vectra/web build`; guarded VPS deployment synced release slice `/tmp/vectra-web-release-libc-20260504-135409.tar.gz`, rebuilt `vectra-web`, preserved health on both public API contours, and live `/install/ax3000t-bootstrap.sh` now contains the installed-first check plus the new error text while the old `В доступных OpenWrt feeds...` marker is absent.
- The next Cudy/WR3000 field rerun then exposed a second preflight-only stop: one OpenWrt target feed returned `wget returned 4`, so `opkg update` exited non-zero even though the other package lists had updated and no package-specific failure was known yet. The public bootstrap generator now treats `opkg update` as a warning-only refresh step and relies on the later package-specific preflight to abort before changes if a required package is truly unavailable. Local `install-presets` Vitest, `@vectra/web typecheck`, and `@vectra/web build` are green; production `/install/ax3000t-bootstrap.sh` exposes the warning-only `if ! run_opkg update` branch, public health is green, and the standard smoke settled back to `307/200/200/200/400` after one startup-transient `502`.
- A third Cudy/WR3000 field rerun showed `libc` still failing under the new installed-first check, meaning this firmware does not expose `libc` through the same `opkg status` path even though musl libc is necessarily present in the base image. The generated bootstrap now has an explicit `openwrt_base_prereq_present()` detector for `libc` via `/lib/libc.so` or `/lib/ld-musl-*.so.1` before any `opkg status/list` checks. Local targeted tests, typecheck, and build are green; production route is redeployed and live `/install/ax3000t-bootstrap.sh` contains the musl detector plus repeated public smoke `307/200/200/200/400`.

## 2026-05-04 controller state self-heal addendum

- Controller/LuCI `0.1.13-r10` is now the production controller artifact line for the zero-byte/corrupt state fix. The agent now writes `state.json.last-good`, backs up unreadable `state.json` files, restores/salvages credentials when possible, and avoids crash-looping on empty JSON; local `go test -count=1 ./...` is green. Public feed files and panel metadata both advertise `0.1.13-r10`, AndreyVK has already self-updated successfully, and the latest live snapshot reports controller/LuCI `0.1.13-r10` with controller/passwall/dnsmasq healthy plus Telegram `4/4` and YouTube `3/3`.

## 2026-05-05 recovery guard addendum

- DmitryGubenko incident triage found a router stuck in direct/operator-attention even though the controller was fresh and concrete PassWall2 shunt proxy nodes still answered `url_test_node`. The controller-agent now guards the failed PassWall retry path: before disabling PassWall again because service-level foreign probes are blocked, it probes concrete shunt outbound slots and keeps proxy mode if at least one configured proxy node responds. Controller/LuCI `0.1.13-r11` was built from the guarded source, published to the signed stable OpenWrt feed, synced into production artifact metadata, installed first on DmitryGubenko, and then rolled to all fresh active routers except stale `testrouter`; version drift now shows every active production router on `0.1.13-r11`, no controller updates pending, and fleet overview has `openIncidents=0`. Local validation is green on targeted recovery/rescue tests plus `cd router/vectra-controller-agent && go test ./... -count=1`.


## 2026-05-05 controller-only bootstrap addendum

- The public Cudy/WR3000 bootstrap now has a controller-only fallback for fresh low-overlay installs. When the starter PassWall2/Xray stack does not fit on `/overlay`, the script no longer aborts before changes; it logs the low-space condition, skips dnsmasq-full, PassWall2 runtime packages, managed stack refresh, baseline, subscriptions, shunt rebind, rule update, and PassWall2 start, then installs and starts the Vectra controller so the router can check in. PassWall2/Xray convergence is intentionally deferred to the existing managed panel/controller update lane. Local proof is green on targeted `install-presets` Vitest, generated-shell `bash -n` and `sh -n`, `@vectra/web typecheck`, and `@vectra/web build`; production was redeployed through the guarded release slice `/tmp/vectra-web-release-controller-only-final-20260505-093111.tar.gz`, `vectra-web` is healthy, both public health endpoints are green, live `/install/ax3000t-bootstrap.sh` contains the controller-only markers, and standard smoke returned `307/200/200/200/400`.


## 2026-05-05 opkg-architecture libc addendum

- The next Cudy WR3000H rerun showed controller package installation failing with `Cannot satisfy ... libc` and many `Packages ... found, but incompatible with the architectures configured` messages. The root cause is not a missing musl file to install manually, but an `opkg` architecture configuration mismatch on this firmware path. The public bootstrap now repairs `/etc/opkg/arch.conf` before `opkg update`/controller install: it backs up the old file, ensures `all`, `noarch`, and the detected `DISTRIB_ARCH` (`aarch64_cortex-a53`) are present, then continues through the controller-only lane. Local proof is green on targeted `install-presets` Vitest, generated-shell `bash -n`/`sh -n`, `@vectra/web typecheck`, and `@vectra/web build`; production `/install/ax3000t-bootstrap.sh` is redeployed and exposes `ensure_opkg_architecture()` plus `opkg-arch.conf` backup, with health and smoke green.


Легенда:

- `reference` — справочный или исследовательский слой
- `scaffold` — каркас есть, бизнес-наполнение еще слабое
- `active` — модуль развивается и уже имеет рабочую структуру
- `pilot` — есть контур для практического применения, но без широкой валидации

Статусы ниже подтверждены по текущему состоянию репозитория и live contour на 2026-04-08. Это всё ещё не отчет о широкой продакшн-эксплуатации.

| Area | Path | Stage | Confidence | Evidence | Next Step |
|---|---|---|---|---|---|
| Knowledge base and runbooks | `ai_docs/`, `scripts/`, `RTK.md` | `active` | high | curated KB, helper scripts, agent runbook, private local registry flow; post-sysupgrade restore helper now passes live dry-run over pinned SSH after the `plink -> sh -s` quoting fix, confirming certified AX3000T stock-layout facts and pinned baseline package set without writes; attended live PassWall2 recovery on the same AX3000T is now also proven via custom `geoip/geosite` refresh plus restore of `dnsmasq-full`, `chinadns-ng`, `kmod-nft-socket`, and `kmod-nft-tproxy`, with `xray`/`chinadns-ng`/PassWall dnsmasq processes back up | turn that attended recovery proof into the official post-sysupgrade restore path, including custom geodata refresh and the direct-SSH command pattern for PassWall runtime actions |
| Web control plane | `apps/web`, `apps/install-helper` | `pilot` | high | production Next.js control plane is live on `router.vectra-pro.net` with operator auth, import-review/apply flows, public bootstrap assets, and the earlier PassWall-parity/operator-shell work already deployed; the newest install slice now also serves live public `/install` outside login middleware, desktop one-click through the localhost-only Go helper in `apps/install-helper`, honest mobile/manual fallback to the existing public bootstrap asset, and register-side rate limiting plus explicit pending-review audit metadata for open enrollment. Local verification for this slice is green across `pnpm --filter @vectra/web test --run public-install public-install-rate-limit`, `pnpm --filter @vectra/web exec next typegen`, `pnpm --filter @vectra/web typecheck`, `pnpm --filter @vectra/web build`, `cd apps/install-helper && go test ./...`, and `cd apps/install-helper && go build ./...`; a guarded same-day VPS release plus browser smoke now also confirms `https://router.vectra-pro.net/install` returns `200`. A later same-day release correction then re-uploaded the verified tar after a stale payload mismatch, deleted AppleDouble `._*` files before sync, rebuilt `vectra-web` again, and converted that install lane from “fallback visible” into explicit live proof of the simplified helper-first UX: browser snapshot now shows `helper: нужен helper`, one primary download CTA plus `Я уже запустил helper`, all four `/install-helper/*.zip` archives return `200`, and helper-side API proof still returns `Роутер 10.255.255.1 не отвечает по SSH.` for an SSH-unreachable target. The published macOS helper zip now also contains a real `Vectra Install Helper.app`, but it remains unsigned because the current workstation has no valid Apple code-signing identity configured. | run one LAN-attended install from the new public `/install` desktop-helper lane on a disposable router, then verify the same public bootstrap path from the honest mobile/manual fallback and confirm the resulting router lands in the pending-review lane end-to-end; after that, provision Apple Developer ID signing/notarization so the macOS helper stops tripping Gatekeeper |
| Shared contracts | `packages/contracts` | `active` | medium | workspace package exporting Zod-based contracts, richer router inventory schemas and typed update-job payload schemas for controller/passwall/firmware lanes | add contract-level tests for update payloads and reduce TS/Go drift risk |
| Shared database | `packages/db` | `active` | medium | Drizzle schema/migrations now exercised on live PostgreSQL; enum migration defect fixed from real boot logs; production-like upgrade harness and wrapper now exist locally, including sanitation pass verification | run the DB upgrade harness once Docker daemon is available and then decide whether it becomes a release gate |
| Router agent | `router/vectra-controller-agent` | `pilot` | high | pure-Go agent cross-built into real `aarch64_cortex-a53` `.ipk`, published under `api.vectra-pro.net/artifacts/openwrt/...`; live AX3000T install, register, check-in, multiline-UCI import and post-approval steady state confirmed; apply/rollback cycles preserved `enabled=1`, `node=myshunt`, `rescueMode=proxy`; the keep.d/sysupgrade persistence fix was validated live during the `0.1.9-r1` cycle, and the current recovered router runtime has already moved forward to `0.1.10-r1` while staying on the same `router_id` after the repeated real sysupgrade; controller `0.1.11-r1` is now published with end-to-end post-sysupgrade PassWall2 recovery by restoring `dnsmasq-full`/`chinadns-ng`/`v2ray-geo*`/`kmod-nft-*`, refreshing `geoip,geosite` through PassWall2 itself, and then recovering the service with a discrete start-or-restart command; local importer tests now cover preserving PassWall `shunt_rules` match extras including `protocol`, `inbound`, `network`, `source`, `port`, `invert`, but this backfill is not yet published as a controller package; the diagnostics lane is now live-proven too: after closing a latent self-update bug where the running AX3000T process still pointed at `/usr/sbin/vectra-controller-agent (deleted)`, release `0.1.12-r2` added restart-in-`postinst`, was published/installed on the router, and `collect_router_logs` from the operator panel now succeeds for `all/200` and `system/50` with structured snapshots rendered in production UI. A new live triage on `AndreyVK` then exposed a regression in that same `0.1.12-r2` self-update lane on non-AX3000T hardware: controller jobs died at `Configuring vectra-controller-agent.` with `signal: killed`, snapshots stayed on `controllerVersion = unknown`, and system logs showed `context canceled` while the agent was trying to submit the terminal result. A same-night follow-up now also closes that regression in published release `0.1.12-r4`: the self-update lane uses a temporary `/tmp/vectra-skip-postinst-restart` sentinel in addition to the env guard, the init script ignores package-driven `start/reload` when that env is set, and the controller log source now uses a real `grep -E` filter instead of the broken literal pipe pattern. Controller/LuCI `0.1.12-r4` were built, published as direct production artifacts, synced into PostgreSQL metadata, a live `update_controller` on `AndreyVK` completed `success`, later snapshots now stably report `0.1.12-r4` for both controller packages, and `Watch Logs -> Controller` now returns the earlier failure lines instead of an empty snapshot. A later `0.1.12-r7` publish now also closes the cleanup durability hole on the live AX3000T operator path: after `0.1.12-r6` proved self-update still skipped `90_vectra_controller_defaults`, the reordered `postinst` in `r7` was published, synced into PostgreSQL, and browser plus LAN verification confirmed the router now lands on `0.1.12-r7` without recreating `/etc/config/vectra-controller-opkg`. The newest 2026-04-14 local follow-up then isolated a second apply-path bug on `AndreyVK_Sochi`: imported anonymous subscription ids like `@subscribe_list[0]` were rendered into invalid UCI section names during `apply_passwall_config`, so the live router reimport came back with `0 подписок` even though the web draft still showed the preserved secret-backed subscription row; `internal/passwall/apply.go` now sanitizes those ids correctly and local Go regression coverage is green, but this fix is not yet published or deployed. | continue with queued `0.1.11-r1` post-sysupgrade PassWall2 recovery proof on AX3000T without manual SSH, expand attended `collect_router_logs` validation to `passwall` and `dnsmasq`, decide whether to republish public signed feed metadata with the original usign key now that the PostgreSQL-backed direct-artifact lane is proven on `AndreyVK` and cleanup-safe on AX3000T via `0.1.12-r7`, publish the new controller package that fixes imported subscription ids, install it on `AndreyVK_Sochi`, and then re-apply the preserved draft to restore the live subscription without re-entering the secret manually; after that, choose whether the shunt-extras importer fix ships in the same or next controller package |
| LuCI controller package | `router/luci-app-vectra-controller` | `pilot` | high | JS-only LuCI package is manually packed into real `.ipk`, published in the signed feed, avoids legacy Lua lane, and now exposes a Russian local bootstrap/status/rescue surface with controller/passwall service status, selected node label, import/apply summary, stale-safe rescue explanation, and a local `Отключить аварийный режим` action; package `0.1.11-r1` is now published together with the controller release | rerun local LuCI browser smoke on AX3000T against the released `0.1.11-r1` package |
| Deployment stack | `deploy/`, `docker-compose.yml`, `Caddyfile` | `pilot` | high | live VPS deploy on `72.56.14.52`, dual-host TLS issued, compose services healthy, controller/LuCI direct artifacts `0.1.12-r7` are now published to the production artifact volume, production metadata sync is active against PostgreSQL, and repeated live web rebuilds completed without breaking health endpoints; SSH access to the VPS is restored, public smoke-check still returns `307/200/200/200/400`, and this release also proved that partial file uploads on the non-git deploy root must be detected before `docker compose build web`; the latest 2026-04-08 full staging sync via backup + `/tmp` extract + `rsync --delete` removed stale top-level `apps/web/src` files left by earlier partial uploads, the later clean-tar web redeploy for `Rule Manage` parity again rebuilt `vectra-web`, the final single-file `scp` polish for `router-detail-workspace.tsx` kept compose healthy while preserving the standard public smoke `307/200/200/200/400`, the router-deletion follow-up reused the safer slice-based staging sync for `apps/web/packages/*`, the latest controller/LuCI `0.1.12-r2` feed publish via the safe `/tmp` build -> metadata sync -> direct `.ipk` install lane closed the stale `(deleted)` binary issue on AX3000T and enabled live `Watch Logs` success in production, and the newest 2026-04-09 controller hotfix reused that same safe lane again: an isolated temp build with a temporary usign key produced controller/LuCI `0.1.12-r4`, only the `.ipk` files were copied into the public runtime artifact path, the full temp feed was staged under `deploy/runtime/artifacts/tmp/controller-r4-feed` for metadata sync, and the public signed `Packages` / `Packages.sig` in the stable path were intentionally left untouched because the original long-lived usign key is still unavailable. A later 2026-04-10 controller cleanup publish then reused the same direct-artifact lane for `0.1.12-r7`: build in temp, copy only the `.ipk` files into the public runtime path, sync PostgreSQL metadata, and prove on the real AX3000T operator path that the published packages now no longer recreate `/etc/config/vectra-controller-opkg`. The newest enrollment hotfix sequence proved a narrow `scp` lane is still safe for pure in-place web edits: first a backup under `/opt/vectra-prorouter-backups/web-enrollment-20260408-173202` updated the empty-router PassWall release/dnsmasq fix, then a second backup under `/opt/vectra-prorouter-backups/web-enrollment-20260408-175214` updated the tmpfs fix so the live bootstrap route now uses `/root/vectra-bootstrap-work` and selective bundle extraction while keeping the standard public smoke contour intact. A further production hardening pass now also publishes the fully validated bootstrap mirror with `tcping`, optional mirrored packages, and `manifest.json` under the public artifact path, rebuilds `vectra-web` with the new preflight-aware generator, keeps `vectra-web` healthy, and confirms public `200` on `tcping`, `manifest.json`, `/install/ax3000t-bootstrap.sh`, and both `/api/health` endpoints while saving a post-deploy source snapshot under `/opt/vectra-prorouter-backups/web-enrollment-postdeploy-20260408-162224`. A same-day infrastructure hardening pass now also proves safe host-level protection on the live VPS: SSH/PAM/MOTD files were backed up under `/root/vectra-hardening-backups/20260409-092002`, `fail2ban` is active with a live `sshd` jail integrated through `UFW`, and SSH now enforces key-only root access via `/etc/ssh/sshd_config.d/00-vectra-hardening.conf`; the third-party `z4r`, `AntiScanner`, and `distillium/motd` installers were intentionally withheld on this host because they would otherwise rewrite the live firewall/netfilter or `PAM`/`sshd` stack without enough staging safety for the current production control-plane contour. A same-night conservative disk cleanup then reclaimed headroom without touching runtime data: Docker build cache dropped from about `7.9G` to `0B`, stale `/tmp` release residue was removed, `apt` cache shrank to about `44K`, root volume usage moved from `74%` to `53%`, and the standard public smoke still stayed green. The same deployment lane now also includes a live weekly `systemd` hygiene timer on the VPS: `vectra-vps-disk-cleanup.timer` is enabled, one real run already completed with `0/SUCCESS`, the next run is scheduled for `2026-04-12 04:30 UTC` (`07:30 MSK`), and the bundled unit files were cleaned up so `systemd` no longer emits an invalid `Documentation=` warning. A follow-up monitoring check then confirmed that Timeweb Zabbix had already been installed earlier on the host: `zabbix-agent-timeweb` stays active on `10050/tcp`, local `agent.ping` succeeds, and the only missing piece for panel reachability was the firewall, which is now restricted to the three Timeweb Zabbix source IPs from `/etc/zabbix/zabbix_agentd.conf` instead of opening `10050` to the whole internet | keep the documented build -> artifact sync -> metadata sync -> staging sync -> redeploy -> smoke lane as the default, allow the narrow `scp` hotfix lane only for strictly in-place existing files with an explicit backup dir first, add an explicit stale-file parity check under `apps/web/src` and tar-content exclude check before each web release, preserve the safe direct-`.ipk` install fallback for router packages, keep reviewing the weekly cleanup timer after heavy build/deploy bursts so the current Docker/apt/tmp rules remain sufficient for the `38G` root volume, keep the Timeweb monitoring lane tied to the current `Server=` IP list in `zabbix_agentd.conf` rather than a broad `ufw allow 10050/tcp`, decide whether to republish the public signed controller feed metadata with the original usign key now that direct `.ipk` publication is carrying `0.1.12-r7`, then run attended restore/rescue/package-update plus broader web-driven diagnostics-job tests and a controlled cleanup pass for stale test routers; if extra host hardening beyond `fail2ban + SSH key-only root` is still desired, first replay `distillium/motd`, `z4r`, and `AntiScanner` on an isolated VPS twin and diff `nft`, `ufw`, and `PAM` state before touching production again |
| Source mirrors | `passwall2/`, `openwrt-24.10-src/`, `procd-src/` | `reference` | high | local mirrors present by convention; PassWall2 switch-path review confirms only `clear_all_nodes()` forces `@global[0].enabled=0`, while node-deletion paths mainly clear node refs | keep read-only unless explicitly requested |

2026-04-09 addendum:
- The current live AX3000T `1111111111` check shows that controller self-update still has one remaining operator-visible edge case even after the `0.1.12-r4` hotfix. This router now runs a live controller binary that already contains the `VECTRA_SKIP_POSTINST_RESTART` and `/tmp/vectra-skip-postinst-restart` logic, but `/usr/lib/opkg/status` leaves both Vectra packages in `Status: install ... not-installed`, so inventory falls back to `controllerVersion = unknown` even while the router keeps polling successfully. This shifts the next review from “does the new binary land at all?” to “how do we recover or at least surface half-installed opkg state cleanly on live routers?”
- A later same-night LAN hotfix on that AX3000T proves the immediate router-side workaround: after replacing only `render-config.sh` with the local `.control` fallback and restarting the agent, the router's own runtime files report `controllerVersion = 0.1.12-r4` again. A 2026-04-10 VPS-side recheck then confirmed the control plane is ingesting that state too: production `api/router/check-in` is returning `200`, Caddy access logs show successful check-ins for `1111111111`, and PostgreSQL already stores the latest snapshot with `controllerVersion = 0.1.12-r4`.
- A later 2026-04-10 follow-up then closes the operator-visible problem at the package level too: controller/LuCI `0.1.12-r5` were built, published into the production direct-artifact lane, synced into PostgreSQL metadata, and manually reinstalled on the live AX3000T `1111111111` via the same public `.ipk` URLs. That reinstall restored both Vectra packages to `Status: install user installed`, `/var/run/vectra-controller/{config,status}.json` now reports `controllerVersion = 0.1.12-r5`, and production PostgreSQL stores the latest inventory snapshot for `bdfdb919-5e06-4344-ad8b-67a16f3b6fcf` as `0.1.12-r5`.
- A final same-day attended production UI check now also proves the actual operator button path: `App Update -> Controller -> Переустановить 0.1.12-r5` on the live router page created job `e4e664d8-39f8-4076-ba0c-10dc30e26bfd`, completed `succeeded`, returned the page to `очередь задач: 0`, and kept the router on `Controller 0.1.12-r5`. A follow-up read-only LAN check confirmed both packages still sit in `Status: install user installed`; the remaining cleanup issue is only the recreated shadow conffile `/etc/config/vectra-controller-opkg`.
- A later same-night cleanup pass then removed that shadow conffile on the live AX3000T after first backing it up under `/root/vectra-cleanup-20260410-024806/`, with no regression in package state or controller runtime. The local source tree now also carries the next agent-side fix for this class of issue: package-owned UCI defaults no longer persist auto-discovered board/version fields, render-time inventory now prefers live system facts over UCI shadow state, and `uci-defaults` removes stale `vectra-controller-opkg`; this still needs a published controller package before the fix becomes durable across future reinstalls.
- A later 2026-04-10 publish then closes that durability gap too: `0.1.12-r6` reproduced the real operator-path miss, showing self-update still skipped `90_vectra_controller_defaults` before exit; published `0.1.12-r7` reordered `postinst`, production metadata was resynced, and the live AX3000T now reaches `controllerVersion = 0.1.12-r7` with no regenerated `/etc/config/vectra-controller-opkg` and only stable operator-managed fields left in `/etc/config/vectra-controller`.
- A same-night deployment recovery then hardens the live VPS against the transient Docker-DNS failure that showed up during this window: both public `/api/health` endpoints briefly degraded to `503/000`, `caddy` logged `lookup web: i/o timeout`, `web` logged `EAI_AGAIN postgres`, and `dockerd` got stuck in `activating` until a controlled VPS reboot. The recovered production `docker-compose.yml` now pins `caddy`, `web`, `postgres`, and `db-backup` onto `vectra_internal` with static IPs plus `extra_hosts` for the hot `web/postgres` names; after the recreate, six consecutive external health rounds returned `200/200` and fresh logs no longer showed those DNS/upstream errors.
- A later local 2026-04-10 enrollment hardening pass now also upgrades the unreleased AX3000T bootstrap generator from “install packages one by one” to a real storage-aware planner for low-overlay routers: the web code classifies the router as `fresh install` / `upgrade existing PassWall stack` / `repair drifted managed packages`, models staging and `/overlay` budgets from explicit package metadata, skips already-current packages, blocks reclaim when `opkg whatdepends` reveals external dependents, and aborts before the first `opkg remove/install` with a blocking package name when the full target stack cannot fit. The same local slice extends `Sync-PasswallBootstrapMirror.ps1` so `manifest.json` carries per-package `version`, download size, and installed size, and the locally generated bootstrap script now passes `bash -n` after green `@vectra/web` `typecheck/test/build`.
- A 2026-04-13 bootstrap-baseline hotfix now also corrects a user-visible runtime regression in the published AX3000T install asset: `apps/web/public/install/ax3000t-passwall2-baseline.uci` no longer ships `default_fakedns '1'` or `direct_fakedns '1'`, so freshly bootstrapped routers do not start with FakeDNS forced on for `Default` and `Direct`; local `@vectra/web` `build`, `typecheck`, and `test --run` are green, and the enrollment test file now asserts both values stay pinned to `'0'`.

2026-04-14 addendum:
- Production `apps/web` is now redeployed with both the bounded router-terminal surface and the later UX-clarity shell slice: the VPS build completed after a safe local warm-up/tag of the Node base image, `drizzle-kit migrate` applied successfully on startup, and production PostgreSQL now includes `run_terminal_command` in `vectra_job_type`.
- Production direct artifacts and DB metadata already advertise `vectra-controller-agent` plus `luci-app-vectra-controller` `0.1.12-r10`, and the local production router `1111111111` now shows that version in the live shell after a real `App Update -> Controller` reinstall while still serving as the first end-to-end terminal proof point through successful `run_terminal_command` job `d54da8ba-d405-427b-9a3c-c00a8762cbea`.
- The UX clarification slice is no longer local-only: production browser walkthrough now covers `Парк`, `Установка`, `Обновления`, and router `1111111111`, with grouped router actions, clearer onboarding/rollout copy, clean desktop/mobile layout, and no console issues during the smoke pass.
- A later 2026-04-14 production diagnostics follow-up now closes the remaining Telegram visibility gap too: router inventory contracts accept optional `telegramReachability`, the controller-agent collector probes `telegram.org`, `web.telegram.org`, `t.me`, and `api.telegram.org`, controller/LuCI `0.1.12-r10` were installed on safe router `1111111111`, and production snapshots first showed Telegram `partial` and then `reachable` `4/4`. Router detail rendered the new Telegram block correctly immediately; `Парк` initially still showed `нет данных` because fleet monitoring dropped `telegramReachability`, but that server mapping bug was fixed in the web layer, local `@vectra/web` `test --run`, `build`, and post-build `typecheck` stayed green, production was redeployed, and browser smoke now confirms the real Telegram status in both router detail and fleet for the updated router. Older routers still need the Telegram-capable controller release before they can report anything beyond `нет данных`.
- A later same-day mobile-first shell polish is now also production-proven rather than local-only: the redeployed web build shortens mobile headers and workflow copy, collapses non-critical router-detail help into disclosures, switches `Обновления` to one heavy editor at a time on phones, densifies `Черновики`/`Установка`, and keeps `Парк` router cards short on mobile by moving rescue/version diagnostics behind a disclosure. Local `@vectra/web` `typecheck/test/build` stayed green, production `/api/health` returned `200` after redeploy, and live mobile smoke covered `Парк`, `Черновики`, `Обновления`, `Установка`, `Восстановление`, and router detail.

2026-04-15 addendum:
- Local operator tooling now also includes authenticated GitHub CLI access: `gh 2.89.0` is installed on the workstation, `github.com` login for `Waizor1` is active with repo/workflow-capable scopes, `gh repo view` confirms `ADMIN` on private `Waizor1/Vectra-ProRouter`, and HTTPS `git ls-remote origin main` succeeds non-interactively after `gh auth setup-git`.
- The same local-access hygiene pass now also re-audits SSH inventory: exactly one workstation SSH alias is currently configured and re-verified for live server access, a separate GitHub SSH key is confirmed independently, and the raw mapping stays confined to the gitignored local-access note instead of tracked project memory.
- A later same-day local web cleanup pass now also removes more duplicated framing from the non-fleet operator shell while preserving the user-liked fleet/router list direction: `apps/web` simplifies `operator-shell-header.tsx`, drops repeated workflow-map usage on the touched utility pages, makes `Установка` default to command + next steps with deeper detail behind disclosures, tightens `Обновления` around the real baseline/rollout workspace, and narrows `Черновики` / `Восстановление` into denser utility surfaces. Local `@vectra/web` LSP diagnostics plus `typecheck`, `test --run`, and `build` are green; this slice is still local-only and not yet production-walked.
- A final same-day local fleet/login hierarchy follow-up now also targets the original smartphone/tablet QA complaint directly instead of only the non-fleet shell: `apps/web` keeps `operator-shell-header.tsx` compact through tablet widths on `/login` and `/fleet`, removes the redundant fleet `PageHeader` below `lg`, reorders the mobile fleet top around filter -> alerts -> list, and fixes a real `?q=` sync regression in `fleet-monitoring-workspace.tsx` so shell search results stay aligned with the URL during client navigation. Local `@vectra/web` LSP diagnostics plus `test --run`, `build`, `next typegen`, and post-typegen `typecheck` are green; direct local browser QA also confirmed the shorter login shell on phone and the preserved `/fleet` command route behavior for unauthenticated sessions.
- A later same-day local visual redesign pass now consolidates that work into one coherent operator/admin language for `apps/web` instead of a set of page-local cleanups: the shared shell/tokens/layout grammar now lives in `src/styles/globals.css`, `src/app/layout.tsx`, and the updated primitive layer (`panel.tsx`, `page-header.tsx`, `action-strip.tsx`, `data-table.tsx`, `status-tile.tsx`, `tab-bar.tsx`), `operator-shell-header.tsx` is reworked as the persistent shell anchor, `router-detail-workspace.tsx` is visually reordered around current state and save/apply actions before deep editing, `fleet-monitoring-workspace.tsx` stays table-first with context moved into secondary surfaces, and the utility routes (`Черновики`, `Обновления`, `Установка`, `Восстановление`) now reuse the same shell code. Route behavior, query params, disabled tabs, mutations, and backend contracts remain unchanged; local LSP diagnostics are clean on all touched TS/TSX files, but this redesign is still local-only and has not yet been production-smoked.
- A later 2026-04-16 production release now puts that redesign-reset line live through the guarded non-git VPS lane: the committed `apps/web` shell/fleet/router-detail refresh plus `deploy-web-release.sh` guardrails were pushed to `origin/main`, a fresh PostgreSQL backup was taken before sync, the release slice was staged under `/tmp` and synced into `/opt/vectra-prorouter` through the new allowlist script, `docker compose --env-file .env build web && up -d web` returned the stack to healthy state, and post-deploy proof covered the public smoke contour `307/200/200/200/400` plus authenticated operator checks `303 /api/operator/login`, `200 /fleet`, and `200` on the live router detail page for `bdfdb919-5e06-4344-ad8b-67a16f3b6fcf`.
- A later same-day local follow-up now sharpens only the router-detail trust layer for the next production candidate instead of reopening the broader redesign: `apps/web/src/components/router-detail-workspace.tsx` now spells out unsaved-vs-saved revision state, clarifies that apply always runs from a saved draft, promotes the next safe action ahead of diagnostics and danger actions, adds explicit blocked-reason copy for save/apply states, and makes the delete panel read as a serious panel-data deletion rather than a generic destructive button. This Phase 1A slice stays local-only, preserves existing backend/query/tab/apply semantics, and currently has clean LSP diagnostics on the touched file; broader typecheck/test/build and browser walkthrough still remain the next verification step.
- A later same-day local Phase 1B fleet follow-up now strengthens only the fleet trust/notification layer for the next production candidate instead of widening scope into router detail or rollout behavior: `apps/web/src/components/fleet-monitoring-workspace.tsx` now spells out whether a router row is truly live, merely aging, or only last-known/offline, the top rail now makes push vs in-tab-only notification limits explicit in-place, and `apps/web/src/components/router-card.tsx` mirrors the same trust language on compact/mobile cards. This slice stays local-only, preserves backend contracts, polling cadence, route semantics, and search flow, and currently has clean LSP diagnostics on both touched files; broader typecheck/test/build and browser walkthrough still remain the next verification step.
- A later 2026-04-16 local Phase 3 utility-surface follow-up now hardens the dense non-fleet workspaces in `apps/web` without widening scope into backend or system behavior: `Обновления` now enters through operator order (edit baseline → choose rollout window → consult release channels), `Черновики` now reads as an explicit reserve JSON lane with stronger save-vs-apply hierarchy, `Установка` now starts with the real bootstrap/check-in/import path before deeper technical material, and `Восстановление` now puts current direct-mode/incidents ahead of policy reference. The slice stays local-only, preserves existing routes, queries, mutations, rollout/apply/rescue mechanics, and has clean LSP diagnostics on all six touched files; broader browser/runtime proof still remains the next validation step if this candidate moves forward.
- A later same-day local Phase 4 editorial follow-up now closes the remaining apps/web UX-hardening gaps without widening scope beyond copy: the already-touched operator surfaces now use more deliberate and consistent trust-language across shell, fleet, router detail, updates, drafts, enrollment, rescue, and route-state messaging, awkward mixed Russian/English UI phrases were removed where they were not product truth, and operator help text now reads more explicitly as panel-state guidance instead of internal implementation jargon. The slice stays local-only, preserves backend behavior and route semantics, and has clean LSP diagnostics on all fifteen touched files.
- A 2026-04-17 repo-tooling follow-up now also initializes AST navigation for the workspace itself instead of relying on per-session manual setup: `scripts/ast-index.sh` is the canonical repo-side wrapper, root `pnpm` now exposes `ast:rebuild`, `ast:update`, and `ast:stats`, and a local rebuild verified live symbol search over the JS/TS control-plane contour. This improves everyday navigation for `apps/web` and shared packages without changing runtime behavior, while Lua/shell/OpenWrt mirror work still stays on plain-text search.
- A later same-day local-plus-live bootstrap fix now also closes the remaining Xray update gap in the web-generated install script for small-storage Filogic routers: `apps/web/src/app/enrollment/install-presets.ts` now mirrors LuCI `App Update` by converting GitHub asset `data.size` to KiB before calling PassWall2 `api.to_download()`, and the generated shell now runs a dedicated post-install Xray convergence step through the built-in updater across every bootstrap lane instead of only the reuse path. Local `@vectra/web` `test --run`, `typecheck`, and generated-script `sh -n` are green, and attended LAN proof on live test router `OpenWrt` (`192.168.99.1`, `cudy,wr3000e-v1`, OpenWrt `24.10.5`) confirmed both the bug and the fix end-to-end: the old direct API flow failed with false `/tmp not enough space`, the corrected flow completed successfully, and runtime `xray version` moved from `26.2.6` to `26.4.15` while `opkg` stayed on package record `25.10.15-r1`, matching the known PassWall component-drift model.
- A final same-day live bootstrap run now also proves the current generated script itself on real hardware, not only the isolated Xray updater path: the locally generated shell was executed end-to-end over SSH on the same test router `OpenWrt` (`192.168.99.1`, `cudy,wr3000e-v1`, OpenWrt `24.10.5`) and completed successfully in `upgrade existing PassWall stack` / reuse-lane mode. The run preserved the existing `subscribe_list`, kept imported nodes, refreshed `xray` and `geoview` through the built-in PassWall updater, rebound all four `myshunt` slots by remark, left `passwall2` and `vectra-controller` running, and kept the router on `passwall_node=myshunt`; this is still reuse-lane proof on non-AX3000T hardware, so the separate fresh-install and clean-router AX3000T validation remains open.
- A later same-day guarded production hotfix release now also closes that remaining gap on the public install lane: after a first failed VPS build revealed macOS AppleDouble `._*` contamination in the release tar, a clean release slice was rebuilt from a minimal patched worktree, synced again through `deploy/scripts/deploy-web-release.sh`, and redeployed successfully. Production `vectra-web` returned to `healthy`, `https://router.vectra-pro.net/api/health` returned `{"ok":true}`, and the live `/install/ax3000t-bootstrap.sh` route now serves the expected script with `download_size_kb`, `refresh_bootstrap_xray_runtime_via_builtin_updater()`, and the explicit Xray runtime convergence log, so the bootstrap hotfix is no longer local-only.
- A later same-day public bootstrap hotfix now also removes the next real install blocker reported from a live OpenWrt router after that Xray release: the generated shell no longer aborts storage-aware preflight just because feed prerequisite `libc` lacks `Installed-Size` metadata in `opkg info`. `apps/web/src/app/enrollment/install-presets.ts` now separates plain feed-availability checks from storage-budget parsing, limiting the stricter path to the actual runtime packages that the bootstrap sizes and installs. Production revalidation is green on the panel contour: `vectra-web` remains `healthy`, `https://router.vectra-pro.net/api/health` still returns `{"ok":true}`, and the live `/install/ax3000t-bootstrap.sh` now exposes `require_feed_packages_with_storage`, `feed_package_storage_budget_bytes`, and the new `storage budget` error path instead of the earlier `Installed-Size ... libc` false-stop. Real router rerun after the public hotfix is still pending, so this is now production-served proof, not yet a second post-fix hardware proof.
- A final same-day guarded production bootstrap follow-up now also removes the next immediate false-stop from that same user lane: the live public shell had still been serving an older `require_vectra_package()` implementation that aborted on missing `Installed-Size` for `luci-app-vectra-controller`, despite the source tree already having the newer storage-budget-aware controller path. The guarded VPS redeploy synced only the patched release slice, rebuilt `vectra-web`, and returned the service to `healthy`; public smoke now confirms `https://router.vectra-pro.net/api/health` is back to `{"ok":true}` and the live `/install/ax3000t-bootstrap.sh` contains both `require_feed_packages_with_storage` and `vectra_package_storage_budget_bytes()` while no longer exposing the old `Не удалось определить Installed-Size для пакета Vectra ...` branch. The remaining stage-gate is now user-side or attended hardware rerun, not another known panel-side preflight bug.
- A later same-day guarded bootstrap follow-up now also removes the next field-only false-stop in that same install lane: some routers print `opkg whatdepends` with human header lines such as `What depends on ...` and `Root set:`, and the public shell had been parsing those headers as if they were real package names. That produced the false blocker `Автоматический reclaim для xray-core запрещён: внешние зависимости -> Root What` even when no external package actually blocked reclaim. The generated shell now routes both external-dependent detection and PassWall-app dependency detection through a dedicated `whatdepends_packages()` helper that accepts only indented package rows, and the guarded production redeploy is confirmed live: `vectra-web` returned to `healthy`, `https://router.vectra-pro.net/api/health` again returns `{"ok":true}`, and the public `/install/ax3000t-bootstrap.sh` now exposes `whatdepends_packages()` plus the filtered dependency checks. This converts the install lane from “two known metadata false-stops fixed” to “three known panel-side preflight false-stops fixed”; the remaining proof gap is now the next user-side rerun on the affected router.
- A final same-day guarded bootstrap follow-up now also removes the next production-lag blocker in that same lane: the live public shell had still been serving an older path that tried to full-refresh managed packages on `/overlay` before reusing an already working PassWall stack, which is why user-side manual `App Update` could succeed for `xray` while the public bootstrap still failed on `xray-core` overlay budget. The guarded production redeploy now restores the intended reuse behavior on the live route through `passwall_runtime_ready_for_reuse()` and `REUSE_EXISTING_PASSWALL_STACK`, skips heavy managed refresh in that lane, and then continues into the existing built-in `App Update` step for `xray`. Production proof is live again: `vectra-web` is `healthy`, `https://router.vectra-pro.net/api/health` returns `{"ok":true}`, and the public `/install/ax3000t-bootstrap.sh` now exposes the reuse-lane markers instead of only the earlier full-refresh contour. This converts the current install lane from “three known false-stops fixed” to “three false-stops plus one route-drift overlay mismatch fixed”; the remaining gate is now the user-side rerun on the affected router.

2026-04-17 update-lane addendum:
- A new local PassWall/Xray update slice now spans all three layers that matter for the operator path: `apps/web` queues explicit artifact-driven `update_passwall_packages` jobs with managed-stack semantics and per-package target/source metadata, `packages/contracts` models per-package results including `pathUsed` and `driftDetected`, and `router/vectra-controller-agent` now has a storage-aware PassWall executor with adaptive runtime fallback for low-overlay devices.
- Local proof is strong on the web/publishing contour: targeted `@vectra/web` tests, `typecheck`, and `lint` are green, and a dry-run `sync-artifact-metadata.mjs` against a synthetic mirror directory now emits correct `passwall_package` plus `passwall_bundle` records for `26.4.10-1` with proper `aarch64_cortex-a53` architecture metadata.
- The remaining gap is agent/runtime proof, not design ambiguity: `go` is unavailable in this workspace, so the new controller-agent lane has only static review here. The next stage-gate is an attended `Update Xray` followed by `Update PassWall2` on one safe `pilot/certified` Filogic router after publishing and syncing the new PassWall mirror metadata, plus one low-overlay repetition to verify adaptive fallback/drift reporting under pressure.

2026-04-18 VPS hygiene addendum:
- A live production check on VPS `72.56.14.52` shows the latest “memory is full” report was actually root-disk pressure, not RAM exhaustion: memory stayed normal at about `741Mi` used with `1.2Gi` available and only `145Mi` swap in use, while `/dev/sda1` had climbed to `91%` used with `3.6G` free. The dominant new consumer was `/opt/vectra-prorouter-backups` at `13G`, where repeated `web-release-*` rollback directories had been copying `deploy/runtime/postgres` into every backup payload. Attended cleanup safely pruned the older large `web-release-*` plus stale `web-deploy-ready-*` directories, shrinking the backup root to `1.9G` and returning `/dev/sda1` to `63%` used with about `15G` free; `vectra-web`, `vectra-caddy`, and `vectra-postgres` all remained healthy, and the public smoke contour stayed `307/200/200/200/400`.
- The deploy guardrail is now tightened in code and on the live host: `deploy/scripts/deploy-web-release.sh` backs up `deploy/` through an `rsync --exclude runtime` path instead of a raw recursive copy, and it now hard-fails if `deploy/runtime` appears inside a rollback backup. The same deep audit also shows the remaining heavy but non-urgent footprint is mostly intentional build inventory rather than another leak: `/opt` is still about `12G`, led by three extracted OpenWrt SDK trees (`2.1G`, `3.3G`, `3.3G`) plus their three source archives (`~623M`), while `/var/lib/docker` is about `2.1G`, `/var/log` about `463M`, `/root` about `476M`, and stale `/tmp` Vectra release payloads have already been cleaned from about `286M` down to `72K`. Local temp-dir verification is green, so the next review point is no longer “can we recover space once” but “do new rollback directories stay small after the next web release window, and do we really want to keep all SDK versions on this VPS”.
- A later same-day local web follow-up now also closes the current smartphone overflow issue in the operator shell without broadening the design scope: `apps/web` no longer globally clips horizontal overflow, fleet now prefers router cards on phones while preserving the dense table for `lg+`, the known rollout note inputs no longer force small screens wider than the viewport, and router detail uses only minimal `min-w-0` containment instead of another structural rewrite. Local verification is green across `next typegen`, `typecheck`, and `build`; a real `390x844` browser check against the correct local runtime (`127.0.0.1:3300`) confirms no page-level horizontal overflow on `/login`, static assets load successfully, and protected `/fleet` still redirects to `/login`. The remaining local QA gap is still an authenticated phone walkthrough of `Парк`, router detail, and `Обновления`.
- A later same-day authenticated local QA pass now materially narrows that remaining gap with route-by-route phone evidence on a fresh runtime at `127.0.0.1:3400`: `/fleet`, router detail (`basic-settings/main`, `node-list`, `node-subscribe`, `app-update`, `rule-manage`, `geo-view`), `/updates`, `/drafts`, `/rescue`, `/enrollment`, and `/install` were all rechecked at `390x844` and measured without page-level horizontal overflow. The pass also found one additional real blocker rather than only reconfirming the earlier shell fix: `/enrollment` still expanded past the viewport because bootstrap/install code and address surfaces leaked intrinsic width through local containers. `apps/web/src/app/enrollment/page.tsx` and shared `apps/web/src/components/panel.tsx` were tightened just enough to contain those blocks, and after a fresh `next typegen`, `typecheck`, `build`, and QA-runtime restart the route also measured clean. The remaining local QA limits are now interaction-depth gaps rather than raw layout containment: destructive actions were not executed, and disabled/gated surfaces (`Other Settings`, `Access Control`, `Server-Side`, local-fixture `Watch Logs`) still remain outside the proved path.
- A later same-session safe mobile action pass now confirms that the operator UI is not only viewable but also interactable on a phone for the checked non-destructive paths: on the same `127.0.0.1:3400` authenticated runtime at `390x844`, router detail accepted note entry, `Сохранить только в панели` completed with a real `draft.save` `200 OK`, core router tabs remained tappable without blocked zones, `/updates` reached live working surfaces, and `/enrollment` disclosures plus command blocks stayed reachable after expansion. The remaining limits are now explicitly operational rather than spatial: destructive or stateful actions like `save+apply`, router deletion, and bulk version-update execution were intentionally not run, and `/updates` still deserves one follow-up pass that proves every subworkspace tab through stable element handles instead of rerender-sensitive snapshot refs.

2026-04-18 update-delivery addendum:
- The local PassWall update-delivery slice is now stronger than the earlier “artifact-driven but not yet compile-proven” state: controller-agent `go test ./...` is green, the web/control-plane side is green on targeted Vitest plus `typecheck` and `lint`, and the UI now distinguishes managed-stack, app-package, package-record, and runtime truth instead of one ambiguous version label.
- The remaining blocker is now deployment/runtime proof rather than local correctness. The test router `192.168.99.1` still proves the earlier backend-delivery failure via `2026-04-18` `check-in` log lines with server-side `No space left on device`, while a fresh VPS check now shows root disk back down to about `63%` used. This moves the next stage gate from “fix obvious disk pressure” to “deploy the stronger write-path health probe and re-run attended `Update Xray` / `Update PassWall2` from the operator UI”.
- A later same-session production rollout now closes the server-side part of that gate for real. `router.vectra-pro.net/api/health` returns the stronger payload with `checks.browserPushMonitor`, `checks.dbRead`, and `checks.dbWriteProbe`, the standard public smoke contour is back at `307/200/200/200/400`, and Caddy logs again show fresh `POST /api/router/check-in -> 200` instead of the earlier storage-backed write failures.
- The controller publish lane is also advanced on production beyond the older `0.1.12-r10` state: controller/LuCI `0.1.12-r11` were built in an isolated `/tmp` SDK workspace on the VPS, copied into the public direct-artifact path, and inserted into PostgreSQL metadata without rotating the stale signed feed pointer.
- Read-only follow-up on the current test router `192.168.99.1` now separates delivery from package rollout more honestly: `/var/run/vectra-controller/status.json` reports `last_check_in_at = 2026-04-17T23:26:01Z`, which confirms server-side delivery resumed for this device after the rollout, but the router itself is still on controller `0.1.11-r1` and still needs an attended controller/Xray/PassWall update pass from the operator UI.
- A later same-session attended operator-UI pass now closes part of that proof on the real test router instead of leaving it at read-only facts. `Update Controller` from the production panel moved both installed controller packages and the reported controller version on `192.168.99.1` to `0.1.12-r11`; however, router detail still surfaces a false controller failure hint with `signal: killed` even though the installed package state is already converged.
- The same attended pass also exposed and then closed a production rollout gap in the PassWall lane itself. Production initially had no `passwall_package` / `passwall_bundle` rows in `vectra_artifact` and only the older `bootstrap/passwall2/26.4.5-1` mirror on disk, so `Update Xray` failed before queueing with a server-side validation error on empty `sha256`. After publishing `bootstrap/passwall2/26.4.10-1/aarch64_cortex-a53` to the VPS runtime artifact path and running `sync-artifact-metadata.mjs --apply`, `update.artifacts` started returning the managed-stack bundle plus per-package metadata and the panel could show the new target semantics honestly.
- Standalone `Update Xray` is now live-proven from the production panel on `192.168.99.1`: the queued job carried `strategy = xray-built-in-first`, `targetReleaseTag = 26.4.10-1`, and package target `26.3.27-r1`, while the final router-detail result came back `success` with per-package `status = runtime-only-converged`, `pathUsed = not-needed`, and the expected runtime/package drift because the router was already on runtime `Xray 26.4.15`.
- Full `Update PassWall2` still does not have a clean success or storage-block proof on this router. The panel now queues the correct managed-stack payload (`targetVersion = 26.4.10-1`, `packageTargetVersion = 26.4.10-r1`), but the live result currently fails with `opkg update: exit status 7`; a same-window read-only SSH probe on the router showed about `14.9M` free on `/overlay`, about `111.6M` on `/tmp`, and a hanging/slow `opkg update` feed refresh rather than an immediate storage-preflight rejection.
- A later same-session hotfix and attended rerun now close the stale parts of that note on the real test router instead of only narrowing them. The controller had to be bootstrapped once by direct `.ipk` install because the older `0.1.12-r11` agent still used unconditional `opkg update` in its self-update lane; after that manual bridge, fresh inventory and the production panel both report controller/LuCI `0.1.12-r13`, and router detail no longer surfaces the stale controller failure once the installed version is already converged.
- The production web hotfix now also closes the managed-stack queueing bug that kept external feed refresh in scope even when recovery packages were already present on the router. `update.ts` now omits unpinned recovery deps from the queued `packageList` when inventory already shows `dnsmasq-full` and the required `kmod-nft-*` packages installed, so the live `Update PassWall2` rerun on `192.168.99.1` queued only pinned artifacts (`xray-core`, `v2ray-geoip`, `v2ray-geosite`, `geoview`, `chinadns-ng`, `tcping`, `luci-app-passwall2`) and did not spawn `opkg update` or `wget Packages.gz` on the router.
- The managed-stack lane is now live-proven as honest even though it is still not fully converged on this low-overlay router. The rerun updated `luci-app-passwall2` to `26.4.10-r1`, preserved `xray` and `geoview` as runtime-current with explicit `runtime-only-converged` results, and surfaced the remaining blockers concretely: `v2ray-geoip` was skipped for overlay space, `v2ray-geosite` refused a downgrade-like package version, and the panel summary now names the storage-block reason instead of the old generic `opkg update` failure.
- A later same-session controller self-update follow-up now pushes the live operator proof one step further: production web defaults controller updates to the terminal self-update lane for terminal-capable agents, and an attended `Переустановить 0.1.12-r13` on test router `192.168.99.1` now shows the full panel-driven contour `run_terminal_command purpose=controller-self-update -> accepted -> running -> success` with both controller `.ipk` packages reinstalled and fresh post-restart check-ins. The remaining controller-update gap is no longer delivery or the need for manual bridge steps on current agents, but only smaller operator-facing polish around how partial warnings such as conffile carry-forward should be summarized.
- A later same-session fleet-monitoring hotfix now closes a separate panel-only regression on the production `/fleet` surface itself. Fresh PostgreSQL snapshots still carried `controller_version`, `payload.telegramReachability`, and PassWall package truth, but the optimized raw-SQL loader in `apps/web/src/server/vectra/fleet-monitoring-data.ts` selected `snake_case` snapshot fields and then read them as `camelCase`, so every fleet card lost its latest snapshot and fell back to `Telegram нет данных / Controller Не удалось определить / PassWall2 неизвестно`. Local proof is green on targeted Vitest plus `build`, and live operator proof is now explicit too: after a narrow hot patch of the current Next bundle on the VPS, a `vectra-web` restart, and an emergency `docker commit` back into local `vectra-web:latest`, authenticated `/fleet` browser snapshots again show real values, including the test router card with `Telegram доступна`, controller `0.1.12-r13`, and PassWall2 `26.4.10-r1`.
- A final same-day PassWall freshness follow-up now also turns the artifact lane itself from “manually current today” into “auto-rechecked on the VPS”. Live read-only SSH confirmed the production artifact root keeps both historical `bootstrap/passwall2/26.4.5-1` and current `26.4.10-1`, while production `vectra_artifact` already resolves the latest upstream tag on `2026-04-18`. The repo now carries `deploy/scripts/refresh-passwall-mirror.py` plus `vectra-passwall-mirror-refresh.{service,timer}`, the new units were installed on the live VPS, and the first manual service run finished `0/SUCCESS` while correctly treating the current upstream tag as a no-op refresh. This shifts the next freshness review from “did someone remember to upload the new mirror?” to “does the scheduled VPS check still keep DB metadata and mirrored files aligned when the next upstream PassWall tag appears?”.
- A later same-session VPS hygiene follow-up now also closes the “we keep filling the disk and only notice it late” gap with an installed guard instead of another manual note. Live root usage is currently stable around `69%`, but the same repeatable report still shows the real pressure mix: about `3.2G` Docker build cache, about `2.1G` under `/var/lib/docker`, about `2.2G` rollback backups, about `304M` in `/tmp`, about `485M` in `/var/log`, and about `8.8G` across the retained OpenWrt SDK trees plus their archives under `/opt`. The repo now carries `deploy/scripts/vps-disk-guard.sh` plus `vectra-vps-disk-guard.{service,timer}`, those units are installed and enabled on the live VPS, and the policy is intentionally safety-split: conservative cleanup starts at `75%`, while aged rollback backups are still preserved until a separate `82%` backup-prune threshold is crossed. Live proof for that safer branch exists already: a forced cleanup-path dry-run logged the hotspot report and cleanup actions but still explicitly preserved rollback backups at current `69%`, and the first default-threshold service run ended `0/SUCCESS` in report-only mode. This changes the disk lane from “weekly cleanup plus occasional SSH archaeology” to “6-hour hotspot reports with threshold-driven cleanup and a separately gated rollback-backup retention path”.
- A later same-evening local router-detail navigation follow-up now closes another operator-facing paper cut without widening the architecture: `apps/web/src/components/router-detail-workspace.tsx` now keeps PassWall `tab/section` switching (`DNS`, `App Update`, and peers) inside local tab state while syncing the URL through route-local `history.replaceState(...)`, so the workspace swaps content without the old page-level jump back to the top. Local proof for this narrow fix is green on targeted `router-console` Vitest plus file-level ESLint, and the later `2026-04-20` cleanup also closes the old unrelated `tests/mobile-action-qa.spec.js` tail by switching the spec to `@playwright/test`, adding explicit JSDoc typing, declaring the devDependency, and making `typecheck` self-generate Next route types before `tsc`.

2026-04-19 local addendum:
- Web control plane now carries an explicit honest live-state trust model across `fleet`, `router detail`, `draft.editorSurface`, and import/apply copy. The new read-model uses only existing snapshot digests plus live-import revisions to decide whether deep PassWall sections are `live-import`, merely `authoritative`, `stale-authoritative`, or `inventory-only`; approved routers with fresh digest drift now surface `re-import needed` instead of falsely implying that the panel knows current `ShuntRules` from heartbeat alone.
- Local verification for this trust refactor is green on targeted `config-trust`, `editor`, `fleet-monitoring`, `fleet-monitoring-data`, and `presentation` Vitest suites together with `pnpm --filter @vectra/web lint` and `pnpm --filter @vectra/web build`. The earlier unrelated `tests/mobile-action-qa.spec.js` Playwright/typecheck debt is now closed too: cold `pnpm typecheck` from the repo root regenerates `.next/types` through `next typegen` and completes successfully.
- The local operator toolchain now also has a working live panel CLI contour instead of relying only on browser sessions or ad hoc curls. `scripts/VectraPanelCli.sh` now wraps a tracked `apps/web/scripts/vectra-panel-cli.mjs` helper that can bootstrap operator credentials from the already-proven `ssh vectra-prod` access path, perform the real `/api/operator/login`, cache only a gitignored session cookie, and drive read/write `tRPC` procedures for `fleet`, `router detail`, `logs`, `terminal`, `update`, and `rescue`. Live smoke on 2026-04-19 is green for `status`, `login`, `fleet overview`, `fleet list`, `router show OpenWrt`, `call fleet.overview`, `logs history OpenWrt`, and `terminal history OpenWrt`.
- A same-session coverage follow-up now turns that CLI from “useful subset” into “explicit operator API map plus split auth model”. `catalog` prints the real current `tRPC` surface, first-class wrappers now also cover `draft`, `notifications`, `fleet approve-import/request-reimport/delete`, and the router-facing API has its own `router-api` contour with live `health` proof against `https://api.vectra-pro.net`; the important boundary is now documented in both code and notes too: operator cookie access covers protected `tRPC`, but router control endpoints still require a real router id/token pair and are not implicitly unlocked by panel auth.
- A later same-day live QA tail now closes the main uncertainty around the new drift UX more honestly. Production router-detail and fleet data confirm the exact operator-facing lanes are working on real routers: `OpenWrt` still shows router-side pending import review with a single exact `log.level error -> warning` diff, `1111111111` still shows panel-side saved-draft-pending-apply with exact itemized changes, and additional out-of-sync routers (`VagrandRouter`, `Vasily_Filicity`) also currently surface fresh exact imports rather than digest-only stale-authoritative fallback. The remaining gap is therefore not “does the UI render drift honestly?” but “can the current controller/check-in timing still produce a live `re-import needed` digest-only state before a matching imported revision arrives?”
- A later same-evening local UX-structure follow-up now narrows the main operator reading path further without changing backend or workflow behavior: router detail, fleet table, and compact router cards in `apps/web` now present the same three-layer order — what is happening now on the router, what is saved in the panel, and how much the panel trusts that comparison and why. The shared trust wording in `router-config-trust.ts` and fleet presentation copy was rewritten into plainer Russian panel guidance instead of internal trust-model jargon; local proof is clean LSP diagnostics on all touched files plus green `@vectra/web` `next typegen` and targeted `vitest --run presentation`, and the later `2026-04-20` cleanup now also leaves both cold repo-wide `typecheck` and `@vectra/web build` green instead of failing on the old Playwright/typegen tail.
- A later same-night local subscription/runtime follow-up now closes the next operator truth gap in `apps/web` without adding DB schema or treating any single router as a global source of truth. `draft-selection.ts` keeps historical `operator_draft(status=applied)` revisions only in history, but no longer lets them become the editable workspace base once that router already has a newer confirmed live import; `subscription-runtime.ts` and `subscription-audit.ts` now dedupe subscriptions by `remark + sha256(url)`, fingerprint nodes by semantics instead of raw PassWall section id, split router detail into editable manual nodes plus read-only managed/orphan subscription runtime, and return only safe audit metadata (`urlHash`, payload mode, counts, drift) instead of raw subscribe URLs. Local verification is green on targeted `draft-selection/subscription-runtime/editor-surface/editor` Vitest, `@vectra/web` lint, focused `tsc --noEmit`, and `@vectra/web build`; deployment is still required for final UI proof because the current production server does not yet expose the new `subscriptionRuntime` / `workspaceRevisionId` fields even though the motivating `VagrandRouter` facts remain the same (`17` live nodes and `1` subscription versus a stale historical panel draft at `46` nodes and `1` subscription).

2026-04-20 deploy parity addendum:
- A fresh production parity check now resolves the earlier uncertainty more cleanly: there is no remaining undeployed committed web delta between `origin/main` and the current `HEAD`. For the five product commits in that range, production already contains every non-doc app/runtime file, and live `draft.editorSurface` on `VagrandRouter` now exposes `workspaceRevisionId` plus `subscriptionRuntime`, which means the subscription/runtime slice is active on production rather than still waiting for rollout.
- The current mismatch has flipped direction. Several production files now match the local worktree beyond `HEAD`, and new uncommitted source files from the same trust/subscription area already exist on the VPS. The next close-out step is therefore git/deploy provenance hygiene, not another prod web deploy.

2026-04-20 fleet stability addendum:
- A same-day production stability follow-up now closes the separate `vectra-web` OOM suspicion more concretely. The bug was not another subscription/source-of-truth regression, but oversized fleet read models: `fleet.list` still pulled full desired-revision configs and full snapshot history into Node, while `fleet.monitoring` still loaded live-import revisions as full rows even though config-trust only needed metadata. The local web slice now moves both surfaces onto metadata-only revision loading and the optimized latest-snapshot path, with local proof green on targeted fleet tests plus `@vectra/web build`.
- The production hotfix is already deployed through a narrow backup-first lane because the workspace and VPS both contained unrelated live drift. Only `fleet.ts`, `fleet-monitoring-data.ts`, and new `revision-metadata.ts` were copied into `/opt/vectra-prorouter`, `vectra-web` rebuilt cleanly, and fresh production evidence now shows `fleet.list` at about `1.49s` instead of the earlier `14s+`, `fleet.monitoring` at roughly `0.5s-1.5s`, `vectra-web` `oom=false restart=0 health=healthy`, and memory around `141MiB / 1.9GiB`. This keeps `Web control plane` and `Deployment stack` at `pilot/high`, but shifts the immediate next review away from “is the fleet panel still crashing?” toward normal provenance hygiene and longer-horizon rollout proof.
- A later same-day production subscription audit now shows the remaining runtime problem is broader than the original `VagrandRouter` example. Read-only `fleet.monitoring` plus per-router `draft.editorSurface` checks confirm `subscriptionRuntime.audits[*].status = drift` on all `8` active routers, and every router currently shows only orphaned subscription nodes in the read-model with `managedNodes = 0`. The worst live outlier is `AndreyVK` at `31` total nodes, `2` runtime subscriptions, and `30` orphan nodes while the currently audited subscribe payload resolves to `1` node; `VagrandRouter` still shows `17` total nodes / `16` orphan nodes and its audit still exposes a stale panel-draft footprint at `44` managed subscription nodes versus only `1` current payload node. A deeper live check on `AndreyVK` also confirms two live subscription entries with the same URL under different internal ids, which explains the duplicate subscription count in the panel. The next operational step for this slice is therefore a real cleanup/apply lane for orphaned subscription runtime on routers, not more read-model work.

2026-04-21 passwall update investigation addendum:
- A code review of the current workspace now narrows the earlier operator-visible symptom “PassWall updates from `Обновления`, then only reboot restores runtime” to the panel-driven `update_passwall_packages` lane rather than upstream PassWall2 LuCI `App Update`.
- Upstream PassWall2 LuCI still does not auto-update `luci-app-passwall2`; it only checks/report self-update as unsupported and exposes per-component `update_<component>` routes. That component path already wraps the move with `passwall2 stop` and background `restart`, so it does not fit the historical “package updated, router needs reboot” behavior.
- The historical Vectra-side bug was missing post-install convergence after package replacement. Before the recovery work that shipped with controller `0.1.11-r1`, the operator lane could complete the package install but leave PassWall without an immediate `rule_update.lua log geoip,geosite` plus discrete `passwall2 restart || start`, so reboot merely hid the gap by rebuilding runtime state on boot.
- The current source tree keeps that recovery explicit, and later `0.1.12-r13` work further removed unnecessary `opkg update` from fully pinned managed-stack jobs. This shifts the remaining live PassWall update risk from “needs reboot to come back” toward storage headroom, package-version drift, and honest partial-convergence reporting.

2026-04-21 node-reference addendum:
- The local `Web control plane` slice now also closes a separate operator-visible PassWall parity gap in `draft.editorSurface`: when live/runtime subscription nodes rotate their random section ids after update but keep the same visible label, the editor merge now rebinds node references by label/group instead of treating the target as missing. This covers the main selected node plus shunt targets, SOCKS references, backup-node lists, and PassWall-specific extras that previously drifted toward invalid ids and UI fallback states such as `Close (Not use)`.
- Local proof for that narrow fix is green on `pnpm --filter @vectra/web test --run src/server/vectra/editor-surface.test.ts` and `pnpm --filter @vectra/web typecheck`.
- Safe test-router proof is now also explicit on `OpenWrt` (`e80b2f5b-d80a-4903-85e1-57d8ce747f15`) without writing to the router: a fresh live `draft.editorSurface` snapshot confirmed the current approved config shape, and the same captured router data was then fed through the real local `mergeCurrentLiveRouterDataIntoDraftConfig` logic with a synthetic selected-node id rotation that preserved the live node label/group. The targeted Vitest check passed, which proves the new rebinding logic keeps the logical selected node on real test-router topology instead of falling through to a missing-target state when only the volatile id changes.
- The attended production rollout on `2026-04-21` also exposed and then closed one more real PassWall contour before the slice was accepted. On live `OpenWrt`, `WorldProxy.outboundNodeId` currently resolves to opaque runtime tag `AuyH33PD`, which is not present in `nodes[]`; the first production rebuild therefore proved the initial node-only rebinding logic could still collapse some shunt targets to empty instead of preserving the current live tag. A follow-up one-file hotfix to `apps/web/src/server/vectra/editor-surface.ts` now falls back to the current live shunt/rule-manage `outboundNodeId` by stable rule identity when node-based rebinding has no match. After that second rebuild, production `draft.editorSurface` for `OpenWrt` reports `selectedNodeId = RoscomShunt`, `selectedNodeLabel = "Маршрутизатор BloopCat"`, and `WorldProxy` aligned across live/draft/rule-manage as `AuyH33PD`, with `draftChanged = false` instead of the earlier empty draft value.

2026-04-21 updates-reboot addendum:
- The local `Web control plane` now closes two concrete operator gaps in `Обновления`: mass-selection is no longer row-by-row (`Выбрать все по фильтру` / `Снять выбор`), and the same workspace now has a destructive bulk reboot action wired through `update.queueBulkRouterReboot`.
- The implementation deliberately reuses the current agent terminal contract instead of introducing a new router job type. The first attended live proof on safe test router `OpenWrt` (`192.168.99.1`) exposed a real BusyBox shell incompatibility in the initial delayed reboot payload (`&;`), the local builder was corrected to newline-separated shell, and the second attended run then proved end-to-end reboot behavior by changing the router `boot_id` from `c66e065a-1d3d-4033-8a8b-bcf9fc89708a` to `3bbb25e8-0fb9-4884-9a7f-c3f4b4381731` with fresh panel check-in/snapshot timestamps at `2026-04-20T22:16:42Z`.
- Local verification for the undeployed source slice is green on targeted `@vectra/web` Vitest plus `build`; deployment is still the remaining step before the new bulk reboot button itself exists on production `/updates`.

2026-04-21 VPS disk hygiene addendum:
- A same-day attended production check now closes the immediate “auto-cleanup no longer copes” concern on the live VPS more concretely. The root filesystem had returned to `88%` used (`~4.8G` free) even though both `vectra-vps-disk-cleanup.timer` and `vectra-vps-disk-guard.timer` were enabled; the real failure mode was the conservative policy itself, not a dead timer. `vectra-vps-disk-guard.service` was running every `6h` but exiting `1` because recent Docker build residue and `/tmp` release/staging artifacts were younger than the existing cleanup thresholds.
- The attended remediation is already proven live: aggressive prune of unused Docker builder cache plus unused images, and removal of stale `/tmp` release/staging artifacts older than `12h`, reduced root usage from `88%` to `64%` (`~14G` free) without touching running containers, volumes, PostgreSQL, or rollback backups. Post-cleanup proof remains green on production with `docker compose ps` showing `caddy/postgres/web` healthy and public `GET https://router.vectra-pro.net/api/health -> 200` plus `GET https://api.vectra-pro.net/healthz -> 200`.
- The repo and VPS are now aligned on the follow-up fix: `deploy/scripts/vps-disk-cleanup.sh` gained an `--aggressive` mode plus broader safe `/tmp` release-pattern cleanup, and `deploy/scripts/vps-disk-guard.sh` now escalates into that aggressive path before declaring failure at high usage. This keeps `Deployment stack` at `pilot/high`, but shifts the next storage decision from emergency cleanup toward deliberate retention policy on the three local OpenWrt SDK trees and archives still consuming about `9.3G`.

2026-04-21 Xray runtime-target rollout addendum:
- The production `Обновления` surface is now live-proven to track PassWall2 Xray freshness by runtime target rather than mirrored package version. The earlier false “already current” state came from the panel comparing router runtime against mirrored `xray-core 26.3.27-r1`; production `update.versionDriftWorkspace` now resolves `xrayAvailable = 26.4.17`, `xrayNeedsUpdate = true`, and `passwallAvailable = 26.4.20-1` on routers still behind upstream runtime.
- The deployment close-out was intentionally narrow and backup-first because `/opt/vectra-prorouter` is a non-git root with live drift. The attended production web release copied only the Xray runtime-target web slice plus the missing/stale dependency files needed for a clean `docker compose build web`, then refreshed the PassWall mirror on the VPS and verified that the refreshed manifest now carries `runtimeTargets.xray-core.remoteVersion = 26.4.17`.
- The same attended window also closes the controller/runtime loop on the safe test router `OpenWrt`: controller `0.1.12-r14` was published through the direct-artifact/spec lane, installed successfully, and a subsequent `Update Xray` job moved runtime `Xray 26.4.15 -> 26.4.17` while `packageVersions["xray-core"]` stayed at `25.10.15-r1`. That runtime/package split is the expected live proof that the PassWall2 built-in updater path now works from the panel instead of silently degenerating into a no-op or package reinstall.

2026-04-21 runtime-target components follow-up:
- The same production slice now extends the runtime-target contract beyond Xray instead of leaving the other runtime-only buttons on package-only truth. The refreshed PassWall bootstrap manifest on the VPS now publishes `runtimeTargets` for `sing-box = 1.13.9`, `hysteria = 2.8.1`, and `geoview = 0.2.5` alongside `xray-core = 26.4.17`, and the deployed web/API slice now uses that metadata when queueing scoped component jobs and when deciding whether a component is already current on the router page.
- Production rollout for that follow-up is complete: after syncing the missing adjacent source files required by the live non-git tree, `docker compose --env-file .env build web` succeeded, `docker compose --env-file .env up -d web` recreated `vectra-web`, and fresh `scripts/VectraPanelCli.sh status` still reports green `browserPushMonitor/dbRead/dbWriteProbe`. This keeps both `Web control plane` and `Deployment stack` at `pilot/high`.
- The remaining gap is now intentionally narrower. Real attended router-side update proof exists for `Xray` only: the safe test router `OpenWrt` already proves runtime `26.4.15 -> 26.4.17` while `opkg` stayed on `25.10.15-r1`. `sing-box`, `hysteria`, and `geoview` now have deployed metadata and UI/job semantics, but still need their own attended update runs before this stage can claim runtime-proof parity across every runtime-only component button.

2026-04-21 subscription-runtime authority addendum:
- A same-night live review now narrows the node-duplication/orphan symptom to the operator read-model rather than to the router inventing bad nodes. `scripts/VectraPanelCli.sh draft editor VagrandRouter --json` showed `configSourceMode = live-import`, `15` live `add_mode=2` nodes in subscription group `BloopCat`, and only `1` node in the server-side advisory payload audit for the same subscribe link; this was the concrete reason the old UI rendered live router subscription nodes as `payload-mismatch`/orphan even though they were already confirmed in the router import itself.
- Upstream PassWall2 source now explains why the plain server fetch was the wrong authority for the node list. `passwall2/.../subscribe.lua` fetches subscriptions with router-specific `user_agent` plus headers such as `x-device-os`, `x-ver-os`, `x-device-model`, and `x-hwid`, so server-side fetch can legitimately see a different payload than the router gets from the same URL. The local `Web control plane` slice now treats live router import as authoritative for subscription-node listing, keeps the server fetch only as advisory audit metadata, and explicitly separates live subscription nodes from panel-only history and local non-`add_mode=2` group leftovers.
- Local proof for this read-model correction is green on `pnpm --filter @vectra/web test --run subscription-runtime`, `pnpm --filter @vectra/web typecheck`, and `pnpm --filter @vectra/web build`. This keeps `Web control plane` at `pilot/high`, but shifts the next subscription-runtime step from “find more read-model evidence” to “deploy the slice and verify that the old `не совпала с payload` badges disappear from live router pages without hiding real panel-only or local group drift.”

2026-04-21 subscription-runtime rollout addendum:
- The production rollout for that authority fix is now complete too. Only `apps/web/src/server/vectra/subscription-runtime.ts`, `editor-surface.ts`, and `secrets.ts` were pushed onto the non-git VPS deploy root through the narrow backup-first lane, then `vectra-web` was rebuilt and returned to `healthy`; operator/router public smoke remained `307/200/200/200/400`, and live `/api/health` still reports `browserPushMonitor/dbRead/dbWriteProbe = true`.
- Fresh post-deploy operator proof now shows the intended bucket split on real routers instead of the earlier “everything is orphaned” failure mode. `VagrandRouter` currently resolves `managedNodes = 21`, `panelOnlyNodes = 42`, and only `orphanNodes = 1`; `AndreyVK_Sochi` resolves `managedNodes = 21` and `orphanNodes = 0`; `AndreyVK` resolves `managedNodes = 42` and `orphanNodes = 0`. This means the control plane is no longer collapsing confirmed router subscription nodes into the error bucket just because the advisory payload fetch returns only `1` node for the same subscribe URL.
- The next subscription-runtime review is therefore narrower and more operational: implement cleanup/apply handling for stale `panelOnlyNodes` and real router-local leftovers, not another source-of-truth redesign of the read model.

2026-04-21 subscription cleanup action addendum:
- That next operator step is now implemented locally too: the router workspace can directly persist a live-aligned draft back into panel history when the only remaining drift is `panelOnlyNodes`, and it can prepare a cleanup draft that removes true router-local leftovers from subscription groups before the normal save/apply flow. The underlying cleanup helper also repairs the most important dependent references instead of leaving dangling `selectedNode`, SOCKS, shunt, or PassWall extra references behind.
- This new cleanup layer is locally verified on targeted Vitest plus file-level ESLint, but it is not yet deployed. The current repo-wide `@vectra/web typecheck` remains blocked by unrelated active errors in the updates surface (`update-launch-monitor-dialog.tsx`, `update-version-drift-workspace.tsx`, and `server/api/routers/update.ts`), so the next close-out choice for this area is either isolate/fix that separate drift and deploy the cleanup UI, or keep the cleanup slice local until the update surface settles.

2026-04-21 subscription cleanup rollout addendum:
- The cleanup UI is now deployed as well. `router-detail-workspace.tsx` and `router-editor-state.ts` were rolled out through the same narrow backup-first VPS lane, `vectra-web` rebuilt successfully through the host-side background runner, public smoke stayed `307/200/200/200/400`, and the running router-detail bundle now contains `Subscription Cleanup`, `Нормализация draft и live runtime`, and `Подготовить cleanup draft`.
- This means the current `Web control plane` slice now covers the full operator path for this issue at the panel layer: authoritative live subscription listing, explicit separation of panel-only vs router-local leftovers, and live cleanup actions for both categories directly from the router page. The remaining next step is no longer rollout but an attended cleanup/apply on one agreed router to confirm the intended runtime effect against real duplicate history or local leftovers.

2026-04-21 updates live monitor addendum:
- The local `Web control plane` now closes a third operator ergonomics gap on `/updates`: after single-router or bulk update/reboot actions, the page immediately opens a dedicated live progress modal for that exact launch instead of leaving the operator on the table with only background queueing. The monitor tracks the returned `jobId` set, auto-refreshes, groups progress per router, and surfaces both immediate queue failures and router-reported errors with real detail blocks.
- The implementation keeps the backend narrow and compatible with existing job semantics. `apps/web/src/server/api/routers/update.ts` now exposes `update.launchProgress`, while `apps/web/src/components/update-version-drift-workspace.tsx` and new `apps/web/src/components/update-launch-monitor-dialog.tsx` handle the overlay without adding another router job type or a separate persistence layer.
- Local proof for this slice is green on targeted monitor test plus `@vectra/web` `typecheck` / `lint` / `build`, which also closes the earlier local updates-surface typecheck drift. This keeps `Web control plane` at `pilot/high`, but the new monitor itself is still local-only until the next VPS web rollout.

2026-04-21 updates rollout addendum:
- The same `/updates` slice is now deployed on production as well. Browser verification on live `https://router.vectra-pro.net/updates` confirms that `Контроллер версий` renders the new bulk controls (`Выбрать все по фильтру`, `Перезагрузить выбранные роутеры`) after operator login, so the earlier bulk-reboot ergonomics gap is no longer local-only.
- Safe API proof for the monitor path is live too: `update.launchProgress` now answers on production and returns the expected schema validation for empty `jobIds` rather than a missing-procedure error. This verifies the deployed monitor route without queueing a real update or reboot against the park during smoke.

2026-04-21 subscription cleanup UX simplification addendum:
- A same-day local UX pass now reduces the operator-reading burden on the already-deployed subscription cleanup flow instead of changing its backend behavior. `router-detail-workspace.tsx` now shows the node area through three plain-language states (`Редактируете`, `На роутере`, `Разобрать`), keeps only the authoritative live subscription list permanently visible, hides panel-only/router-local leftovers behind a disclosure, and collapses the earlier split cleanup wording into one primary sync action whose copy changes with the situation.
- The separate payload/live/panel audit is still present for debugging, but it no longer competes with the main node workflow on first paint; it now lives behind a `Техническая диагностика` disclosure inside `Подписки`. Local proof for this narrower pass is green on `eslint` for the touched component, targeted runtime/editor Vitest, and `@vectra/web build`.
- The same UX pass is already live on production through a narrow one-file VPS rollout with background rebuild `status=ok`; post-rollout `vectra-web` is healthy, public smoke stayed `307/200/200/200/400`, and panel health still reports `browserPushMonitor/dbRead/dbWriteProbe = true`.
- This does not change the current stage (`pilot/high`) or the earlier source-of-truth decision. It changes only operator ergonomics, and it shifts the remaining next step for this area more cleanly to one attended real-router cleanup using the simplified panel flow.

2026-04-21 subscription cleanup attended + parity closure addendum:
- That attended cleanup proof now exists. On live router `AndreyVK_Sochi`, the simplified sync path was used against a pure panel-drift case (`managedNodes = 21`, `panelOnlyNodes = 13`, `orphanNodes = 0`, `configSourceMode = live-import`), and saving the current live-aligned draft back into panel history produced revision `2a5ca24c-d939-4bed-8acc-aadc27408164`. Fresh reads now show `panelOnlyNodes = 0`, `orphanNodes = 0`, and `managedNodes = 21`, so the simplified `Синхронизировать панель с роутером` action is no longer only a local or deploy-level claim.
- The production deploy contour for this workset is now also closed more strongly than before. A later full parity audit against the live non-git VPS root checked every currently changed deployable file under `apps/`, `packages/`, and `deploy/`; the result is `29/29 SYNC`, with no remaining `DIFF` or `MISSING` files in the current deployable workset and with `scripts/VectraPanelCli.sh status` plus public smoke still green.
- This keeps both `Web control plane` and `Deployment stack` at `pilot/high`, but changes the remaining next step again. The panel-sync branch is now live-proven on a real router and fully present on the VPS; the next attended subscription-cleanup proof should target the other branch, `Подготовить чистый draft`, on a router that still has true router-local leftovers or non-zero `orphanNodes`.
2026-04-21 updates confirm-dialog addendum:
- The production `Обновления` surface now also closes the remaining browser-popup rough edge on the bulk reboot lane. A one-file hotfix to `apps/web/src/components/update-version-drift-workspace.tsx` replaced the native `window.confirm(...)` with a site-native confirmation modal, the VPS rebuild completed `ok`, `vectra-web` returned to `healthy`, and public health/smoke stayed green.
- Live operator proof is explicit on safe test router `OpenWrt` without queueing another reboot: after selecting only that router in `Контроллер версий`, `Перезагрузить выбранные роутеры` now opens the in-page dialog `Поставить перезагрузку в очередь?` with the router chip and `Отмена` / `Подтвердить перезагрузку` buttons instead of the old browser confirm window. This keeps `Web control plane` and `Deployment stack` at `pilot/high`, but removes one more operator-visible polish gap from the deployed `/updates` flow.

2026-04-21 live UX audit addendum:
- A same-day live operator walkthrough across production `/fleet`, `/drafts`, `/updates`, `/rescue`, `/enrollment`, and router detail now shows a cross-cutting ergonomics problem that is separate from feature completeness. The current web slice is rich and accurate, but many screens still front-load explanation, onboarding, and trust-model narrative before the operator reaches the main working surface. One additional live router-detail navigation to `AndreyVK` also stayed on the verbose loading state for more than `20s`, so the route-transition pattern itself now counts as part of the UX debt.
- The heaviest concentration remains in `Web control plane`: `fleet` adds explanatory cards above search and the operations table, `drafts` spends the top of the screen re-explaining that it is an expert route, `updates` opens with tutorial and release-summary blocks before the active workspace, and router detail still combines live state, panel truth, audit/trust explanation, diagnostics, and destructive controls on one long page. `Rescue` and `Enrollment` remain the clearest current surfaces because they stay closer to one job per screen.
- This keeps `Web control plane` at `pilot/high`, but changes the near-term ergonomics priority. The next UX cut should trim shared chrome and route headers, move explanatory and trust text behind disclosure or secondary panels, default work-heavy routes directly into their actionable workspace, and replace the current narrative loading screens with compact skeleton-based transitions.

2026-04-21 operator UX simplification implementation addendum:
- The planned safe UX cut is now implemented locally in source for `Web control plane` and keeps the slice at `pilot/high`. The pass stays presentation-first: no auth flow, route structure, backend contract, or router action semantics were changed.
- Shared shell/header/loading primitives are slimmer, `/fleet` `/drafts` `/updates` `/rescue` `/enrollment` now expose the main task earlier in the viewport, and router detail follows a clearer three-layer contour: compact summary, explicit next safe action, then PassWall workspace.
- The `AndreyVK` long wait is now narrowed in source to server-side `draft.editorSurface` preparation rather than client render, with secondary subscription audit work bounded so it no longer holds the whole detail route open as long as the core editing surface.
- Current next step for this slice is attended browser/live-panel smoke on the deployed panel before making any rollout claim for the new UX contour.

2026-04-21 second UX simplification rollout addendum:
- That attended proof now exists on the live panel, so the current `Web control plane` and `Deployment stack` evidence should now be read against the newer contour rather than the pre-rollout UX audit. The second simplification pass is deployed on production for `/updates`, router detail, and `/fleet`, and the live browser checks now confirm the intended rule of one primary operator surface above the fold on those heavier routes.
- The production evidence is specific rather than generic. `/updates` now opens into the active workspace instead of tutorial-first paint, router detail keeps compact summary plus one next-safe-action block above the PassWall editors, and `/fleet` now starts with status/filter/list while trust/help copy stays collapsed by default. The same pass was checked again in a narrow mobile viewport, where `/updates` and `/fleet` still keep the working surface first instead of regressing back to hero-style explainers.
- Local engineering proof for this deployed UX slice is also stronger than plain lint/build. `@vectra/web` `typecheck`, `lint`, `build`, and targeted Vitest all passed for the touched surfaces (`34` files, `191` tests), while platform health and public smoke remained green through the backup-first VPS rollout. The only blocked verification branch in this window was local router-detail smoke on workstation data, where `draft.editorSurface` still fails with a local decrypt/auth mismatch; production route smoke for the same surface passed, so the remaining issue is environment-specific rather than a blocker for the deployed UX contour.
- This does not change the current stage labels. It changes the near-term UX backlog shape: the second simplification pass is no longer a planned refactor but the active production baseline. The next operator-facing step should be narrower polish only, such as trimming residual row-level verbosity in mobile fleet cards or revisiting any real future loading stalls, not another broad above-the-fold hierarchy rewrite.

2026-04-21 responsive hardening addendum:
- A same-day local follow-up now closes the remaining responsive debt in source instead of treating horizontal scroll as acceptable on the operator panel. Shared mobile/card primitives were tightened in `apps/web/src/components`, router detail and `/updates` gained explicit below-`lg` card/disclosure alternatives for their action-heavy tables, and the remaining `/fleet` plus public-route shell/pre/code leaks were trimmed so narrow viewports do not require page-level overflow clipping.
- The local proof for this slice is concrete. On a clean non-Turbopack `next dev` run at `http://127.0.0.1:3412`, `apps/web/tests/mobile-action-qa.spec.js` now passes as a real phone/tablet route matrix (`390x844` and `820x1180`) across `/login`, `/fleet`, router detail tabs, `/updates`, `/drafts`, `/rescue`, `/enrollment`, and `/install`, with hard `scrollWidth <= clientWidth` checks on every route. `pnpm --filter @vectra/web typecheck` and `pnpm --filter @vectra/web build` also pass from a clean `.next` state after the same patch set.
- One real regression found by that matrix was fixed at the shared-primitive layer rather than as a route-local hack: router detail `App Update` still overflowed on tablet because `ActionStrip` switched to horizontal layout from `sm` upward; it now stays vertical until `lg`, which aligns with the mobile/tablet operator rule for notes, filters, and CTA stacks.
- This keeps `Web control plane` at `pilot/high`, but the responsive hardening itself is still local-only until the next VPS web rollout. The near-term acceptance step for the web slice is now narrower again: deploy this responsive pass and repeat the same phone/tablet walkthrough against live production routes.

2026-04-21 stabilization sweep closure addendum:
- The responsive/stabilization slice is now closed as the current production baseline rather than as an undeployed local pass. The final real blocker in the sweep was a production phone overflow on safe-router detail (`OpenWrt`, `390x844`, `scrollWidth 625`) caused by the shared `TabBar` single-row constraint plus width leaks inside unconfirmed-change cards; after the narrowed web fix and redeploy, the live router-detail route now returns `scrollWidth == clientWidth`.
- Whole-slice evidence is stronger than a single smoke. Clean local validation stayed green across `@vectra/web lint/typecheck/build`, root `pnpm test`, targeted Vitest for the touched responsive surfaces, deploy shell syntax checks, Python compile smoke for the mirror helper, and the Playwright mobile route matrix against local production-mode `next start`.
- Production acceptance is also explicit across all required operator routes and devices. Attended browser walkthroughs on phone `390x844`, tablet `820x1180`, and desktop `1440x900` confirmed no `500`s, no hidden primary controls, and no page-level overflow on `/login`, `/fleet`, safe-router detail tabs, `/updates`, `/drafts`, `/rescue`, `/enrollment`, and `/install`; public smoke and panel health remained green throughout.
- The required safe live writes are now closed on safe router `OpenWrt`. A panel-side sync-save created revision `b782da24-78e1-43c7-a0c6-a43468bcf58c`, the normal `Сохранить и применить на роутере` path then completed successfully, queue depth returned to `0`, and live CLI/browser facts confirm the router remained reachable with the same selected node/mode and healthy PassWall services.
- Stage impact remains intentionally narrow: `Web control plane` and `Deployment stack` stay at `pilot/high`, but no additional router-agent, DB, or deploy-script stabilization slice is currently justified by evidence. The next production-facing step should return to narrow bug/polish work only when a concrete regression is reproduced, while the existing `re-import needed` cue on `OpenWrt` stays a known operator follow-up rather than a blocker for the release-ready baseline.

2026-04-22 controller 0.1.13-r3 low-overlay live-proof addendum:
- `Router agent` and `Deployment stack` now both have stronger same-day proof on the exact low-overlay PassWall contour that had remained open after `0.1.13-r2`. Controller/LuCI `0.1.13-r3` are published in the stable signed feed plus production artifact metadata, public `index.json` reports `release = 3`, both `.ipk` URLs return `200`, and panel `update.versionDriftWorkspace` now exposes `controllerAvailable = 0.1.13-r3`.
- The attended router path is also closed on safe router `OpenWrt`, not just the artifact lane. Controller self-update job `693bfb91-ae47-49da-84e3-cdf57f38a4e9` moved the router from `0.1.13-r2` to `0.1.13-r3`, and subsequent inventory snapshots report controller/LuCI `0.1.13-r3` with `controller`, `passwall`, `passwallServer`, and `dnsmasq` all still `running`.
- The original user-facing bug is now live-proven fixed on that same router. Follow-up PassWall update job `c697cb43-b9b3-437f-a349-ed747f91ebf2` finished `succeeded` on roughly `14 MB` free overlay and no longer produced terminal `v2ray-geoip storage-blocked` plus `v2ray-geosite did not reach target through package path`; final package results now show `v2ray-geoip = updated/not-needed` and `v2ray-geosite = already-current/not-needed`.
- The remaining next step is narrower than “retest low-overlay geodata in general” because that proof now exists. The next real review point for this area is the separate artifact-resolution mismatch still visible in the live managed-stack payload, where some `v2ray-geo*` and `luci-app-passwall2` artifacts still resolve from mirror `26.4.10-1` while the bundle release tag is `26.4.20-1`.

2026-04-21 responsive tail-fix closure addendum:
- A final live responsive pass then found and closed two smaller production leaks that survived the broader sweep: `/fleet` still overflowed at tablet-landscape `1024x768`, and router-detail `App Update` still overflowed at both `1024x768` and desktop `1440x900`. The fixes stayed UI-only: fleet keeps cards through `xl`, and `App Update` copy now wraps as a full-width block instead of widening the toolbar row.
- Local engineering proof for that tail-fix is green on new/updated breakpoint regression tests plus `@vectra/web lint`, `@vectra/web typecheck`, and clean-`.next` `@vectra/web build`. The production rollout also stayed narrow and backup-first: only the two existing component files were replaced on the VPS, background rebuild `/tmp/vectra-web-rebuild-20260421-165310.status` finished `ok`, and smoke/status remained green.
- The strongest current acceptance evidence for `Web control plane` and `Deployment stack` is now the post-hotfix production route matrix itself: authenticated headless QA passed `32/32` checks across phone, tablet, tablet-landscape, and desktop on `/fleet`, router detail, `/updates`, `/drafts`, `/rescue`, `/enrollment`, and `/install` with no login bounce, no `500`, and no page-level horizontal overflow. This keeps both areas at `pilot/high`, but leaves no known responsive blockers in the current main operator contour.

2026-04-22 triage-cleanup addendum:
- The current dirty worktree has now gone through the planned hygiene split instead of keeping local debugging residue inside the candidate baseline. Root-level evidence artifacts (`check-in`, `operator`, `router-api`, `output/`, `release-web-unconfirmed-changes.tar.gz`, `router-app-update-3400.png`) were removed from the workspace, added to root `.gitignore`, and `scripts/Sync-ProRouterVault.py` regenerated `ProRouter/00 Dashboard/Repo Map.md` so the generated map no longer treats those leftovers as part of repository structure.
- The responsive acceptance lane is now aligned with that same cleanup rule. Root `.gitignore` no longer hides `apps/web/tests/mobile-action-qa.spec.js`, so the phone/tablet route matrix is now a real repo-visible regression harness rather than a fake repo test path that only exists locally. The one late web issue found in the cleanup sweep was real but narrow: `/fleet` still overflowed on phone `390x844`, and `/enrollment` still overflowed on tablet `820x1180`; both are now closed in source by tighter header breakpoint behavior plus forced wrapping for long `pre/code` content.
- Final local stabilization evidence is green across the requested cross-repo gate: `@vectra/web lint/typecheck/build`, root `pnpm test`, `router/vectra-controller-agent go test ./...`, deploy shell syntax checks, Python compile smoke for `deploy/scripts/refresh-passwall-mirror.py`, and the Playwright mobile matrix against a clean local production-mode `next start`. No new router-agent or deploy-script bug was reproduced by that sweep, so the remaining work is commit/branch hygiene rather than further speculative fixes.

2026-04-22 router-identical subscription preview stage addendum:
- `Web control plane` and `Router Agent` now have the local source slice for router-identical subscription preview implemented and verified, but not yet rolled out or live-attended. The panel no longer relies on server-side payload fetch for the main subscription count/read-model path in local source; instead it expects a router-side `inspect_subscriptions` preview keyed to the live subscription digest.
- Current local verification is green on `@vectra/web lint/typecheck` plus targeted subscription/editor/router-detail Vitest, and on `router/vectra-controller-agent go test ./...`. Current production status remains intentionally unchanged until deployment: affected routers can still show the older overcount/mismatch contour on the live panel because the new read-only preview job and the matching UI semantics are not yet claimed as deployed.
- Operational next step for this stage is narrow and concrete: run one attended router preview on a known bad case such as `AndreyVK` or `AndreyVK_Sochi`, confirm the returned preview count is lower than the old server-side overcount when appropriate, then roll the web/agent slice together instead of deploying another subscription UX change in isolation.

2026-04-22 managed-stack tcping false-failure addendum:
- A same-day live router triage now closes one fresh `Router Agent` failure mode more narrowly than “PassWall update is broken on DmitryGubenko”. On router `DmitryGubenko` (`35ab62ff-71bc-4364-b383-a724c98d4cf6`), two production `update_passwall_packages` jobs for PassWall mirror `26.4.20-1` failed with `tcping did not reach target through package path`, but the router itself stayed in `stable/fresh`, kept `controller/passwall/dnsmasq` at `running`, and reported enough free storage that the failure does not match the existing storage-gating branch.
- Read-only live proof plus source review agree on the real cause. The router currently has `tcping 0.3-r1` installed (`opkg status tcping`, `/usr/bin/tcping`, and `/usr/lib/opkg/info/tcping.*` all present), while local `router/vectra-controller-agent/internal/inventory/collector.go` omitted `tcping` from `passwallInventoryPackages`. Because the managed-stack executor verifies package-only components against a fresh post-install inventory snapshot, the job could mark `tcping` as failed even after successful `opkg` convergence.
- The source fix is already local and verified: `tcping` is restored to tracked package inventory and a regression test now locks that expectation, with `go test ./internal/inventory ./cmd/vectra-controller-agent` green. Stage impact stays intentionally narrow: `Router Agent` remains `pilot/high`, but the next operational step for this slice is now specific rather than generic PassWall work. Publish/install the next controller package with the inventory fix, then rerun one attended managed-stack update on `DmitryGubenko` and confirm the old false `tcping` failure disappears without requiring any router-side manual cleanup.

2026-04-22 controller version source bump addendum:
- The local release target for the controller pair is now explicitly advanced in source to the next OpenWrt package version line: `0.1.13-r1`. This closes the earlier repo-local ambiguity where package Makefiles still pointed at `0.1.12-r13` while nearby notes and operator proofs had already moved on to published `0.1.12-r14`.
- The change is intentionally source-only at this stage. OpenWrt package metadata in both controller Makefiles now resolves to `0.1.13-r1`, and the web-side test fixtures that model current controller artifact/version behavior were aligned to that same target with passing targeted Vitest coverage. No production artifact publication, PostgreSQL metadata sync, or live router install has been claimed yet for `0.1.13-r1`.

2026-04-22 controller publish-visibility addendum:
- A same-day release-lane check now makes the visibility boundary explicit instead of assuming the panel was stale. Live operator metadata and the signed public feed are currently on older published controller lines, so the panel not showing `0.1.13-r1` is expected until a real artifact publish plus metadata sync happens. The current live split is concrete: production panel metadata exposes controller `0.1.12-r14`, while the public signed feed pointer still advertises `0.1.11-r1`.
- Source-side guardrails for the next publish are now stronger than they were during the first version bump. `scripts/build-vectra-openwrt-feed.sh` no longer defaults silently to stale `0.1.12-r11`; it derives package version/release from the controller Makefiles and fails if agent/LuCI package sources diverge. This keeps `Deployment stack` at `pilot/high`, but narrows the next controller-release risk from “wrong version may be built by default” to the still-manual publish/sync close-out.

2026-04-22 controller 0.1.13-r1 operator-visible release addendum:
- The operator-visible half of that release is now complete. Production panel metadata no longer tops out at `0.1.12-r14`: direct-artifact publish plus spec sync inserted controller `0.1.13-r1`, and live `update.versionDriftWorkspace` now reports `controllerAvailable = 0.1.13-r1` for safe test router `OpenWrt`.
- Stage impact stays intentionally narrow. This closes the user-facing “panel does not see the new controller version” problem, but it does not yet close the older signed-feed split or prove a live router install of `0.1.13-r1`; `index.json` / `Packages` still advertise `0.1.11-r1`, and the next concrete proof step remains one attended controller update on a safe router.

2026-04-22 controller 0.1.13-r1 bootstrap alignment addendum:
- The signed-feed split is now closed for the controller pair as well. Public `index.json` and `Packages` now point at `0.1.13-r1`, so the existing bootstrap/install script, which installs controller packages by name from the signed Vectra feed, will now also resolve to the latest controller release instead of the earlier stale `0.1.11-r1`.
- This keeps `Deployment stack` at `pilot/high`, but narrows the current release risk further than the earlier panel-only closure. The remaining proof gap for this slice is no longer “does bootstrap still pull an older controller?”; it is now only the next attended end-to-end bootstrap or controller install on a safe router after the feed close-out.

2026-04-23 full worktree rollout addendum:
- The current releasable worktree is now rolled out on the live VPS rather than only prepared locally. Before any new write, a checksum dry-run against `/opt/vectra-prorouter` showed that the already-uploaded release slice matched the current local deployable tree exactly; the actual production changes were therefore narrowed to publishing controller/LuCI `0.1.13-r4`, syncing artifact metadata, and rebuilding `vectra-web` from the already-synced current source.
- `Router agent`, `Web control plane`, `Shared Contracts`, and `Deployment stack` all gain stronger same-day production evidence from that rollout. Public feed `index.json` now reports controller `release = 4`, both `0.1.13-r4` `.ipk` URLs return `200`, `update.artifacts` lists `0.1.13-r4`, `update.versionDriftWorkspace` now reports `controllerAvailable = 0.1.13-r4` across live rows, and the rebuilt panel is running with green `browserPushMonitor/dbRead/dbWriteProbe` health plus the usual public smoke `307/200/200/200/400`.
- The current web/contracts/db slice is also confirmed schema-safe in production instead of only source-safe: live PostgreSQL already contains enum value `inspect_subscriptions`, so the rebuilt panel is not waiting on an unapplied DB migration for the current router-preview path.
- Stage labels do not change from this rollout alone. The remaining proof gap is now narrow and router-side: install controller `0.1.13-r4` on one safe router, then run attended outage-supervisor drills plus one live subscription-preview / hostname-change proof before treating that controller line as production-proven beyond panel visibility.

2026-04-24 read-only Telegram triage addendum:
- A same-day read-only triage on certified router `1111111111` does not change any current stage label, but it narrows one operator-visible complaint materially. Fresh production snapshot and read-only terminal jobs show the router still `active/approved` with `controller/passwall/dnsmasq/passwallServer = running`, `passwallEnabled = true`, and `telegramReachability = reachable 4/4`; this is not a standing router-wide Telegram outage.
- The same proof shows that Telegram on this router is not following only the generic default path. Current live-approved config still routes `geosite:TELEGRAM` through shunt rule `WorldProxy`, which points at a dedicated Germany node, while local resolver answers for Telegram domains are `198.18.x.x` FakeDNS addresses via `127.0.0.1`. Operationally, that shifts the likely fault domain from "PassWall is generally broken again" to "that specific Telegram route or the macOS client's long-lived session over FakeDNS/tproxy is intermittently stalling".
- This keeps `Router agent` at `pilot/high` and `Knowledge base and runbooks` at `active/high`, but improves the diagnostic boundary for future incidents. The next high-signal step for this complaint is to capture one failure-window triage from the affected Mac plus one immediate router-side Telegram/domain/node check, not another speculative package or service change on the router.

2026-04-22 low-overlay geodata false-failure addendum:
- A same-day local agent fix now closes a second false managed-stack failure beyond the earlier `tcping` inventory bug. On low-overlay routers that already carry newer `v2ray-geoip` / `v2ray-geosite` package records than the mirrored PassWall target, the controller no longer treats those packages as stale just because the version strings differ. `cmd/vectra-controller-agent/passwall_update.go` now accepts non-runtime package convergence at installed version `>=` target and normalizes the mixed geodata timestamp shapes used by upstream packages (`YYYYMMDDHHMM(.release)` versus `YYYYMMDDHHMMSS-rN`).
- This narrows the real runtime question again instead of leaving the operator with two misleading blockers. Local regression tests now prove both the datestamp ordering and the early no-op exit for an already-ahead `v2ray-geosite`, and `cd router/vectra-controller-agent && go test ./...` is green. Stage impact stays intentionally narrow: `Router Agent` remains `pilot/high`, but the next live proof for PassWall updates is now specifically “install the next controller build on one safe low-overlay router and confirm the old false `storage-blocked` / `did not reach target through package path` pair disappears” rather than another broad package-update investigation.

2026-04-22 controller 0.1.13-r2 geodata-fix publish addendum:
- The release-lane half of that geodata fix is now closed too. Production feed and panel metadata no longer stop at controller `0.1.13-r1`: an isolated VPS build published controller/LuCI `0.1.13-r2`, synced the signed feed under `deploy/runtime/artifacts/openwrt/stable/aarch64_cortex-a53/`, and upserted the new controller artifact rows into PostgreSQL through the existing `sync-artifact-metadata.mjs` lane.
- Verification is stronger than a local source claim. Public `index.json` now reports controller `release = 2`, both new `.ipk` URLs return `HTTP 200`, panel `update.artifacts` exposes `0.1.13-r2`, and `update.versionDriftWorkspace` now resolves `controllerAvailable = 0.1.13-r2` across current rows. This keeps `Router Agent` and `Deployment stack` at `pilot/high`, but changes the next PassWall-update proof again: the remaining gap is now only an attended router install/retest of `0.1.13-r2` on a low-overlay box, not another publish or metadata-sync action.

2026-04-22 low-overlay geodata retest addendum:
- That attended retest now narrows the remaining `Router Agent` gap more precisely. Safe router `OpenWrt` is already on controller `0.1.13-r2`, but live PassWall job `861650de-3294-4b72-b371-d590babacc30` still reproduced the paired geodata contour: `v2ray-geoip` reported `storage-blocked` on low overlay, and `v2ray-geosite` still failed even though `opkg` itself refused the target as a downgrade-like version string.
- The current local source state for `Router Agent` closes that narrower contour with a second follow-up patch: package comparison now uses `opkg`-style ordering for mixed geodata version formats, and successful `rule_update.lua log geoip,geosite` repair now reconciles `v2ray-geo*` as rule-managed convergence instead of leaving them as terminal package-path failures. Local `go test ./...` is green after the new coverage.
- This keeps `Router Agent` at `pilot/high`, but changes the immediate next step again. The next proof is no longer “investigate low-overlay geodata in general”; it is specifically “publish/install the next controller build from the current source state and rerun `Update PassWall2` on a safe low-overlay router to confirm the old paired geodata failure summary disappears”.

2026-04-22 PassWall package-target hotfix addendum:
- `Web control plane` and `Deployment stack` now also close the separate production no-op that remained after the `0.1.13-r3` low-overlay proof. Earlier live job `c697cb43-b9b3-437f-a349-ed747f91ebf2` showed the exact mismatch: bundle `targetVersion = 26.4.20-1`, but queued `packageTargetVersion = 26.4.10-r1`, so the panel could say the update succeeded while `luci-app-passwall2` stayed on `26.4.10-r1`.
- The new production hotfix is already live and verified. A narrowed `apps/web/src/server/api/routers/update.ts` patch was deployed from the backed-up VPS baseline, `vectra-web` rebuilt cleanly back to healthy, and public/app health returned to green `200` after restart.
- The live rerun on safe router `OpenWrt` is decisive. New managed-stack job `623e35a0-0a03-4b6a-9f8f-0b85d7e85db3` queued `targetVersion = 26.4.20-1`, `packageTargetVersion = 26.4.20-r1`, and artifact URL `.../luci-app-passwall2_26.4.20-r1_all.ipk`; the job then completed `succeeded`, and fresh inventory snapshots now report `passwallAppVersion = 26.4.20-r1`.
- Stage impact stays intentionally narrow. The user-facing “panel says updated, but PassWall app did not actually update” bug is now production-proven fixed, while the remaining follow-up for this area is only result-truth polish around already-ahead/runtime-only components, not another package-targeting investigation.

2026-04-22 second AX3000T controller-gap closure addendum:
- The later same-day report from certified router `1111111111` looked at first like “errors continue after the fixes,” but live evidence narrows it further: that router was still on controller `0.1.12-r13`, so it was still executing the older managed-stack logic even though the web-side package-target hotfix was already live.
- Live job `0e6d3ea8-b812-4ba6-80c9-26e04d1cb7a0` confirms the old contour precisely. `luci-app-passwall2` had already moved to `26.4.20-r1`, but final job status still failed on `v2ray-geosite did not reach target through package path` plus false `tcping did not reach target through package path`, while stdout simultaneously showed `Package tcping (0.3-r1) installed in root is up to date.` and `Not downgrading package v2ray-geosite ...`.
- The attended fix path on the same router is now also closed. Controller self-update job `4d5dc9b3-2e56-46d3-9052-41bcdccc758c` moved `1111111111` to `0.1.13-r3`, and immediate rerun job `75a9f5e5-cb33-4a28-a264-544012224bb7` then completed `succeeded` with `tcping = already-current/not-needed`, `v2ray-geosite = already-current/not-needed`, and no remaining fatal managed-stack package errors.
- This changes the operational interpretation of the new screenshot from “the fixes did not work” to “another certified AX3000T had not yet received the fixed controller line.” The current controller/runtime slice is therefore stronger than before: the old false `tcping` and `v2ray-geosite` failures are now live-proven gone on two separate routers after `0.1.13-r3`, while any remaining red card on older routers should first be treated as a controller-version gap, not as a reopened web bug.

2026-04-22 router rename local addendum:
- `Web control plane` now has the local operator slice the user actually asked for, not the earlier panel-only alias detour. Router detail queues a real `run_terminal_command purpose=router-hostname-update` job that changes OpenWrt `system.@system[0].hostname` on the router itself, and the management task log now reports that job explicitly as hostname work instead of collapsing it into generic terminal/controller copy.
- The local proof for this corrected slice is green on targeted rename/router-detail/editor-surface/router-control Vitest plus `@vectra/web lint/typecheck/build` and `router/vectra-controller-agent go test ./...`. Stage labels remain unchanged (`Web control plane = pilot/high`, `Router Agent = pilot/high`, `Shared Contracts = active/medium`) because this hostname control is still source-only until a production rollout and one attended live router rename are completed.

2026-04-22 router-detail reboot shortcut addendum:
- `Web control plane` now also has a narrow local-only ergonomics fix on router detail: the `App Update` section exposes a direct `Перезагрузить роутер` action so an operator working on one concrete router does not need to jump back to `/updates` just to queue a reboot.
- The implementation deliberately reuses the existing reboot execution path rather than adding a new router-side mechanism. `apps/web/src/server/api/routers/update.ts` now exposes single-router mutation `queueRouterReboot`, and `apps/web/src/components/router-detail-workspace.tsx` uses it with an in-panel confirmation dialog, pending-job dedupe, and the existing router management task log summary.
- Local proof is green on targeted `src/components/router-detail-workspace.test.ts`, `@vectra/web typecheck`, and `@vectra/web build`. Stage labels remain unchanged (`Web control plane = pilot/high`) because this reboot shortcut has not yet been deployed or live-smoked on production router detail.

2026-04-22 control-plane outage supervisor addendum:
- `Router Agent`, `Shared Contracts`, and `Web control plane` now have the local source slice for a second recovery contour dedicated to control-plane outage rather than proxy-path degradation. The agent persists `controlPlaneRecovery`, classifies grouped `panel/ru/foreign` reachability after one hour of continuous `control_url` outage, budgets automatic reboot to at most one action per router per `12h`, and leaves the router in direct mode with `operator_attention` instead of looping destructive actions when foreign reachability still does not recover.
- The matching web/backend slice stays migration-free by design. Existing incident types (`server_unreachable`, `proxy_outage`, `recovered`) are reused, grouped reachability plus recovery phase travel through the shared contracts, and `router-control.ts` synthesizes at most one active incident per router from the new check-in fields instead of adding new DB enums or a second incident pipeline.
- Current proof is strong at the local engineering level but intentionally not yet a deployment claim. `cd router/vectra-controller-agent && go test ./...`, targeted `@vectra/web` Vitest for `router-control`, and the earlier `@vectra/web typecheck/build` lane are green; two late hardening gaps were also closed in source during this implementation pass, namely honoring `controller_restart_settle` and forcing a fresh `direct_settle` window even when the router was already in direct mode before the control-plane supervisor took over.
- Stage impact remains narrow for now: `Router Agent` stays `pilot/high`, `Web control plane` stays `pilot/high`, and `Shared Contracts` stays `active/medium` because the new supervisor is still source-only in the current workspace. The next operational step is concrete: publish the next controller package, deploy the matching web slice, and run attended safe-router drills for both “panel-only outage” and “foreign blocked after one hour” before treating the contour as production-proven.

2026-04-22 control-plane outage supervisor safety review addendum:
- A later same-day deep review changed the immediate rollout recommendation for that local supervisor slice. The current worktree still contains at least one production-blocking agent-side defect and multiple high-risk behavioral gaps: grouped `panel/ru/foreign` probes are cached long enough to survive direct/reboot/PassWall transitions, reconnect does not clear sticky `operator_attention` ownership, RU-only outages can enter the destructive branch, and the web-side synthetic incident resolver can close unrelated open incidents.
- Because those issues directly touch destructive actions, incident truth, and ownership of PassWall state, the local contour should still be treated as `source-only / not rollout-ready` despite green targeted tests. The next step is not “publish and drill” anymore; it is first to close those blockers in source, add the missing regression coverage, and only then publish the next controller/web slice for attended outage drills.

2026-04-22 control-plane outage supervisor blocker-fix addendum:
- The reviewed supervisor blockers are now closed in local source and re-verified. Router-agent recovery now invalidates stale grouped probe cache across direct/reboot/PassWall transitions, clears sticky supervisor ownership only on successful manual/terminal proxy restore, refuses RU-only destructive escalation, and does not treat already-direct routers as supervisor-owned reboot candidates.
- The matching web/backend slice now tags supervisor incidents with explicit origin metadata and resolves only those owned incidents, so synthetic `recovered` no longer closes unrelated open records or rewrites router status for another incident origin.
- Local verification is green on `cd router/vectra-controller-agent && go test ./cmd/vectra-controller-agent ./internal/config ./internal/state ./internal/recovery ./internal/rescue` plus `pnpm --filter @vectra/web test -- --run src/server/vectra/router-control.test.ts`. The contour remains `source-only / not yet rollout-proven`, but the immediate state has moved from “known blockers present” to “blockers fixed locally, pending publish + safe-router attended outage drills”.

2026-04-22 DmitryGubenko live complaint addendum:
- A later same-day live incident check now narrows the DmitryGubenko complaint more precisely than "VPN is still broken on the router." Current production evidence on router `DmitryGubenko` (`35ab62ff-71bc-4364-b383-a724c98d4cf6`, `netis,nx31`) shows `active/approved`, fresh check-ins, `passwallEnabled = true`, selected node `myshunt`, `controller/passwall/dnsmasq/passwallServer = running`, controller `0.1.13-r3`, PassWall app `26.4.20-r1`, and runtime `xray = 26.4.17`.
- The current runtime dataplane is also explicitly live-proven rather than only inferred from service status. Read-only terminal job `c4ba8392-1bc5-45ac-8b81-c0117a09700f` returned `rc=0` for three real PassWall node tests (`M0OjiPBJ`, `dqnDBqfQ`, `H25TQVlY`), and the fresh telemetry window right after that check reports `telegramReachability = reachable 4/4` for `telegram.org`, `web.telegram.org`, `t.me`, and `api.telegram.org`.
- The only active anomalies in the same evidence window sit below the VPN core, on the client access layer. `collect_router_logs source=passwall` returned no PassWall/Xray error lines, while system logs show repeated `dnsmasq-dhcp` `DHCPNAK` for client `64:d2:c4:b0:77:96` still requesting old `192.168.0.147` while the router offers `192.168.98.105`, plus repeated `hostapd` `did not acknowledge authentication response` for station `a8:80:55:ad:81:0e`; a follow-up lease/Wi-Fi dump also showed both radios up but no currently associated stations.
- A second read-only same-day L2 pass narrowed that suspicion to a specific physical contour. Live terminal data still shows intended separation (`lan/br-lan = 192.168.98.1`, `wan/eth1 = 192.168.0.182`, upstream DHCP server `192.168.0.1`) and only one local DHCP pool (`192.168.98.100-249`, `no-dhcp-interface=eth1`), but the upstream gateway MAC `cc:2d:21:06:17:20` now appears on both `eth1` and `br-lan`. `brctl showmacs br-lan` places that MAC on bridge port `3`, and `brctl showstp br-lan` maps port `3` to `lan3`, which means the upstream Tenda `192.168.0.x` segment is physically visible inside the Netis/OpenWrt LAN bridge rather than only cached by a client.
- A final same-day bridge-table pass makes the contour even tighter. The earlier DHCP-noisy client `64:d2:c4:b0:77:96` is also learned on bridge port `3` (`lan3`), while active Wi‑Fi stations learn on the expected radio-backed ports (`port 4 = phy0-ap0`, `port 5 = phy1-ap0`). That shifts the working diagnosis from broad "client access churn" to a narrower physical conclusion: the complaining client and the upstream `192.168.0.1` gateway are currently visible through the same wired `lan3` path.
- Stage reading should therefore move again: the Dmitry item is no longer just "maybe stale lease or weak Wi-Fi". It is now "PassWall core healthy, but upstream `192.168.0.x` network likely leaks into `lan3` and creates a real second-gateway / wrong-segment path"; the open operator action is to inspect the live cabling and any downstream switch/AP tied to `lan3`, not to keep treating this as a proxy-runtime regression.
- Stage impact stays intentionally narrow. `Router Agent` remains `pilot/high`, but the Dmitry item should now be read as a closed PassWall-core outage and an open client reconnect / stale lease / Wi-Fi-association suspicion, not as evidence that the current `0.1.13-r3` router runtime has regressed again.

2026-04-22 DmitryGubenko Wi-Fi isolation workaround addendum:
- The open Dmitry item is now narrower than "inspect lan3 cabling sometime later" because a same-night live mitigation already stabilized the user-facing Wi-Fi path without disturbing WAN or controller reachability. Current production SSIDs `Netis_2.4G` and `Netis_5G` were moved off the contaminated `br-lan` segment onto a dedicated interface `wifi_clean`, so the practical operator question is no longer "is VPN broken?" but "does any remaining issue still reproduce on the wired side after Wi-Fi isolation?".
- Live evidence for that workaround is explicit. Terminal history shows the final rollback-cancel verification job `0ab9f4c1-5e94-4445-bb3c-7be27e71dcb7` with `ifstatus wan` still up on `192.168.0.182/24` and `ifstatus wifi_clean` up on `br-wifi_clean` with `192.168.99.1/24`; the preceding verification window also showed both radios up and both production SSIDs bound to `wifi_clean`. The panel-side router record kept fresh check-ins through `2026-04-22T21:29:49.711Z`, so this was not a silent management-path outage.
- The residual red contour is now cleaner to reason about. Wi-Fi clients should receive isolated `192.168.99.x` leases from the Netis SSIDs, while the older upstream leak through `lan3` still exists on the wired `br-lan` side and still explains `dnsmasq-dhcp: no address range available for DHCP request via br-lan`. Stage impact therefore stays narrow: `Router Agent` remains `pilot/high`, PassWall core remains read as healthy on DmitryGubenko, and the remaining follow-up is a physical/topology correction for the wired path rather than another VPN-runtime investigation.

2026-04-23 DmitryGubenko Keenetic capture addendum:
- The wired follow-up is now narrower than "inspect the repeater manually" because a live remote capture on the same Netis router is already proven. Only `lan3` was repointed from `br-lan` to `br-wifi_clean` with a pre-staged rollback timer, so WAN stayed untouched on `eth1 192.168.0.182/24` throughout the change.
- The downstream device is explicitly identified and attached to the intended clean segment. Reset Keenetic Orbiter Pro `KN-2810` in `Ext` mode now receives `192.168.99.128` as `Keenetic-1151` on `br-wifi_clean`, and the final read-back confirms `lan3` is `LOWER_UP` under that bridge rather than drifting back into `br-lan`.
- The management surface is also partially live-proven rather than left as guesswork: the Keenetic first answered ICMP and plain HTTP from the Netis side, returning `302 Moved Temporarily` to `/netfriend` with `Ndm-Sysmode: extender`; a later read-back still showed the same MAC reachable on `192.168.99.128` at ARP/L2, but the HTTP endpoint timed out. Treat the HTTP wizard as transient/unfinished unless a local browser session confirms it again.
- Stage impact remains intentionally narrow. This does not change `Router Agent` stage, but it does close the concrete operator question for Dmitry's repeater path: remote Netis-side capture of the Keenetic onto the isolated `192.168.99.x` segment is production-proven, and any remaining work is now local Keenetic onboarding or physical reset/retry rather than more L2/L3 rescue on the Netis.

2026-04-25 router-detail DNS Clear IPSET/NFTSet addendum:
- A narrow local `Web control plane` ergonomics gap is closed in source: router detail `Basic Settings -> DNS` now includes the missing PassWall2-style `Clear IPSET/NFTSet` action. The server mutation queues a dedicated `run_terminal_command` with `purpose = passwall-clear-ipsets`, sets `passwall2.@global[0].flush_set='1'`, commits the config, and restarts PassWall2 so the active IPSET/NFTSet cleanup path runs on the router.
- Verification is local-only but green across the targeted Vitest lane, `@vectra/web typecheck`, `@vectra/web lint`, and `@vectra/web build`. Stage remains `Web control plane = pilot/high`; the remaining acceptance step is a safe attended click on one router after deploy to confirm the job result and UI task-log summary against live runtime.

2026-04-26 addendum:
- Controller/LuCI `0.1.13-r5` closes the `0.1.13-r4` LuCI package-loss incident. The packaging script now strips and rejects macOS AppleDouble metadata before and after `.ipk` creation, production artifacts/metadata were republished immutably as `r5`, web self-update semantics now fail closed on LuCI install/check errors, and live rollout restored both `vectra-controller-agent` and `luci-app-vectra-controller` to `0.1.13-r5` on every affected `r4` router. `AndreyVK_Sochi` remains intentionally held at working `0.1.13-r3` pending diagnosis of its earlier `Operation not permitted` terminal failure.

2026-04-28 Kilo configuration addendum:
- `Knowledge base and runbooks` now includes an explicit Kilo configuration layer. Global Kilo config exposes Sugar Memory, ICM, Context7, and Playwright MCPs, while project-local `.kilo/` defines the default `vectra-prorouter-steward` agent, focused Vectra workstream agents, `/vectra-*` commands, and reusable skills for project workflow, OpenWrt/PassWall2, web control plane, router-agent release, and VPS deploy hygiene. This is tooling/status infrastructure only; it does not change application runtime stage labels.

2026-04-29 VagrandRouter live PassWall incident addendum:
- Certified `VagrandRouter` was recovered from a real PassWall no-proxy state without package changes or reboot. Root cause was a subscribed Russia YouTube shunt node (`2UNaVJtF`) generating invalid Xray REALITY config (`empty "password"`), which prevented `global.json` from starting and left Telegram probes blocked. Minimal containment backed up `/etc/config/passwall2`, remapped `myshunt.YouTube` to tested working node `C9sSnDgU`, restarted PassWall2, and verified fresh production snapshot `telegramReachability = reachable` with all four Telegram probes green. Follow-up: make the replacement durable in panel/draft/subscription mapping so scheduled subscription refresh cannot restore the broken node.

2026-04-29 Vagrand subscription preview addendum:
- `Web control plane` and `Shared contracts` have a local source fix for the Vagrand false-apply/over-refresh report. Live read-only evidence showed Vagrand's RU YouTube subscription node was missing `reality_publicKey` while router `1111111111` had the same RU endpoint with that key present, so the issue is not that Russia is absent globally. The source patch narrows panel behavior: no-op live reimports should not propose router apply, managed subscription node id/order churn is ignored semantically, and subscription/rule refresh flags are tied to real changed paths rather than whole-config presence. Stage labels remain unchanged until production deployment and browser smoke.

2026-04-30 DmitryGubenko direct-mode repair addendum:
- The fresh Dmitry direct-mode repeat is recovered rather than still open. A normal reconnect job restored proxy mode and cleared the supervisor incident; live fleet now reports stable/proxy, PassWall enabled on `myshunt`, Telegram 4/4 reachable, and router-local HTTPS fetches to Vectra API, Telegram, and YouTube succeed through FakeDNS. The reason others worked is also evidence-backed: in the same 03:45-05:25 UTC window only Dmitry had Telegram blocked snapshots, while all other routers stayed reachable; treat this as a Dmitry-local topology/proxy-path transient on the known double-NAT/Tenda/lan3 contour, not as a fleet-wide package or panel regression.


2026-04-30 DmitryGubenko auto-retry source addendum:
- `Router Agent` now has a local source hardening for the Dmitry direct-mode repeat: after control-plane recovery in `direct_settle`, the agent performs one bounded PassWall proxy auto-retry when RU/local WAN is reachable and PassWall is disabled, then waits in `passwall_retry_wait` for fresh inventory instead of immediately requiring operator reconnect.
- Safety boundary remains narrow: the retry budget is one per persisted outage window (`LastPasswallRetryAt` after `OutageStartedAt` blocks another attempt), and the old `operator_attention` path remains for already-retried, already-enabled, or RU-not-reachable states.
- Verification is local and green on the scoped controller-agent package set plus full `cd router/vectra-controller-agent && go test ./...`; stage stays `pilot/high` because this patch has not yet been built into a new controller release or deployed to live routers.

## 2026-04-30 Local Guardrail Addendum

- Web control plane remains `pilot/high`, but the local candidate now includes saved-draft/apply safety guardrails: stale editable drafts are filtered against active/live imports, `draft.queueApply` rejects superseded or non-current revision ids, active authoritative re-apply no longer demotes approved revisions to `queued`, and operators can discard saved pending drafts before editing. Verified locally with targeted Vitest, typecheck, lint, build, plus read-only `VectraPanelCli.sh fleet list`. This is not yet deployed and did not mutate live drafts/jobs.
